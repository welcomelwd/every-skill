# Skill Template

复制此目录后，将目录名改为具体的 Skill 名称，并完成以下内容：

1. 修改 SKILL.md 的元数据和任务说明。
2. 将可执行逻辑放入 scripts。
3. 将参数和运行配置放入 config。
4. 将外部规范和参考资料放入 references。
5. 将静态文件或输入样例放入 assets。
6. 将验证脚本、测试数据或回归结果放入 tests。

模板中的示例文件仅用于说明目录和格式，不能替代真实验证。
