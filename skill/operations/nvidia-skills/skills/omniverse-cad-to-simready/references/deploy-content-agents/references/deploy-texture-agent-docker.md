# deploy-texture-agent-docker

Upstream source of truth:
`https://github.com/nvidia-omniverse/content-agents/blob/v0.5.2/.codex/skills/deploy-texture-agent-docker/SKILL.md`.

Use the authenticated local clone at
`${CONTENT_AGENTS_UPSTREAM_ROOT:-${OMNIVERSE_CAD_TO_SIMREADY_UPSTREAM_ROOT:-$HOME/.omniverse-cad-to-simready/upstreams}/content-agents}`
checked out to `main` when browser or raw-file fetches are unavailable.
