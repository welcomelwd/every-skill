{
  "skill_name": "doca-compress",
  "evals": [
    {
      "id": "doca-compress.explicit-positive.v1",
      "prompt": "Use the doca-compress skill to help me. I'm working on: hands-on DOCA Compress programming on a BlueField DPU, ConnectX NIC, or host with DOCA \u2014 enabling compress-deflate, decompress-deflate, decompress-lz4-stream, or decompress-lz4-block tasks on a doca_compress context (the hardware supports DEFLATE both directions plus LZ4 decompress; LZ4 encode is NOT supported), sizing source / destination doca_buf against the per-task cap query, setting mmap permissions, deciding offload vs CPU zlib / zstd, validating with a round-trip smoke, or debugging DOCA_ERROR_* from a Compress call.",
      "expected_skill": "doca-compress",
      "expected_output": "The agent selects the doca-compress skill and answers from its documented workflow, quoting only real symbols/commands from the bundle (no invented tokens).",
      "assertions": [
        "The doca-compress skill is selected and its SKILL.md/TASKS.md drive the answer.",
        "Every symbol, flag, or command named is traceable to the bundle (no invented tokens)."
      ]
    },
    {
      "id": "doca-compress.implicit-positive.v1",
      "prompt": "I'm running into this: \"offload this gzip\". Can you walk me through it?",
      "expected_skill": "doca-compress",
      "expected_output": "The agent recognizes this as a doca-compress issue even though the skill is not named, loads it, and answers grounded in the bundle.",
      "assertions": [
        "The doca-compress skill is triggered from the implicit phrasing without the user naming it.",
        "The answer stays grounded in real bundle symbols/commands."
      ]
    },
    {
      "id": "doca-compress.implicit-positive.v2",
      "prompt": "I'm running into this: \"decompress incoming network data\". Can you walk me through it?",
      "expected_skill": "doca-compress",
      "expected_output": "The agent recognizes this as a doca-compress issue even though the skill is not named, loads it, and answers grounded in the bundle.",
      "assertions": [
        "The doca-compress skill is triggered from the implicit phrasing without the user naming it.",
        "The answer stays grounded in real bundle symbols/commands."
      ]
    },
    {
      "id": "doca-compress.negative.v1",
      "prompt": "I actually need help with non-DEFLATE / non-LZ4 algorithms. Which skill should handle this, and why is it not doca-compress?",
      "expected_skill": null,
      "expected_output": "The agent does NOT force doca-compress; it explains that non-DEFLATE / non-LZ4 algorithms is out of doca-compress's scope and routes to the appropriate skill instead.",
      "assertions": [
        "doca-compress is not selected as the primary skill for this out-of-scope request.",
        "The answer routes to the appropriate skill(s) instead."
      ]
    }
  ]
}
