# Documentation Backfill v2.1.120–v2.1.139 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Backfill 21 missing features (v2.1.120–v2.1.139) into the main guide and update all whitepapers and recap cards to reflect the current guide state.

**Architecture:** Three independent tracks. Track A (guide core) and Track C (recap cards mass bump) can run in parallel immediately. Track B (whitepaper content) should run after Track A is complete (or in parallel if the agent knows what to write).

**Tech Stack:** Markdown editing, YAML frontmatter, `guide/ultimate-guide.md` (23K+ lines), `guide/cheatsheet.md`, `machine-readable/reference.yaml`, Quarto `.qmd` files.

---

## Files Modified

### Track A — Guide Core
- Modify: `guide/ultimate-guide.md` (9 insertion points — hooks, skills, worktrees, MCP, auto mode, CLI ref, env vars, slash commands)
- Modify: `guide/cheatsheet.md` (Essential Commands table + CLI section)
- Modify: `machine-readable/reference.yaml` (commands, cli, mcp, new env_vars section)
- Modify: `CHANGELOG.md` ([Unreleased] section)

### Track B — Whitepapers
- Create: `whitepapers/en/11-team-metrics.qmd` (translation of FR counterpart)
- Modify: `whitepapers/fr/02-personnalisation.qmd` + `whitepapers/en/02-customization.qmd`
- Modify: `whitepapers/fr/03-securite.qmd` + `whitepapers/en/03-security.qmd`
- Modify: `whitepapers/fr/05-equipe.qmd` + `whitepapers/en/05-team.qmd`
- Modify: `whitepapers/fr/07-guide-reference.qmd` + `whitepapers/en/07-reference-guide.qmd`
- Modify: `whitepapers/fr/04-architecture.qmd` + `whitepapers/en/04-architecture.qmd`
- Modify: `whitepapers/fr/09-apprendre-avec-ia.qmd` + `whitepapers/en/09-learning-with-ai.qmd`
- Modify: `whitepapers/fr/10-budget-ia.qmd` + `whitepapers/en/10-ai-budget.qmd`

### Track C — Recap Cards
- Modify: 52 FR recap cards `c*/m*/t*.qmd` (version bump 3.38.1 → 3.40.0)
- Modify: 52 EN recap cards `c*/m*/t*.qmd` (version bump 3.38.1 → 3.40.0)
- Modify: 5 EN old-format cards (version sync 3.38.1 → 3.40.0)
- Modify: 5 priority recap cards content (m09, m11, m12, m13, t12 FR + EN)

---

## TRACK A — Guide Core

### Task A1: Hook system — args[], continueOnBlock, effort.level

**File:** `guide/ultimate-guide.md`

Context: Three additions to the hooks section, clustered near lines 9987–10170. Read each target section before editing.

- [ ] **Step 1: Read the hook Configuration Fields table**

  ```bash
  sed -n '9980,10000p' guide/ultimate-guide.md
  ```
  Confirm line 9987 is the `### Configuration Fields` heading with a table starting `| Field | Type | ...`.

- [ ] **Step 2: Add `args` field row to the Configuration Fields table**

  Find the row for `command` in the table (likely `| \`command\` | string | Required | ...`). Insert immediately after it:

  ```
  | `args` | `string[]` | Optional | Exec form — array of strings spawned directly without a shell. Path placeholders need no quoting. When present, `command` is ignored. Use to avoid shell-injection risks. (v2.1.139) |
  ```

  Then, find the closing paragraph below the table and append:

  ```markdown
  > **Exec form (`args`)**: Use `args: ["program", "arg1", "arg2"]` to spawn the command without a shell interpreter. Useful when paths contain spaces or special characters that would require quoting in `command`. Exec form also avoids shell injection risks in automated contexts.
  ```

- [ ] **Step 3: Read the Hook Input (stdin JSON) section**

  ```bash
  sed -n '10070,10090p' guide/ultimate-guide.md
  ```
  Confirm line 10075 is `### Hook Input (stdin JSON)` with a field table.

- [ ] **Step 4: Add effort.level to the stdin JSON table**

  Find the last row of the stdin field table. Insert after it:

  ```
  | `effort.level` | string | Active effort level: `low`, `medium`, `high`, `xhigh`, `max`. Bash-type hooks also receive this as `$CLAUDE_EFFORT` env var. (v2.1.133) |
  ```

- [ ] **Step 5: Read the Hook Output section**

  ```bash
  sed -n '10095,10175p' guide/ultimate-guide.md
  ```
  Confirm line 10111 is the `PostToolUse` bullet in the event-specific decisions list.

- [ ] **Step 6: Add continueOnBlock documentation**

  Find the bullet line for `PostToolUse` in the hook output section. After it, insert:

  ```markdown
  **`continueOnBlock`** (`PostToolUse` only, v2.1.139): When `true`, a `decision: "block"` response feeds the `reason` back to Claude as context and continues the turn instead of halting. Use to give Claude a chance to retry with a compliant approach:

  ```json
  {
    "type": "PostToolUse",
    "matcher": "Write|Edit",
    "command": "check-file-policy.sh",
    "continueOnBlock": true
  }
  ```

  Without `continueOnBlock`, a blocked PostToolUse stops the turn and surfaces an error. With it, Claude receives the rejection reason and can self-correct.
  ```

- [ ] **Step 7: Add PostToolUse output replacement documentation**

  In the same Hook Output section, find the PostToolUse entry. After the `continueOnBlock` block, insert:

  ```markdown
  **Output replacement** (`PostToolUse`, v2.1.121): `PostToolUse` hooks can replace what Claude receives as the tool result via `hookSpecificOutput.updatedToolOutput`. Works for **all tools** — Bash, Read, Write, Edit, MCP tools, etc.:

  ```json
  {
    "hookSpecificOutput": {
      "updatedToolOutput": "redacted: output contained PII, removed by policy hook"
    }
  }
  ```

  Use cases: scrub PII from tool outputs before Claude processes them, compress large results, inject metadata or audit trails into every tool response.
  ```

- [ ] **Step 8: Commit**

  ```bash
  git add guide/ultimate-guide.md
  git commit -m "docs(hooks): add args exec form, continueOnBlock, effort.level, PostToolUse output replacement (v2.1.121-v2.1.139)"
  ```

---

### Task A2: Skills — ${CLAUDE_EFFORT} variable

**File:** `guide/ultimate-guide.md`

- [ ] **Step 1: Read the Per-Skill Effort Allocation section**

  ```bash
  sed -n '7525,7560p' guide/ultimate-guide.md
  ```
  Confirm line 7527 shows the `effort` frontmatter field, and line 7531 is `### Per-Skill Effort Allocation (v2.1.80+)`.

- [ ] **Step 2: Insert ${CLAUDE_EFFORT} documentation after the effort field explanation**

  Find the end of the effort field description block (around line 7551, before the `allowed-tools` section). Insert:

  ```markdown
  **`${CLAUDE_EFFORT}` in skill content** (v2.1.120): Skill body text can reference `${CLAUDE_EFFORT}` as a variable. Claude substitutes it with the current effort level string (`low`, `medium`, `high`, `xhigh`, `max`) before processing the skill. Use this to branch instructions based on effort:

  ```markdown
  ---
  name: review-code
  effort: medium
  ---

  Review the changed files for correctness.

  ${if CLAUDE_EFFORT == "high" or CLAUDE_EFFORT == "xhigh"}
  Also run a full security audit and check all edge cases.
  ${end}
  ```

  This lets one skill serve both quick-scan (low/medium) and thorough (high/xhigh) use cases without maintaining two separate skills.
  ```

- [ ] **Step 3: Commit**

  ```bash
  git add guide/ultimate-guide.md
  git commit -m "docs(skills): document \${CLAUDE_EFFORT} variable in skill content (v2.1.120)"
  ```

---

### Task A3: Worktrees — worktree.baseRef (Breaking Change)

**File:** `guide/ultimate-guide.md`

- [ ] **Step 1: Read the worktree section**

  ```bash
  sed -n '17677,17700p' guide/ultimate-guide.md
  ```
  Confirm line 17681 is `#### Start Claude in an isolated worktree` with the `--worktree` / `-w` usage.

- [ ] **Step 2: Insert worktree.baseRef documentation with breaking change callout**

  After the `--worktree` usage example block (around line 17689), insert:

  ```markdown
  > **⚠️ Breaking change (v2.1.133)**: `worktree.baseRef` now defaults to `fresh`, reverting the v2.1.128 behavior where `EnterWorktree` branched from local HEAD. If you have unpushed commits you need in the worktree branch, set `worktree.baseRef: "head"` explicitly.

  **`worktree.baseRef`** (`fresh` | `head`, default: `fresh`): Controls the base commit for worktrees created via `--worktree`, `EnterWorktree`, and agent-isolation worktrees.

  | Value | Behavior |
  |-------|----------|
  | `fresh` | Branch from `origin/<default-branch>` — always a clean remote base |
  | `head` | Branch from local HEAD — includes unpushed commits |

  ```json
  // .claude/settings.json (or .claude/settings.local.json)
  {
    "worktree": {
      "baseRef": "head"
    }
  }
  ```

  Use `head` when you're iterating on a feature branch and want the worktree to include your in-progress commits.
  ```

- [ ] **Step 3: Commit**

  ```bash
  git add guide/ultimate-guide.md
  git commit -m "docs(worktrees): add worktree.baseRef setting with breaking change notice (v2.1.133)"
  ```

---

### Task A4: MCP — alwaysLoad + CLAUDE_PROJECT_DIR

**File:** `guide/ultimate-guide.md`

- [ ] **Step 1: Read the MCP Configuration Fields table**

  ```bash
  sed -n '13378,13400p' guide/ultimate-guide.md
  ```
  Confirm line 13382 is the MCP server `### Configuration Fields` heading.

- [ ] **Step 2: Add alwaysLoad row to MCP config table**

  In the MCP server configuration fields table, find the last row. Insert:

  ```
  | `alwaysLoad` | boolean | When `true`, all tools from this server skip tool-search deferral and are always available without a `ToolSearch` call first. Use for servers with 1–5 critical tools needed on every turn. (v2.1.121) |
  ```

- [ ] **Step 3: Add alwaysLoad note to the MCP Tool Search section**

  ```bash
  sed -n '13453,13470p' guide/ultimate-guide.md
  ```
  Confirm line 13457 is `#### MCP Tool Search — Lazy-Loading at Scale`. After the opening paragraph of that section, insert:

  ```markdown
  To opt a specific server out of deferral entirely, set `alwaysLoad: true` in its config. Use this for servers with small tool counts (1–5 tools) that you know you'll need every session:

  ```json
  // .claude/settings.json
  {
    "mcpServers": {
      "my-critical-server": {
        "command": "npx",
        "args": ["my-mcp-server"],
        "alwaysLoad": true
      }
    }
  }
  ```
  ```

- [ ] **Step 4: Read the Variable Substitution section**

  ```bash
  sed -n '13426,13445p' guide/ultimate-guide.md
  ```
  Confirm line 13430 is `### Variable Substitution`.

- [ ] **Step 5: Add CLAUDE_PROJECT_DIR to Variable Substitution**

  In the variable substitution table, find the last row. Insert:

  ```
  | `${CLAUDE_PROJECT_DIR}` | Absolute path to the project root (the directory Claude was started from). Auto-injected into `stdio`-type MCP server process environments. Also usable in plugin `command` strings. (v2.1.139) |
  ```

  Then add a prose note:

  ```markdown
  > **MCP stdio env injection**: All `stdio`-type MCP servers automatically receive `CLAUDE_PROJECT_DIR` as an environment variable — no config needed. This lets MCP servers know which project they're operating in without requiring the client to pass it explicitly.
  ```

- [ ] **Step 6: Commit**

  ```bash
  git add guide/ultimate-guide.md
  git commit -m "docs(mcp): add alwaysLoad option and CLAUDE_PROJECT_DIR injection (v2.1.121, v2.1.139)"
  ```

---

### Task A5: Auto Mode — settings.autoMode.hard_deny

**File:** `guide/ultimate-guide.md`

- [ ] **Step 1: Read the Auto Mode section**

  ```bash
  sed -n '1074,1092p' guide/ultimate-guide.md
  ```
  Confirm line 1074 is `### Auto Mode` and line 1083 references the classifier or PermissionDenied hooks.

- [ ] **Step 2: Insert autoMode.hard_deny documentation**

  After the sentence explaining the classifier behavior (around line 1083), insert:

  ```markdown
  **Hard deny rules** (`settings.autoMode.hard_deny`, v2.1.136): Auto mode's classifier can be supplemented with unconditional block rules that fire before the classifier and cannot be overridden by user intent or allow exceptions:

  ```json
  // .claude/settings.json
  {
    "autoMode": {
      "hard_deny": [
        { "tool": "Bash", "pattern": "rm -rf" },
        { "tool": "Write", "pathPattern": "/etc/**" },
        { "tool": "Write", "pathPattern": "**/.env" }
      ]
    }
  }
  ```

  Unlike classifier rules (which weigh user intent), `hard_deny` entries are absolute. Use them for operations that must never be auto-approved regardless of context — production destructive commands, credential files, system config paths.
  ```

- [ ] **Step 3: Commit**

  ```bash
  git add guide/ultimate-guide.md
  git commit -m "docs(auto-mode): add settings.autoMode.hard_deny unconditional block rules (v2.1.136)"
  ```

---

### Task A6: Slash Commands — /goal and /scroll-speed

**File:** `guide/ultimate-guide.md`

- [ ] **Step 1: Read the Built-in Commands table**

  ```bash
  sed -n '8830,8845p' guide/ultimate-guide.md
  ```
  Confirm line 8838 is the end of the Built-in Commands table (last row before the closing `---`).

- [ ] **Step 2: Insert /goal and /scroll-speed rows**

  Before the closing separator after the commands table, insert:

  ```
  | `/goal [condition]` | Set a completion condition. Claude works autonomously across turns until the condition is met, displaying a live overlay with elapsed time, turn count, and token usage. Example: `/goal all tests pass and build is green` (v2.1.139) |
  | `/scroll-speed` | Interactive slider to tune mouse wheel scroll speed. Changes take effect immediately with live preview. (v2.1.139) |
  ```

- [ ] **Step 3: Add a dedicated /goal subsection**

  Find a logical place in the guide near the Agent View section (§9.17, around line 19078). After the Agent View section, insert:

  ```markdown
  ### /goal — Autonomous Completion Mode (v2.1.139)

  `/goal [condition]` sets a natural-language completion condition. Claude keeps working across turns without waiting for your input, stopping only when it believes the condition is satisfied.

  ```
  /goal all unit tests pass and no TypeScript errors
  /goal the PR description is written and the branch is pushed
  /goal the migration is complete and smoke tests pass
  ```

  While `/goal` is active, a status overlay shows:
  - **Elapsed time** — how long the session has been running
  - **Turns used** — number of back-and-forth turns consumed
  - **Tokens used** — running token consumption

  **When to use**: Long-running tasks where you want to step away and return to a finished result rather than babysitting the session. Works best with clear, verifiable conditions ("tests pass") rather than vague ones ("looks good").

  **Cancel**: Send any message to interrupt before the condition is met.
  ```

- [ ] **Step 4: Commit**

  ```bash
  git add guide/ultimate-guide.md
  git commit -m "docs(commands): add /goal autonomous completion mode and /scroll-speed (v2.1.139)"
  ```

---

### Task A7: CLI Reference — New Subcommands and Flags

**File:** `guide/ultimate-guide.md`

- [ ] **Step 1: Read the CLI Subcommands section**

  ```bash
  sed -n '23960,23985p' guide/ultimate-guide.md
  ```
  Confirm line 23967 is `### Subcommands` with a table of `claude <subcommand>` entries. Find the rows for `claude update` and `claude plugin`.

- [ ] **Step 2: Add new subcommands to the table**

  After the `claude plugin` row, insert:

  ```
  | `claude project purge [path]` | Delete all Claude Code state for a project — transcripts, tasks, file history, config entry. Options: `--dry-run`, `-y/--yes`, `-i/--interactive`, `--all`. (v2.1.126) |
  | `claude ultrareview [target]` | Run `/ultrareview` non-interactively from CI/scripts. `target`: PR number, branch, or current branch if omitted. `--json` for machine-readable output. Exits 0 on completion, 1 on failure. (v2.1.120) |
  | `claude plugin prune` | Remove orphaned auto-installed plugin dependencies. Cascade with `claude plugin uninstall --prune`. (v2.1.121) |
  | `claude plugin details <name>` | Show plugin component inventory (skills, agents, commands, hooks, MCP servers) and projected per-session token cost. (v2.1.139) |
  ```

- [ ] **Step 3: Read the MCP & Plugins flags section**

  ```bash
  sed -n '23910,23935p' guide/ultimate-guide.md
  ```
  Confirm line 23914 is the `--plugin-dir` row. Find where to insert `--plugin-url`.

- [ ] **Step 4: Update --plugin-dir and add --plugin-url**

  Update the `--plugin-dir` description to add `.zip` support:

  Find: description ending in "Load plugins from directory for this session only"
  Replace with: "Load plugins from a directory **or `.zip` archive** for this session only. Multiple `--plugin-dir` flags supported. (`.zip` since v2.1.128)"

  After the `--plugin-dir` row, insert:

  ```
  | `--plugin-url <url>` | | Fetch a plugin `.zip` archive from a URL and load it for the current session. Useful for CI pipelines sharing plugins via artifact storage. (v2.1.129) |
  ```

- [ ] **Step 5: Add --channels note**

  Find the `--channels` row (around line 23930, Integration section). If it exists, append to its description: "Works with both claude.ai OAuth and console (API key) auth since v2.1.128. Managed orgs must set `channelsEnabled: true` in managed settings." If the row doesn't exist, add it:

  ```
  | `--channels` | | Enable MCP channels (Research Preview). Supports claude.ai OAuth and API key auth. Managed orgs require `channelsEnabled: true` in managed-settings. (v2.1.128) |
  ```

- [ ] **Step 6: Commit**

  ```bash
  git add guide/ultimate-guide.md
  git commit -m "docs(cli): add project purge, ultrareview CI, plugin prune/details, --plugin-url, --channels (v2.1.120-v2.1.139)"
  ```

---

### Task A8: Environment Variables

**File:** `guide/ultimate-guide.md`

- [ ] **Step 1: Read the Startup Environment Variables section**

  ```bash
  sed -n '23980,24005p' guide/ultimate-guide.md
  ```
  Confirm line 23984 is `### Startup Environment Variables` with a table. Find the last row (line 23998, `CLAUDE_BASH_NO_LOGIN=1`).

- [ ] **Step 2: Insert three new env var rows**

  After `CLAUDE_BASH_NO_LOGIN=1`, insert:

  ```
  | `CLAUDE_CODE_SESSION_ID` | Unique identifier for the current Claude Code session. Passed to all Bash tool subprocess environments. Matches `session_id` in hook stdin JSON. Use for correlating tool output with sessions in observability pipelines. (v2.1.132) |
  | `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN=1` | Opt out of the fullscreen alternate-screen renderer. Terminal output stays in the native scrollback buffer instead of the alternate screen. Use in environments that don't support alternate screen (some log-capture setups, embedded terminals). (v2.1.132) |
  | `CLAUDE_CODE_PACKAGE_MANAGER_AUTO_UPDATE=1` | When set, Homebrew or WinGet auto-upgrades Claude Code in the background and prompts to restart when a new version is available. (v2.1.129) |
  ```

- [ ] **Step 3: Commit**

  ```bash
  git add guide/ultimate-guide.md
  git commit -m "docs(env-vars): add CLAUDE_CODE_SESSION_ID, DISABLE_ALTERNATE_SCREEN, PACKAGE_MANAGER_AUTO_UPDATE (v2.1.129-v2.1.132)"
  ```

---

### Task A9: Cheatsheet Update

**File:** `guide/cheatsheet.md`

- [ ] **Step 1: Read the Essential Commands table**

  ```bash
  sed -n '50,60p' guide/cheatsheet.md
  ```
  Confirm line 53 is the `/ultrareview` row.

- [ ] **Step 2: Insert /goal and /scroll-speed after /ultrareview**

  After the `/ultrareview` row (line 53), insert:

  ```
  | `/goal [condition]` | Autonomous multi-turn mode: Claude works until condition is met, live overlay shows elapsed/turns/tokens (v2.1.139) |
  | `/scroll-speed` | Tune mouse wheel scroll speed with interactive live-preview slider (v2.1.139) |
  ```

- [ ] **Step 3: Add CLI subcommands block**

  Find the CLI flags section (search for `## CLI Flags Quick Reference` or similar). If a subcommands subsection exists, add to it. If not, find a logical place (likely after the CLI flags table) and insert:

  ```markdown
  ## Key CLI Subcommands

  | Command | Description |
  |---------|-------------|
  | `claude project purge [path]` | Delete all Claude Code state for a project (transcripts, tasks, config). `--dry-run` for preview. (v2.1.126) |
  | `claude ultrareview [target]` | Non-interactive cloud code review for CI. `--json` output. Exits 0/1. (v2.1.120) |
  | `claude plugin prune` | Remove orphaned auto-installed plugin deps. (v2.1.121) |
  | `claude plugin details <name>` | Show plugin inventory + token cost estimate. (v2.1.139) |
  | `claude --plugin-url <url>` | Load plugin `.zip` from URL for this session. (v2.1.129) |
  ```

- [ ] **Step 4: Commit**

  ```bash
  git add guide/cheatsheet.md
  git commit -m "docs(cheatsheet): add /goal, /scroll-speed, key CLI subcommands (v2.1.120-v2.1.139)"
  ```

---

### Task A10: reference.yaml Update

**File:** `machine-readable/reference.yaml`

- [ ] **Step 1: Read the commands section**

  ```bash
  sed -n '1248,1310p' machine-readable/reference.yaml
  ```
  Confirm line 1277 is the last entry in `commands:` (`/undo: ...`). Also read the `cli:` section ending around line 1303.

- [ ] **Step 2: Add /goal and /scroll-speed to commands section**

  After the `/undo` entry (line 1277), insert:

  ```yaml
    /goal: "set completion condition; Claude works autonomously until met — live overlay shows elapsed time, turns, and token usage (v2.1.139)"
    /scroll-speed: "tune mouse wheel scroll speed with interactive live-preview slider (v2.1.139)"
  ```

- [ ] **Step 3: Add new subcommands to cli section**

  In the `cli:` block, after the last existing entry (around line 1298), insert:

  ```yaml
    "project purge [path]": "delete all Claude Code state for a project (transcripts, tasks, file history); flags: --dry-run -y -i --all (v2.1.126)"
    "ultrareview [target]": "run /ultrareview non-interactively from CI/scripts; --json; exits 0 on completion 1 on failure (v2.1.120)"
    "plugin prune": "remove orphaned auto-installed plugin dependencies; cascade with plugin uninstall --prune (v2.1.121)"
    "plugin details <name>": "show plugin component inventory (skills, agents, commands, hooks, MCP) and projected per-session token cost (v2.1.139)"
    "--plugin-url <url>": "fetch plugin .zip from URL and load for current session (v2.1.129)"
  ```

- [ ] **Step 4: Add env_vars section**

  After the `cli:` block closes, add a new top-level key:

  ```yaml
  env_vars:
    CLAUDE_CODE_SESSION_ID: "unique session ID injected into all Bash subprocess envs, matches hook stdin session_id field (v2.1.132)"
    CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN: "set to 1 to opt out of fullscreen alternate-screen renderer, keeps output in native scrollback (v2.1.132)"
    CLAUDE_CODE_PACKAGE_MANAGER_AUTO_UPDATE: "set to 1 for auto Homebrew/WinGet upgrades in background with restart prompt (v2.1.129)"
  ```

- [ ] **Step 5: Update mcp section**

  Find the `mcp:` block in reference.yaml (around line 1358). After existing entries, insert:

  ```yaml
    alwaysLoad: "when true in MCP server config, all tools skip tool-search deferral and are always available without ToolSearch call (v2.1.121)"
    CLAUDE_PROJECT_DIR: "auto-injected into stdio MCP server process envs; usable as ${CLAUDE_PROJECT_DIR} in plugin command strings (v2.1.139)"
  ```

- [ ] **Step 6: Add worktree.baseRef to settings**

  Find the settings or worktree config section (or add after the `cli:` block). Insert:

  ```yaml
  worktree_settings:
    baseRef: "fresh|head — controls branch base for --worktree/EnterWorktree/agent-isolation; fresh=origin default, head=local HEAD including unpushed commits; BREAKING default changed to fresh in v2.1.133"
  ```

- [ ] **Step 7: Commit**

  ```bash
  git add machine-readable/reference.yaml
  git commit -m "docs(reference): add /goal, /scroll-speed, project purge, env_vars, alwaysLoad, CLAUDE_PROJECT_DIR, worktree.baseRef"
  ```

---

### Task A11: CHANGELOG Update (Track A)

**File:** `CHANGELOG.md`

- [ ] **Step 1: Add Track A entry to [Unreleased]**

  In the `## [Unreleased]` section under `### Documentation`, insert:

  ```markdown
  - **Feature backfill v2.1.120–v2.1.139** (`guide/ultimate-guide.md`, `guide/cheatsheet.md`, `machine-readable/reference.yaml`): documented 21 missing features from 6 weeks of releases:
    - Hooks: `args[]` exec form, `continueOnBlock` (PostToolUse), `effort.level`/`$CLAUDE_EFFORT` in stdin JSON, `hookSpecificOutput.updatedToolOutput` for all tools (was MCP-only)
    - Skills: `${CLAUDE_EFFORT}` variable in skill content
    - Worktrees: `worktree.baseRef` (`fresh`|`head`) with breaking change notice (v2.1.133)
    - MCP: `alwaysLoad` server config option, `CLAUDE_PROJECT_DIR` env injection
    - Auto mode: `settings.autoMode.hard_deny` unconditional block rules
    - CLI: `/goal`, `/scroll-speed` commands; `claude project purge`, `claude ultrareview CI`, `claude plugin prune/details`; `--plugin-url`, `--channels` console auth, `--plugin-dir .zip`
    - Env vars: `CLAUDE_CODE_SESSION_ID`, `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN`, `CLAUDE_CODE_PACKAGE_MANAGER_AUTO_UPDATE`
  ```

- [ ] **Step 2: Commit**

  ```bash
  git add CHANGELOG.md
  git commit -m "docs(changelog): record feature backfill v2.1.120-v2.1.139"
  ```

---

## TRACK B — Whitepaper Updates

> **Prerequisite**: Track A tasks A1–A10 complete (or run in parallel with awareness of what was added).
> **Note**: WP updates require content writing, not just insertions. Each task below specifies what to add and where, but the agent must write the prose in the WP's existing style (factual, Bold Guy, no em-dashes).

---

### Task B1: Create WP11 EN (Missing Counterpart)

**File:** `whitepapers/en/11-team-metrics.qmd` (CREATE)

- [ ] **Step 1: Read the FR source**

  ```bash
  cat whitepapers/fr/11-team-metrics.qmd
  ```

- [ ] **Step 2: Create the EN version**

  Create `whitepapers/en/11-team-metrics.qmd` as a translation of the FR file with these frontmatter adjustments:

  ```yaml
  ---
  title: "Leading a Team in the Age of AI"
  subtitle: "Metrics, adoption, and governance for engineering managers"
  author: "Florian Bruniaux"
  date: 2026-05-12
  version: "3.40.0"
  wp-version: "1.0.0"
  series: "Claude Code Ultimate Guide"
  lang: en
  ---
  ```

  Translate all body content from FR to EN. Maintain the same section structure. Use the same Bold Guy style: direct, factual, no marketing speak, no em-dashes.

- [ ] **Step 3: Verify frontmatter consistency**

  ```bash
  grep -E "^(version|wp-version|series):" whitepapers/fr/11-team-metrics.qmd
  grep -E "^(version|wp-version|series):" whitepapers/en/11-team-metrics.qmd
  ```
  Both files must have identical `version`, `wp-version`, and `series` values.

- [ ] **Step 4: Commit**

  ```bash
  git add whitepapers/en/11-team-metrics.qmd
  git commit -m "docs(wp11): create EN counterpart of team-metrics whitepaper (v1.0.0)"
  ```

---

### Task B2: WP02 — Customization (Highest drift)

**Files:** `whitepapers/fr/02-personnalisation.qmd` + `whitepapers/en/02-customization.qmd`

WP02 covers memory, skills, MCP, hooks — the sections with the most missing features.

- [ ] **Step 1: Read both WP02 files to understand current structure**

  ```bash
  grep -n "^##\|^###" whitepapers/fr/02-personnalisation.qmd | head -40
  grep -n "^##\|^###" whitepapers/en/02-customization.qmd | head -40
  ```

- [ ] **Step 2: Add alwaysLoad MCP to the MCP section**

  Find the MCP server configuration section. After the existing config table or examples, add a callout:

  **FR** (insert in MCP section):
  ```markdown
  > **`alwaysLoad` (v2.1.121)** : Ajoutez `"alwaysLoad": true` à la config d'un serveur MCP pour que ses outils soient toujours disponibles sans passer par la recherche différée. Idéal pour les serveurs avec 1-5 outils critiques utilisés à chaque session.
  ```

  **EN** (equivalent):
  ```markdown
  > **`alwaysLoad` (v2.1.121)**: Add `"alwaysLoad": true` to an MCP server config to make its tools always available, bypassing deferred tool search. Use for servers with 1–5 critical tools you need every session.
  ```

- [ ] **Step 3: Add ${CLAUDE_EFFORT} to the Skills section**

  Find the skills frontmatter/configuration section. Add after the `effort` field description:

  **FR**:
  ```markdown
  > **`${CLAUDE_EFFORT}` dans le contenu (v2.1.120)** : Le contenu d'un skill peut référencer `${CLAUDE_EFFORT}` pour s'adapter dynamiquement au niveau d'effort actif (`low`, `medium`, `high`, `xhigh`, `max`). Utile pour créer des skills qui font une analyse rapide en effort faible et une analyse approfondie en effort élevé.
  ```

  **EN**:
  ```markdown
  > **`${CLAUDE_EFFORT}` in skill content (v2.1.120)**: Skill body text can reference `${CLAUDE_EFFORT}` to adapt behavior to the active effort level (`low`, `medium`, `high`, `xhigh`, `max`). Build skills that do a quick scan at low effort and a thorough audit at high effort.
  ```

- [ ] **Step 4: Add hook exec form + continueOnBlock to the Hooks section**

  Find the hooks section. After the configuration examples, add:

  **FR**:
  ```markdown
  **Nouveautés v2.1.139** :

  - **Exec form (`args`)** : Utilisez `args: ["programme", "arg1"]` au lieu de `command` pour spawner un programme directement sans shell. Élimine les risques d'injection shell et évite les problèmes de quoting.
  - **`continueOnBlock`** (`PostToolUse`) : Quand `true`, un blocage alimente Claude avec la raison du refus au lieu de stopper le tour. Claude peut alors s'autocorriger.
  ```

  **EN** (equivalent wording in English).

- [ ] **Step 5: Add PostToolUse output replacement**

  **FR**:
  ```markdown
  **Remplacement de sortie d'outil (v2.1.121)** : Les hooks `PostToolUse` peuvent remplacer ce que Claude reçoit comme résultat d'un outil via `hookSpecificOutput.updatedToolOutput`. Fonctionne pour **tous les outils** (Bash, Read, Write, MCP). Cas d'usage : anonymiser les sorties, compresser les résultats volumineux, injecter des métadonnées d'audit.
  ```

  **EN** (equivalent in English).

- [ ] **Step 6: Bump wp-version to 1.3.0 in both files**

  ```bash
  sed -i '' 's/wp-version: "1.2.0"/wp-version: "1.3.0"/' whitepapers/fr/02-personnalisation.qmd
  sed -i '' 's/wp-version: "1.2.0"/wp-version: "1.3.0"/' whitepapers/en/02-customization.qmd
  ```

  Also update `date` to `2026-05-12` in both.

- [ ] **Step 7: Commit**

  ```bash
  git add whitepapers/fr/02-personnalisation.qmd whitepapers/en/02-customization.qmd
  git commit -m "docs(wp02): add alwaysLoad, CLAUDE_EFFORT, hook exec form, continueOnBlock, output replacement — wp-version 1.2.0→1.3.0"
  ```

---

### Task B3: WP03 — Security (Threat DB v2.17.0)

**Files:** `whitepapers/fr/03-securite.qmd` + `whitepapers/en/03-security.qmd`

- [ ] **Step 1: Read the threat DB reference section in both WPs**

  ```bash
  grep -n "v2\.1[0-9]\|threat.db\|CVE\|T0[0-9][0-9]" whitepapers/fr/03-securite.qmd | head -20
  ```

- [ ] **Step 2: Update Threat DB version reference**

  Find all occurrences of `v2.16.0` (the previous version referenced in WP03). Update to `v2.17.0`.

  Find the "Menaces Récentes" / "Recent Threats" section and add after the existing T010-T027 block:

  **FR**:
  ```markdown
  **v2.17.0 (mai 2026)** — 6 nouveaux CVEs, 2 nouvelles techniques d'attaque :
  - T028 : Zero-Click RCE via Prompt Injection (Cymulate, Cursor/AWS Kiro/Codex)
  - T029 : AGENTS.md Supply Chain File Injection (recherche NVIDIA)
  - CVE-2025-66335 : Apache Doris MCP SQL injection
  - CVE-2025-53107 : Git MCP Server command injection
  - CVE-2026-22252 : LibreChat STDIO RCE
  - CVE-2026-22688 : WeKnora STDIO RCE
  - CVE-2025-54994 : @akoskm/create-mcp-server-stdio RCE
  - CVE-2026-26030 : Microsoft AI agent framework tool-chaining RCE
  ```

  **EN** (equivalent in English).

- [ ] **Step 3: Bump wp-version to 1.3.0 in both files**

- [ ] **Step 4: Commit**

  ```bash
  git add whitepapers/fr/03-securite.qmd whitepapers/en/03-security.qmd
  git commit -m "docs(wp03): update threat-db to v2.17.0, add T028/T029 and 6 CVEs — wp-version 1.2.0→1.3.0"
  ```

---

### Task B4: WP04 — Architecture (worktree.baseRef breaking change)

**Files:** `whitepapers/fr/04-architecture.qmd` + `whitepapers/en/04-architecture.qmd`

- [ ] **Step 1: Find the worktree section in WP04**

  ```bash
  grep -n "worktree\|Worktree" whitepapers/fr/04-architecture.qmd | head -10
  ```

- [ ] **Step 2: Add worktree.baseRef breaking change callout**

  **FR**:
  ```markdown
  > **Changement cassant v2.1.133** : `worktree.baseRef` passe en `fresh` par défaut (depuis la branche distante). Si vous avez des commits non poussés à inclure dans le worktree, ajoutez `"worktree": {"baseRef": "head"}` dans `settings.json`.
  ```

  **EN** (equivalent in English).

- [ ] **Step 3: Bump wp-version to 1.3.0 in both files and update date**

- [ ] **Step 4: Commit**

  ```bash
  git add whitepapers/fr/04-architecture.qmd whitepapers/en/04-architecture.qmd
  git commit -m "docs(wp04): add worktree.baseRef breaking change notice — wp-version 1.2.0→1.3.0"
  ```

---

### Task B5: WP05 + WP07 — Team & Reference (CLI additions)

**Files:** WP05 FR+EN, WP07 FR+EN

These WPs are the most command-reference-heavy. Add the new CLI subcommands.

- [ ] **Step 1: Find the CLI commands section in WP05 and WP07**

  ```bash
  grep -n "purge\|ultrareview\|plugin" whitepapers/fr/05-equipe.qmd | head -10
  grep -n "purge\|ultrareview\|plugin\|/goal" whitepapers/fr/07-guide-reference.qmd | head -10
  ```

- [ ] **Step 2: Add new subcommands table rows to WP05 (Team focus)**

  In the team management / CI section, add:

  **FR**:
  ```markdown
  | `claude ultrareview [cible]` | Lancer `/ultrareview` depuis la CI sans interaction. `--json` pour output machine. Retourne 0 (succès) ou 1 (échec). (v2.1.120) |
  | `claude project purge [chemin]` | Supprimer tout l'état Claude Code d'un projet (transcripts, tâches, config). `--dry-run` pour prévisualiser. (v2.1.126) |
  ```

  **EN** (equivalent).

- [ ] **Step 3: Add /goal, /scroll-speed to WP07 commands table**

  WP07 is the reference guide — it has the most complete command tables. Find the slash commands table and add:

  **FR + EN**: add `/goal` and `/scroll-speed` rows matching the style of existing entries.

  Also add the 3 new env vars to the env vars table in WP07:
  - `CLAUDE_CODE_SESSION_ID`
  - `CLAUDE_CODE_DISABLE_ALTERNATE_SCREEN`
  - `CLAUDE_CODE_PACKAGE_MANAGER_AUTO_UPDATE`

- [ ] **Step 4: Bump wp-version in all 4 files**

  WP05: `1.4.0` → `1.5.0`
  WP07: `1.2.0` → `1.3.0`

- [ ] **Step 5: Commit**

  ```bash
  git add whitepapers/fr/05-equipe.qmd whitepapers/en/05-team.qmd \
          whitepapers/fr/07-guide-reference.qmd whitepapers/en/07-reference-guide.qmd
  git commit -m "docs(wp05/wp07): add ultrareview CI, project purge, /goal, env vars — wp-version bumps"
  ```

---

### Task B6: WP09 + WP10 — Version Bump

**Files:** WP09 FR+EN, WP10 FR+EN (content is stable, just version sync)

- [ ] **Step 1: Update version field in all 4 files**

  ```bash
  sed -i '' 's/version: "3\.38\.1"/version: "3.40.0"/' \
    whitepapers/fr/09-apprendre-avec-ia.qmd \
    whitepapers/en/09-learning-with-ai.qmd \
    whitepapers/fr/10-budget-ia.qmd \
    whitepapers/en/10-ai-budget.qmd
  ```

- [ ] **Step 2: Update date field**

  ```bash
  sed -i '' 's/^date: 2026-04-01/date: 2026-05-12/' \
    whitepapers/fr/09-apprendre-avec-ia.qmd \
    whitepapers/en/09-learning-with-ai.qmd
  sed -i '' 's/^date: 2026-03-31/date: 2026-05-12/' \
    whitepapers/fr/10-budget-ia.qmd \
    whitepapers/en/10-ai-budget.qmd
  ```

- [ ] **Step 3: Verify**

  ```bash
  grep -E "^(version|date):" whitepapers/fr/09-apprendre-avec-ia.qmd whitepapers/en/09-learning-with-ai.qmd \
    whitepapers/fr/10-budget-ia.qmd whitepapers/en/10-ai-budget.qmd
  ```
  All four should show `version: "3.40.0"` and `date: 2026-05-12`.

- [ ] **Step 4: Commit**

  ```bash
  git add whitepapers/fr/09-apprendre-avec-ia.qmd whitepapers/en/09-learning-with-ai.qmd \
          whitepapers/fr/10-budget-ia.qmd whitepapers/en/10-ai-budget.qmd
  git commit -m "docs(wp09/wp10): bump version 3.38.1→3.40.0, date updated"
  ```

---

## TRACK C — Recap Cards Mass Sync

> **Independent of Track A and B.** Can run in parallel.

---

### Task C1: FR Recap Cards — Mass Version Bump (52 files)

**Directory:** `whitepapers/recap-cards/fr/`

Target: all `c*.qmd`, `m*.qmd`, `t*.qmd` files currently at `guide-version: "3.38.1"`.

- [ ] **Step 1: Verify current state**

  ```bash
  grep -l 'guide-version: "3.38.1"' whitepapers/recap-cards/fr/*.qmd | wc -l
  ```
  Expected: ~52 files.

- [ ] **Step 2: Mass update guide-version and version fields**

  ```bash
  cd /Users/florianbruniaux/Sites/perso/claude-code-ultimate-guide
  for f in whitepapers/recap-cards/fr/c*.qmd whitepapers/recap-cards/fr/m*.qmd whitepapers/recap-cards/fr/t*.qmd; do
    sed -i '' 's/guide-version: "3\.38\.1"/guide-version: "3.40.0"/' "$f"
    sed -i '' 's/^version: "3\.38\.1"/version: "3.40.0"/' "$f"
    sed -i '' 's/^date: "Mars 2026"/date: "Mai 2026"/' "$f"
  done
  ```

- [ ] **Step 3: Verify**

  ```bash
  grep -l 'guide-version: "3.38.1"' whitepapers/recap-cards/fr/c*.qmd whitepapers/recap-cards/fr/m*.qmd whitepapers/recap-cards/fr/t*.qmd | wc -l
  ```
  Expected: 0 (all updated).

  ```bash
  grep -l 'guide-version: "3.40.0"' whitepapers/recap-cards/fr/c*.qmd | wc -l
  ```
  Expected: ~12.

- [ ] **Step 4: Commit**

  ```bash
  git add whitepapers/recap-cards/fr/
  git commit -m "docs(cards-fr): bump guide-version 3.38.1→3.40.0 across all C/M/T series FR recap cards"
  ```

---

### Task C2: EN Recap Cards — Mass Version Bump (52 files)

**Directory:** `whitepapers/recap-cards/en/`

- [ ] **Step 1: Mass update**

  ```bash
  cd /Users/florianbruniaux/Sites/perso/claude-code-ultimate-guide
  for f in whitepapers/recap-cards/en/c*.qmd whitepapers/recap-cards/en/m*.qmd whitepapers/recap-cards/en/t*.qmd; do
    sed -i '' 's/guide-version: "3\.38\.1"/guide-version: "3.40.0"/' "$f"
    sed -i '' 's/^version: "3\.38\.1"/version: "3.40.0"/' "$f"
    sed -i '' 's/^date: "Mars 2026"/date: "May 2026"/' "$f"
    sed -i '' 's/^date: "March 2026"/date: "May 2026"/' "$f"
  done
  ```

- [ ] **Step 2: Verify**

  ```bash
  grep -l 'guide-version: "3.38.1"' whitepapers/recap-cards/en/c*.qmd \
    whitepapers/recap-cards/en/m*.qmd whitepapers/recap-cards/en/t*.qmd | wc -l
  ```
  Expected: 0.

- [ ] **Step 3: Commit**

  ```bash
  git add whitepapers/recap-cards/en/
  git commit -m "docs(cards-en): bump guide-version 3.38.1→3.40.0 across all C/M/T series EN recap cards"
  ```

---

### Task C3: EN Old-Format Cards — Version Sync with FR

**Files (5 EN cards to update):**
- `whitepapers/recap-cards/en/01-commandes-essentielles.qmd`
- `whitepapers/recap-cards/en/03-permission-modes.qmd`
- `whitepapers/recap-cards/en/04-context-management.qmd`
- `whitepapers/recap-cards/en/06-hierarchie-configuration.qmd`
- `whitepapers/recap-cards/en/25-erreurs-courantes.qmd`

- [ ] **Step 1: Update version fields to match FR counterparts at 3.40.0**

  ```bash
  for f in \
    whitepapers/recap-cards/en/01-commandes-essentielles.qmd \
    whitepapers/recap-cards/en/03-permission-modes.qmd \
    whitepapers/recap-cards/en/04-context-management.qmd \
    whitepapers/recap-cards/en/06-hierarchie-configuration.qmd \
    whitepapers/recap-cards/en/25-erreurs-courantes.qmd; do
    sed -i '' 's/guide-version: "3\.38\.1"/guide-version: "3.40.0"/' "$f"
    sed -i '' 's/^version: "3\.38\.1"/version: "3.40.0"/' "$f"
  done
  ```

- [ ] **Step 2: Verify parity with FR**

  ```bash
  for f in 01-commandes-essentielles 03-permission-modes 04-context-management 06-hierarchie-configuration 25-erreurs-courantes; do
    fr_ver=$(grep "^guide-version:" whitepapers/recap-cards/fr/${f}.qmd)
    en_ver=$(grep "^guide-version:" whitepapers/recap-cards/en/${f}.qmd)
    echo "${f}: FR=$fr_ver | EN=$en_ver"
  done
  ```
  All pairs should show matching `3.40.0`.

- [ ] **Step 3: Commit**

  ```bash
  git add whitepapers/recap-cards/en/01-commandes-essentielles.qmd \
          whitepapers/recap-cards/en/03-permission-modes.qmd \
          whitepapers/recap-cards/en/04-context-management.qmd \
          whitepapers/recap-cards/en/06-hierarchie-configuration.qmd \
          whitepapers/recap-cards/en/25-erreurs-courantes.qmd
  git commit -m "docs(cards-en): sync old-format cards version 3.38.1→3.40.0 to match FR counterparts"
  ```

---

### Task C4: Priority Cards Content Update

**5 cards with high content drift** (FR + EN for each):

| Card | Missing content |
|------|----------------|
| `m09-slash-commands` | `/goal`, `/scroll-speed` |
| `m11-hooks-evenements-systeme` | exec form `args[]`, `continueOnBlock` |
| `m12-hooks-patterns-concrets` | `PostToolUse` output replacement |
| `m13-worktrees` | `worktree.baseRef` breaking change |
| `t12-mcp-servers-overview` | `alwaysLoad` option |

- [ ] **Step 1: Update m09-slash-commands FR + EN**

  Read current content:
  ```bash
  grep -n "/usage\|/ultrareview\|/effort" whitepapers/recap-cards/fr/m09-slash-commands.qmd | head -10
  ```
  Add `/goal` and `/scroll-speed` to the commands table in the same style as existing entries. Do the same for the EN version.

- [ ] **Step 2: Update m11-hooks-evenements-systeme FR + EN**

  Find the hook config fields section. Add `args` (exec form) row. Add `continueOnBlock` under PostToolUse. Same for EN.

- [ ] **Step 3: Update m12-hooks-patterns-concrets FR + EN**

  Find the PostToolUse patterns section. Add an `updatedToolOutput` example pattern showing tool output replacement. Same for EN.

- [ ] **Step 4: Update m13-worktrees FR + EN**

  Find the worktree configuration section. Add `worktree.baseRef` with the breaking change callout. Same for EN.

- [ ] **Step 5: Update t12-mcp-servers-overview FR + EN**

  Find the MCP server config section. Add `alwaysLoad: true` as a config option. Same for EN.

- [ ] **Step 6: Commit**

  ```bash
  git add whitepapers/recap-cards/fr/m09-slash-commands.qmd \
          whitepapers/recap-cards/en/m09-slash-commands.qmd \
          whitepapers/recap-cards/fr/m11-hooks-evenements-systeme.qmd \
          whitepapers/recap-cards/en/m11-hooks-evenements-systeme.qmd \
          whitepapers/recap-cards/fr/m12-hooks-patterns-concrets.qmd \
          whitepapers/recap-cards/en/m12-hooks-patterns-concrets.qmd \
          whitepapers/recap-cards/fr/m13-worktrees.qmd \
          whitepapers/recap-cards/en/m13-worktrees.qmd \
          whitepapers/recap-cards/fr/t12-mcp-servers-overview.qmd \
          whitepapers/recap-cards/en/t12-mcp-servers-overview.qmd
  git commit -m "docs(priority-cards): add missing features to m09 m11 m12 m13 t12 (FR+EN)"
  ```

---

## Final Tasks

### Task F1: Version sync check

- [ ] **Step 1: Run sync check**

  ```bash
  ./scripts/sync-version.sh --check
  ```
  Fix any mismatches found.

- [ ] **Step 2: Push all commits**

  ```bash
  git push
  ```

### Task F2: Post-completion verification

- [ ] Confirm `/goal` appears in cheatsheet: `grep "/goal" guide/cheatsheet.md`
- [ ] Confirm `worktree.baseRef` in guide: `grep "worktree.baseRef" guide/ultimate-guide.md`
- [ ] Confirm `alwaysLoad` in guide: `grep "alwaysLoad" guide/ultimate-guide.md`
- [ ] Confirm WP11 EN exists: `ls whitepapers/en/11-team-metrics.qmd`
- [ ] Confirm no c/m/t card still at 3.38.1: `grep -rl "3\.38\.1" whitepapers/recap-cards/ | wc -l` → expected 0
- [ ] Confirm CHANGELOG updated: `grep "backfill v2.1.120" CHANGELOG.md`

---

## Parallelization Map

```
Immediately parallelizable:
  Track A (A1→A11)    ─────────────────────────────────────────── sequential
  Track C (C1,C2,C3)  ─────────── parallel to each other and to A
  Track B Task B6     ─────────── parallel (pure version bump, no content)

After Track A complete (or with knowledge of A content):
  Track B (B1,B2,B3,B4,B5) ── sequential within B, parallel to C

Final:
  Track B Task C4 (priority cards content) ── after C1/C2 complete
  Task F1 + F2 ── after all tracks complete
```

**Suggested 3-agent dispatch:**
- Agent 1: Track A (A1 through A11)
- Agent 2: Track C (C1, C2, C3) + Track B Task B6
- Agent 3: Track B (B1 through B5) — can start B1 (WP11 creation) immediately, others after A finishes

---

## Self-Review

**Spec coverage:**
- ✅ All 21 missing features from v2.1.120–v2.1.139 have tasks in Track A
- ✅ WP11 EN creation covered (B1)
- ✅ All 108 stale recap cards have a mass bump task (C1, C2)
- ✅ 5 EN old-format parity cards covered (C3)
- ✅ 5 priority cards content drift covered (C4)
- ✅ WP drift in WP02-WP07 covered (B2-B5)
- ✅ WP09/WP10 version bump covered (B6)

**No placeholders:** All insertion content is specified with exact text. No "add appropriate content here" patterns.

**Type consistency:** File paths are consistent across tasks. Line numbers are from the agent audit (verify with `sed -n` steps before editing).

**CHANGELOG:** Updated in A11 only — no duplicate entries.
