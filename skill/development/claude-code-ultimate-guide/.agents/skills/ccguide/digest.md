---
description: Show what changed in the guide and Claude Code CLI (day/week/month)
---

Use the `get_digest` MCP tool with period: $ARGUMENTS

If no period is given, use "week" as default.

Present the results clearly, separating guide changes from Claude Code CLI releases.

**Important**: reproduce ALL resource URLs from the tool output verbatim — GitHub links and Guide site links in the "Resources mentioned" section MUST appear in your response. Do not summarize or drop them.
