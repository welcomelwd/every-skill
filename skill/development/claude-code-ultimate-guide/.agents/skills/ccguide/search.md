---
description: Search the Claude Code Ultimate Guide
---

Use the `search_guide` MCP tool with this query: $ARGUMENTS

Show the results with scores, locations, and GitHub links. For any result with a file location, follow up with `read_section` to show the relevant content.

Respond in the same language the user used to invoke this command.
