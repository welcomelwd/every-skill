---
description: Show recent guide CHANGELOG entries
---

Use the `get_changelog` MCP tool.

<% if (typeof $ARGUMENTS !== 'undefined' && $ARGUMENTS.trim()) { %>
Count: $ARGUMENTS
<% } else { %>
Show last 5 entries.
<% } %>

Reproduce ALL resource URLs from the tool output verbatim — GitHub links and Guide site links in the "Resources mentioned" section MUST appear in your response.
