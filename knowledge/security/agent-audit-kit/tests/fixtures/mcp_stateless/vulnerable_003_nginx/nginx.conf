# Vulnerable: sticky routing assumed by an MCP server deployment.
upstream mcp_backend {
    ip_hash;
    server mcp-1.internal:8080;
    server mcp-2.internal:8080;
}

server {
    listen 443 ssl;
    location /mcp {
        proxy_pass http://mcp_backend;
    }
}
