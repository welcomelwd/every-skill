name: Release

on:
  workflow_dispatch:
    inputs:
      package:
        description: 'Library to release'
        required: true
        type: choice
        options:
          - giskard
          - giskard-core
          - giskard-llm
          - giskard-agents
          - giskard-checks
          - giskard-scan
      bump_type:
        description: 'Bump the release number, e.g. 1.2.3 -> 2.0.0 / 1.3.0 / 1.2.4. Combine with alpha/beta/rc below to open a new pre-release line (1.2.3 + minor + rc -> 1.3.0rc1). Leave as none to only move the pre-release, or when release_type is stable.'
        required: true
        type: choice
        options:
          - major
          - minor
          - patch
          - none
        default: none
      release_type:
        description: 'alpha/beta/rc starts or advances a pre-release (1.2.3rc1 -> 1.2.3rc2). stable drops the suffix to publish the final version (1.2.3rc2 -> 1.2.3) and requires bump_type=none. Leave as none to publish a plain version bump.'
        required: true
        type: choice
        options:
          - alpha
          - beta
          - rc
          - stable
          - none
        default: none
      dry_run:
        description: 'If true, the release will not be published to PyPI'
        required: false
        type: boolean
        default: true
      run_functional_tests:
        description: 'If true, functional/integration tests will run before release'
        required: false
        type: boolean
        default: true

permissions: {}

concurrency:
  group: release-${{ github.event.inputs.package }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    name: release
    runs-on: ubuntu-latest
    permissions:
      contents: write # push commits, tags, and create releases
      id-token: write # PyPI trusted publishing (OIDC)
    environment: release
    env:
      PACKAGE: ${{ github.event.inputs.package }}
      BUMP_TYPE: ${{ github.event.inputs.bump_type }}
      RELEASE_TYPE: ${{ github.event.inputs.release_type }}
    steps:
      - name: Authorize
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ACTOR: ${{ github.actor }}
        run: |
          if ! gh api "orgs/Giskard-AI/members/$ACTOR" --silent 2>/dev/null; then
            echo "::error::Only Giskard-AI organization members can create releases."
            exit 1
          fi

      - name: Generate app token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        with:
          app-id: ${{ vars.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}

      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          fetch-tags: true
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9 # v9.0.0
        with:
          enable-cache: false
          python-version: "3.12"

      - name: Setup development environment
        run: make setup

      - name: Run checks
        run: make check

      - name: Run unit tests
        run: make test-unit

      # OpenAI credentials; TEST_PROVIDER=openai makes libs/giskard-llm/tests/conftest.py
      # skip non-OpenAI provider tests (bare is included via groups). Full `make test` still
      # runs other workspace packages—only giskard-llm reads TEST_PROVIDER for now.
      - name: Install OpenAI extras for functional tests
        if: ${{ github.event.inputs.run_functional_tests == 'true' }}
        run: |
          uv pip install "giskard-llm[openai]"
          uv pip install "giskard-agents[openai]"
      - name: Run functional tests
        if: ${{ github.event.inputs.run_functional_tests == 'true' }}
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          TEST_MODEL: "openai/gpt-4.1-nano"
          TEST_PROVIDER: openai
          TEST_LITELLM_MODEL: "openai/gpt-4.1-nano"
          TEST_EMBEDDING_MODEL: "openai/text-embedding-3-small"
        run: |
          make test-functional

      - name: Resolve package paths
        id: pkg
        run: |
          if [ "$PACKAGE" = "giskard" ]; then
            echo "dir=." >> "$GITHUB_OUTPUT"
          else
            echo "dir=libs/${PACKAGE}" >> "$GITHUB_OUTPUT"
          fi

      - name: Bump version
        id: bump
        working-directory: ${{ steps.pkg.outputs.dir }}
        run: |
          CMD=("uv" "version")

          if [ "$BUMP_TYPE" != "none" ]; then
            CMD+=("--bump" "$BUMP_TYPE")
          fi

          if [ "$RELEASE_TYPE" != "none" ] && [ -n "$RELEASE_TYPE" ]; then
            CMD+=("--bump" "$RELEASE_TYPE")
          fi

          if [ ${#CMD[@]} -gt 2 ]; then
            "${CMD[@]}"
          else
            echo "No version bump"
            exit 1
          fi

          NEW_VERSION=$(uv version --short)
          echo "new_version=$NEW_VERSION" >> $GITHUB_OUTPUT

          # The meta package is tagged vX.Y.Z, continuing the scheme used up to
          # v2.19.2, so downstream packagers that scrape tags keep matching it.
          # Workspace members are namespaced to keep their version lines distinct.
          if [ "$PACKAGE" = "giskard" ]; then
            echo "tag=v$NEW_VERSION" >> $GITHUB_OUTPUT
          else
            echo "tag=$PACKAGE/v$NEW_VERSION" >> $GITHUB_OUTPUT
          fi

      - name: Update lockfile
        run: uv lock

      - name: Build package
        working-directory: ${{ steps.pkg.outputs.dir }}
        run: |
          rm -rf dist
          uv build --out-dir dist

      - name: Commit version bump
        env:
          NEW_VERSION: ${{ steps.bump.outputs.new_version }}
          PKG_DIR: ${{ steps.pkg.outputs.dir }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add "$PKG_DIR/pyproject.toml" "uv.lock"
          git commit -m "chore($PACKAGE): bump version to $NEW_VERSION"

      - name: Push to main
        if: ${{ github.event.inputs.dry_run == 'false' && github.ref_name == 'main' }}
        env:
          APP_TOKEN: ${{ steps.app-token.outputs.token }}
          REPO: ${{ github.repository }}
        run: |
          git remote set-url origin "https://x-access-token:${APP_TOKEN}@github.com/${REPO}.git"
          git push origin main

      - name: Create tag
        if: ${{ github.event.inputs.dry_run == 'false' }}
        env:
          APP_TOKEN: ${{ steps.app-token.outputs.token }}
          REPO: ${{ github.repository }}
          TAG: ${{ steps.bump.outputs.tag }}
        run: |
          git remote set-url origin "https://x-access-token:${APP_TOKEN}@github.com/${REPO}.git"
          git tag "$TAG"
          git push origin "$TAG"


      - name: Create GitHub Release
        if: ${{ github.event.inputs.dry_run == 'false' }}
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.bump.outputs.tag }}
          NEW_VERSION: ${{ steps.bump.outputs.new_version }}
          PKG_DIR: ${{ steps.pkg.outputs.dir }}
        run: |
          gh release create "$TAG" \
            --title "$PACKAGE v$NEW_VERSION" \
            --generate-notes \
            "$PKG_DIR"/dist/*.tar.gz \
            "$PKG_DIR"/dist/*.whl

      - name: Push to PyPI
        if: ${{ github.event.inputs.dry_run == 'false' }}
        working-directory: ${{ steps.pkg.outputs.dir }}
        run: uv publish
