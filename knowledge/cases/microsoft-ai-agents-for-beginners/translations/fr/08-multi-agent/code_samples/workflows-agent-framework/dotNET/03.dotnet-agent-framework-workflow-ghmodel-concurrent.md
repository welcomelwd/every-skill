# ⚡ Flux de travail d’agents concurrents avec Azure OpenAI (API Réponses) (.NET)

## 📋 Tutoriel de traitement parallèle haute performance

Ce carnet illustre les **patrons de flux de travail concurrents** en utilisant le Microsoft Agent Framework pour .NET et Azure OpenAI (API Réponses). Vous apprendrez à construire des flux de travail de traitement parallèle haute performance qui maximisent le débit en exécutant plusieurs agents IA simultanément tout en maintenant la coordination et la cohérence des données.

## 🎯 Objectifs d’apprentissage

### 🚀 **Fondamentaux du traitement concurrent**
- **Exécution parallèle des agents** : Exécutez plusieurs agents IA simultanément pour une performance maximale
- **Modèles Async/Await** : Utilisez le modèle de programmation asynchrone de .NET pour une concurrence efficace
- **Azure OpenAI (API Réponses)** : Coordonnez plusieurs appels concurrents à l’API Réponses Azure OpenAI
- **Gestion des ressources** : Gérez efficacement les ressources des modèles IA lors des opérations concurrentes

### 🏗️ **Architecture avancée de la concurrence**
- **Parallélisme basé sur les tâches** : Utilisez la Task Parallel Library de .NET pour une exécution concurrente optimale
- **Patrons de synchronisation** : Coordonnez les agents concurrents tout en évitant les conditions de course
- **Équilibrage de charge** : Répartissez efficacement le travail sur la capacité de traitement concurrente disponible
- **Tolérance aux pannes** : Gérez les échecs d’agents individuels sans interrompre le flux complet

### 🏢 **Applications d’entreprise concurrentes**
- **Traitement de documents à fort volume** : Traitez plusieurs documents simultanément
- **Analyse de contenu en temps réel** : Analyse concurrente des flux de données entrants
- **Optimisation du traitement par lots** : Maximisez le débit pour les opérations de traitement de données à grande échelle
- **Analyse multi-modale** : Traitement parallèle de différents types et formats de contenu

## ⚙️ Prérequis et configuration

### 📦 **Packages NuGet requis**

Packages essentiels pour des flux de travail concurrents haute performance :

```xml
<!-- Core AI Framework with Async Support -->
<PackageReference Include="Microsoft.Extensions.AI" Version="10.*" />

<!-- Azure OpenAI (Responses API) -->
<PackageReference Include="Azure.AI.OpenAI" Version="2.*" />

<!-- Azure Identity and Async LINQ for Advanced Operations -->
<PackageReference Include="Azure.Identity" Version="1.15.0" />
<PackageReference Include="System.Linq.Async" Version="6.0.3" />

<!-- Local Agent Framework References -->
<!-- Microsoft.Agents.AI.dll - Core agent abstractions with async support -->
<!-- Microsoft.Agents.AI.OpenAI.dll - Azure OpenAI (Responses API) integration with concurrency -->
```

### 🔑 **Configuration Azure OpenAI**

**Configuration de l’environnement (fichier .env) :**
```env
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-5-mini
```

**Considérations sur le traitement concurrent :**
```csharp
// Configure for concurrent operations
var clientOptions = new AzureOpenAIClientOptions()
{
    // Configure network timeout for concurrent requests
    NetworkTimeout = TimeSpan.FromMinutes(5)
};
```

### 🏗️ **Architecture du flux de travail concurrent**

```mermaid
graph TD
    A[Entrée du flux de travail] --> B[Répartition des tâches]
    B --> C[Pool d'agents concurrents]
    C --> D[Tâche de l'agent 1]
    C --> E[Tâche de l'agent 2]
    C --> F[Tâche de l'agent 3]
    C --> G[Tâche de l'agent N]
    
    D --> H[Agrégation des résultats]
    E --> H
    F --> H
    G --> H
    
    H --> I[Sortie synchronisée]
    
    J[Azure OpenAI (API des réponses)] --> D
    J --> E
    J --> F
    J --> G
    
    K[Planificateur de tâches .NET] --> C
```

**Composants clés :**
- **Task Parallel Library** : Support intégré de .NET pour opérations concurrentes
- **Pool d’agents** : Plusieurs instances d’agents pour un traitement parallèle
- **Agrégation des résultats** : Coordination et fusion des résultats des agents concurrents
- **Points de synchronisation** : Assurent la cohérence des données à travers les opérations concurrentes

## 🎨 **Patrons de conception des flux de travail concurrents**

### 🔍 **Recherche et analyse parallèles**
```
Research Topic → Concurrent Research Agents → Result Synthesis → Final Report
```

### 📊 **Traitement de données multi-sources**
```
Data Sources → Parallel Processing Agents → Data Integration → Unified Output
```

### 🎭 **Pipeline de génération de contenu**
```
Content Requirements → Concurrent Content Generators → Quality Review → Final Content
```

### 🔄 **Traitement Fan-Out/Fan-In**
```
Single Input → Multiple Concurrent Processors → Result Aggregation → Single Output
```

## 🏢 **Avantages de la performance en entreprise**

### ⚡ **Débit et évolutivité**
- **Évolutivité linéaire** : Ajoutez plus d’agents concurrents pour augmenter le débit
- **Utilisation des ressources** : Efficacité maximale de la capacité des modèles IA disponibles
- **Réduction du temps de traitement** : Réduction significative du temps grâce à l’exécution parallèle
- **Élasticité** : Ajuste dynamiquement le nombre d’agents concurrents en fonction de la charge

### 🛡️ **Fiabilité et résilience**
- **Isolation des pannes** : Les pannes d’agents individuels n’affectent pas les autres opérations concurrentes
- **Dégradation contrôlée** : Le système continue de fonctionner avec une capacité d’agents réduite
- **Récupération d’erreur** : Mécanismes de nouvelle tentative automatique pour les opérations concurrentes échouées
- **Distribution de la charge** : Répartition équitable du travail entre les agents disponibles

### 📊 **Surveillance de la performance**
- **Mesures d’exécution concurrente** : Suivez la performance de toutes les opérations parallèles
- **Analyse de l’utilisation des ressources** : Surveillez CPU, mémoire et réseau
- **Analyse du débit** : Mesurez les gains d’efficacité liés au traitement concurrent
- **Détection des goulets d’étranglement** : Identifiez et résolvez les limitations de performance

### 🔧 **Développement & opérations**
- **Modèle de programmation asynchrone** : Bénéficiez des patrons matures async/await de .NET
- **Coordination des tâches** : Capacités intégrées de gestion et coordination de tâches
- **Gestion des exceptions** : Gestion complète des erreurs dans les opérations concurrentes
- **Support de débogage** : Outils de débogage Visual Studio pour flux de travail concurrents

Construisons des flux de travail IA concurrents haute performance avec .NET ! 🚀

## 💻 Exécution du code

L’implémentation complète est disponible dans `03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs`. Ce fichier démontre un **flux de travail concurrent Fan-Out/Fan-In** pour la planification de voyage :

### 🏗️ **Architecture du flux de travail**

```
User Request → ConcurrentStartExecutor → [Researcher Agent || Planner Agent] → ConcurrentAggregationExecutor → Final Output
```

**Composants clés :**

1. **ConcurrentStartExecutor** : Diffuse la requête utilisateur à tous les agents simultanément
2. **Agent Chercheur** : Analyse destinations et attractions de manière concurrente
3. **Agent Planificateur** : Crée des plans de voyage détaillés en parallèle
4. **ConcurrentAggregationExecutor** : Collecte et fusionne les résultats des deux agents

### 🎯 **Patron Fan-Out/Fan-In**

Ce flux de travail illustre le patron classique **Fan-Out/Fan-In** :
- **Fan-Out** : Un message d’entrée est envoyé en diffusion à plusieurs agents simultanément
- **Traitement concurrent** : Plusieurs agents travaillent en parallèle sur la même tâche
- **Fan-In** : Les résultats de tous les agents sont collectés et agrégés en une sortie unique

### 🚀 Exécution de l’exemple

```bash
# Rendre le script exécutable (Unix/Linux/macOS)
chmod +x 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs

# Exécuter le flux de travail concurrent
./03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

Ou sous Windows :
```powershell
dotnet run 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

### 📝 Résultat attendu

Le flux de travail va :
1. **Diffuser la requête** : Envoyer « Planifiez un voyage à Seattle en décembre » aux deux agents
2. **Traitement concurrent** : Les deux agents travaillent simultanément :
   - Le Chercheur identifie attractions et détails
   - Le Planificateur crée itinéraire et logistique
3. **Agrégation** : Combiner les deux réponses en une sortie complète
4. **Afficher les résultats** : Afficher le plan de voyage fusionné avec toutes les informations

### 🔧 Options de personnalisation

**Ajouter plus d’agents concurrents :**
```csharp
// Create additional specialized agents
AIAgent budgetAgent = azureClient.GetChatClient(deployment).AsAIAgent(
    name: "Budget-Agent", instructions: "Calculate travel costs...");

// Add to fan-out
var workflow = new WorkflowBuilder(startExecutor)
    .AddFanOutEdge(startExecutor, targets: [researcherAgent, plannerAgent, budgetAgent])
    .AddFanInBarrierEdge(sources: [researcherAgent, plannerAgent, budgetAgent], aggregationExecutor)
    .WithOutputFrom(aggregationExecutor)
    .Build();

// Update aggregation count
if (this._messages.Count == 3) { ... }
```

**Modifier les instructions des agents :**
```csharp
const string ResearcherAgentInstructions = "Your custom instructions for research...";
const string PlanAgentInstructions = "Your custom instructions for planning...";
```

**Changer la tâche :**
```csharp
StreamingRun run = await InProcessExecution.RunStreamingAsync(
    workflow, 
    "Plan a European vacation for 2 weeks in summer"
);
```

### 🎯 Applications réelles

Ce patron concurrent est idéal pour :
- **Création de contenu** : Plusieurs rédacteurs créant différentes sections simultanément
- **Revue de code** : Plusieurs réviseurs analysant le code sous différents angles
- **Études de marché** : Analyses parallèles de différents segments de marché
- **Traitement de documents** : Extraction, analyse, et validation concurrentes
- **Analyse multi-perspectives** : Obtenir des points de vue divers sur la même entrée

### 🔍 Comprendre les exécuteurs personnalisés

**ConcurrentStartExecutor :**
- Implémente `IMessageHandler<string>` pour accepter une entrée chaîne
- Diffuse les messages à tous les agents connectés
- Envoie `TurnToken` pour déclencher le traitement concurrent

**ConcurrentAggregationExecutor :**
- Implémente `IMessageHandler<ChatMessage>` pour recevoir les réponses des agents
- Collecte les messages de manière thread-safe
- Agrège quand toutes les réponses attendues arrivent
- Produit la sortie finale via `context.YieldOutputAsync()`

### ⚡ Avantages de performance

**Concurrent vs séquentiel :**
- Séquentiel : Agent1 (30s) → Agent2 (30s) = **60 secondes au total**
- Concurrent : Agent1 (30s) || Agent2 (30s) = **30 secondes au total**

**Amélioration du débit** : Jusqu’à N× plus rapide pour N agents concurrents (selon charge et ressources)

### 🛡️ Gestion des erreurs

Le flux gère les pannes d’agents individuels avec élégance :
- Si un agent échoue, les autres continuent le traitement
- L’agrégateur peut implémenter une logique de timeout
- Des résultats partiels peuvent être renvoyés si nécessaire

### 📊 Fonctionnalités avancées

**Nombre d’agents dynamique :**
Modifiez la logique d’agrégation pour supporter un nombre variable d’agents :

```csharp
private int _expectedAgentCount;
private readonly List<ChatMessage> _messages = [];

public override async ValueTask HandleAsync(List<ChatMessage> message, IWorkflowContext context, CancellationToken cancellationToken = default)
{
    this._messages.AddRange(message);
    if (this._messages.Count == _expectedAgentCount)
    {
        // Process aggregation
    }
}
```

Ce patron de flux concurrent est essentiel pour construire des systèmes d’agents IA à haute performance et évolutifs !

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Avertissement** :
Ce document a été traduit à l'aide du service de traduction automatique [Co-op Translator](https://github.com/Azure/co-op-translator). Bien que nous nous efforçions d'assurer l'exactitude, veuillez noter que les traductions automatisées peuvent contenir des erreurs ou des inexactitudes. Le document original dans sa langue native doit être considéré comme la source faisant autorité. Pour les informations critiques, il est recommandé de recourir à une traduction professionnelle réalisée par un humain. Nous ne saurions être tenus responsables des malentendus ou erreurs d'interprétation découlant de l'utilisation de cette traduction.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->