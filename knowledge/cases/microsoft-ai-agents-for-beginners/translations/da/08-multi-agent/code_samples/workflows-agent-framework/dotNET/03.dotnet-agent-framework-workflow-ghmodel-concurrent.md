# ⚡ Samtidige Agentarbejdsgange med Azure OpenAI (Svar-API) (.NET)

## 📋 Tutorial i højtydende parallelbearbejdning

Denne notesbog demonstrerer **samtidige arbejdsgangsmønstre** ved hjælp af Microsoft Agent Framework for .NET og Azure OpenAI (Svar-API). Du vil lære, hvordan man bygger højtydende, parallelle arbejdsgange, der maksimerer gennemløb ved at køre flere AI-agenter samtidig, mens der opretholdes koordinering og datakonsistens.

## 🎯 Læringsmål

### 🚀 **Grundlæggende i samtidig behandling**
- **Parallelt agentkørsel**: Kør flere AI-agenter samtidig for maksimal ydeevne
- **Async/Await-mønstre**: Udnyt .NET's asynkrone programmeringsmodel til effektiv samtidig kørsel
- **Azure OpenAI (Svar-API)**: Koordiner flere samtidige kald til Azure OpenAI Svar-API
- **Ressourcestyring**: Effektiv håndtering af AI-modelressourcer på tværs af samtidige operationer

### 🏗️ **Avanceret samtidighedsarkitektur**
- **Opgavebaseret parallelisme**: Brug .NET Task Parallel Library til optimal samtidig udførelse
- **Synkroniseringsmønstre**: Koordiner samtidige agenter og undgå race conditions
- **Loadbalancering**: Fordel arbejde effektivt på tilgængelig samtidig behandlingskapacitet
- **Fejltolerance**: Håndter fejl i individuelle agenter uden at stoppe hele arbejdsgangen

### 🏢 **Virksomheds samtidige applikationer**
- **Højvolumen dokumentbehandling**: Behandl flere dokumenter samtidig
- **Real-time indholdsanalyse**: Samtidig analyse af indkommende datastreams
- **Batch-behandlingsoptimering**: Maksimer gennemløb for storskala databehandlingsoperationer
- **Multi-modal analyse**: Parallel behandling af forskellige indholdstyper og formater

## ⚙️ Forudsætninger & opsætning

### 📦 **Nødvendige NuGet-pakker**

Vigtige pakker til højtydende samtidige arbejdsgange:

```xml
<!-- Core AI Framework with Async Support -->
<PackageReference Include="Microsoft.Extensions.AI" Version="9.9.0" />

<!-- Azure OpenAI (Responses API) -->
<PackageReference Include="Azure.AI.OpenAI" Version="2.1.0" />

<!-- Azure Identity and Async LINQ for Advanced Operations -->
<PackageReference Include="Azure.Identity" Version="1.15.0" />
<PackageReference Include="System.Linq.Async" Version="6.0.3" />

<!-- Local Agent Framework References -->
<!-- Microsoft.Agents.AI.dll - Core agent abstractions with async support -->
<!-- Microsoft.Agents.AI.OpenAI.dll - Azure OpenAI (Responses API) integration with concurrency -->
```

### 🔑 **Azure OpenAI-konfiguration**

**Miljøopsætning (.env-fil):**
```env
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com
AZURE_OPENAI_DEPLOYMENT=gpt-5-mini
```

**Overvejelser ved samtidig behandling:**
```csharp
// Configure for concurrent operations
var clientOptions = new AzureOpenAIClientOptions()
{
    // Configure network timeout for concurrent requests
    NetworkTimeout = TimeSpan.FromMinutes(5)
};
```

### 🏗️ **Samtidig arbejdsgangsarkitektur**

```mermaid
graph TD
    A[Arbejdsgangsinput] --> B[Opgavefordeling]
    B --> C[Samtidig agentpulje]
    C --> D[Agentopgave 1]
    C --> E[Agentopgave 2]
    C --> F[Agentopgave 3]
    C --> G[Agentopgave N]
    
    D --> H[Resultatopsamling]
    E --> H
    F --> H
    G --> H
    
    H --> I[Synkroniseret output]
    
    J[Azure OpenAI (Respons-API)] --> D
    J --> E
    J --> F
    J --> G
    
    K[.NET Opgaveplanlægger] --> C
```

**Vigtige komponenter:**
- **Task Parallel Library**: .NETs indbyggede støtte til samtidige operationer
- **Agentpool**: Flere agentinstanser til parallel behandling
- **Resultataggregation**: Koordinering og sammenlægning af samtidige agentresultater
- **Synkroniseringspunkter**: Sikrer datakonsistens på tværs af samtidige operationer

## 🎨 **Mønstre for samtidig arbejdsgangsdesign**

### 🔍 **Parallel forskning & analyse**
```
Research Topic → Concurrent Research Agents → Result Synthesis → Final Report
```

### 📊 **Databehandling fra flere kilder**
```
Data Sources → Parallel Processing Agents → Data Integration → Unified Output
```

### 🎭 **Indholdsgenereringspipeline**
```
Content Requirements → Concurrent Content Generators → Quality Review → Final Content
```

### 🔄 **Fan-Out/Fan-In behandling**
```
Single Input → Multiple Concurrent Processors → Result Aggregation → Single Output
```

## 🏢 **Virksomhedens ydelsesfordele**

### ⚡ **Gennemløb & skalerbarhed**
- **Lineær ydelsesskalering**: Tilføj flere samtidige agenter for øget gennemløb
- **Ressourceudnyttelse**: Maksimal effektivitet af tilgængelig AI-modelkapacitet
- **Reduceret behandlingstid**: Betydelig tidsreduktion via parallel udførelse
- **Elastisk skalering**: Dynamisk justering af antal samtidige agenter baseret på arbejdsbyrde

### 🛡️ **Pålidelighed & modstandsdygtighed**
- **Fejisolation**: Fejl i enkelte agenter påvirker ikke andre samtidige operationer
- **Graciøs nedtrapning**: Systemet fortsætter med reduceret agentkapacitet
- **Fejlgenopretning**: Automatisk genforsøgslogik for fejlede samtidige operationer
- **Arbejdsfordeling**: Jævn fordeling af arbejde blandt tilgængelige agenter

### 📊 **Ydelsesovervågning**
- **Samtidige udførelsesmetriker**: Overvåg ydeevne for alle parallelle operationer
- **Ressourcebrugsanalyser**: Monitorer CPU, hukommelse og netværksudnyttelse
- **Gennemløbsanalyse**: Mål effektivitetstjenester ved samtidig behandling
- **Flaskehalsopdagelse**: Identificer og løs ydelsesbegrænsninger

### 🔧 **Udvikling & drift**
- **Asynkront programmeringsmodel**: Udnyt .NETs modne async/await-mønstre
- **Opgavekoordinering**: Indbyggede opgavestyrings- og koordineringsfunktionaliteter
- **Undtagelseshåndtering**: Omfattende fejlhåndtering for samtidige operationer
- **Fejlsøgning**: Visual Studio fejlfindingsværktøjer til samtidige arbejdsgange

Lad os bygge højtydende samtidige AI-arbejdsgange med .NET! 🚀

## 💻 Kørsel af koden

Den komplette implementering findes i `03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs`. Denne fil demonstrerer en **Fan-Out/Fan-In samtidig arbejdsgang** til rejseplanlægning:

### 🏗️ **Arbejdsgangsarkitektur**

```
User Request → ConcurrentStartExecutor → [Researcher Agent || Planner Agent] → ConcurrentAggregationExecutor → Final Output
```

**Vigtige komponenter:**

1. **ConcurrentStartExecutor**: Sender brugerens anmodning til alle agenter samtidig
2. **Researcher Agent**: Analyserer destinationer og attraktioner samtidigt
3. **Planner Agent**: Udarbejder detaljerede rejseplaner samtidigt
4. **ConcurrentAggregationExecutor**: Samler og sammenlægger resultater fra begge agenter

### 🎯 **Fan-Out/Fan-In-mønster**

Denne arbejdsgang demonstrerer det klassiske **Fan-Out/Fan-In** mønster:
- **Fan-Out**: Én inputbesked sendes til flere agenter samtidig
- **Samtidig behandling**: Flere agenter arbejder parallelt på samme opgave
- **Fan-In**: Resultater fra alle agenter samles og aggregeres til en enkelt output

### 🚀 Kørsel af eksemplet

```bash
# Gør scriptet eksekverbart (Unix/Linux/macOS)
chmod +x 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs

# Kør den samtidige arbejdsgang
./03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

Eller på Windows:
```powershell
dotnet run 03.dotnet-agent-framework-workflow-ghmodel-concurrent.cs
```

### 📝 Forventet output

Arbejdsgangen vil:
1. **Udsend Anmodning**: Sende "Planlæg en tur til Seattle i december" til begge agenter
2. **Samtidig Behandling**: Begge agenter arbejder samtidig:
   - Researcher identificerer attraktioner og detaljer
   - Planner udarbejder rejseplan og logistik
3. **Aggregation**: Kombiner begge svar til en omfattende output
4. **Vis Resultater**: Vis den sammenlagte rejseplan med alle informationer

### 🔧 Tilpasningsmuligheder

**Tilføj flere samtidige agenter:**
```csharp
// Create additional specialized agents
AIAgent budgetAgent = azureClient.GetOpenAIResponseClient(deployment).CreateAIAgent(
    name: "Budget-Agent", instructions: "Calculate travel costs...");

// Add to fan-out
var workflow = new WorkflowBuilder(startExecutor)
    .AddFanOutEdge(startExecutor, targets: [researcherAgent, plannerAgent, budgetAgent])
    .AddFanInEdge(aggregationExecutor, sources: [researcherAgent, plannerAgent, budgetAgent])
    .WithOutputFrom(aggregationExecutor)
    .Build();

// Update aggregation count
if (this._messages.Count == 3) { ... }
```

**Ændr agentinstruktioner:**
```csharp
const string ResearcherAgentInstructions = "Your custom instructions for research...";
const string PlanAgentInstructions = "Your custom instructions for planning...";
```

**Skift opgave:**
```csharp
StreamingRun run = await InProcessExecution.StreamAsync(
    workflow, 
    "Plan a European vacation for 2 weeks in summer"
);
```

### 🎯 Anvendelser i den virkelige verden

Dette samtidige mønster er ideelt til:
- **Indholdsskabelse**: Flere forfattere skaber forskellige afsnit samtidigt
- **Kodegennemgang**: Flere gennemgåere analyserer kode fra forskellige perspektiver
- **Markedsanalyse**: Parallel analyse af forskellige markedssegmenter
- **Dokumentbehandling**: Samtidig udtræk, analyse og validering
- **Multi-perspektiv analyse**: Få forskellige synspunkter på samme input

### 🔍 Forståelse af brugerdefinerede eksekutorer

**ConcurrentStartExecutor:**
- Implementerer `IMessageHandler<string>` for at acceptere input som streng
- Sender beskeder til alle tilkoblede agenter
- Sender `TurnToken` for at udløse samtidig behandling

**ConcurrentAggregationExecutor:**
- Implementerer `IMessageHandler<ChatMessage>` for at modtage agenters svar
- Samler beskeder på en trådsikker måde
- Agregerer, når alle forventede svar er modtaget
- Leverer slutoutput ved hjælp af `context.YieldOutputAsync()`

### ⚡ Ydelsesfordele

**Samtidig vs Sekventiel:**
- Sekventiel: Agent1 (30s) → Agent2 (30s) = **60 sekunder samlet**
- Samtidig: Agent1 (30s) || Agent2 (30s) = **30 sekunder samlet**

**Gennemløbsforbedring**: Op til N× hurtigere for N samtidige agenter (afhængig af arbejdsbyrde og ressourcer)

### 🛡️ Fejlhåndtering

Arbejdsgangen håndterer fejl i individuelle agenter graciøst:
- Hvis én agent fejler, fortsætter de andre med behandling
- Aggregatoren kan implementere timeout-logik
- Delvise resultater kan returneres, hvis nødvendigt

### 📊 Avancerede funktioner

**Dynamisk agentantal:**
Juster aggregeringslogikken til at understøtte variabelt agentantal:

```csharp
private int _expectedAgentCount;
private readonly List<ChatMessage> _messages = [];

public async ValueTask HandleAsync(ChatMessage message, IWorkflowContext context)
{
    this._messages.Add(message);
    if (this._messages.Count == _expectedAgentCount)
    {
        // Process aggregation
    }
}
```

Dette samtidige arbejdsgangsmønster er essentielt til at bygge højtydende, skalerbare AI-agentsystemer!

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Ansvarsfraskrivelse**:
Dette dokument er blevet oversat ved hjælp af AI-oversættelsestjenesten [Co-op Translator](https://github.com/Azure/co-op-translator). Selvom vi bestræber os på nøjagtighed, skal du være opmærksom på, at automatiserede oversættelser kan indeholde fejl eller unøjagtigheder. Det originale dokument på dets oprindelige sprog bør betragtes som den autoritative kilde. For kritisk information anbefales professionel menneskelig oversættelse. Vi påtager os intet ansvar for misforståelser eller fejltolkninger, der opstår som følge af brugen af denne oversættelse.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->