#!/usr/bin/env bash
set -e

echo "Running cargo fmt --check..."
cargo fmt --check
if [ $? -ne 0 ]; then
    echo "❌ Formatting issues found. Run 'cargo fmt' to fix."
    exit 1
fi

echo "✅ Formatting OK"
