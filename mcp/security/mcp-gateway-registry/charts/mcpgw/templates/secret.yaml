{{- if not .Values.app.existingSecret }}
{{- /*
In the stack, mcpgw calls the registry API pod-to-pod via nginx.
The registry's nginx location blocks for /api/* and /v0.1/* are
prefixed with ROOT_PATH (see registry/templates/secret.yaml). In path
routing mode that prefix is e.g. "/registry" — so mcpgw must include
it in the URL, otherwise calls fall through to the nginx catch-all
(which bypasses auth_request) and FastAPI returns 401.

In subdomain mode ROOT_PATH is empty so no path suffix is appended.
*/ -}}
{{- $registryUrl := .Values.app.registryUrl }}
{{- if .Values.global.domain }}
  {{- $registryUrl = printf "http://registry.%s.svc.cluster.local:8000" .Release.Namespace }}
  {{- if eq (.Values.global.ingress.routingMode | default "subdomain") "path" }}
    {{- $registryPath := .Values.global.ingress.paths.registry | default "/registry" }}
    {{- $registryUrl = printf "%s%s" $registryUrl $registryPath }}
  {{- end }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.app.envSecretName }}
  namespace: {{ .Release.Namespace | quote }}
data:
  PORT: {{ .Values.app.port | toString | b64enc | quote }}
  REGISTRY_BASE_URL: {{ $registryUrl | b64enc | quote }}
{{- if .Values.global.domain }}
  {{- $domain := .Values.global.domain }}
  {{- $routingMode := "subdomain" }}
  {{- if .Values.global.ingress }}
    {{- $routingMode = .Values.global.ingress.routingMode | default "subdomain" }}
  {{- end }}
  {{- if eq $routingMode "path" }}
  REGISTRY_EXTERNAL_URL: {{ printf "https://%s" $domain | b64enc | quote }}
  {{- else }}
  REGISTRY_EXTERNAL_URL: {{ printf "https://mcpregistry.%s" $domain | b64enc | quote }}
  {{- end }}
{{- end }}
{{- if not .Values.global.sharedSecretName }}
  {{/* SECRET_KEY managed per-chart in standalone deployment */}}
  SECRET_KEY: {{ required "app.secretKey or global.secretKey is required for standalone deployment" (.Values.global.secretKey | default .Values.app.secretKey) | b64enc | quote }}
{{- end }}
{{- end }}
