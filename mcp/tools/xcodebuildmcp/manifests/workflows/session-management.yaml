id: session-management
title: Session Management
description: Manage session defaults for project/workspace paths, scheme, configuration, simulator/device settings.
targetPlatforms: []
availability:
  cli: false
selection:
  mcp:
    defaultEnabled: true
    autoInclude: true
tools:
  - session_show_defaults
  - session_use_defaults_profile
  - session_set_defaults
  - session_clear_defaults
  - sync_xcode_defaults
