id: xcode_tools_bridge_sync
module: mcp/tools/xcode-ide/xcode_tools_bridge_sync
names:
  mcp: xcode_tools_bridge_sync
  cli: bridge-sync
description: One-shot connect + tools/list sync (manual retry; avoids background prompt spam).
outputSchema:
  schema: xcodebuildmcp.output.xcode-bridge-sync
  version: "2"
predicates:
  - debugEnabled
annotations:
  title: Sync Xcode Tools Bridge
  readOnlyHint: false
  destructiveHint: false
  openWorldHint: false
