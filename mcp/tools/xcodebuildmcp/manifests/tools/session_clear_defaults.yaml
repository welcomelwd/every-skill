id: session_clear_defaults
module: mcp/tools/session-management/session_clear_defaults
names:
  mcp: session_clear_defaults
  cli: clear-defaults
description: Clear session defaults for the active profile or a specified profile.
outputSchema:
  schema: xcodebuildmcp.output.session-defaults
  version: "2"
annotations:
  title: Clear Session Defaults
  readOnlyHint: false
  destructiveHint: true
  openWorldHint: false
