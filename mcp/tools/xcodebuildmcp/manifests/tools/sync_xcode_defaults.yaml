id: sync_xcode_defaults
module: mcp/tools/xcode-ide/sync_xcode_defaults
names:
  mcp: sync_xcode_defaults
  cli: sync-xcode-defaults
description: Sync session defaults (scheme, simulator) from Xcode's current IDE selection.
outputSchema:
  schema: xcodebuildmcp.output.session-defaults
  version: "2"
predicates:
  - xcodeAutoSyncDisabled
annotations:
  title: Sync Xcode Defaults
  readOnlyHint: false
  destructiveHint: false
  openWorldHint: false
