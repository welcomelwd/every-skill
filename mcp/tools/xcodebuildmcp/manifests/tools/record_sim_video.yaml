id: record_sim_video
module: mcp/tools/simulator/record_sim_video
names:
  mcp: record_sim_video
  cli: record-video
description: Record sim video.
outputSchema:
  schema: xcodebuildmcp.output.capture-result
  version: "2"
routing:
  stateful: true
annotations:
  title: Record Simulator Video
  readOnlyHint: false
  destructiveHint: false
  openWorldHint: false
nextSteps:
  - label: Stop and save the recording
    toolId: record_sim_video
    priority: 1
    when: success
