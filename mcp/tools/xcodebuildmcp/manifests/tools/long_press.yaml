id: long_press
module: mcp/tools/ui-automation/long_press
names:
  mcp: long_press
  cli: long-press
description: Long press a UI element by elementRef from a current rs/1 runtime snapshot.
outputSchema:
  schema: xcodebuildmcp.output.ui-action-result
  version: "2"
routing:
  stateful: true
annotations:
  title: Long Press
  readOnlyHint: true
  destructiveHint: false
  openWorldHint: false
nextSteps:
  - label: Refresh after UI action
    toolId: snapshot_ui
    when: success
    condition: ui_action_needs_refresh
