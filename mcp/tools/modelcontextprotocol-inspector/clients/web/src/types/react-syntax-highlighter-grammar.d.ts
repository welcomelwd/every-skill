// Type stub for a `react-syntax-highlighter` prism language grammar import. See
// react-syntax-highlighter-prism-light.d.ts for why this redirect exists. The
// grammar object is opaque — it's only handed back to `registerLanguage`.
declare const grammar: unknown;
export default grammar;
