---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  labels:
    toolhive.stacklok.dev/auto-migrate-storage-version: "true"
  name: mcpwebhookconfigs.toolhive.stacklok.dev
spec:
  group: toolhive.stacklok.dev
  names:
    categories:
    - toolhive
    kind: MCPWebhookConfig
    listKind: MCPWebhookConfigList
    plural: mcpwebhookconfigs
    shortNames:
    - mwc
    singular: mcpwebhookconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Valid')].status
      name: Valid
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MCPWebhookConfig is the Schema for the mcpwebhookconfigs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MCPWebhookConfigSpec defines the desired state of MCPWebhookConfig
            properties:
              mutating:
                description: Mutating webhooks are called to transform MCP requests
                  before processing.
                items:
                  description: WebhookSpec defines the configuration for a single
                    webhook middleware
                  properties:
                    failurePolicy:
                      default: fail
                      description: |-
                        FailurePolicy defines how to handle errors when communicating with the webhook.
                        Supported values: "fail", "ignore". Defaults to "fail".
                      enum:
                      - fail
                      - ignore
                      type: string
                    hmacSecretRef:
                      description: |-
                        HMACSecretRef references a Kubernetes Secret containing the HMAC signing key
                        used to sign the webhook payload. If set, the X-Toolhive-Signature header will be injected.
                      properties:
                        key:
                          description: Key is the key within the secret
                          type: string
                        name:
                          description: Name is the name of the secret
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    name:
                      description: Name is a unique identifier for this webhook
                      maxLength: 63
                      minLength: 1
                      type: string
                    timeout:
                      description: |-
                        Timeout configures the maximum time to wait for the webhook to respond.
                        Defaults to 10s if not specified. Maximum is 30s.
                      format: duration
                      type: string
                    tlsConfig:
                      description: TLSConfig contains optional TLS configuration for
                        the webhook connection.
                      properties:
                        caSecretRef:
                          description: |-
                            CASecretRef references a Secret containing the CA certificate bundle used to verify the webhook server's certificate.
                            Contains a bundle of PEM-encoded X.509 certificates.
                          properties:
                            key:
                              description: Key is the key within the secret
                              type: string
                            name:
                              description: Name is the name of the secret
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        clientCertSecretRef:
                          description: |-
                            ClientCertSecretRef references a Secret containing the client certificate for mTLS authentication.
                            The referenced key must contain a PEM-encoded client certificate.
                            Use ClientKeySecretRef to provide the corresponding private key.
                          properties:
                            key:
                              description: Key is the key within the secret
                              type: string
                            name:
                              description: Name is the name of the secret
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        clientKeySecretRef:
                          description: |-
                            ClientKeySecretRef references a Secret containing the private key for the client certificate.
                            Required when ClientCertSecretRef is set to enable mTLS.
                          properties:
                            key:
                              description: Key is the key within the secret
                              type: string
                            name:
                              description: Name is the name of the secret
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        insecureSkipVerify:
                          description: |-
                            InsecureSkipVerify disables server certificate verification.
                            WARNING: This should only be used for development/testing and not in production environments.
                          type: boolean
                      type: object
                    url:
                      description: URL is the endpoint to call for this webhook. Must
                        be an HTTP/HTTPS URL.
                      format: uri
                      type: string
                  required:
                  - name
                  - url
                  type: object
                type: array
              validating:
                description: Validating webhooks are called to approve or deny MCP
                  requests.
                items:
                  description: WebhookSpec defines the configuration for a single
                    webhook middleware
                  properties:
                    failurePolicy:
                      default: fail
                      description: |-
                        FailurePolicy defines how to handle errors when communicating with the webhook.
                        Supported values: "fail", "ignore". Defaults to "fail".
                      enum:
                      - fail
                      - ignore
                      type: string
                    hmacSecretRef:
                      description: |-
                        HMACSecretRef references a Kubernetes Secret containing the HMAC signing key
                        used to sign the webhook payload. If set, the X-Toolhive-Signature header will be injected.
                      properties:
                        key:
                          description: Key is the key within the secret
                          type: string
                        name:
                          description: Name is the name of the secret
                          type: string
                      required:
                      - key
                      - name
                      type: object
                    name:
                      description: Name is a unique identifier for this webhook
                      maxLength: 63
                      minLength: 1
                      type: string
                    timeout:
                      description: |-
                        Timeout configures the maximum time to wait for the webhook to respond.
                        Defaults to 10s if not specified. Maximum is 30s.
                      format: duration
                      type: string
                    tlsConfig:
                      description: TLSConfig contains optional TLS configuration for
                        the webhook connection.
                      properties:
                        caSecretRef:
                          description: |-
                            CASecretRef references a Secret containing the CA certificate bundle used to verify the webhook server's certificate.
                            Contains a bundle of PEM-encoded X.509 certificates.
                          properties:
                            key:
                              description: Key is the key within the secret
                              type: string
                            name:
                              description: Name is the name of the secret
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        clientCertSecretRef:
                          description: |-
                            ClientCertSecretRef references a Secret containing the client certificate for mTLS authentication.
                            The referenced key must contain a PEM-encoded client certificate.
                            Use ClientKeySecretRef to provide the corresponding private key.
                          properties:
                            key:
                              description: Key is the key within the secret
                              type: string
                            name:
                              description: Name is the name of the secret
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        clientKeySecretRef:
                          description: |-
                            ClientKeySecretRef references a Secret containing the private key for the client certificate.
                            Required when ClientCertSecretRef is set to enable mTLS.
                          properties:
                            key:
                              description: Key is the key within the secret
                              type: string
                            name:
                              description: Name is the name of the secret
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        insecureSkipVerify:
                          description: |-
                            InsecureSkipVerify disables server certificate verification.
                            WARNING: This should only be used for development/testing and not in production environments.
                          type: boolean
                      type: object
                    url:
                      description: URL is the endpoint to call for this webhook. Must
                        be an HTTP/HTTPS URL.
                      format: uri
                      type: string
                  required:
                  - name
                  - url
                  type: object
                type: array
            type: object
            x-kubernetes-validations:
            - message: at least one validating or mutating webhook must be defined
              rule: '(has(self.validating) ? size(self.validating) : 0) + (has(self.mutating)
                ? size(self.mutating) : 0) > 0'
          status:
            description: MCPWebhookConfigStatus defines the observed state of MCPWebhookConfig
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configHash:
                description: ConfigHash is a hash of the spec, used for detecting
                  changes
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation corresponding
                  to the current status
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
