# {{PROJECT_NAME}}

An MCP server built with [mcp-use](https://mcp-use.com).

## Getting Started

```bash
# Install dependencies
uv sync

# Run in development mode (streamable-http with debug)
python server.py

# Or run with stdio transport (for Claude Desktop, etc.)
# Change transport in server.py to "stdio"
```
