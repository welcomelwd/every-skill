# Basic server

The smallest useful `mcp-use` server: a tool, a static resource, and a prompt.

```sh
pnpm dev
```

Connect to `http://localhost:3000/mcp`. Call `greet` with `{ "name": "Ada" }`,
read `example://about`, or get the `introduce` prompt.

```sh
pnpm verify
```
