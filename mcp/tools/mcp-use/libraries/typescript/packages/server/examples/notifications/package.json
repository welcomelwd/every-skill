{
  "name": "mcp-use-example-notifications",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "Example: MCP list and resource change notifications with mcp-use.",
  "engines": { "node": ">=22.22.2" },
  "scripts": {
    "dev": "mcp-use dev",
    "build": "mcp-use build",
    "start": "mcp-use start",
    "typecheck": "tsc --noEmit",
    "verify": "node ../verify-examples.mjs --example=notifications"
  },
  "dependencies": { "mcp-use": "workspace:*" },
  "devDependencies": { "@types/node": "^22.20.0", "typescript": "^7.0.2", "vite": "^8.0.16" }
}
