# Documentation

Every public TypeScript export, including public type properties, must have meaningful TSDoc. Update its TSDoc when its behavior changes. Lint enforces TSDoc syntax and public-export coverage.
