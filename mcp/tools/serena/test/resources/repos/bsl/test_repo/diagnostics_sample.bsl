// diagnostics_sample.bsl — намеренно сломанный файл для проверки диагностик BSL LS.
// Содержит синтаксическую ошибку (незакрытая строка), которую bsl-language-server гарантированно флагнет.

Процедура СломаннаяПроцедура() Экспорт
    Текст = "незакрытая строка;
КонецПроцедуры
