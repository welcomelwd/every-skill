# Отстраняване на грешки с MCP Inspector

**MCP Inspector** е незаменим инструмент за отстраняване на грешки, който ви позволява интерактивно да тествате и диагностицирате вашите MCP сървъри без необходимостта от пълно AI хост приложение. Мислете за него като "Postman за MCP" - той предоставя визуален интерфейс за изпращане на заявки, преглед на отговори и разбиране на поведението на вашия сървър.

## Защо да използвате MCP Inspector?

При изграждането на MCP сървъри често ще срещате тези предизвикателства:

- **"Сървърът ми изобщо работи ли?"** - Inspector показва състоянието на връзката
- **"Инструментите ми регистрирани ли са правилно?"** - Inspector изброява всички налични инструменти
- **"Какъв е форматът на отговора?"** - Inspector показва пълните JSON отговори
- **"Защо този инструмент не работи?"** - Inspector показва подробни съобщения за грешки

## Предварителни условия

- Инсталиран Node.js 18+
- npm (включен с Node.js)
- MCP сървър за тестване (вижте [Модул 3.1 - Първи сървър](../01-first-server/README.md))

## Инсталация

### Опция 1: Стартиране с npx (Препоръчва се за бързо тестване)

```bash
npx @modelcontextprotocol/inspector
```

### Опция 2: Глобална инсталация

```bash
npm install -g @modelcontextprotocol/inspector
mcp-inspector
```

### Опция 3: Добавяне към вашия проект

```bash
cd your-mcp-server-project
npm install --save-dev @modelcontextprotocol/inspector
```

Добавете към `package.json`:
```json
{
  "scripts": {
    "inspector": "mcp-inspector"
  }
}
```

---

## Свързване към вашия сървър

### stdio сървъри (локален процес)

За сървъри, които комуникират чрез стандартен вход/изход:

```bash
# Python сървър
npx @modelcontextprotocol/inspector python -m your_server_module

# Node.js сървър
npx @modelcontextprotocol/inspector node ./build/index.js

# С променливи на околната среда
OPENAI_API_KEY=xxx npx @modelcontextprotocol/inspector python server.py
```

### SSE/HTTP сървъри (мрежови)

За сървъри, работещи като HTTP услуги:

1. Стартирайте първо вашия сървър:
   ```bash
   python server.py  # Сървър работи на http://localhost:8080
   ```

2. Стартирайте Inspector и се свържете:
   ```bash
   npx @modelcontextprotocol/inspector --sse http://localhost:8080/sse
   ```

---

## Преглед на интерфейса на Inspector

Когато Inspector се стартира, ще видите уеб интерфейс (обикновено на `http://localhost:5173`):

```
┌─────────────────────────────────────────────────────────────┐
│  MCP Inspector                              [Connected ✅]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   🔧 Tools  │  │ 📄 Resources│  │ 💬 Prompts  │         │
│  │    (3)      │  │    (2)      │  │    (1)      │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  📋 Message Log                                       │ │
│  │  ─────────────────────────────────────────────────── │ │
│  │  → initialize                                         │ │
│  │  ← initialized (server info)                          │ │
│  │  → tools/list                                         │ │
│  │  ← tools (3 tools)                                    │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## Тестване на инструменти

### Изброяване на наличните инструменти

1. Кликнете върху раздела **Tools**
2. Inspector автоматично извиква `tools/list`
3. Ще видите всички регистрирани инструменти с:
   - Име на инструмента
   - Описание
   - Входна схема (параметри)

### Извикване на инструмент

1. Изберете инструмент от списъка
2. Попълнете необходимите параметри във формуляра
3. Натиснете **Run Tool**
4. Прегледайте отговора в панела с резултати

**Пример: Тестване на калкулаторен инструмент**

```
Tool: add
Parameters:
  a: 25
  b: 17

Response:
{
  "content": [
    {
      "type": "text",
      "text": "42"
    }
  ]
}
```

### Отстраняване на грешки при инструмента

Когато инструментът се провали, Inspector показва:

```
Error Response:
{
  "error": {
    "code": -32602,
    "message": "Invalid params: 'b' is required"
  }
}
```

Често срещани кодове за грешки:
| Код | Значение |
|------|---------|
| -32700 | Грешка при парсване (невалиден JSON) |
| -32600 | Невалидна заявка |
| -32601 | Методът не е намерен |
| -32602 | Невалидни параметри |
| -32603 | Вътрешна грешка |

---

## Тестване на ресурси

### Изброяване на ресурси

1. Кликнете върху раздела **Resources**
2. Inspector извиква `resources/list`
3. Ще видите:
   - URI на ресурсите
   - Имена и описания
   - MIME типове

### Четене на ресурс

1. Изберете ресурс
2. Натиснете **Read Resource**
3. Прегледайте върнатото съдържание

**Примерен изход:**

```
Resource: file:///config/settings.json
Content-Type: application/json

{
  "config": {
    "debug": true,
    "maxConnections": 10
  }
}
```

---

## Тестване на подканящи съобщения

### Изброяване на подканящи съобщения

1. Кликнете върху раздела **Prompts**
2. Inspector извиква `prompts/list`
3. Прегледайте наличните шаблони за подканящи съобщения

### Получаване на подканящо съобщение

1. Изберете подканящо съобщение
2. Попълнете необходимите аргументи
3. Натиснете **Get Prompt**
4. Вижте визуализираните подканящи съобщения

---

## Анализ на дневника със съобщения

Дневникът със съобщения показва всички MCP протоколни съобщения:

```
14:32:01 → {"jsonrpc":"2.0","id":1,"method":"initialize",...}
14:32:01 ← {"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"2025-11-25",...}}
14:32:02 → {"jsonrpc":"2.0","id":2,"method":"tools/list"}
14:32:02 ← {"jsonrpc":"2.0","id":2,"result":{"tools":[...]}}
14:32:05 → {"jsonrpc":"2.0","id":3,"method":"tools/call","params":{"name":"add",...}}
14:32:05 ← {"jsonrpc":"2.0","id":3,"result":{"content":[...]}}
```

### Какво да търсите

- **Двойки заявка/отговор**: Всяка стрелка `→` трябва да има съответстваща `←`
- **Съобщения за грешки**: Търсете `"error"` в отговорите
- **Времеви интервали**: Големи разстояния могат да показват проблеми с производителността
- **Версия на протокола**: Проверете дали сървърът и клиентът са на една и съща версия

---

## Интеграция с VS Code

Можете да стартирате Inspector директно от VS Code:

### Използване на launch.json

Добавете към `.vscode/launch.json`:

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug with MCP Inspector",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npx",
      "runtimeArgs": [
        "@modelcontextprotocol/inspector",
        "python",
        "${workspaceFolder}/server.py"
      ],
      "console": "integratedTerminal"
    },
    {
      "name": "Debug SSE Server with Inspector",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "preLaunchTask": "Start MCP Inspector"
    }
  ]
}
```

### Използване на задачи (Tasks)

Добавете към `.vscode/tasks.json`:

```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start MCP Inspector",
      "type": "shell",
      "command": "npx @modelcontextprotocol/inspector node ${workspaceFolder}/build/index.js",
      "isBackground": true,
      "problemMatcher": {
        "pattern": {
          "regexp": "^$"
        },
        "background": {
          "activeOnStart": true,
          "beginsPattern": "Inspector",
          "endsPattern": "listening"
        }
      }
    }
  ]
}
```

---

## Чести сценарии за отстраняване на грешки

### Сценарий 1: Сървърът не се свързва

**Симптоми:** Inspector показва "Disconnected" или блокира на "Connecting..."

**Контролен списък:**
1. ✅ Командата за сървъра е правилна?
2. ✅ Всички зависимости са инсталирани?
3. ✅ Пътят към сървъра е абсолютен или относителен към текущата директория?
4. ✅ Нужните променливи на средата са зададени?

**Стъпки за отстраняване:**
```bash
# Тестване на сървъра ръчно първо
python -c "import your_server_module; print('OK')"

# Проверка за грешки при импортиране
python -m your_server_module 2>&1 | head -20

# Потвърдете, че MCP SDK е инсталиран
pip show mcp
```

### Сценарий 2: Инструментите не се показват

**Симптоми:** Разделът за инструменти е празен

**Възможни причини:**
1. Инструментите не са регистрирани по време на инициализацията на сървъра
2. Сървърът е претърпял срив след старт
3. Обработчикът `tools/list` връща празен масив

**Стъпки за отстраняване:**
1. Проверете дневника със съобщения за отговора на `tools/list`
2. Добавете логиране във вашия код за регистрация на инструменти
3. Проверете дали присъстват декораторите `@mcp.tool()` (Python)

### Сценарий 3: Инструмент връща грешка

**Симптоми:** Извикването на инструмента връща грешка

**Подход за отстраняване:**
1. Прочетете внимателно съобщението за грешка
2. Проверете дали типовете параметри съответстват на схемата
3. Добавете try/catch с подробни съобщения за грешки
4. Проверете логовете на сървъра за стек трасове

**Пример за подобрено обработване на грешки:**

```python
@mcp.tool()
async def my_tool(param1: str, param2: int) -> str:
    try:
        # Логика на инструмента тук
        result = process(param1, param2)
        return str(result)
    except ValueError as e:
        raise McpError(f"Invalid parameter: {e}")
    except Exception as e:
        raise McpError(f"Tool failed: {type(e).__name__}: {e}")
```

### Сценарий 4: Съдържанието на ресурса е празно

**Симптоми:** Ресурсът се връща, но съдържанието е празно или null

**Контролен списък:**
1. ✅ Пътят към файла или URI е правилен
2. ✅ Сървърът има разрешение за четене на ресурса
3. ✅ Съдържанието на ресурса се връща правилно

---

## Разширени възможности на Inspector

### Потребителски заглавки (SSE)

```bash
npx @modelcontextprotocol/inspector \
  --sse http://localhost:8080/sse \
  --header "Authorization: Bearer your-token"
```

### Подробно логиране

```bash
DEBUG=mcp* npx @modelcontextprotocol/inspector python server.py
```

### Запис на сесии

Inspector може да експортира дневници със съобщения за по-късен анализ:
1. Кликнете върху **Export Log** в панела със съобщения
2. Запишете JSON файла
3. Споделете с членовете на екипа за отстраняване на грешки

---

## Добри практики

1. **Тествайте рано и често** - Използвайте Inspector по време на разработка, не само когато възникнат проблеми
2. **Започнете с основното** - Тествайте базова свързаност преди сложни повиквания към инструменти
3. **Проверявайте схемата** - Много грешки идват от несъответствия в типовете параметри
4. **Четете съобщенията за грешки** - MCP грешките обикновено са описателни
5. **Дръжте Inspector отворен** - Той помага да хванете проблеми по време на разработка

---

## Какво следва

Вие завършихте Модул 3: Започване! Продължете с ученето си:

- [Модул 4: Практическа реализация](../../04-PracticalImplementation/README.md)

---

## Допълнителни ресурси

- [MCP Inspector GitHub Repository](https://github.com/modelcontextprotocol/inspector)
- [MCP Specification - Protocol Messages](https://spec.modelcontextprotocol.io/specification/2025-11-25/)
- [JSON-RPC 2.0 Specification](https://www.jsonrpc.org/specification)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Отказ от отговорност**:  
Този документ е преведен с помощта на AI преводаческа услуга [Co-op Translator](https://github.com/Azure/co-op-translator). Въпреки че се стремим към точност, имайте предвид, че автоматизираните преводи може да съдържат грешки или неточности. Оригиналният документ на неговия роден език трябва да се счита за официален източник. За критична информация се препоръчва професионален човешки превод. Ние не носим отговорност за каквито и да е недоразумения или неправилни тълкувания, произтичащи от използването на този превод.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->