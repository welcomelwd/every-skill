# التصحيح باستخدام MCP Inspector

تعد **MCP Inspector** أداة تصحيح أساسية تتيح لك اختبار خوادم MCP الخاصة بك بشكل تفاعلي واستكشاف الأخطاء وإصلاحها دون الحاجة إلى تطبيق مضيف AI كامل. فكر فيها على أنها "بوستمان لـ MCP" - فهي توفر واجهة مرئية لإرسال الطلبات، عرض الاستجابات، وفهم كيف يعمل الخادم الخاص بك.

## لماذا تستخدم MCP Inspector؟

عند بناء خوادم MCP، ستواجه غالبًا هذه التحديات:

- **"هل الخادم الخاص بي يعمل أصلاً؟"** - يعرض Inspector حالة الاتصال
- **"هل الأدوات الخاصة بي مسجلة بشكل صحيح؟"** - يعرض Inspector جميع الأدوات المتوفرة
- **"ما هو تنسيق الاستجابة؟"** - يعرض Inspector استجابات JSON كاملة
- **"لماذا لا تعمل هذه الأداة؟"** - يعرض Inspector رسائل خطأ مفصلة

## المتطلبات المسبقة

- تثبيت Node.js 18 أو أحدث
- npm (يأتي مع Node.js)
- خادم MCP للاختبار (راجع [الوحدة 3.1 - أول خادم](../01-first-server/README.md))

## التثبيت

### الخيار 1: التشغيل باستخدام npx (مُوصى به للاختبار السريع)

```bash
npx @modelcontextprotocol/inspector
```

### الخيار 2: التثبيت على مستوى النظام

```bash
npm install -g @modelcontextprotocol/inspector
mcp-inspector
```

### الخيار 3: الإضافة إلى مشروعك

```bash
cd your-mcp-server-project
npm install --save-dev @modelcontextprotocol/inspector
```

أضف إلى `package.json`:
```json
{
  "scripts": {
    "inspector": "mcp-inspector"
  }
}
```

---

## الاتصال بالخادم الخاص بك

### خوادم stdio (العملية المحلية)

لخوادم تتواصل عبر الدخل/الخرج القياسي:

```bash
# خادم بايثون
npx @modelcontextprotocol/inspector python -m your_server_module

# خادم Node.js
npx @modelcontextprotocol/inspector node ./build/index.js

# مع متغيرات البيئة
OPENAI_API_KEY=xxx npx @modelcontextprotocol/inspector python server.py
```

### خوادم SSE/HTTP (الشبكة)

لخوادم تعمل كخدمات HTTP:

1. قم بتشغيل الخادم أولاً:
   ```bash
   python server.py  # الخادم يعمل على http://localhost:8080
   ```

2. شغّل Inspector وقم بالاتصال:
   ```bash
   npx @modelcontextprotocol/inspector --sse http://localhost:8080/sse
   ```

---

## نظرة عامة على واجهة Inspector

عند تشغيل Inspector، سترى واجهة ويب (عادة على `http://localhost:5173`):

```
┌─────────────────────────────────────────────────────────────┐
│  MCP Inspector                              [Connected ✅]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │   🔧 Tools  │  │ 📄 Resources│  │ 💬 Prompts  │         │
│  │    (3)      │  │    (2)      │  │    (1)      │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
│                                                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │  📋 Message Log                                       │ │
│  │  ─────────────────────────────────────────────────── │ │
│  │  → initialize                                         │ │
│  │  ← initialized (server info)                          │ │
│  │  → tools/list                                         │ │
│  │  ← tools (3 tools)                                    │ │
│  └───────────────────────────────────────────────────────┘ │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## اختبار الأدوات

### قائمة الأدوات المتوفرة

1. انقر على علامة التبويب **Tools**
2. يقوم Inspector تلقائيًا بنداء `tools/list`
3. سترى جميع الأدوات المسجلة مع:
   - اسم الأداة
   - الوصف
   - مخطط الإدخال (المُعاملات)

### استدعاء أداة

1. اختر أداة من القائمة
2. املأ المعلمات المطلوبة في النموذج
3. انقر على **Run Tool**
4. عرض الاستجابة في لوحة النتائج

**مثال: اختبار أداة الآلة الحاسبة**

```
Tool: add
Parameters:
  a: 25
  b: 17

Response:
{
  "content": [
    {
      "type": "text",
      "text": "42"
    }
  ]
}
```

### تصحيح أخطاء الأداة

عندما تفشل أداة، يعرض Inspector:

```
Error Response:
{
  "error": {
    "code": -32602,
    "message": "Invalid params: 'b' is required"
  }
}
```

رموز الأخطاء الشائعة:
| الرمز | المعنى |
|------|---------|
| -32700 | خطأ في التحليل (JSON غير صالح) |
| -32600 | طلب غير صالح |
| -32601 | الطريقة غير موجودة |
| -32602 | معلمات غير صحيحة |
| -32603 | خطأ داخلي |

---

## اختبار الموارد

### قائمة الموارد

1. انقر على علامة التبويب **Resources**
2. يقوم Inspector بنداء `resources/list`
3. سترى:
   - عناوين URI للموارد
   - الأسماء والوصف
   - أنواع MIME

### قراءة مورد

1. اختر موردًا
2. انقر على **Read Resource**
3. عرض المحتوى المُعاد

**مثال على الناتج:**

```
Resource: file:///config/settings.json
Content-Type: application/json

{
  "config": {
    "debug": true,
    "maxConnections": 10
  }
}
```

---

## اختبار المطالبات

### قائمة المطالبات

1. انقر على علامة التبويب **Prompts**
2. يقوم Inspector بنداء `prompts/list`
3. عرض قوالب المطالبات المتاحة

### الحصول على مطالبة

1. اختر مطالبة
2. املأ أي وسائط مطلوبة
3. انقر على **Get Prompt**
4. شاهد رسائل المطالبة المعروضة

---

## تحليل سجل الرسائل

يعرض سجل الرسائل جميع رسائل بروتوكول MCP:

```
14:32:01 → {"jsonrpc":"2.0","id":1,"method":"initialize",...}
14:32:01 ← {"jsonrpc":"2.0","id":1,"result":{"protocolVersion":"2025-11-25",...}}
14:32:02 → {"jsonrpc":"2.0","id":2,"method":"tools/list"}
14:32:02 ← {"jsonrpc":"2.0","id":2,"result":{"tools":[...]}}
14:32:05 → {"jsonrpc":"2.0","id":3,"method":"tools/call","params":{"name":"add",...}}
14:32:05 ← {"jsonrpc":"2.0","id":3,"result":{"content":[...]}}
```

### ما الذي يجب ملاحظته

- **أزواج الطلب/الاستجابة**: يجب أن يكون لكل `→` نظير `←`
- **رسائل الخطأ**: ابحث عن `"error"` في الاستجابات
- **التوقيت**: الفجوات الكبيرة قد تشير إلى مشاكل أداء
- **إصدار البروتوكول**: تأكد من أن الخادم والعميل متفقان على الإصدار

---

## دمج VS Code

يمكنك تشغيل Inspector مباشرة من VS Code:

### باستخدام launch.json

أضف إلى `.vscode/launch.json`:

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug with MCP Inspector",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npx",
      "runtimeArgs": [
        "@modelcontextprotocol/inspector",
        "python",
        "${workspaceFolder}/server.py"
      ],
      "console": "integratedTerminal"
    },
    {
      "name": "Debug SSE Server with Inspector",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "preLaunchTask": "Start MCP Inspector"
    }
  ]
}
```

### باستخدام المهام

أضف إلى `.vscode/tasks.json`:

```json
{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start MCP Inspector",
      "type": "shell",
      "command": "npx @modelcontextprotocol/inspector node ${workspaceFolder}/build/index.js",
      "isBackground": true,
      "problemMatcher": {
        "pattern": {
          "regexp": "^$"
        },
        "background": {
          "activeOnStart": true,
          "beginsPattern": "Inspector",
          "endsPattern": "listening"
        }
      }
    }
  ]
}
```

---

## السيناريوهات الشائعة للتصحيح

### السيناريو 1: الخادم لا يتصل

**الأعراض:** يعرض Inspector "Disconnected" أو يتوقف عند "Connecting..."

**قائمة التحقق:**
1. ✅ هل أمر تشغيل الخادم صحيح؟
2. ✅ هل تم تثبيت كل التبعيات؟
3. ✅ هل مسار الخادم مطلق أم نسبي للمجلد الحالي؟
4. ✅ هل تم تعيين متغيرات البيئة المطلوبة؟

**خطوات التصحيح:**
```bash
# اختبار الخادم يدويًا أولاً
python -c "import your_server_module; print('OK')"

# فحص وجود أخطاء في الاستيراد
python -m your_server_module 2>&1 | head -20

# التحقق من تثبيت MCP SDK
pip show mcp
```

### السيناريو 2: عدم ظهور الأدوات

**الأعراض:** تبويب الأدوات يظهر فارغًا

**الأسباب المحتملة:**
1. الأدوات لم تُسجل أثناء تهيئة الخادم
2. تعطل الخادم بعد بدء التشغيل
3. تابع معالجة `tools/list` يعيد مصفوفة فارغة

**خطوات التصحيح:**
1. تحقق من سجل الرسائل لاستجابة `tools/list`
2. أضف تسجيلات إلى كود تسجيل الأدوات الخاص بك
3. تحقق من وجود الزخارف `@mcp.tool()` (بايثون)

### السيناريو 3: الأداة تعيد خطأ

**الأعراض:** استدعاء الأداة يعيد استجابة خطأ

**نهج التصحيح:**
1. اقرأ رسالة الخطأ بعناية
2. تحقق من تطابق أنواع المعلمات مع المخطط
3. أضف try/catch مع رسائل خطأ مفصلة
4. تحقق من سجلات الخادم لتتبع الأخطاء

**مثال على تحسين التعامل مع الخطأ:**

```python
@mcp.tool()
async def my_tool(param1: str, param2: int) -> str:
    try:
        # منطق الأداة هنا
        result = process(param1, param2)
        return str(result)
    except ValueError as e:
        raise McpError(f"Invalid parameter: {e}")
    except Exception as e:
        raise McpError(f"Tool failed: {type(e).__name__}: {e}")
```

### السيناريو 4: محتوى المورد فارغ

**الأعراض:** يعيد المورد لكن المحتوى فارغ أو null

**قائمة التحقق:**
1. ✅ مسار الملف أو URI صحيح
2. ✅ لدى الخادم إذن لقراءة المورد
3. ✅ يتم إعادة محتوى المورد بشكل صحيح

---

## ميزات Inspector المتقدمة

### رؤوس مخصصة (SSE)

```bash
npx @modelcontextprotocol/inspector \
  --sse http://localhost:8080/sse \
  --header "Authorization: Bearer your-token"
```

### التسجيل المفصل

```bash
DEBUG=mcp* npx @modelcontextprotocol/inspector python server.py
```

### تسجيل الجلسات

يمكن لـ Inspector تصدير سجلات الرسائل للتحليل لاحقًا:
1. انقر على **Export Log** في لوحة الرسائل
2. احفظ ملف JSON
3. شارك مع أعضاء الفريق للتصحيح

---

## أفضل الممارسات

1. **اختبر مبكرًا وغالبًا** - استخدم Inspector أثناء التطوير، وليس فقط عند حدوث مشاكل
2. **ابدأ ببساطة** - اختبر الاتصال الأساسي قبل استدعاءات الأدوات المعقدة
3. **تحقق من المخطط** - العديد من الأخطاء تأتي من عدم تطابق أنواع المعلمات
4. **اقرأ رسائل الخطأ** - أخطاء MCP عادة ما تكون وصفية
5. **احتفظ بـ Inspector مفتوحًا** - يساعدك في اكتشاف المشاكل أثناء التطوير

---

## ماذا بعد؟

لقد أكملت الوحدة 3: البدء! تابع تعلمك:

- [الوحدة 4: التنفيذ العملي](../../04-PracticalImplementation/README.md)

---

## موارد إضافية

- [مستودع GitHub لـ MCP Inspector](https://github.com/modelcontextprotocol/inspector)
- [مواصفات MCP - رسائل البروتوكول](https://spec.modelcontextprotocol.io/specification/2025-11-25/)
- [مواصفات JSON-RPC 2.0](https://www.jsonrpc.org/specification)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**إخلاء المسؤولية**:  
تمت ترجمة هذا المستند باستخدام خدمة الترجمة الآلية [Co-op Translator](https://github.com/Azure/co-op-translator). بينما نسعى لتحقيق الدقة، يُرجى الانتباه إلى أن الترجمات الآلية قد تحتوي على أخطاء أو عدم دقة. يجب اعتبار المستند الأصلي بلغته الأصلية المصدر الموثوق به والمعتمد. للمعلومات الهامة، يُنصح بالترجمة البشرية المهنية. نحن غير مسؤولين عن أي سوء فهم أو تفسير ناتج عن استخدام هذه الترجمة.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->