McpToolEvents
    | summarize arg_max(EventTime, *) by ToolId
    | where EventType != 'Deleted'
    | project EventTime, ToolId, ToolName, ToolArea, ServerVersion, ServerName, EventType, ReplacedByToolName, ReplacedByToolArea
