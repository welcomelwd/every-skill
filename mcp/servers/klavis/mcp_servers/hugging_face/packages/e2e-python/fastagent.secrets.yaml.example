# Example of setting an MCP Server environment variable
mcp:
  servers:
    test_hf:
      headers:
        Authorization: Bearer TEST_ACCOUNT_HF_TOKEN
    test_all_hf:
      headers:
        Authorization: Bearer TEST_ACCOUNT_HF_TOKEN
