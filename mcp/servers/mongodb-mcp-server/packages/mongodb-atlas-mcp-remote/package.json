{
  "name": "mongodb-atlas-mcp-remote",
  "description": "MCP stdio proxy that forwards requests to the MongoDB Remote MCP server with OAuth2 client credentials authentication",
  "version": "1.0.0",
  "type": "module",
  "license": "Apache-2.0",
  "author": "MongoDB <info@mongodb.com>",
  "repository": {
    "url": "https://github.com/mongodb-js/mongodb-mcp-server.git",
    "directory": "packages/mongodb-atlas-mcp-remote"
  },
  "bin": {
    "mongodb-atlas-mcp-remote": "./dist/cli.js"
  },
  "main": "./dist/cli.js",
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": "^20.19.0 || ^22.13.0 || >= 24.0.0"
  },
  "scripts": {
    "compile": "tsc --project tsconfig.json",
    "test": "vitest --run",
    "build:update-package-version": "node scripts/updatePackageVersion.ts",
    "pretest": "pnpm compile"
  },
  "dependencies": {
    "@modelcontextprotocol/client": "^2.0.0",
    "@modelcontextprotocol/server": "^2.0.0",
    "@mongodb-js/devtools-proxy-support": "^0.7.16",
    "mongodb-log-writer": "^2.5.13",
    "mongodb-redact": "^1.4.6"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10"
  }
}
