# Triage brief — 2026-08-11

**Read path used:** GitHub MCP tools (`gh` CLI not checked; MCP tools worked directly).
**Cutoff:** 2026-08-10T01:58:23Z — the merge timestamp of PR #261 (the 2026-08-10 brief itself),
per the precedent set by the last several briefs. A bare filename-date comparison would either
re-report same-day activity that landed before the prior brief merged, or silently drop same-day
activity that landed after. Everything below is activity strictly after that timestamp.

## Needs your reply

### #275 — [Bug]: peer review tools still not working (khagyard, first-time reporter)

Reporter says `get_my_peer_reviews_todo` reports "no pending peer reviews" despite having a real,
incomplete peer review assigned on a past-due assignment — even when prompting with the specific
course/assignment ID (which the tool didn't accept as a parameter at the time).

This already moved fast between the cutoff and now, all your own work:
- You replied (2026-08-10T16:12Z) ruling out field-mapping and anonymization as causes, and
  shipped **PR #277** (merged 16:45Z) adding an `assignment_identifier` parameter that queries
  `/courses/:id/assignments/:id/peer_reviews` directly, bypassing the per-course discovery scan's
  `peer_reviews`-flag gate. You asked the reporter to retry with it.
- A separate Copilot-authored PR (**#276**) proposed adding `include[]=all_dates`/`include[]=submission`
  to the assignment listing as the fix; you closed it (unmerged) since `include[]` only adds fields
  to the response, it doesn't change which assignments are returned.
- The reporter replied again (2026-08-10T18:07Z, the most recent comment): **"The tool still is not
  working."** They confirmed it's a regular assignment, not anonymous, assigned before the due date
  — ruling out the two things you'd have asked about next.

**One thing worth checking before reading too much into "still not working": PR #277 merged at
16:45Z, but v1.9.0 had already published at 12:02Z that same day** (confirmed via the releases
list). The `assignment_identifier` parameter only exists on `main` right now — it isn't in any
tagged release. If the reporter is running the PyPI package or the `.mcpb` extension pinned to
v1.9.0, they have no way to have actually exercised the new parameter yet, and "still not working"
may just mean "same as before," not "the fix doesn't work." Worth confirming which distribution
they're on before spending more diagnostic effort.

**Unverified lead, not a confirmed cause** (I read the code, didn't reproduce against Canvas):
`get_my_peer_reviews_todo`'s direct-lookup path (`student_tools.py:348-361`) still goes through
`/courses/:id/assignments/:id/peer_reviews`, the same permission-gated listing endpoint the
per-course scan uses — your own comment in the source (`student_tools.py:396`) already flags this
endpoint as "permission-gated on some instances." If a student-scoped token gets a truncated or
empty response there (rather than an error the code would catch), the direct lookup would fail the
same way the scan does. Canvas's own `/users/self/todo` endpoint (already used by
`get_my_todo_items`, `student_tools.py:273-276`) surfaces peer-review to-dos scoped to the caller's
own permissions natively, which could be a more reliable path if the assessor-listing endpoint
really is permission-truncated for students — but this is a hypothesis from reading two call
sites, not something I measured.

```
Thanks for confirming — that rules out the two things I'd have guessed at next, and it's useful
that #277 still didn't surface it.

One thing to check first: #277 (the assignment_identifier parameter) merged after v1.9.0 had
already shipped, so it's only on main right now, not in any release yet. Are you running the
PyPI package / .mcpb extension, or pulling from main directly? If you're on the packaged v1.9.0,
you haven't actually been able to try the new parameter yet, and I want to rule that out before
digging further.

If you are able to try it (or once the next release ships), the exact prompt would be: "Do I have
a peer review to do for assignment <the assignment's ID> in <the course>?" — that goes straight at
the specific assignment instead of scanning the whole course.

If it's still empty even with that, I want to look at whether the peer-reviews listing endpoint
itself is returning a permission-truncated response for a student token rather than an error — that
would explain why both paths come back empty. Not confirmed yet, just my next suspect.
```

## PRs

- **#264, #265, #266, #272** (dependabot, opened 2026-08-10 ~05:14–05:21Z) — new since the cutoff,
  but **already triaged by you**, not waiting on a decision. I missed this on the first pass (only
  checked CI/mergeable state, not the PR comment threads) — a bot reviewer on this brief's own PR
  caught it. You left explicit hold-for-human comments directly on the PRs within the hour of them
  opening: **#264** (05:44Z) — the CI matrix (`.github/workflows/canvas-mcp-testing.yml:47`) and
  package classifiers (`pyproject.toml:20-24`) both stop at 3.13, so a green check on the 3.14-slim
  bump wouldn't actually cover the new interpreter; matrix + classifiers need to move first. **#266**
  (05:44Z, explicitly covers **#265** too) — both are `execute_typescript`-sandbox devDeps that the
  Python test suite never exercises, so a green check here measures the wrong thing; wants a real
  sandbox smoke test before merging either. **#272** (06:28Z) — the major Actions bumps land in four
  workflows nothing in CI exercises (`create-release.yml`, `publish-mcp.yml`, the two deploy
  workflows), and at the time of that comment `create-release.yml`'s PR #273 restructuring hadn't
  had a live run yet, so you wanted one clean release on the current pins first before stacking this
  on top. **That precondition has since been met** — v1.9.0 published successfully through the
  restructured workflow — so #272 may be ready by your own stated bar now, though you haven't said
  so yourself yet. All four: `mergeable_state: behind` (main moved past their base since
  #258/#243/#277 merged), still 0 reported CI checks as of this writing (re-verified directly, not
  from the session log). Nothing here needs a fresh decision from you — just a rebase + CI run
  whenever you pick each one back up, in the order your own comments already set.
- **#253** (`ChickenisLegit`, first-time contributor) — `fix: use form-data for create_announcement`,
  addresses #252. Unchanged since your 2026-08-09 comment holding off pending zqian's v1.7.0+
  retest. `mergeable_state: behind` (rebase, not a real conflict). Still 0 reported CI checks — fork
  PR, workflow run not yet approved, expected. No action until zqian responds.
- **#191** (Copilot, targets #172, draft) — unchanged. Still has the merge conflict against `main`
  that only you can resolve (`mergeable_state: dirty`, re-verified), and the PR body still contains
  an unmodified `Fixes`-trailer for issue 172 (verified directly against the current body, not
  quoted verbatim here on purpose) — merging as-is would trigger GitHub's auto-close on [issue 172]
  a third time. Also still blocked on the New-Quizzes detection correctness question (needs a
  New-Quizzes-enabled sandbox per #172). No change worth re-flagging beyond what's already tracked.

Merged since cutoff (context, not action items): **#258** (the #239 fencing/confirmation-token PR,
merged 04:30Z — CodeQL/review status from the prior brief is now moot), **#243** (docs follow-up to
#232, merged 02:24Z), **#277** (peer-review `assignment_identifier`, merged 16:45Z, see above),
**#278** (your own merge, 17:46Z — a Copilot Autofix suggestion for code-scanning alert #70,
tightening a test assertion in `tests/test_http_transport.py` from a substring check to a parsed
`.hostname` comparison; test-only, no runtime behavior change).

## Also merged since cutoff: v1.9.0 release + supply-chain work

Between the cutoff and now you also merged **#263** (OSSF Scorecard publishing, pinned actions,
Token-Permissions fixes), **#267**/**#268** (dependabot: mypy →2.3.0, tsx →4.23.10), **#273** (attest
the `.mcpb` bundle, split build/release authority), and **#274** (lint job's mypy constraint now
derived from `pyproject.toml` instead of a second hardcoded copy that had silently drifted from
#267). This is the same release work already logged in this repo's session history as "Release
v1.9.0 shipped, all five channels verified" — flagging here only so this brief's "what changed"
sweep doesn't have a gap where that work is invisible. No action needed; already closed out.

## New since 2026-08-10T01:58:23Z

- **#275** — bug report from `khagyard` (first-time reporter). See "Needs your reply" above.
- **#264 / #265 / #266 / #272** — dependabot dependency-bump PRs. See "PRs" above.
- **#276** — Copilot-authored PR proposing an `include[]` fix for #275; opened 14:36Z, closed
  unmerged by you at 15:47Z as not addressing a plausible mechanism. Superseded by #277.

## Going stale

- **#172** ([Feature]: Canvas Quizzes, zqian) — last non-you touch was `jonespm` on 2026-08-04
  (an outside observer flagging that #191 still wasn't merged after a bad auto-close). You already
  answered fully on 2026-08-07. Blocked on #191's merge conflict + New-Quizzes verification sandbox,
  not on you.
- ⚠️ **#170** ([Feature]: student-role write tools, zqian, FERPA-adjacent) — last touch from zqian
  was 2026-07-29 (12+ days). Two open design questions are still waiting on them: default posture
  for `COURSE_AGENT_POLICY_DEFAULT`, and whether the syllabus-as-policy-carrier is acceptable to
  their service owner. High-sensitivity topic; a light nudge is reasonable, no draft reply
  authored here per prior brief's convention of leaving this one to you directly.
- **#142** (blocked-upstream watch item) / **#157** (sandbox hardening backlog) — both long-parked
  by design (you already reframed #142 as watch-only and unassigned it; #157 is deprioritized to
  self-hosted-only). No new activity, nothing actionable.

## Checked, nothing needed

#270, #271, #262, #239, #236 — your own issues, only touched by the `auto-label-issues.yml` bot
since the cutoff (labeling only, per its normal job — not counted as "waiting on you"). #252 —
last comment is yours (2026-08-09), zqian hasn't replied yet but it's only 3 days, not stale.
#142, #157 — reviewed under "Going stale" above, no action beyond what's already recorded. #191,
#253 — reviewed under "PRs" above, unchanged, no new information since the last brief.

## Executor results (2026-08-11T01:53Z)

*Note on concurrency: the brief's author pushed two corrections (`77ae072`, then `4912bb1`→reworded as `fc667db`) to this
branch while I was verifying, in response to bot review on PR #280. I rebased onto them and revised
this section to match — where we independently reached the same finding it's marked as agreement,
and where we still disagree (the CI-status question) the evidence for both readings is laid out
rather than one simply overwriting the other.*

### ⛔ Blocker found: `main` is red on the required `lint` check

The headline item is not in the brief, because it isn't about any open PR — it's about `main`
itself. **The required `lint` check currently fails on `main`, so every open PR against `main` is
blocked, including this brief's own PR (#280).** Reproduced locally on `origin/main` (09ccc90):

```
$ ruff check src/ tests/
I001 [*] Import block is un-sorted or un-formatted
  --> tests/test_http_transport.py:3:1
Found 1 error.
[*] 1 fixable with the `--fix` option.
```

Origin: **PR #278** (Copilot Autofix for code-scanning alert #70), merged 2026-08-10T17:46:33Z. It
added `from urllib.parse import urlparse` *above* the existing `from unittest.mock import ...`,
leaving the import block unsorted. The one-line fix is `ruff check --fix tests/test_http_transport.py`
(swap the two import lines).

**This was visible before the merge and merged over.** #278's own `Canvas MCP Enhancement Testing`
run concluded `failure` on head `6a3e93f2` at both 16:55Z and 17:46Z — the `lint` job failed at the
`Run Ruff` step. The merge went in anyway (admin bypass on branch protection). Worth noting because
the brief lists #278 under "no action needed, already closed out" — it is in fact the one merge
since the cutoff that left `main` broken.

I did **not** fix it: `tests/test_http_transport.py` is outside `internal/issue-triage/`, and
pushing to `main` is outside my authority. Top item for Vishal below.

### Corrections to the brief

1. **"0 reported CI checks" for #264/#265/#266/#272 — the number is right, the conclusion drawn
   from it is not. All four have already run CI, and all four are green.** The brief re-verified
   this after bot-review feedback and kept the claim (*"still 0 reported CI checks as of this
   writing (re-verified directly)"*), so this needs settling with evidence rather than assertion.

   Both readings come from different APIs. The **combined-status** endpoint really does return
   `total_count: 0` here — for every PR in this repo, including ones that are unambiguously green —
   because nothing in this repo publishes legacy commit *statuses*. Every check is a GitHub Actions
   **check run**, which that endpoint does not report. Querying workflow runs by branch instead
   (`actions_list` → `list_workflow_runs`) shows completed runs on each PR's current head:

   | PR | head | Canvas MCP Enhancement Testing | Security Testing | Closing Keyword Guard | Claude Code Review |
   |----|------|-------------------------------|------------------|----------------------|--------------------|
   | #264 | `e519af9` | ✅ success | ✅ success | ✅ success | ❌ failure |
   | #265 | `35dc720` | ✅ success | ✅ success | ✅ success | ❌ failure |
   | #266 | `d2cee9b` | ✅ success | ✅ success | ✅ success | ❌ failure |
   | #272 | `ccfc5c5` | ✅ success | ✅ success | ✅ success | ❌ failure |

   Both required checks (`test-enhancements`, `lint`) are green on all four. `Claude Code Review` is
   not a required check and fails on every dependabot PR (restricted secrets — the same structural
   reason `claude-review` was dropped from required checks in #188). **No PR is stalled for CI, and
   no retrigger commit was needed anywhere.**

   The practical difference: the brief's closing line for these four is "just a rebase + CI run
   whenever you pick each one back up," which reads as *CI still owes us an answer*. It doesn't —
   CI has already answered, and the answer was green on every required check. What's actually left
   on each is the human prerequisite the brief documents so well in the paragraph above (matrix +
   classifiers for #264, a sandbox smoke test for #265/#266, your own sign-off for #272). This also
   means a green check on a rebase would tell you nothing new about #264/#265/#266 that your own
   hold comments haven't already discounted — which is exactly your point in those comments.

2. **#191's PR body contains a live closing keyword** — *self-corrected by the brief's author at
   `4912bb1` (now `fc667db`) while I was working, so this is now agreement rather than a correction.* I found it
   independently before that commit landed and am keeping the verification here because it's the
   higher-stakes of the two disputed facts. Its last line is the literal token `Fixes` directly
   preceding a bare `#`-reference to [issue 172] — *not* the defused `[issue 172]` bracket form the
   brief originally reported. Verified against the repo's own detector (reference redacted below so
   this file doesn't itself become the vector):

   ```
   $ python3 scripts/check_closing_keywords.py --strict <pr-191-body>
   ✗ GitHub closing keyword in prose
     ...:1: - Fixes #<NNN>
     Landing this on the default branch will CLOSE #<NNN>.
   ```

   The brief's conclusion (merging #191 as-is would auto-close [issue 172] a third time) is right;
   its evidence understated the risk. **The body must be edited before #191 merges** — and #191 is
   the one PR here where the guard cannot save you, since the `commit-msg` hook and the CI backstop
   scan commits and the *brief's* files, not a Copilot-authored PR body written months ago.

   Worth recording: writing this correction **tripped the guard on my own draft** — quoting the
   offending line verbatim was itself enough to arm it, which is precisely how [issue 172] got
   closed the second time (commit `98643ce`, whose message documented the first accident and
   repeated the phrase). The guard did its job on me; the wording above is the rephrased version.

3. **The brief's recommended next action on the dependabot PRs — "rebase each onto current `main`,
   let CI run" — should be held until `main` is green.** All four are `behind`, and their green runs
   predate #278. Updating any of them now would merge the broken import into the branch and turn a
   genuinely-green PR red for a reason that has nothing to do with it, destroying the only clean
   signal we currently have. I deliberately skipped the update-branch step for that reason; it is
   ~30 seconds of work once the one-line lint fix lands. Their stated prerequisites are unchanged
   (#264 wants 3.14 in the CI matrix; #265/#266 want a sandbox smoke test; #272 is otherwise ready).

4. **#253 has no merge conflict and passes the full suite merged with `main`.** The brief calls it
   `behind` ("rebase, not a real conflict") — confirmed by trial merge in my sandbox.

5. Minor: #277's test plan records `1325 passed, 21 skipped`; I measure `1324 passed, 22 skipped` on
   the same tree. One test is environment-skipped here rather than run. Not a regression, noted only
   so the counts below don't read as a discrepancy.

### Local verification (real output)

Full suite `uv run python -m pytest tests/ -q` + `uv run ruff check src/ tests/`, each PR at its own
head (not merged with `main`, except where noted):

| Target | Tests | Ruff |
|--------|-------|------|
| `origin/main` (09ccc90) — baseline | `1324 passed, 22 skipped` in 27.37s | ❌ **1 error (I001)** |
| #272 (`ccfc5c5`) | `1320 passed, 22 skipped` in 25.64s | ✅ All checks passed |
| #265 (`35dc720`) | `1320 passed, 22 skipped` in 25.37s | ✅ All checks passed |
| #266 (`d2cee9b`) | `1320 passed, 22 skipped` in 25.17s | ✅ All checks passed |
| #264 (`e519af9`) | `1320 passed, 22 skipped` in 24.80s | ✅ All checks passed |
| #253 (`5a726f0`, fork) | `1186 passed, 22 skipped` in 23.21s | ✅ All checks passed |
| #253 **merged with `main`** | `1324 passed, 22 skipped` in 26.23s | (merge clean, no conflicts) |

The four dependabot branches sit at 1320 rather than 1324 because they branch from before #277/#278
added tests; #253 sits at 1186 because it branches from 2026-08-08, before #258 landed its test
additions. Every branch is green on its own base — the ruff failure appears **only** on `main` and
on anything containing `main`'s current tip, which is the cleanest available confirmation that #278
is the sole source.

**Not tested, and why:** #191 (draft, and `mergeable_state: dirty` — it has a real merge conflict
against `main`, so there is no mergeable tree to test). No other open PR was skipped.

### Draft-reply review (#275)

The brief's central factual claim for the draft reply **checks out**: v1.9.0 published
2026-08-10T12:02:23Z, #277 merged 16:45:24Z — the `assignment_identifier` parameter is on `main`
only and is in no tagged release. The code-reading lead also checks out as *described*:
`get_my_todo_items` does call `/users/self/todo` (`student_tools.py:273-276`), the direct-lookup
path does go through `/courses/:id/assignments/:id/peer_reviews` (`:348-361`), and the
"permission-gated on some instances" comment is really there (`:396`). It is correctly labelled
unverified, and nothing in the draft asserts unmeasured Canvas behavior as confirmed — no flag from
me on that front.

**One thing the draft doesn't account for:** Vishal's 16:12Z comment already said *"Once released,
could you try…"*, so the reporter has already been told the parameter isn't available yet. Their
18:07Z *"The tool still is not working"* is therefore almost certainly a report on the **old**
behavior, not a failed test of #277 — which makes the draft's opening question ("are you running
PyPI or main?") read as re-asking something already covered. Suggest tightening to acknowledge that
directly, e.g. *"Just to set expectations — that parameter still isn't in a release, so what you're
seeing is the same pre-#277 behavior. Nothing's wrong on your end."* Substance of the reply is
otherwise sound.

No new comments on #275 since the brief was written (last is still `khagyard` at 18:07Z).

### Needs Vishal

1. **🔴 Unbreak `main` (one line, blocks everything).** `ruff check --fix tests/test_http_transport.py`
   — reorder the two imports so `unittest.mock` precedes `urllib.parse`. Until this lands, the
   required `lint` check fails on every open PR, this brief's PR #280 included. Follow-on worth
   considering: #278 was merged over a visibly failing required check, which is the same class of
   gap as #247 (CI that looks green but isn't) — an admin-bypass merge is exactly when the check
   most needed to be honoured.
2. **Edit #191's PR body** — rewrite its final line so the `Fixes` token no longer directly precedes
   the `#`-reference (e.g. `- Addresses [issue 172]`) before it goes anywhere near a merge. I am not
   permitted to edit PR bodies.
3. **Post the #275 reply** (draft above, with the suggested tightening). Drafts stay drafts on my
   side.
4. **Merge decision on #272** — genuinely green on both required checks and locally verified; its
   stated prerequisite (a live `create-release.yml` run) was met by v1.9.0. Rebase *after* item 1,
   not before.
5. **#264 / #265 / #266** — green, but hold for their stated prerequisites (3.14 in the CI matrix;
   sandbox smoke test). No action needed today beyond not rebasing them into a red `main`.
6. **Nudge zqian on #170** (12+ days, two open design questions). FERPA-adjacent — left entirely to
   you, no draft authored, per the brief's convention.
7. **#253** — no code blocker found (clean merge, full suite green). Still gated on zqian's v1.7.0+
   retest per your 2026-08-09 comment; that's a decision, not a technical obstacle.

### Actions taken by me

Read-only verification, plus this section. No PR was updated, rebased, commented on, labeled, or
merged; no retrigger commits were pushed (none were warranted — see correction 1); no file outside
`internal/issue-triage/` was modified.

### Executor addendum (2026-08-11T02:04Z) — PR #280 left open, one blocker remains

I waited for CI on my own head commit rather than merging on the stale green from before my push.
Two required checks came back red. One has since been resolved by the brief's author; one has not,
and it is why this PR is still open.

**Blocker A — `Closing Keyword Guard`: RESOLVED (by the brief's author, not by me).** The guard
failed on commit `4912bb1` — the commit that *fixed* the brief's misquote of #191's body. Its
**commit message** quoted the offending line verbatim to explain the fix, which armed it:

```
✗ GitHub closing keyword in prose
  commit 4912bb1:5: actual body directly: it reads "- Fixes #<NNN>" (no brackets). The
                    ^^^^^^^^^^
  Landing this on the default branch will CLOSE #<NNN>.
```
*(reference redacted as `#<NNN>` — reproducing it verbatim would arm this file too, which is the
whole point.)*

That is the exact shape of commit `98643ce`, which re-closed [issue 172] four seconds after being
pushed because its message documented the first accident and repeated the phrase. **The guard
caught it; the message has since been reworded (`4912bb1` → `fc667db`) and the branch force-pushed,
with my section rebased on top intact.** No action left here.

Three things worth keeping from it, though:

1. **The `commit-msg` hook did not catch this at authoring time** — CI did. `./scripts/install-hooks.sh`
   is a per-clone step, and these agent sessions run in fresh ephemeral containers where it hasn't
   been run. The CI backstop is currently the *only* live layer for agent-authored commits. That's
   a real durability gap, close in spirit to the guard follow-up already tracked as #262.
2. **Writing the correction tripped the guard on my own draft too**, twice — quoting the offending
   line was enough both times. Between the brief's author and me, one accurate description of this
   bug produced three separate armings in a single day. The bracket convention isn't fussiness.
3. Had this merged with the `allow-closing-keyword` label instead of a reword, it would have closed
   [issue 172] a third time. The label is for a deliberate close; this was narration.

**Blocker B — `lint` (`Canvas MCP Enhancement Testing`): STILL RED.** Fails at `Run Ruff` with
`main`'s I001 break, inherited by every branch cut from current `main`. On my commit the four
matrix jobs (`test (3.10/3.11/3.12/3.13)`) all passed and only `lint` failed. Fixing it means
editing `tests/test_http_transport.py` — outside `internal/issue-triage/`, so outside my authority.

**PR #280 is therefore left open, unmerged.** It needs the one-line ruff fix on `main` first (item 1
under Needs Vishal); once that lands, this branch needs a rebase and should go green with no further
edits. Nothing else about the brief or this section is blocking.
