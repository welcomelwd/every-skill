# GitHub Activity Triage — 2026-07-31

**Read path used:** GitHub MCP tools (`gh` CLI not installed in this sandbox).
**Cutoff:** 2026-07-30T01:36 UTC (24 hours before this run — no prior `internal/issue-triage/*.md` files exist, so no stored cutoff was available).

---

## Needs your reply

None. Every open issue with a comment posted after the cutoff has **you** as the most recent commenter (#172, #142, #170, #179) — nothing is sitting in your court waiting on a reply. The three brand-new issues below have zero comments yet, so they fall under "New since cutoff," not this section — but see the draft replies there, since they're real asks from your top external collaborator.

---

## PRs

Two open PRs, both authored by the **Copilot coding agent** (same-repo branches, not forks — so the "withheld first-timer CI" caveat does not apply here; zero reported checks means CI hasn't reported yet, not that it's gated on approval).

### #201 — Add missing MCP tool annotations and document `list_courses` flags
- Draft, `mergeable_state: blocked`, **0 checks reported** (opened ~5 min before this run).
- Fixes #200 directly (the annotations bug reported below).
- **Next action:** nothing to do yet — wait for CI to report on the first run, then review. Flag if checks are still empty next triage cycle (may indicate Actions isn't triggering for this bot's branches).

### #191 — feat: Add dual-engine read tools for Canvas Quizzes (Classic and New)
- Draft, `mergeable_state: behind` (main advanced past this branch — v1.6.0 bump + #195 doc fix landed after it was opened), **0 checks reported**.
- Fixes #172 (Tier A quiz read layer, scoped per your design comment on that issue).
- You already left a **detailed blocking review** (2026-07-30T10:25): the New Quizzes detection logic (`is_quiz_assignment AND submission_types contains external_tool`) is unverified against a live instance and, per your own measurement, `is_quiz_assignment=True` actually marks **Classic** quizzes — so the `AND` condition may make New Quizzes detection always return empty, silently defeating the tool's whole purpose. You also flagged the `engine` parameter as an unvalidated free string routed inconsistently across the three quiz tools.
- **Next action:** none from you — ball is with Copilot to fix the detection logic + validate `engine` + rebase onto main. Nothing to reply to; this is tracked as "waiting on Copilot," re-check next cycle.

---

## New since 2026-07-30T01:36 UTC

All three new issues are from **zqian (University of Michigan)** — your highest-priority active evaluator, filed within a ~4-hour window on 2026-07-30 evening.

### #200 — [Bug]: lack of annotations in several mcp tool definition
Local CI checker output shows ~29 tools missing `readOnlyHint`/`destructiveHint`/`idempotentHint` annotations, plus two undocumented `list_courses` params. Already has a fix in flight: **PR #201** (draft, see above). No reply needed — the PR is the response.

### #199 — [Bug]: check_enrollment tool not working ⚠️ HIGH SENSITIVITY (identity/FERPA-adjacent)
zqian (Teacher role in course 505) asked `check_enrollment` about their own NetID (`zqian`) and got "No active enrollment found," even though `list_enrollments` on the same course clearly shows them on the roster. They note NetID is "a UIUC thing" — at U-M this is called a "uniqname," and their email is `zqian@umich.edu`.

**Unverified root-cause candidates** (not confirmed against a live instance):
1. `core/enrollment.py:82-87` matches `net_id` against Canvas's `login_id`, **falling back to `sis_user_id`** if that doesn't match, both with case-insensitive exact string equality (both sides are `.strip().lower()`'d first). If U-M's Canvas `login_id` is formatted differently from UIUC's (e.g. a full email/SSO identifier rather than the bare uniqname `zqian`) *and* `sis_user_id` is either unpopulated or also not the bare uniqname, the equality check silently fails on both fields and returns a confident "NO" even though the person is enrolled — case-folding wouldn't help there, since the mismatch would be in content, not casing. This is exactly the kind of institution-specific format assumption that would explain a UIUC-tested tool breaking at U-M.
2. `active_only=True` is the tool's default (`tools/enrollment.py:31`) and independently filters out any enrollment whose `enrollment_state != "active"` (`core/enrollment.py:79`). The bug report says they tried "checking any role" but doesn't confirm they also tried `active_only=False` — if their Teacher enrollment state is something other than exactly `"active"` (e.g. `"invited"`), this alone would reproduce the symptom regardless of the NetID-matching issue.

Both are plausible; only zqian's own instance can confirm which. Draft reply below asks for the two data points needed to disambiguate without asking them to share the raw roster.

```
Thanks for the detailed repro — this is useful, and I want to run down the
right cause rather than guess.

Two things would let me pin this down without you having to share any
roster data:

1. Can you re-run with `active_only=false` explicitly (not just role="any")?
   The tool has two independent filters — role/type and active-only — and
   your report confirms you varied role but I want to rule out the
   active-only filter as a separate cause.

2. Do you know whether U-M's Canvas `login_id` for your account is the bare
   uniqname ("zqian") or something else (e.g. your @umich.edu email, or an
   SSO-issued identifier)? The tool matches net_id against login_id first,
   falling back to sis_user_id if that doesn't match — if U-M's login_id
   format differs from UIUC's bare NetID *and* sis_user_id is either unset
   or in the same non-uniqname format, that would explain a false "not
   enrolled" even though you're clearly on the roster. You may be able to
   see this via Canvas's own People page if you have access to it, or your
   Canvas admin could confirm.

If it's #2, the real fix is probably relaxing the match to handle common
login_id shapes (email-prefix match, etc.) rather than assuming bare NetID
everywhere — I'd rather get that right than patch around your instance
specifically.
```

### #198 — [Bug]: file upload without a folder creates a spurious "unfiled" parent folder
zqian uploaded a file to a course without specifying a folder, expecting it to land in the course files root (per the tool's own docstring: `folder_path: Canvas folder path (default: "course files" root)`). Instead Canvas created and used a new "unfiled" folder.

**Likely root cause, fairly confident** — `tools/files.py:312-315`:
```python
if folder_path:
    upload_request_params["parent_folder_path"] = folder_path
```
When `folder_path` is `None`, **no folder parameter is sent to Canvas at all** in the `POST /courses/:id/files` request. Canvas's own default behavior when no parent folder is specified is to place the file in "unfiled," not the course files root — so the docstring's claimed default doesn't match what actually happens when the parameter is simply omitted. This is a straightforward reproduction, not a guess, but I haven't run it against a live instance to confirm Canvas's exact default-folder behavior matches this reading of the code.

**Suggested fix (unverified, not applied):** explicitly send `parent_folder_path: "/"` when `folder_path` is `None`, rather than omitting the key.

```
Thanks for catching this — I think I see it. When no folder_path is given,
the tool currently sends no folder parameter to Canvas at all, and Canvas's
own default in that case is "unfiled," not the course root, despite what
our docstring says. Fix should be a one-liner (explicitly request the root
path instead of omitting it) — I'll verify against a live course and get a
patch out.
```

### PRs opened since cutoff
- **#201** (Copilot, fixes #200) — see PRs section above.
- **#191** (Copilot, fixes #172) — see PRs section above.

---

## Going stale

- **#106** — Address 186 mypy errors uncovered by adding mypy to dev deps. No non-you activity since the sole `claude[bot]` labeling comment on 2026-05-14 (78 days idle). Pure backlog, nobody waiting.
- **#157** — Harden `execute_typescript` sandbox. No non-you activity since the `claude[bot]` labeling comment on 2026-07-04 (your own follow-up comment on 07-10 was the last word). Backlog; you already downgraded priority in-thread (self-hosted-only concern, hosted has the flag disabled).

---

## Checked, nothing needed

- **#172** (Quizzes feature) — last comment is yours (assigning to Ash with a concrete first task); nothing pending from your side.
- **#142** (mcp v2 pin watch) — last comment is yours (re-verified still blocked upstream, converted to watch item, unassigned Ash); no action pending.
- **#170** (student write tools) — last comment is yours (Tier 1 merged, announcing to zqian); two questions to zqian are still open in-thread but that's on him, not you.
- **#179** (anonymization consolidation) — last comment is yours (status update, remaining scope stays open); no action pending.
- **#173** (TOOL_MANIFEST coverage) — only comment is the `claude[bot]` labeling pass on 2026-07-29, recent enough not to count as stale yet.
- **#168** (weekly maintenance report) — covered by existing `weekly-maintenance.yml` automation per your instructions; not duplicated here.
- **#181** (rubric not showing on assignment page, closed) — worth noting even though closed and out of scope: zqian confirmed the #189 fix works on 2026-07-30T21:13 ("Confirmed the fix works as expected. Thanks!"). Clean close, no action needed.
