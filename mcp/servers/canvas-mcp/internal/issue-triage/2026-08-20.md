# Triage brief — 2026-08-20

**Read path used:** GitHub MCP tools (`mcp__github__*`).
**Cutoff:** 2026-08-19. `internal/issue-triage/2026-08-19.md` is the newest prior brief.

Quiet day on the "needs your reply" front — you already replied to everything that was
waiting on you, including closing #170. One new issue landed afterward, from `zqian`.
Category 3 is non-empty, so proceeding past early exit.

## Needs your reply

**Correction (see "Executor results" below): #302 is waiting and was miscategorized in
this brief's original pass — it belongs here, not under "Checked, nothing needed."**
`rpsimon-ai` (first-time contributor) opened it 2026-08-18T16:10 UTC and it has never
received a reply; the draft carried from the 2026-08-18 brief is still accurate and
unsent.

Everything else: no comment-thread reply is waiting on you. You are the last commenter on
every other open issue that had one waiting (#301, #299, #283, #275, all answered
2026-08-19T12:46 UTC in one pass, and #170 closed the same minute). No comment postdating
that pass has landed from anyone else.

## PRs

- **#308 — `chore: refresh impact stats`** (you, ready, not draft) — all 17 checks
  **green**, including `claude-review` (runs cleanly here since it's your own branch, not
  a fork). `mergeable_state` reads `unknown` — GitHub hasn't finished computing it, not a
  conflict signal on a 1-file docs/data PR. One thing worth a look before merging: the
  `claude-review` bot flagged that `pypi.last_month` drops from 7426 → 2208 (~70%) while
  `last_week`/`last_day` and all GitHub/npm figures moved normally in the same window —
  consistent with a bot/mirror spike aging out of the 30-day rolling window, but also
  consistent with the collection script hitting a different pypistats window. Worth a
  30-second glance at pypistats.org/packages/canvas-mcp before merging; not a blocker if
  it checks out.
- **#295, #296, #297, #298 (dependabot)** — same heads as the last brief, all required
  checks still show green, only `claude-review` red (secrets withheld from
  dependabot-triggered runs, not a required check per #188). **Correction to the last
  brief's framing:** those green runs are from **2026-08-17T04:40 UTC**, when each PR was
  opened — GitHub does not re-run a PR's checks just because the base branch moves, so
  they have not been re-tested against `main` as it stands now, which has since picked up
  several merges (most recently #310/#311). None of the four look likely to conflict with
  those changes at a glance, but "still safe to merge" was premature without an actual
  re-run; `update_pull_request_branch` (or closing/reopening) would force one if you want
  that confirmation before merging rather than after.
- **#191 — `feat: Add dual-engine read tools for Canvas Quizzes (Classic and New)`**
  (Copilot, draft, targets #172) — **unchanged for an eighth cycle.** Same head
  (`9e2d146`), `mergeable_state: unknown`/dirty, zero CI checks, body still ends with a
  live `Fixes` line pointing at [issue 172] that would auto-close it on merge. Same three
  blockers as every prior brief: (1) the one-file conflict in `assignments.py`
  (non-mechanical), (2) reword the closing line to `Addresses` + the same reference, (3) still needs a
  New-Quizzes sandbox before the correctness bug (`is_quiz_assignment AND external_tool`
  measured live as a *Classic*-quiz signal) can even be fixed.

## New since 2026-08-19

- **#309 — "[Feature]: add support for content migration"**, opened by **`zqian`**
  (University of Michigan — high-priority external collaborator, owns #172), 2026-08-19
  19:25 UTC. No comments yet.

  Requests a `content_migrations`-backed tool so an instructor can copy/import content
  from one course (or a template course) into another — assignments, discussions, etc.,
  with `date_shift_options` and progress polling via the Progress API. Checked the repo:
  grep for `content_migrations` / `course_copy` / `migration_type` across `src/` returns
  **zero hits** — no tool calls that Canvas API. PR #191's quiz work only *mentions*
  `content_migrations` as a future QTI-import prerequisite, nothing implemented.

  **Not wholly new ground, though:** `skills/canvas-course-builder/SKILL.md` already has
  a "Clone from Existing Course" mode (`get_course_structure` on the source, preview, then
  recreate matching modules/pages/assignments/discussions in the target). It's explicit
  that this is **structure only** — "Content is not copied when cloning... For content
  migration, copy page bodies using `get_page_content` + `create_page`" — so it's a
  manually-orchestrated, per-item workaround built from existing tools, not the native
  Canvas copy API. #309 is asking for the real thing (single async call, full fidelity
  including files/quizzes/rubrics, no per-item recreation), which is a materially
  different and larger piece of work than the skill covers — but the reply should
  acknowledge the existing partial workflow rather than imply there's nothing here at all.

  Worth flagging as a design question before scoping: content migration is a
  **write across two courses at once**, with delayed/queued completion (the Progress API
  callback), and can silently overwrite grading structure, due dates, or existing content
  in the target course depending on `selective_import`. That's a materially bigger blast
  radius than the single-course write tools already shipped, and doesn't fit neatly into
  the existing `STUDENT_WRITE_TOOLS`-style allowlist since it's instructor-only by nature
  (Canvas requires manage-content permission on both courses).

  Draft reply:
  ```
  Thanks for filing this with the payload example — that's helpful, and it's a real gap.
  The closest thing today is canvas-course-builder's "clone from existing course" mode,
  but that only recreates structure (module/page/assignment names) by reading one course
  and rebuilding items in another — it explicitly doesn't copy content or use Canvas's
  own copy API. Nothing in the codebase calls content_migrations.

  Before I scope a tool, a few things I want to get right given the blast radius: a course
  copy is a write into an existing target course, it runs async (Progress API polling,
  not a single request/response), and depending on selective_import it can touch grading
  structure, due dates, and existing content the target course already has. I'd want the
  tool to default to a dry-run/preview of what would be copied, require explicit
  confirmation before the real migration kicks off, and surface Progress API failures
  (partial imports, individual item errors) rather than reporting bare "started" success.

  Two questions that would shape the design: (1) do you need selective import (pick
  specific assignments/discussions/etc.) from day one, or is "copy everything" enough for
  a first version with selective import as a fast follow; (2) is date_shift_options a hard
  requirement for your workflow, or can that wait? Selective import is the bigger lift of
  the two (Canvas's copy-parameters format for choosing individual items is its own small
  API surface), so knowing whether you need it now changes the estimate a lot.
  ```

No other issues or PRs were opened since the cutoff.

## Going stale

- **#172** (zqian/jonespm, quizzes): last external touch was jonespm's 2026-08-04
  comment — now 16 days waiting on New-Quizzes sandbox access, which is also what's
  blocking #191.
- **#142** (Ash, `blocked-upstream` watch item): last external touch from
  `ashcastelinocs124` was 2026-07-01. Deliberately a no-action watch item since
  2026-07-30 — flagged for awareness only.

## Checked, nothing needed

#283, #275 (you are the most recent commenter on both — final retest requests sent
2026-08-19, waiting on `khagyard`; no action pending on your side), #170 (closed by you
2026-08-19, `completed`), #301, #299 (you replied 2026-08-19 with scoping questions for
`rpsimon-ai`/`turner-moore`; both await their response, not yours), #236 (self-filed by
you; only activity since is an automated labeling comment, no external ask), #157, #293
(automated weekly-maintenance report, opened 2026-08-16 — not duplicated per standing
instructions, no new comments). **#302 is not in this list — see "Needs your reply"
above.**

---

## Executor results (2026-08-20T01:52Z)

Brief read at **`9b60d40`**, not the `4cff562` the PR was opened at — the triage agent pushed
an amendment ("fix two Codex review findings") seven minutes after opening #312. Both revisions
were read; everything below is against the amended tip. Independently re-listed open issues and
PRs at this timestamp: nothing has moved since the amendment.

### Closing-keyword scan

`scripts/check_closing_keywords.py --strict` exits **0** on the brief at `4cff562`, at `9b60d40`,
on a verbatim copy of PR #312's body, and again on this section after writing it. Nothing to
reword before merge.

### Repo health on this branch

`triage/2026-08-20` at `9b60d40`: `uv run python -m pytest tests/ -q` →
**1374 passed, 21 skipped, 2 warnings** (23.4s); `uv run ruff check .` → **All checks passed!**

### PR work

| PR | head | pytest | ruff |
|----|------|--------|------|
| #308 impact stats | `a65e913` | 1374 passed, 21 skipped (27.04s) | All checks passed |
| #295 `python` digest `a7fb1e6`→`ce40764` | `b2ad3d3` | 1358 passed, 21 skipped (25.36s) | All checks passed |
| #296 `tsx` 4.23.10→4.23.12 | `6acd6a3` | 1358 passed, 21 skipped (25.40s) | All checks passed |
| #297 `@types/node` 26.1.2→26.2.0 | `db30044` | 1358 passed, 21 skipped (24.31s) | All checks passed |
| #298 actions group (5 updates) | `eb497f2` | 1358 passed, 21 skipped (24.81s) | All checks passed |

All five heads verified byte-identical to the values in the brief. The 1358-vs-1374 gap is not a
regression: the four dependabot branches are still based at `77f55a4` and predate the tests added
since. **Not tested: #191** — draft, and its merge is dirty, so there is no mergeable tree to test.

- **#191** — re-measured, unchanged for an eighth cycle. Head still `9e2d146`, `updated_at` still
  2026-08-14, `mergeable_state: dirty`, `get_check_runs` → `total_count: 0`, body still ends with a
  live auto-close line pointing at [issue 172]. **No empty retrigger commit pushed:** `9e2d146` *is*
  already that commit (2026-08-15) and it produced zero checks in this exact dirty state, because a
  `pull_request` workflow evaluates against a merge ref an un-mergeable PR does not have. Re-attempt
  only after the conflict in `assignments.py` is resolved.
- **No branch updates pushed anywhere.** `update_pull_request_branch` writes a non-empty merge commit
  onto a branch this routine did not create, which is outside its boundary — see the dependabot note
  below for why that now matters more than it did yesterday.

### The #308 pypi anomaly — resolved, measured

The brief asked for a 30-second glance at pypistats before merging #308. Done properly, and the
`claude-review` bot's flag **does not hold up**:

- **Live fetch, today:** `https://pypistats.org/api/packages/canvas-mcp/recent` →
  `last_day: 51, last_week: 451, last_month: 2228`. PR #308's snapshot (collected 2026-08-17) reads
  `48 / 551 / 2208`. Live data three days later corroborates it.
- **"A different pypistats window" is not a mechanism this collector has.**
  `scripts/collect-impact-stats.sh:55` hits one fixed endpoint (`/recent`) and reads the
  `data.last_month` field, every run. There is no window parameter to drift.
- **Internal consistency points the other way.** 2208 ≈ 4 × the same snapshot's `last_week` of 551.
  The *old* 7426 was 13.4 × its own weekly figure of 553, and was recorded byte-identical on both
  2026-08-04 and 2026-08-09. The anomaly is the 7426 era ending, not the 2208 arriving — consistent
  with a mirror/bot spike aging out of the 30-day rolling window, which is the first of the two
  explanations the brief offered.

Nothing on this count should hold up #308.

### Corrections to the brief

1. **"Needs your reply: None" is wrong — #302 is waiting.** `issue_read get_comments` on #302
   returns an **empty array**; `updated_at == created_at == 2026-08-18T16:10:15Z`. Nobody has
   replied to it at all. It is from `rpsimon-ai`, a first-time contributor, and has now been
   unanswered for ~34 hours. The brief does say the draft is "still unsent and still accurate", but
   files it under **Checked, nothing needed**, where it reads as handled. It is the one open issue
   genuinely awaiting Vishal today.
2. **The `content_migrations` mention is not in PR #191.** Grepped the `pr191` head itself
   (`git grep` over `refs/pull/191/head -- src/`): **0 hits**. Grepped the whole working tree
   (`*.md`, `*.py`, `*.ts`, `*.json`, excluding `internal/issue-triage/`): **0 hits**. The mention
   lives in **issue #172's Tier C1 scoping comment** (2026-07-30): "QTI import via
   `content_migrations` can produce New Quizzes." The brief's substance is unchanged and if anything
   stronger than stated — zero references anywhere in the repo, not just under `src/` — but the
   pointer for #309 is #172, not #191.
3. **Both carried #293 P1 doc gaps are now closed** (the 2026-08-19 brief still listed them open).
   `schema_version` is documented at `tools/README.md:1911,1915` and `AGENTS.md:348-349`; the #291
   permission pre-check and downgrade-cleanup are documented at `tools/README.md:941-943` and
   `AGENTS.md:105`. Drop them from the carried list.

### Verification of the amendment's own claims — all hold

- `skills/canvas-course-builder/SKILL.md` Mode 3 "Clone from Existing Course" exists (`:26`), and
  the quoted caveat is verbatim at `:193-194`: "Content is **not** copied when cloning -- only
  structure" / "For content migration, copy page bodies using `get_page_content` + `create_page`".
  All four tools it names are real: `get_page_content` (`courses.py:545`), `create_page`
  (`pages.py:282`), `get_course_structure` (`modules.py:105`), `get_page_details` (`courses.py:592`).
- The dependabot staleness correction is right, and measurable: `git diff --stat 77f55a4..origin/main`
  is **51 files, +2418/−338**, and it is *not* docs-only — it includes new `core/tool_results.py`
  and `core/untrusted_content.py`, plus changes to `server.py`, `tools/messaging.py`,
  `tools/admin_tools.py`, and `core/anonymization.py`. Yesterday's executor rationale for not
  updating those branches ("behind only by docs-only triage merges") no longer describes reality.
  Their green checks date from 2026-08-17T04:40 and have never run against main as it stands.
- Coverage is complete: 11 open issues exist (#309, #302, #301, #299, #293, #283, #275, #236, #172,
  #157, #142) and the brief accounts for all 11. #309 confirmed as the only thing new since the
  cutoff (opened 2026-08-19T19:25:29Z, 0 comments). #172's last external touch is jonespm's
  2026-08-04 comment — 16 days, as stated.

### Unmeasured-behavior audit of the #309 draft reply

One claim is documentation-derived, not measured: *"depending on selective_import it can touch
grading structure, due dates, and existing content the target course already has."* Neither the
brief's author nor this pass can exercise a live Canvas migration, and nothing in this repo has ever
called that API. It reads as design caution rather than a report of observed behavior, and the
recipient is a Canvas developer who knows the API better than we do, so it is low-risk as written —
but it should not harden into "we verified this" in any follow-up. Everything else in the draft is
either a repo fact (all verified above) or a question.

Worth folding into the reply: **zqian owns both #172 and #309**, and `content_migrations` was
already scoped once in #172's Tier C1 as the QTI-import path for New Quizzes. A content-migration
tool would serve two open requests from the same institution, which is a real argument for its
priority.

### Test-suite observation

Across **10 full-suite runs** today (5 branch heads + 4 repeats on the triage tip + 1 initial), zero
failures anywhere, and the collected total was identical (1395) on every run of this branch. One
run — the first — reported **1373 passed / 22 skipped** where the next three reported
**1374 passed / 21 skipped** with byte-identical skip lists. So one test intermittently skips rather
than passes. That matches the previously-flagged `test_pii_access_logged`
(`tests/security/test_ferpa_compliance.py:103`, ~0.6%/run per the 2026-08-18 brief), but the first
run's skip list was not captured, so it is not named with certainty. Main's only change to that file
since `e42e708` is #310's docstring rewording — the flakiness is **unrepaired**.

### Needs Vishal

Everything below is outside this routine's action boundary (no public posting, no touching any PR
but this one).

1. **Reply to #302** (`rpsimon-ai`, first-time contributor, unanswered since 2026-08-18T16:10Z).
   This is correction 1 above — the brief filed it as handled and it is not. The draft from the
   2026-08-18 brief was verified sound by yesterday's pass and no new activity has landed to fold in.
2. **Reply to #309** (`zqian`, high-priority external collaborator). Draft verified against the code
   and safe to send. Two suggested edits: point the "already mentioned" reference at **#172's
   Tier C1 comment** rather than PR #191 (correction 2), and consider adding that this would serve
   both of zqian's open requests.
3. **Merge decision on #308.** The one flag the brief raised is resolved as a false alarm, measured
   against live pypistats above. 17/17 checks green, suite green locally on its head.
4. **Merge decision on #295–#298.** All four verified green locally on their own heads, but their CI
   green is from 2026-08-17 against a materially different main (+2418/−338 including real source).
   Forcing a fresh run needs a branch update, which is outside this routine's boundary. #296/#297
   will still transiently conflict on `package.json` until dependabot rebases whichever merges second.
5. **#191, unchanged asks (eighth cycle):** reword the trailing auto-close line so a merge cannot
   close [issue 172]; resolve the one-file conflict in `assignments.py`; then approve the fresh CI.
   The correctness blocker still needs zqian's New-Quizzes sandbox, so it should stay draft regardless.
6. **The intermittent FERPA test** — a one-line repair outside `internal/issue-triage/`, so not
   mine to make. Carried since 2026-08-18 and confirmed unrepaired on main today.
