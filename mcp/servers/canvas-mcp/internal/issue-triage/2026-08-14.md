# Triage brief — 2026-08-14

**Read path used:** GitHub MCP tools (`gh` CLI not installed in this sandbox, as expected).
**Cutoff:** 2026-08-13T01:57:58Z — the merge timestamp of PR #284 (the 2026-08-13 brief itself),
per the precedent set by prior briefs. Everything below is activity strictly after that timestamp.

Quiet day: every issue thread that saw activity since the cutoff already has `vishalsachdev` as
the most recent commenter — he replied to all of it himself yesterday evening, so nothing is
sitting unanswered. No new issues or PRs were opened by anyone else since the cutoff. The one
open PR (#191) is unchanged. This brief exists only because #191's standing blocked/draft state
still counts as an open "problem" under this routine's rules — there is no new news in it.

## Needs your reply

None. No comment from a non-`vishalsachdev` commenter landed on any open issue after the cutoff.

## PRs

Only one open PR in the repo. Unchanged since the 2026-08-13 brief — re-verified live rather than
carried over:

| PR | Author | Draft | Mergeable | CI | Next action |
|---|---|---|---|---|---|
| #191 — dual-engine read tools for Canvas Quizzes (Copilot) | Copilot | **Yes** | `unknown`/`dirty` (still merge-conflicted) | 0 checks reported | No change from yesterday. Same-repo Copilot branch with no triggered Actions run; needs manual conflict resolution before CI can even start, then still blocked on the New-Quizzes-detection correctness bug tracked against [issue 172] (`is_quiz_assignment AND external_tool` was measured live to mark *Classic* quizzes, so it may silently report zero New Quizzes). The PR body still ends with a closing-keyword line pointing at [issue 172] — confirmed present again today, same flag as every prior brief. Not safe to merge as-is. |

## New since 2026-08-13T01:57:58Z

No new issues or PRs opened by anyone other than `vishalsachdev` since the cutoff. The activity
below is all on existing threads, and all of it already has a reply from `vishalsachdev` — noted
here for visibility, not because anything is waiting on him.

- **#275** (peer review discovery, `khagyard`) — big movement. `khagyard` posted the real Planner
  API payload vishalsachdev asked for (`assessment_request` items from a live student account).
  It falsified an assumption in the not-yet-merged PR #288 (`plannable` carries no `user_id`/
  `assessor_id` — the fix would have printed "Student None") and confirmed completed reviews still
  appear under `filter=incomplete_items`. **PR #288 is now merged to `main`**, with `khagyard`'s
  three items as the acceptance-replay fixture. `vishalsachdev` asked `khagyard` to retest from
  `main` and confirm before the issue closes. Nothing left for this routine to do; watch for the
  retest reply.
- **#281** (`search_canvas_tools` scope gap, `zqian`) — `vishalsachdev` confirmed PR #286 is merged
  (the tool now also queries the live MCP registry, `schema_version: 2`) and asked `zqian` to
  retest and confirm. Waiting on `zqian`, not on him.
- **#283** (announcement→discussion fallback, `khagyard`) — `jonespm` suggested a docstring warning
  on `post_discussion_entry`; `vishalsachdev` replied that PR #285 (merged) already added that
  warning plus a refusal-message change, and asked `khagyard` to retest. Also used the exchange to
  flag #275 back to `khagyard` for the Planner-API retest once it shipped — which, per the item
  above, it now has. Waiting on `khagyard`, not on him.
- **#270** (`isError` protocol conformance, self-filed) — `vishalsachdev` added a scoping note
  (central registration-time wrapper, ship with #271, deliberately deferred to a supervised
  session). Self-filed, no external asker.
- **#287** (`search_canvas_tools` uncapped `full`-dump, self-filed) — only new activity is the
  `auto-label-issues.yml` bot's labeling comment (`enhancement`, `good first issue`,
  `infrastructure`, `security`). Not a request for reply.

## Going stale

- **#170** (zqian, student-write-tools design) — the 2026-07-30 reply asked two explicit questions
  (default posture; syllabus-as-policy visibility). zqian's last touch was 2026-07-29, now **16
  days** unanswered. Unchanged from prior briefs; no action available to me.
- **#157** (execute_typescript sandbox hardening backlog) — self-filed, no external asker; last
  touch was `vishalsachdev`'s own 2026-07-10 status comment. Flagged for visibility only, as in
  prior briefs — not actually "waiting on you."

## Checked, nothing needed

#142, #172, #236, #239, #262, #270, #271, #275, #281, #283, #287 — reviewed each; no comment from
a non-`vishalsachdev` commenter is currently unanswered on any of them. Notes on the two closest
calls:
- **#172** — last *external* comment (`jonespm`) was 2026-08-04, over 5 days old by the letter of
  the staleness rule, but `vishalsachdev` has the last word (2026-08-07, explaining the reopen and
  the blocked-on-sandbox status) and the thread is actively tracked via PR #191, so — consistent
  with how prior briefs have handled it — this is filed as "checked," not "going stale."
- **#142** — explicitly converted to a watch item by `vishalsachdev` on 2026-07-30 (unassigned,
  `blocked-upstream` label added, re-check trigger documented). Not stale by intent; nothing to
  chase.

---
_Generated by [Claude Code](https://claude.ai/code)_

---

## Executor results (2026-08-14T02:22:43Z)

Ran the mechanical pass over the brief. **No corrections needed** — every factual claim in the
brief was spot-verified live and held. Details below.

### Closing-keyword check

`python3 scripts/check_closing_keywords.py --strict internal/issue-triage/2026-08-14.md` → exit 0,
clean. The PR #289 body is also clean (it says "tracked against issue 172", not the keyword form).
Nothing to reword on this branch.

### a. Stalled / no-CI PRs

**#191 (Copilot, draft) — attempted update-branch, refused; empty-commit retrigger deliberately
not attempted.**

- `PUT /repos/vishalsachdev/canvas-mcp/pulls/191/update-branch` → **HTTP 422, "merge conflict
  between base and head"**. This is the API confirming the brief's `dirty` reading; the branch
  cannot be advanced without a human resolving conflicts.
- Head `9e2d146`, combined status **`pending`, `total_count: 0`** — zero checks, matching the
  brief.
- **Why no empty commit:** every CI workflow that would run here (`canvas-mcp-testing.yml`,
  `closing-keyword-guard.yml`, `claude-code-review.yml`) triggers on `pull_request` against
  `main`. A merge-conflicted PR has no computable merge ref, so those runs cannot be scheduled
  regardless of new commits — this is *not* the Copilot `action_required` stall that the empty-
  commit trick unblocks. Pushing one would add a no-op commit to a branch I did not create and
  change nothing. Base sha on the PR is `5652175`, ~13 commits behind current `main` (`1aba3ba`).
- Its body still ends with a live closing keyword aimed at issue 172 (verified again today,
  verbatim: a `Fixes` line). Editing another author's PR body is outside my boundary — escalated
  below. Note the guard workflow *would* catch it on `pull_request: edited/synchronize`, but
  cannot run while the conflict stands, so the guard is currently not protecting this PR.

### b. Green-but-unreviewed non-draft PRs

**None exist.** The only two open PRs in the repo are #289 (this brief) and #191 (draft,
conflicted). So no PR branch was checked out for testing, and none was skipped for any other
reason.

Ran the suite anyway on this branch (`triage/2026-08-14` = `main` + the brief) as a post-merge
baseline, since three PRs landed on `main` yesterday:

```
uv run python -m pytest tests/ -q
  → 1349 passed, 22 skipped, 2 warnings in 31.66s
uv run ruff check .
  → All checks passed!
```

Python 3.11.15. Both warnings are pre-existing and benign (an un-awaited `AsyncMockMixin` coroutine
in `tests/security/test_untrusted_content.py`, and a Starlette `httpx` deprecation in
`tests/test_fastmcp_compat.py`). 1349 passed is consistent with the 1350 recorded in the
2026-08-13 session log — no regression signal; the delta is collection-level, not a failure.

### c. Spot-verification of the brief's claims

| Claim | Verdict | Evidence |
|---|---|---|
| #285, #286, #288 merged to `main` | **Confirmed** | `93c88a9`, `c6d7d1c`, `52f98cd` on `origin/main` |
| khagyard posted the real Planner payload on #275 | **Confirmed** | comment `5286457447`, 2026-08-13T21:13:35Z — three `assessment_request` items, `plannable` carries no `user_id`/`assessor_id`, two of three `workflow_state: "completed"` under `filter=incomplete_items`. Both falsifications the brief describes are visible in the payload itself |
| #275 last commenter is `vishalsachdev` | **Confirmed** | `5287476246`, 2026-08-13T23:18:48Z |
| #281 last commenter is `vishalsachdev` | **Confirmed** | `5285546048`, 2026-08-13T19:40:37Z |
| #283 last commenter is `vishalsachdev` | **Confirmed** | `5285474738`, 2026-08-13T19:33:34Z (after `jonespm`'s 18:57:58Z suggestion) |
| #191 unchanged: draft, dirty, 0 checks, closing keyword in body | **Confirmed** | see section (a) |
| No new issues/PRs from non-`vishalsachdev` since cutoff | **Confirmed** | 13 open issues, newest external activity is khagyard's 21:13Z payload on #275 — an existing thread |
| Nothing new since the brief was written (01:42Z) | **Confirmed** | most recent update across all open issues is 2026-08-13T23:18:49Z |

No draft replies in this brief to audit — it proposes none. No unmeasured-behavior claims to flag:
the brief's one live-behavior assertion (the New-Quizzes detection bug on #191) is correctly
attributed to a prior measurement rather than presented as fresh.

### Needs Vishal

1. **#191 — strip the closing keyword from the PR body before any merge.** It still ends with a
   `Fixes`-form line pointing at issue 172. Merging as-is auto-closes that issue, which is the
   exact accident that hit this repo twice. I cannot edit another author's PR body. The
   closing-keyword guard cannot protect this PR while the merge conflict blocks CI, so this is
   currently unguarded.
2. **#191 — human decision required, no mechanical unblock exists.** Conflict resolution needs a
   non-empty commit to a branch I did not create. And even resolved, it stays blocked on the
   New-Quizzes-detection correctness bug (`is_quiz_assignment AND external_tool` measured to mark
   *Classic* quizzes), which needs the New-Quizzes-enabled sandbox from #172's scoping question 4.
3. **Three issues waiting on reporter retests, no action from anyone here.** #275 (khagyard),
   #281 (zqian), #283 (khagyard). All three have `vishalsachdev` asking for confirmation as the
   last word; close on their replies. Daily triage watches these threads.
4. **#170 — 16 days unanswered** (zqian, two design questions from 2026-07-30). Unchanged; only a
   nudge from you can move it.

Nothing FERPA-adjacent was touched: this run made no code change, and the only write was to this
brief file.
