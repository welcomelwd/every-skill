# Triage brief — 2026-08-09

**Read path used:** GitHub MCP tools (`gh` CLI not checked; MCP tools worked directly).
**Cutoff:** 2026-08-08. Precisely: the merge commit for that brief (`014c893`, PR #237) landed at
`2026-08-08 08:20:13 -0500` (`2026-08-08T13:20:13Z`) — used that exact timestamp rather than midnight,
following the precedent set by the 2026-08-08 brief, since date-only comparison would hide same-day
activity.

## Needs your reply

None by the letter of the rule. Two issues (#249, #239) picked up a comment after the cutoff, but both are
from `claude[bot]` posting automated label rationale ("Labeled `bug` + `infrastructure`" / an automated PR
review) — not a request directed at you. Logged under "Checked, nothing needed" rather than here, consistent
with the standing instruction to skip labeling entirely. The one item that actually wants a human reply,
**#252**, has zero comments yet (fresh report), so it's covered in full under "New since cutoff" instead,
matching how prior briefs have handled that shape of gap.

## PRs

- **#253** (`ChickenisLegit`, **first-time contributor**) — `fix: use form-data for create_announcement to
  prevent Canvas JSON parsing bug`, addresses #252 (see below). Not a fork-approval gray area to double check:
  it *is* from a fork (`ChickenisLegit/canvas-mcp`), so **0 reported CI checks means "not yet approved to run"
  for a first-time contributor, not "failing."** `mergeable_state: blocked` — most likely the unapproved-workflow
  state, not a real conflict (1 file, +1/-1, no base changes since). No reviews yet. Next action: approve the
  workflow run for this fork PR so CI executes, then review — see root-cause read below, the fix direction looks
  plausible and matches this repo's existing pattern for the same class of bug (#210, #220).
- **#255** (Copilot coding agent, same-repo) — `Tighten maintenance metadata and workflow permissions`, addresses
  #254 (the automated weekly-maintenance report, see below). Draft, `mergeable_state: blocked`, **0 reported CI
  checks** — same-repo Copilot-agent PR, so per standing note this usually means Actions hasn't triggered for the
  branch at all rather than a failure. Known unblock: `update_pull_request_branch` (rebases onto main and
  re-triggers). 13 files touched (dependency floors, workflow `permissions:` blocks, docs, role-count
  reconciliation) — broader than #253, worth a real read before merging, not just a rubber-stamp.
- **#243** (you, docs-only, follow-up to #232) — draft, `mergeable_state: behind` (needs a rebase, not dirty),
  one automated `claude[bot]` review-style comment landing on "LGTM, no code/tests affected." Self-authored,
  small (+7/-6, 4 HTML files), effectively ready — next action is just updating the branch and merging when
  you want it.
- **#191** (Copilot, targets #172) — still draft, `mergeable_state: dirty`, **0 CI checks reported**, unchanged
  since 2026-07-30 (same head SHA `8a7e393`, 10 days now), no response to your 2026-07-30 blocking review on the
  New Quizzes detection logic. Still gated on #172's scoping question 4 (a New-Quizzes sandbox). No new
  information since the last several briefs; repeating only because it's still open.

## New since 2026-08-08

### #252 ⚠️ — created announcement was not saved in Canvas
Filed by **zqian** (UMich, high-priority evaluating collaborator), 2026-08-08 15:36 UTC, v1.6.0 / Claude Desktop
macOS. `create_announcement` reported success and returned an announcement ID, but the announcement never
appeared in the Canvas course site, wasn't retrievable by ID afterward, and didn't show up in a raw Canvas API
listing either. Not 100% reproducible — happened "multiple times." No comments yet.

**Unverified analysis** — `create_announcement` (`discussions.py:968-999`) POSTs a plain **JSON** body
(`data=data`, no `use_form_data=True`) to `/courses/{id}/discussion_topics` with `is_announcement: True`. There's
already a guard for a *related* but different failure mode: `discussions.py:1010-1027` checks whether the
response actually echoes `is_announcement: true` and returns an explicit "unconfirmed write" warning instead of
claiming success if it doesn't (added for #220, where Canvas silently drops the flag when the token lacks
announcement permission). zqian's report describes the tool returning the plain success message
(`discussions.py:1029`), meaning that guard did **not** trigger — so if the root cause is what PR #253 (below)
proposes, it's a step worse than #220: Canvas is apparently echoing `is_announcement: true` back in the
immediate POST response without actually persisting the topic as a real, listable announcement, which would
defeat the existing guard entirely since it only checks the echo, not a follow-up read. This project has hit
this general shape of Canvas quirk before (JSON vs. form-encoded booleans not being honored reliably — see
`discussions.py:154` area for `#207`, and `#210`/`#220` for the same tool family), so the theory is plausible,
but unconfirmed without reproducing against a live instance.

**Community fix already proposed:** PR #253 (external, first-time contributor) switches this POST to
`use_form_data=True`, citing the same mitigation used in #210. Worth testing rather than writing from scratch —
see PRs section above.

```
Thanks for the detailed repro, and sorry for the trouble. I've traced this
to create_announcement (discussions.py:968-999) — it sends the request as
a plain JSON body rather than form-encoded, and we've hit this general
class of Canvas quirk before with boolean flags in JSON payloads (#207,
#210, #220). My working theory is that Canvas is echoing
is_announcement: true back in the immediate response without actually
persisting the topic as a real announcement, which would explain why our
existing "did Canvas actually confirm this" guard didn't catch it here —
that guard only checks the response echo, not a follow-up read.

There's already an open PR (#253) from a community contributor proposing
the standard fix (form-encoding the request, same pattern as #210). I want
to get that verified against a real instance before merging, since a
"multiple times, not 100% reproducible" failure mode is exactly the kind
of thing that's easy to appear-fix without actually closing.

Two things that would help: (1) do you still have the raw tool-call
response from one of the failed attempts (not just the summary), and (2)
can you tell whether it failed every time you tried with this exact
title/course, or intermittently across different announcements? That'll
tell me whether this is a deterministic encoding bug or something more
like a Canvas-side race/permission edge case.
```

### #254 — Weekly maintenance report (automated)
Opened by the `weekly-maintenance.yml` automation (`claude` bot account), 2026-08-09 00:54 UTC. Per standing
instruction this routine doesn't duplicate repo-health output, so no independent analysis here — noting it only
because it already produced actionable output: PR #255 (above) addresses several of its P1/P2 items (stale
README rubric text, `fastmcp`/`uvicorn` dependency floors, missing workflow `permissions:` blocks, tool-count
doc drift). No draft reply — it's a report, not a request.

## Going stale

- **#170** (zqian) — student write-tool defaults / FERPA-adjacent design. Last non-`vishalsachdev` touch was
  zqian's 2026-07-29 comment (11 days old); you posted a full design writeup + implementation update the next
  day, ending on two explicit open questions (default posture; syllabus-as-policy-carrier visibility). Ball is
  in zqian's court. **High-sensitivity** per standing instruction — no draft reply needed, just flagging;
  consider a light nudge if the silence extends further given they're an active evaluating institution.
- **#142, #157** — match tracked backlog status in CLAUDE.md (`blocked-upstream` watch item; self-hosted-only
  sandbox hardening backlog). No activity from anyone since well before the cutoff. No action.

## Checked, nothing needed

- **#249** — new issue (npm setup wizard targets a dead endpoint), filed by `vishalsachdev` himself on
  2026-08-08, so excluded from "New since" (self-authored). Picked up one comment after cutoff, but it's
  `claude[bot]`'s automated labeling rationale, not a request — no reply needed.
- **#239** — security/prompt-injection hardening issue, self-authored by `vishalsachdev`, comment predates this
  cutoff (`claude[bot]` auto-label). No action from this sweep.
- **#236** — OAuth2 developer-key feature request, self-authored by `vishalsachdev`, no activity since filing.
  **⚠️ High-sensitivity** (credential handling) — noted for awareness only, explicitly unscheduled per the
  issue itself.
- **#172** — last comment is your own (2026-08-07, reopening + explaining the New-Quizzes-detection blocker);
  last non-`vishalsachdev` touch (`jonespm`, 2026-08-04) is right at the 5-day stale threshold rather than
  clearly past it, and the issue already carries a clear, current "blocked on #191 / a New-Quizzes sandbox"
  status — not neglect, so not listed under "Going stale." No new information since the last several briefs.
- **#142, #157, #170** — see "Going stale" above; reviewed, no draft reply warranted.
- **#191, #243, #253, #255** — PRs reviewed above.

---

## Superseded by same-day activity (added at merge, 2026-08-09 ~11:45 UTC)

This brief is a snapshot from 01:42 UTC; act on none of its action items without checking:

- **#252/#253**: the draft reply above is superseded — the encoding theory was refuted by
  measurement the same morning (both wire encodings equivalent; likely the pre-#220-guard
  permission failure on v1.6.0). A better-informed reply was posted on #252 at 01:56 UTC.
  Do not post this brief's draft.
- **#255**: merged 02:07 UTC (#254 closed with it). The suggested `update_pull_request_branch`
  unblock was also the wrong mechanism — the reliable retrigger was an empty commit.
- **#249**: closed 11:29 UTC via PR #257 — npm setup wizard retired, `canvas-mcp` npm package
  deprecated, `cli/` and `docs/workshop.html` removed. Any future reference to the npm wizard
  is stale.
