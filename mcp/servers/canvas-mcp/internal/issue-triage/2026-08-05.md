# GitHub Activity Triage — 2026-08-05

**Read path used:** GitHub MCP tools (`gh` CLI not installed in this sandbox).
**Cutoff:** 2026-07-31 (the newest file in `internal/issue-triage/` on `main`). This is the **fifth
consecutive cycle** stuck on this cutoff — see the PRs section for why, and for a recommendation to
finally clear it.

---

## Needs your reply

### ⚠️ #172 — [Feature]: add tools for Canvas Quizzes (currently CLOSED, but see below)

This one doesn't fit the letter of "open issue with a new comment," but it's the most important
finding this cycle, so it goes first.

**What happened:** `jonespm` (first-ever comment in this repo — no prior interaction found) posted
on 2026-08-04T17:38:58Z:

> "#191 still doesn't look merged and this was closed again."

Checked the actual history: `closed_at` is **2026-07-31T13:08:52Z**, `closed_by: vishalsachdev`,
`state_reason: completed`. That's the same day you posted the "Reopened — closed by accident"
comment at 05:54:44 UTC (after PR #202's accidental use of a GitHub closing keyword referencing
this issue). Something closed it *again* about 7 hours later, same day — and nothing in the merge
history between those two timestamps (#203 at 05:46, #205 at 12:16, #206 at 13:53) contains a
closing-keyword reference to this issue, so I can't attribute the second closure to a specific
merge from here. It's stayed closed for 5 days
since, with the underlying PR (#191) still open, draft, and blocked on the same New-Quizzes
detection bug your 07-30 review flagged.

So `jonespm`'s comment is accurate: the feature this issue tracks is not done, the issue reads
closed/completed, and an outside reader has now publicly noticed the mismatch. This is exactly the
kind of confusion the earlier accidental-close caused — just via a different path this time.

```
Thanks for flagging this — you're right, and I appreciate the outside check. #172 is
closed but the underlying work (#191) isn't actually merged; the closure doesn't
reflect reality. Reopening now. Status: #191 implements the read-only "Tier A" quiz
layer but has a known correctness bug in New Quizzes detection that needs a
New-Quizzes-enabled sandbox to fix properly (tracked in the PR review) — it's not
stalled from neglect, it's blocked on that verification. Will update here once #191
is actually mergeable.
```

---

## PRs

Five open PRs. One real feature PR, four are this triage routine's own accumulating output.

### #191 — feat: Add dual-engine read tools for Canvas Quizzes (Copilot, targets #172)
- Still draft, `mergeable_state: dirty`, 0 CI checks reported. **Unchanged for 6 days** — no
  response from Copilot to your detailed 2026-07-30T10:25 blocking review (New Quizzes detection
  logic uses `is_quiz_assignment AND external_tool`, but you measured live that `is_quiz_assignment`
  marks *Classic* quizzes, so the filter may always return empty; also flagged `engine` as an
  unvalidated free string routed inconsistently across three tools).
- Also still gated on #172 scoping question 4 (a New-Quizzes-enabled sandbox from zqian) regardless
  of whether Copilot responds.
- **Next action:** none from you technically, but 6 days of silence plus the #172 closure confusion
  above is a reasonable moment to nudge Copilot directly or reassess whether this PR is still active.

### #209 / #216 / #218 / #223 — this routine's own prior triage-brief PRs
- All draft, all touch only their own single markdown file under `internal/issue-triage/`, all still
  open and unmerged. #209 and #216 are now `behind` main (main has moved through #210–#226 since);
  #218 is `behind`; #223's status shows pending/no checks reported yet.
- **This is why the cutoff hasn't moved off 2026-07-31 in five cycles.** Every prior day's brief
  already covered and resolved real ground (see "New since cutoff" below — the four bugs those
  briefs found are now fixed and closed), but none of that lands in `main`'s
  `internal/issue-triage/` history until one of these merges.
- **Recommended action:** merge #223 (2026-08-04, the most current and complete accounting) and close
  #209/#216/#218 as superseded — their content is fully subsumed by #223 and this brief. That will
  let cutoff finally advance past 2026-07-31.

---

## New since 2026-07-31

**Nothing new and unresolved.** Everything opened since the cutoff has already been closed out by
merged fixes, all landed since the last brief:

- **#207** (`bulk_update_pages` 500 error) and **#208** (`mark_conversations_read` error) — fixed by
  PR #210, merged 2026-08-01.
- **#219** (`get_my_peer_reviews_todo` false negative), **#220** (`create_announcement` silent
  downgrade to a plain discussion), **#221** (`mark_module_item_done` false success) — all three
  reported by `khagyard` testing v1.6.0 with a student account, all fixed by PR #224, merged
  2026-08-04.
- **#222** (`get_my_upcoming_assignments` ignoring `days`, reported and self-diagnosed by `zqian`) —
  fixed by PR #225, merged 2026-08-04, switching to the Planner API.
- **#215** (weekly-maintenance auto-report) — opened and closed same day by existing automation, not
  duplicated here per standing instructions.
- **PR #226** (cryptography 49→50, CVE-2026-69247) — merged 2026-08-04, unrelated to a specific
  issue, kept the Dependency Vulnerability Scan green.

No draft replies needed — these are closed with fixes already shipped. Both `khagyard` and `zqian`
were asked in the merged PR bodies to re-test with their own tokens (student-side behavior isn't
fully verifiable from an educator token); no re-test confirmation has come in yet as of this cycle.

---

## Going stale

- **#157** — Harden `execute_typescript` sandbox. No non-you activity since 2026-07-04; matches its
  tracked status in CLAUDE.md (self-hosted-only backlog, hosted instance already has the flag
  disabled). No action implied.

---

## Checked, nothing needed

- **#170** (student write tools) — last comment is yours (07-30, announcing Tier 1 merged into
  v1.6.0); two questions to zqian (default posture; syllabus-as-policy-carrier) are still open
  in-thread, but that's on him, not a gap on your side.
- **#142** (mcp v2 pin watch) — last comment is yours (07-30, converted to watch item, unassigned);
  matches CLAUDE.md tracked status, no action pending.
- **#179** (anonymization consolidation) — no new activity since before cutoff; last word is yours.
- **#173** (TOOL_MANIFEST coverage) — closed by PR #212 (2026-08-01); resolved.
- **#106** (mypy errors) — closed by PR #213 (2026-08-01); resolved.
- **#168** (weekly maintenance findings) — closed by PR #214 (2026-08-01); resolved.
- **#168-adjacent weekly-maintenance automation** — continues to run independently per standing
  instructions; not duplicated here.
