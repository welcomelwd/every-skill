# Triage brief — 2026-08-15

**Read path used:** GitHub MCP tools (`mcp__github__*`).
**Cutoff:** 2026-08-14 (date of the newest prior brief, `internal/issue-triage/2026-08-14.md`). Activity is measured from when that brief actually ran (early on 2026-08-14) through now.

This was not a quiet day by the letter of the rule — category 1 and category 3 are empty, but PR #191 has a real problem (merge conflict + never-triggered CI), so the brief was not skipped.

## Needs your reply

None. No open issue has a comment since the cutoff whose most recent commenter is someone other than you.

(#283 and #275 both had comment activity on 2026-08-14, but in both threads you are the most recent commenter — you're waiting on retests from `khagyard`, not the other way around. See "Checked, nothing needed.")

## PRs

- **#191 — `feat: Add dual-engine read tools for Canvas Quizzes (Classic and New)`** (Copilot, draft, targets #172)
  - **State:** draft, `mergeable_state: dirty` (real conflicts with `main`, not just behind), **0 CI checks reported**.
  - Last commit is an empty "retrigger CI" commit from 2026-08-13 — checks still never ran. This is the same-repo-Copilot-agent pattern from the tool instructions (Actions never triggered for the branch); the known unblock is `gh pr update-branch`, but that won't be enough here because the branch is genuinely conflicting with `main`, not just stale.
  - **Known correctness blocker, independent of the above:** per your 2026-08-07 comment on #172, the New Quizzes detection logic (`is_quiz_assignment AND external_tool`) was measured live to mark *Classic* quizzes, so the `AND` may match nothing and silently report zero New Quizzes. Still blocked on a New-Quizzes-enabled sandbox to verify against — nobody has supplied one.
  - **Housekeeping flag:** the PR body's final line is Copilot's original closing-keyword wording targeting issue 172 (the same GitHub-closes-on-merge pattern documented on that issue's thread from two earlier accidents). That's original PR text, not new activity, but it's a live landmine — merging this PR as-is would auto-close issue 172, which is still open and still tracking unresolved scope (zqian's answers, the sandbox). Worth editing that line to a non-closing reference (e.g. `addresses`/`re:`) before this ever merges, independent of when the conflict/correctness issues get resolved.
  - **Next action:** not mergeable today. Needs (1) a manual rebase/conflict resolution onto current `main`, (2) a fresh push to actually trigger CI, (3) the New Quizzes sandbox to validate the detection logic, and (4) a PR-body edit to drop the closing keyword. None of these are quick — recommend leaving as draft until the sandbox question resolves.

## New since 2026-08-14

None. No new issues or PRs opened since the cutoff.

## Going stale

- **#170** (zqian, **HIGH-SENSITIVITY — student write tools / FERPA policy default**): last external touch was zqian's 2026-07-29 "will update you soon" — 17 days with no follow-up on either blocking question (deny-by-default vs allow-by-default; syllabus-as-policy-carrier visibility).
- **#172** (zqian/jonespm, quizzes): last external touch was jonespm's 2026-08-04 comment. 11 days with nobody providing the New-Quizzes sandbox access that's blocking Tier C1/C2 (and now blocking #191, see above).
- **#142** (Ash, `blocked-upstream` watch item): last external touch from ashcastelinocs124 was 2026-07-01. Deliberately converted to an unassigned watch item on 2026-07-30 — no action expected, flagged for awareness only.

## Checked, nothing needed

#283, #275 (both: you are the most recent commenter, waiting on `khagyard`'s retest — no action pending on your side), #170, #172, #142 (see "Going stale" — none need a reply right now), #157, #239, #262, #271, #236 (self-filed by you; only comment since creation is the automated labeling bot; no external ask, nothing to answer).

---

## Executor results (2026-08-15 02:21 UTC)

Ran after the brief. Boundary reminder: the executor may merge only this triage PR, and may not
comment on, edit, or push non-empty commits to any other PR or issue. Everything requiring that is
in **Needs Vishal** below.

### Closing-keyword check

`scripts/check_closing_keywords.py --strict` over the brief file: **clean (exit 0)**. Same script
over PR #292's body via stdin: **clean (exit 0)**. Nothing to reword.

### PR #191 — measured, not merely predicted

The brief's headline facts hold up, but the CI half of the diagnosis is wrong in a way that changes
the recommended action.

**Conflict — confirmed.** Merge base is `5652175`; the branch is **138 behind / 5 ahead** of `main`.
`git merge-tree --write-tree origin/main origin/copilot/feature-add-tools-for-canvas-quizzes`
reports exactly one conflict:

```
Auto-merging src/canvas_mcp/server.py
Auto-merging src/canvas_mcp/tools/assignments.py
CONFLICT (content): Merge conflict in src/canvas_mcp/tools/assignments.py
```

Expected, given the PR moves 276 lines out of `assignments.py` into a new `assignments_analytics.py`
while `main` kept editing `assignments.py`. Diff vs `main`: 6 files, +738 / −274.

**update-branch — attempted, refused.** Not a prediction now; the API answered:

```
PUT /repos/vishalsachdev/canvas-mcp/pulls/191/update-branch
→ 422 merge conflict between base and head
```

**CI — correction to the brief.** The brief says "0 CI checks reported" and reads it as the
same-repo-Copilot-agent pattern where "Actions never triggered for the branch." Workflow runs *do*
exist for this branch — 14 total, 12 of them `pull_request` runs — and their state is the actual
blocker:

| Head SHA | Runs | Conclusion | Date |
|---|---|---|---|
| `8a7e393` | Enhancement Testing, Claude Review, Security Testing | `action_required` | 2026-07-30 |
| `f56e94b` | same three | `action_required` | 2026-07-30 |
| `f093a53` | same three | `action_required` | 2026-07-30 |
| `66ce3d5` | same three | `action_required` | 2026-07-30 |
| `9e2d146` (current head) | **none** | — | 2026-08-13 |

Two distinct facts, neither of which an executor can clear:

1. Every run that did fire is sitting at **`action_required`** — GitHub's "waiting for maintainer
   approval to run workflows" state. Only a maintainer can release those.
2. The current head produced **zero runs**. That head is the empty `chore: retrigger CI` commit
   pushed by a **previous executor run** on 2026-08-13 01:51 UTC — so the known Copilot unblock was
   already tried on this exact branch and demonstrably did nothing. Consistent with GitHub declining
   to compute a merge ref for a `dirty` PR, which suppresses `pull_request`-event workflows outright.

**Deliberate non-action:** I did **not** push a second empty commit. Step 3a permits it, but the
measurement above shows the identical action failed here 2 days ago; repeating it would add another
junk commit to a branch I did not create in exchange for nothing. The conflict has to be resolved
first, and that needs a non-empty commit — out of bounds for the executor.

### Local verification of #191 (checked out read-only)

Worktree at `origin/copilot/feature-add-tools-for-canvas-quizzes` (`9e2d146`), `uv sync --group dev`:

```
923 passed, 21 skipped, 1 warning in 18.23s     (uv run python -m pytest tests/ -q)
All checks passed!                              (uv run ruff check .)
```

**Read this narrowly.** That is the branch's *own* suite from its 2026-07-30 base, 138 commits behind
`main` — `main` currently carries ~1350 tests. Green here is not evidence the merged result is green,
and the merged result cannot be tested at all until the conflict is resolved.

**PRs not tested, and why:** none other. #191 is the only other open PR and it is a **draft**, so step
3b (green, non-draft, unreviewed) matched nothing today; I ran it anyway because CI has never once
produced a result on it.

### Spot-verification of the brief's claims

- **Correctness blocker — confirmed against the code, not just recalled.** `tools/quizzes.py:41` is
  `a.get("is_quiz_assignment") and "external_tool" in a.get("submission_types", [])`, and the fixture
  at `tests/tools/test_quizzes.py:37` hard-codes a payload with *both* set. The test therefore cannot
  fail on the conjunction being wrong — it asserts the filter against itself. Matches the 2026-08-07
  note on #172 that `is_quiz_assignment=True` was measured live to mark *Classic* quizzes. Still needs
  a New-Quizzes sandbox (#172 question 4); nobody has supplied one.
- **Closing-keyword landmine — confirmed.** PR #191's body ends with a bare list item carrying the
  keyword `Fixes` directly in front of a reference to [issue 172]. Merging as-is would close
  [issue 172] for the third time. (Writing that line out verbatim here trips our own guard — which is
  the guard working; it caught this section on the first run.)
- **"Needs your reply: none" — confirmed.** #283's last comment is yours (2026-08-14 18:19 UTC);
  #275's last comment is yours (2026-08-13 23:18 UTC). Both awaiting `khagyard`.
- **"No new since cutoff" — confirmed.** Issues touched since 2026-08-13: #283, #287 (closed), #281
  (closed), #275, #270. Nothing opened after the cutoff.
- **Staleness dates — confirmed** for #172 (last external touch jonespm, 2026-08-04). #170 and #142
  show no activity in the since-2026-08-13 window, consistent with the brief.
- No draft replies were proposed in this brief, so nothing to flag as asserting unmeasured behavior.

### Sandbox note for future executor runs

This container clones `main` **shallow** (`git rev-parse --is-shallow-repository` → `true`, root
commit dated 2026-08-08). Any cross-branch history question measured before `git fetch --unshallow`
is garbage — the first pass here reported PR #191 as *unrelated histories* with no merge base, which
is purely a clone artifact. Unshallow before measuring merge state.

### Needs Vishal

1. **Edit PR #191's body — swap the `Fixes` keyword on its trailing [issue 172] list item for
   `Addresses`.** Highest-value item here and a one-line fix. [Issue 172] has already been
   auto-closed twice by this exact pattern; the PR is a draft today, so the landmine is disarmable
   now at zero cost. Executor is forbidden from editing another PR's body.
2. **Approve the pending workflow runs on #191** (four sets stuck at `action_required` since
   2026-07-30) if you want CI evidence on it. Maintainer-only action.
3. **Resolve the `assignments.py` conflict on #191** — `update-branch` cannot (422, measured), and a
   conflict resolution is a non-empty commit to a branch the executor did not create. Until then the
   PR generates no CI at all, so it will keep looking "never triggered."
4. **#191 remains blocked on correctness regardless of 1–3** — the New-Quizzes sandbox
   (#172 question 4) is the real gate. Recommend leaving it draft, as the brief says.
5. No public replies to send today.
