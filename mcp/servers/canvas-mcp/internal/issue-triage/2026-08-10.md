# Triage brief — 2026-08-10

**Read path used:** GitHub MCP tools (`gh` CLI not checked; MCP tools worked directly).
**Cutoff:** 2026-08-09T11:53:23Z — the merge timestamp of PR #256 (the 2026-08-09 brief itself),
not the bare filename date. Following the precedent set by the last two briefs: a date-only
comparison would either re-report or silently drop same-day activity that landed after the prior
brief merged. The 2026-08-09 brief's own "superseded" postscript already covered everything up to
that exact timestamp (the #252/#253 re-diagnosis, #255's merge, #249's closure via PR #257), so
this sweep starts cleanly from there.

## Needs your reply

None. No open issue picked up a comment after the cutoff whose most recent commenter isn't you.
(#172's last non-you touch is real but predates the cutoff by 5 days and you already answered it —
see "Going stale.")

## PRs

- **#258 (you)** — `security: fence Canvas-authored content and gate bulk sends behind
  confirmation (#239)`. Open, not draft, `mergeable_state: blocked`. Huge — 12 commits, 25 files,
  +4467/-286 — implementing the #239 prompt-injection audit (provenance fencing + confirmation
  tokens on the highest-risk read→write loops). ⚠️ **CodeQL flagged 1 new high-severity security
  alert introduced by this PR** (separate from the repo's own "CodeQL Security Analysis" job,
  which passed clean) — see
  https://github.com/vishalsachdev/canvas-mcp/security/code-scanning?query=pr%3A258+tool%3ACodeQL+is%3Aopen.
  I couldn't pull the specific finding text through available tooling, only the summary count, so
  this needs your own look before merging — a security-hardening PR shipping a new high-severity
  alert is worth resolving, not waiving. You requested review from `ashcastelinocs124`; no human
  review has landed yet (three bot comments: GitHub Advanced Security, Codex, a stale Cursor
  comment). All CI (lint/mypy, test 3.10–3.13, Security Test Suite, Secret Detection, SAST,
  Dependency Scan) is green except that one CodeQL check. Next action: resolve or triage the
  CodeQL alert, get Ash's review, then merge.
- **#253 (`ChickenisLegit`, first-time contributor)** — `fix: use form-data for
  create_announcement`, addresses #252. No change since your 2026-08-09 01:56 comment holding off
  merge pending zqian's v1.7.0+ retest (your measured refutation of the encoding theory — see
  yesterday's brief). `mergeable_state: behind` (needs a rebase against main now that #255 merged;
  not a real conflict). Still 0 reported CI checks — fork PR, unapproved workflow run, expected.
  No action until zqian responds on #252.
- **#243 (you)** — docs-only follow-up to #232, draft, `mergeable_state: behind` (rebase, not
  dirty). Unchanged since 2026-08-08. Effectively ready — next action is just updating the branch
  and merging when convenient.
- **#191 (Copilot, targets #172)** — still draft, `mergeable_state: dirty`, 0 CI checks reported,
  unchanged since 2026-07-30 (same head SHA, 11 days). Still blocked on #172's open question 4 (a
  New-Quizzes-enabled sandbox). ⚠️ **Worth fixing before this is ever merged**: the PR body still
  reads literally `- Fixes [issue 172]` (using the closing-keyword phrasing verbatim). [Issue 172]
  has already been auto-closed twice by this exact closing-keyword pattern (once via a triage
  brief describing this same PR, once via a direct commit) — it's the reason the `commit-msg` hook
  and CI guard (#231/#241) exist. The guard covers commit messages and this triage routine's own
  output, but nothing currently rewrites *other people's* PR bodies, so this one is still live. If
  #191 merges as-is before the blocking bug is actually fixed, it will silently re-close
  [issue 172] a third time. Worth a one-line edit in the PR description — swap the closing-keyword
  phrasing for a plain reference — whenever you're next in this PR, independent of the New-Quizzes
  blocker.

## New since 2026-08-09T11:53:23Z

### #259 — "Great Repository!"
Opened by **`ChickenisLegit`**, 2026-08-09 17:31 UTC — the same first-time contributor behind PR
#253. Pure compliment, no ask: says the project is great and they'd like to contribute more in the
future. No action required; noting only because it's a second touch-point from an active
first-time contributor, which is the kind of thing worth a friendly word if you're passing through
anyway.

```
Thanks, really appreciate that — and glad #253 wasn't a one-off, always
good to have more eyes on this. If you're looking for something to pick
up next, #172 (Canvas Quizzes) and the open items in `tools/README.md`
are fair game, or feel free to just poke around and see what's
interesting.
```

### #260 — HVTrust evaluation (Grade B, 70.3/100, ranked 25/718 MCP servers)
Opened by **`YugantM`**, 2026-08-09 23:46 UTC. Third-party trust-registry score report (OSSF
Scorecard-style: build provenance, signed commits, maintenance cadence, download counts). No ask —
explicitly says "No reply needed... feel free to close this," and offers an optional README badge.
Not spam in the sense of asking for anything back (no link exchange, no signup), but also not
something requiring action. Optional reply only if you want to acknowledge or take the badge.

```
Thanks for the writeup — appreciate you sending it directly rather than
just publishing it. No action needed on my end right now; I'll take a
look at the methodology page when I have a minute.
```

## Going stale

- **#172** — last non-you touch was `jonespm`'s 2026-08-04 comment (6 days old, past the 5-day
  threshold). **Not neglect**: you replied in full on 2026-08-07 explaining #191 is blocked on a
  New-Quizzes-enabled sandbox for verification, and reopened the issue after the accidental
  closures. Flagging only because the literal rule now crosses the threshold; the issue's status
  is current, not stalled.
- **#170 (zqian)** ⚠️ high-sensitivity (FERPA-adjacent student-write policy) — last non-you touch
  is zqian's 2026-07-29 "will update you soon about the five Tier 1 tools" comment (12 days old).
  You posted a full implementation update + two explicit open questions (default posture;
  syllabus-as-policy-carrier visibility) the next day; no response since. Given they're an actively
  evaluating institution and the silence has now stretched to nearly two weeks, a light nudge may
  be worth it — no draft included per standing instruction to stay conservative on
  high-sensitivity items, but flagging the duration explicitly this time.
- **#142, #157** — tracked backlog / `blocked-upstream` watch items, status unchanged from
  CLAUDE.md. No activity from anyone since well before the cutoff. No action.

## Checked, nothing needed

- **#239, #236** — self-authored by you; only comment on each is `claude[bot]`'s automated
  labeling rationale. No external activity.
- **#252** — zqian's report; you're holding on PR #253 pending their v1.7.0+ retest (see #253
  above). No new activity from zqian since your 2026-08-09 comment.
- **#253, #243, #191, #258** — reviewed above under "PRs."
- **#142, #157, #170, #172** — reviewed above under "Going stale."

---

## Executor results (2026-08-10T01:52:40Z)

Mechanical pass over the brief. Environment: `gh` CLI absent (expected); GitHub MCP tools used
throughout. **The sandbox is a shallow clone** (`git rev-parse --is-shallow-repository` → `true`) —
this matters below.

### Actions taken

- **#243 — branch updated.** `update_pull_request_branch` succeeded. Head moved
  `c626aa4` → `0384b485`. The PR now reports its true shape against current `main`:
  **4 files, +7/-6**, 2 commits. Before the update, `git diff origin/main pr-243` showed *67 files,
  +1389/-3519* — that was entirely staleness (main's newer work appearing as deletions), not
  content in the PR. Still draft; `mergeable_state: blocked` pending checks.
- **#191 — branch update REFUSED by GitHub**: `422 merge conflict between base and head`. The
  brief's `dirty` reading is confirmed by the API. Resolving this needs a real conflict-resolution
  commit, which is outside my boundary on a branch I did not create → routed below.
- **#253 — untouched.** Fork branch (`ChickenisLegit/canvas-mcp`); I do not touch fork branches.
  Its 0-checks state is the standard unapproved-fork-workflow hold, not a stall to unblock.
- **No empty retrigger commit pushed anywhere.** See "Deviation from procedure" below.

### Test results (measured, this sandbox, Python 3.11.15)

| PR | head | `pytest tests/` | `ruff check .` |
|---|---|---|---|
| **#258** | `50fc728` | **1320 passed, 22 skipped** (3 runs; see flake note) | clean |
| **#253** | `5a726f0` | **1186 passed, 22 skipped** | clean |
| **#243** | `c626aa4` (pre-update) | 1046 passed, 21 skipped, **1 failed** — sandbox artifact, see below | clean |
| **#191** | `8a7e393` | **923 passed, 21 skipped** | clean |
| `main` | `4066619` | (guard suite only) 50 passed, 1 skipped | — |

`mypy src/canvas_mcp` on #258: **Success: no issues found in 48 source files**.

Test counts rise with branch recency (923 → 1186 → 1320) because #191 and #253 predate main's
newer suites; that is expected, not a coverage regression.

**#243's failure was mine, not the PR's.** `test_acceptance_replay_real_history` shells out to
`git log … main`; my sandbox had no local `main` ref, so it exited 128. After
`git branch main origin/main` it still failed — but only because #243's branch predates the fix
that landed on main, which resolves `main` → `origin/main` → `HEAD` and skips on shallow clones.
On current `main` that same test **skips** cleanly. So #243 had no real failure, and the branch
update has now absorbed the fix. This independently confirms the brief's "just needs a rebase."

### Corrections and additions to the brief

1. **#258 — a pre-existing flaky test, ~0.85% per run.** My *first* full run of #258 failed on
   `tests/security/test_ferpa_compliance.py::TestAuditLogging::test_pii_access_logged`; two
   re-runs passed. Root cause is deterministic, not mysterious: `core/audit.py:106` stamps
   `datetime.now(timezone.utc).isoformat()` into the JSON event, and the test asserts
   `"123" not in captured.err` and `"456" not in captured.err` to prove ID sanitization. The
   timestamp's own digits collide. Measured over 20,000 real timestamps: **171/20000 = 0.85%**
   contain `123` or `456` (e.g. `2026-08-10T01:46:46.927123+00:00`).
   **This is NOT introduced by #258** — `git diff origin/main pr-258` is empty for both
   `tests/security/test_ferpa_compliance.py` and `src/canvas_mcp/core/audit.py`. It is on `main`
   today. With CI running a 3.10–3.13 matrix, the per-PR chance of at least one job tripping it is
   roughly **3.4%**. Fix is one line — assert against the sanitized `endpoint` field rather than
   the whole stderr blob, or scrub `timestamp` before asserting.
2. **#258 — the CodeQL check is a real diff finding, not the config conflict three bots guessed
   at.** All three bot reviews on the PR speculated it might be a default-setup-vs-custom-workflow
   conflict. The check run's own output says otherwise, verbatim: *"### New alerts in code changed
   by this pull request / Security Alerts: * 1 high"*, titled *"1 new alert including 1 high
   severity security vulnerability"*. A setup conflict surfaces as a workflow error, not as a
   diff-attributed alert count. I could not retrieve the annotation body — no code-scanning-alerts
   tool is exposed to me, and default-setup runs do not appear in the Actions API — so the finding
   text still needs a human with repo Security-tab access.
3. **#258 — "CodeQL Security Analysis" being green is not evidence of absence.** There is no
   `codeql.yml`; that name is a *job* in `security-testing.yml:204` which runs
   `codeql-action/init@v2` + `analyze@v2` and **has no step that fails on findings** — it only
   uploads. So it cannot gate, and its green says nothing about the alert. Only the repo-level
   default-setup `CodeQL` check gates on new-in-diff alerts. This is the same shape as **#247**
   (a required check that could not fail). Separately: `codeql-action@v2` is deprecated; v3 is
   current.
4. **#258 — unverified candidate for the alert.** Offered as a lead, explicitly NOT a measurement:
   `core/write_confirmation.py` hashes the Canvas API token with SHA-256
   (`hmac.new(self._secret, credentials.api_token.encode(), hashlib.sha256)`). CodeQL's
   `py/weak-sensitive-data-hashing` is **high** severity and matches identifiers like
   `api_token`/`credentials` hashed with a computationally-fast algorithm. If that is the alert it
   is a **false positive in context** — this is a keyed authenticity binding, not credential
   storage needing a slow KDF — and the right disposition is dismiss-as-used-in-tests/false-positive
   with a note, not a code change. **Confirm against the actual annotation before acting on this.**
5. **The #259 draft reply points at two things that do not exist.** It suggests the contributor
   pick up "#172 (Canvas Quizzes) and the open items in `tools/README.md`". Both are wrong:
   - `tools/README.md` has **no** open-items/TODO/roadmap section — it is per-tool reference docs.
     The only `todo` matches are the tool names `get_my_todo_items` / `get_my_peer_reviews_todo`.
   - **#172 is the worst available suggestion for a first-time contributor**: its body opens with
     an IMPORTANT block saying scope changed, Tiers C1/C2 are **blocked** on five scoping
     questions, and Tier A is already in flight as **#191**. Sending a new contributor there points
     them at blocked work that already has an open PR.
   - No currently-open issue carries a `good-first-issue` label (the label exists in
     `auto-label-issues.yml`, but all 9 open issues are unlabeled for it). **There is currently no
     good newcomer task to name**, so an honest reply should thank them without inventing one.
     Suggested replacement wording is in "Needs Vishal" below.
6. **Confirmed as written:** #259 and #260 exist and match their summaries (#260 explicitly asks
   for nothing and offers delisting). #191 `dirty` confirmed by API. #253 behind + fork-held,
   confirmed. #172/#170 open, dates as stated. "Needs your reply: none" holds — no open issue has a
   post-cutoff comment from anyone other than Vishal.

### Deviation from procedure

Procedure step 3a would have me push one empty commit to **#191** since checks are still absent on
a same-repo branch. **I did not**, deliberately, and flag it here for override: #191 has unresolved
merge conflicts that require a human commit regardless, so any CI result is superseded before it is
useful; and per `CLAUDE.md` its New-Quizzes detection (`is_quiz_assignment AND external_tool`) is
measurably wrong with a fixture that hard-codes the assumption — a green CI on that branch would be
actively misleading. I ran its suite locally instead (923 passed, ruff clean at its own head), which
is the same signal without pushing. Say the word and I will push the retrigger next run.

### Needs Vishal

1. **#258 — pull the CodeQL annotation** at
   https://github.com/vishalsachdev/canvas-mcp/security/code-scanning?query=pr%3A258+tool%3ACodeQL+is%3Aopen
   and decide: real finding, or dismiss. This is the only thing between #258 and merge besides
   Ash's still-pending review. Check correction 4 for a candidate lead first.
2. **#258 — Ash's review** (`ashcastelinocs124`) requested, still not landed. Nudge or merge without.
3. **#191 — two separate human actions**, neither of which I may take:
   (a) resolve the merge conflict against `main`;
   (b) **edit the PR body** — it still literally reads `- Fixes [issue 172]`. I cannot edit another
   author's PR body. Until it is reworded, merging #191 auto-closes issue 172 a third time.
4. **#243 — ready.** Branch updated to `0384b485`, now a true 4-file/+7/-6 docs diff, tests and
   ruff clean. Needs un-drafting and a merge decision; nothing else.
5. **Flaky test on `main`** (correction 1) — worth a one-line fix on its own branch; it will keep
   randomly reddening ~3% of PRs until then. Not filed as an issue: opening issues is outside my
   boundary.
6. **CI gap** (correction 3) — the `CodeQL Security Analysis` job cannot fail on findings. Decide
   whether to gate it or drop it in favour of default setup; also bump `codeql-action` v2 → v3.
7. **Public replies to send (drafts only, nothing posted):**
   - **#259** — the brief's draft needs the fix from correction 5. Suggested:
     *"Thanks, that's kind of you — and glad #253 wasn't a one-off. I don't have a clean
     newcomer-sized item open at the moment; most of what's live is either blocked on outside
     answers or already has a PR against it. If you want, keep an eye out and I'll tag you when
     something well-scoped comes up — or just open an issue for anything you hit while using it,
     which is genuinely useful."*
   - **#260** — the brief's draft is fine as-is; no action required by the reporter's own framing.
   - **#170** — 12 days of silence from zqian on two open design questions; brief recommends a
     light nudge. High-sensitivity (FERPA-adjacent), so no draft from me either.
8. **#252/#253** — unchanged, still awaiting zqian's v1.7.0+ retest. #253 measured clean here
   (1186 passed, ruff clean), so when the retest lands the only open question is product, not code
   health.

### Footnote: the guard earned its keep today

Worth recording because it is the first observed live catch by the #231/#241 guard on the very
routine that caused the original incidents. The triage agent's own first commit (`1257a1e`)
**failed** `Closing Keyword Guard` — its message described PR #191 as something that would
auto-close [issue 172] a third time, which is itself the closing-keyword pattern. The agent amended to
`closed [issue 172]` (`3d5b436`, identical file tree, message-only change) and the guard passed.
Had the hook not existed, a brief *warning about* an accidental closure would have caused one.
Two prior briefs' worth of hardening working exactly as designed.
