# Triage brief — 2026-08-08

**Read path used:** GitHub MCP tools (`gh` CLI not checked; MCP tools worked directly).
**Cutoff:** 2026-08-07 (newest file in `internal/issue-triage/` on `main`). Precisely: the merge commit for that
brief (`006ed04`, PR #230) landed at `2026-08-07 06:06:56 -0500` (`2026-08-07T11:06:56Z`) — used that exact
timestamp rather than midnight, since GitHub's date-only search (`updated:>2026-08-07`) treats same-day activity
as not-newer-than and would have silently hidden everything below (all of it happened later on 2026-08-07).
Good news operationally: unlike the last eight runs, the cutoff **did advance** — all six prior triage-brief PRs
(#209/#216/#218/#223/#227/#228) plus #230 are now merged, so there's no repeat of the stuck-cutoff problem to
report this time.

## Needs your reply

None. No open issue has a comment newer than the cutoff whose most recent commenter is someone other than
`vishalsachdev`. The three new zqian bug reports below (#233/#234/#235) have zero comments yet, so they don't
technically qualify for this section by the letter of the rule — but they're fresh, well-reproduced reports from
a high-priority evaluating institution and clearly want replies, so full analysis and drafts are under "New
since" instead, matching how prior briefs (e.g. 2026-08-04) handled the same shape of gap.

## PRs

- **#232** (`vishalsachdev`, docs-only: Agent Plugins watch item + client-agnostic website copy) — draft,
  `mergeable_state: behind` (not dirty — just needs a rebase/update against main, no conflict), **all 13 CI
  checks green** (lint, tests, CodeQL, secret scan, dependency scan, security suite, PR-body check), two
  `claude[bot]` reviews both landing on "approve, nothing to change." Self-authored, already reviewed, ready
  whenever you want it — next action is just marking ready-for-review and merging (may want to update the
  branch first given `behind`).
- **#191** (Copilot, targets #172) — still draft, `mergeable_state: dirty`, **0 CI checks reported**, unchanged
  since 2026-07-30 (10 days now, same head SHA `8a7e393`), no response to your 2026-07-30 blocking review on the
  New Quizzes detection logic. Still gated on #172's scoping question 4 (zqian's New-Quizzes sandbox) regardless
  of any Copilot response — see #172 status below. No new information since the last several briefs; repeating
  only because it's still open. Next action: your call — nudge Copilot, or close as stalled.

## New since 2026-08-07

Three new bug reports, all filed by **zqian** (UMich, high-priority evaluating collaborator) within a ~90-minute
window on 2026-08-07 afternoon/evening, all on v1.6.0 / Claude Desktop macOS. **⚠️ High-sensitivity: all three
concern write-tool or content-return correctness** — flagging per standing instruction, conservative drafts
below.

### #235 ⚠️ — assign_grade also left a comment in SpeedGrader
zqian asked to "assign grade 8" for a specific student/assignment; the grade saved correctly, but Canvas
SpeedGrader also shows an added comment stating the grade update, which wasn't requested.

**Unverified analysis** — there is exactly one grading tool in this codebase, `bulk_grade_submissions`
(`assignments.py:996-1002`), and it only adds `comment[text_comment]` to the outgoing PUT when the caller
explicitly includes a `"comment"` key in that student's entry in the `grades` dict — there is no default or
auto-generated comment text anywhere in our code. That means the most likely explanation is that the assistant
(the LLM turning "assign grade 8" into a tool call) chose to populate the optional `comment` field itself, not
that canvas-mcp added one server-side. Can't confirm without seeing the actual tool-call parameters.

```
Thanks for the repro. I traced through our only grading tool
(bulk_grade_submissions) and it only sends a SpeedGrader comment when the
caller explicitly includes a "comment" key alongside the grade for that
student — there's no default comment text anywhere in our code. So my
working theory is that this came from the assistant's own tool call (i.e.
it chose to add a comment along with the grade) rather than something
canvas-mcp injected.

Could you share the raw tool call/parameters Claude used for that request
(or paste the conversation turn right before "8 point was saved as
grade")? That'll tell me definitively whether the comment key was
populated, and if so, whether that's a client-side prompting issue on our
end (e.g. a tool description that invites adding a comment) rather than a
straight code bug.
```

### #234 ⚠️ — update_page_settings silently no-ops on "Notify users that this content has changed"
zqian asked to set the notify flag to true for a page; the tool reported success, but the checkbox in Canvas's
UI stayed unchecked.

**Unverified analysis** — `pages.py:58-59` passes `notify_of_update` straight through as
`wiki_page[notify_of_update]`, structurally identical to `published`/`front_page` (which zqian hasn't reported
as broken), and the whole payload deliberately goes as JSON, not form-encoded (comment at `pages.py:154-155`
references #207 — form-encoding a nested dict breaks Canvas with a 500, so JSON is the fix, not the bug here).
My best guess, unconfirmed, is a Canvas API quirk rather than an encoding bug: Canvas may only actually flip
that notification flag when the same request also changes the page **body** — i.e., a settings-only call with
no content edit might be silently ignored server-side, similar in shape to the `is_announcement` issue fixed in
#220 (Canvas doing less than the request implied). Also worth noting: our code never verifies the setting stuck
— it builds its success message from the PUT response without checking whether Canvas's response actually
reflects the new value, same failure class as #180/#181/#219-221.

```
Thanks for catching this. I read through update_page_settings and I don't
see anything in our code swallowing notify_of_update — it's sent the same
way as published/front_page, which worked fine for you. My best
unverified guess is a Canvas API quirk: notify_of_update may only take
effect when the same request also edits the page body, so a
settings-only call might get silently ignored server-side (that would be
Canvas's behavior, not ours, but I want to confirm before saying so for
sure).

Were you only changing the notify setting, or also editing the page's
content in the same request? That'll tell me whether to chase a Canvas
API quirk or a bug on our end. Either way, I also want to add a check
that verifies the setting actually took before reporting success, rather
than trusting the PUT response — same fix shape as a few other
false-success bugs we've closed recently.
```

### #233 — get_page_content doesn't surface embedded images/video without a follow-up ask
zqian asked for a page's content; the first response was text-only with a note that the page "also includes
images... a TED Talk and a video," and only a follow-up "list all images and videos" request returned the
actual links.

**Unverified analysis** — `get_page_content` (`courses.py:446-470`) does no filtering at all: it returns the
raw Canvas `body` HTML verbatim, images/iframes/embedded video tags included, with no truncation or stripping in
our code (contrast `get_page_details`, which deliberately regex-strips tags for a 500-char preview — that's a
different tool). So the observed behavior — text-only first pass, media only surfacing on a second explicit ask
— looks more like the assistant's own summarization of a long raw-HTML blob than canvas-mcp withholding
anything, but that's unconfirmed without seeing the actual tool output. Lower sensitivity than the two above
(read-only, no write-tool or FERPA surface).

```
Thanks for flagging this. Looking at get_page_content, our code doesn't
filter or strip anything — it returns Canvas's raw page body HTML as-is,
image and video embeds included, with no truncation on our end (that's
different from get_page_details, which does intentionally trim to a
short preview). So this looks more likely to be the assistant
summarizing a long raw-HTML response in its own words rather than
canvas-mcp holding media back.

Could you paste the raw tool output (not the assistant's paraphrase) for
one of these pages? That'll tell me for certain whether the img/iframe
tags are actually present in what we return, versus Canvas itself
omitting them from the body field for some embed types (e.g. an
LTI-launched video that might not serialize into body HTML the way a
plain image does).
```

## Going stale

- **#170** (zqian) — student write-tool defaults / FERPA-adjacent access-control design. Last non-`vishalsachdev`
  touch was zqian's 2026-07-29 comment (10 days old); you posted a detailed design writeup the next day ending
  in two explicit questions and confirmed Tier 1 shipped. Ball is in zqian's court. **High-sensitivity** per
  standing instruction (student write defaults + FERPA-adjacent design) — no draft reply needed, just flagging;
  consider a light nudge only if the silence extends much further given they're an active evaluating
  institution.
- **#142, #157** — match tracked backlog status in CLAUDE.md (`blocked-upstream` watch item; self-hosted-only
  sandbox hardening backlog). No activity from anyone since well before the cutoff. No action.

## Checked, nothing needed

- **#172** — reopened by `vishalsachdev` himself on 2026-08-07 (11:09 UTC, two minutes after this run's cutoff
  timestamp) after `jonespm` (external, not previously involved in this issue) noted on 2026-08-04 it had been
  closed again despite #191 not being merged. Root cause fully diagnosed in that comment: a second closing-
  keyword accident, this time via a direct commit to `main` rather than a PR body, plus a fix (commit-msg hook +
  CI check covering commit messages) already landed — consistent with the closing-keyword guard now documented
  in this repo's CLAUDE.md/AGENTS.md. Since `vishalsachdev` is the most recent commenter and the substance (#191
  still blocked on a New-Quizzes sandbox) is unchanged, no reply needed — logged here for continuity given the
  issue's history of being closed by accident.
- **#236** — new feature request (OAuth2 developer-key credential flow) filed by `vishalsachdev` himself on
  2026-08-07, summarizing a design discussion from `@TSU-Carrell` in discussion #229. Excluded from "New since"
  above because the author is `vishalsachdev` (not a category-3 item per the sweep criteria), and self-authored
  so no draft reply applies. **⚠️ High-sensitivity** (touches API-token/credential handling) — noted for
  awareness only; the issue itself says it's unscheduled and blocked on Canvas admin access to pilot a scoped
  developer key, no action pending from you right now.
- **#142, #157, #170** — see "Going stale" above; reviewed, no draft reply warranted.
- **#191, #232** — PRs reviewed above.
