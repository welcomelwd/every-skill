# GitHub Activity Triage — 2026-08-03

**Read path used:** GitHub MCP tools (`gh` CLI not installed in this sandbox).
**Cutoff:** 2026-07-31 (newest file in `internal/issue-triage/` on `main`).

**⚠️ Operational note before the sweep:** two prior triage PRs — **#209** (2026-08-01 brief)
and **#216** (2026-08-02 brief) — are still open, draft, and unmerged. Because the cutoff is
read from the newest file *on `main`*, and neither of those PRs has merged, this cutoff has
now resolved to 2026-07-31 for three cycles running (08-01, 08-02, and today). This brief is
consequently re-reporting the same unchanged PR #191 status a third time with no new
information. Recommend merging or closing #209 and #216 (and this one) so the cutoff can
advance — otherwise every future cycle re-derives the same stale window until one gets merged.

---

## Needs your reply

None. No open issue has a comment newer than the cutoff — in fact, none of the three open
issues (#170, #157, #142) have *any* update since 2026-07-30, let alone from someone other
than you.

---

## PRs

One open PR from an external/bot author (plus the still-open triage PRs #209 and #216, which
are procedural, not activity to triage).

### #191 — feat: Add dual-engine read tools for Canvas Quizzes (Classic and New)
- Author: Copilot coding agent (same-repo branch, not a fork — the "withheld first-timer CI"
  caveat doesn't apply). Draft, `mergeable_state: dirty`, **0 checks reported**, 3 comments,
  **completely unchanged since 2026-07-30T10:25:51Z** — the same state as the last two briefs.
- Sequence so far: Copilot opened the PR against the original (stale) issue description →
  you posted a scope-correction comment (New Quizzes is the priority, Tier A dual-engine read
  layer only, explicit "do NOT build" list) → Copilot reverted the out-of-scope plumbing and
  replied that tests were green → you reviewed in depth and left a **detailed blocking
  review**: the New Quizzes detection (`is_quiz_assignment AND submission_types contains
  external_tool`) is unverified and, per your own live measurement, `is_quiz_assignment=True`
  actually marks **Classic** quizzes with `quiz_lti=None` — so the conjunction may make New
  Quizzes detection always return empty, and the test fixture is circular (built to match the
  filter, not to match Canvas). You also flagged `engine` as an unvalidated free string
  branched inconsistently across three tools (typos/casing route to the wrong engine silently).
- **No response from Copilot to that review in 4 days**, and CI has never reported a single
  check run on this branch across that whole span (0 check runs confirmed again today) —
  worth checking whether Actions is actually wired to trigger on this bot's branches at all,
  since a same-repo PR normally shouldn't sit at 0 checks this long.
- **Next action:** nothing new to reply to — ball is still with Copilot. If it stays silent
  another cycle or two, consider a manual nudge (`@copilot` ping) or reassigning/closing as
  stalled.

**⚠️ Correction to this section, added after review:** the original version of this brief
said the tracking issue #172 "is also blocked pending zqian's sandbox" as though it were
still open. It is not — **#172 is currently CLOSED** (`state_reason: completed`), and this
appears to be a second, silent instance of the exact closing-keyword bug that closed it once
already (see #202/the 2026-07-31 incident). The culprit this time is a **direct commit to
`main`**, not a merged PR: commit `98643ce` (2026-07-31T13:08:48Z, "docs: session log
2026-07-31 — closed all three zqian bugs..."), whose message *narrates* the first accidental
closure and contains the literal phrase "closed [issue 172] by accident" (written here with
brackets to avoid re-triggering the same parser). GitHub's keyword scanner matched that phrase
in the commit body regardless of context and re-closed the issue four seconds later. Nobody has commented since — Vishal's last word on #172 is still his
2026-07-31T05:54 "Reopened — closed by accident... still open and still blocked" comment, so
he is likely unaware this happened a second time. This brief cannot comment on or reopen
issues (write-restricted to this file only), so flagging here: **#172 probably needs a manual
reopen**, and the closing-keyword guard needs to cover commit messages pushed to `main`, not
just PR bodies — this routine's own prompt only greps the brief/PR body, which would not have
caught this vector either.

### #209 / #216 — prior triage brief PRs (own automation output, not external activity)
- Both draft, both unmerged, both still reference the 2026-07-31 cutoff. See operational note
  above — recommend closing or merging to let the cutoff advance.

---

## New since 2026-07-31

- **#215** — automated weekly-maintenance report (`weekly-maintenance.yml`), opened and
  auto-closed same day (2026-08-02) by `claude[bot]`. Logged only, not summarized here per
  standing instructions not to duplicate that automation's content. (For reference, its
  findings were folded into PR #217, already merged to main — see commit `2bee9f2`.)

No new issues or PRs from external contributors since the cutoff.

---

## Going stale

- **#157** — Harden `execute_typescript` sandbox. No activity of any kind since your own
  priority-downgrade comment on 2026-07-10 (now 24 days idle). Unchanged status: backlog,
  self-hosted-only concern, hosted has the flag disabled.

(#142 last touched 2026-07-30, 4 days ago — not yet past the 5-day threshold.)

---

## Checked, nothing needed

- **#170** (student write tools) — last comment is yours; two open questions to zqian remain
  in-thread but that's on him, not you.
- **#142** (mcp v2 pin watch) — last comment is yours; still correctly a watch item per today's
  weekly-maintenance findings (mcp 2.0.0 stable shipped, fastmcp-slim still pins `<2.0`).
- **#215** (weekly maintenance report) — covered by existing automation per standing
  instructions; findings already actioned in merged PR #217.
