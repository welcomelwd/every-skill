name: Security Testing

on:
  push:
    branches: [ main, development ]
  pull_request:
    branches: [ main ]
  schedule:
    # Run weekly security scans
    - cron: '0 0 * * 0'

# Least privilege by default; jobs escalate only what they need.
permissions:
  contents: read

jobs:
  security-tests:
    name: Security Test Suite
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      
      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest pytest-asyncio pytest-cov pyyaml
      
      # No continue-on-error: a security suite that cannot fail the build is
      # decoration. It previously passed green through any regression, including
      # the anonymization, authorization, and sandbox invariants it exists to
      # protect.
      - name: Run security tests
        run: |
          pytest tests/security/ -v --cov=src/canvas_mcp --cov-report=html --cov-report=term
      
      - name: Upload coverage report
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: coverage-report
          path: htmlcov/
      
      - name: Generate test summary
        if: always()
        run: |
          echo "## 🔒 Security Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Security test suite executed. See artifacts for detailed results." >> $GITHUB_STEP_SUMMARY

  sast-scan:
    name: Static Analysis Security Testing
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      
      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.11'
      
      - name: Install SAST tools
        run: |
          pip install bandit semgrep
      
      - name: Run Bandit scan
        run: |
          bandit -r src/canvas_mcp/ -f json -o bandit-report.json
        continue-on-error: true
      
      - name: Run Semgrep scan
        run: |
          semgrep --config=auto src/ --json -o semgrep-report.json
        continue-on-error: true
      
      - name: Upload SAST results
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: sast-reports
          path: |
            bandit-report.json
            semgrep-report.json
      
      - name: Generate SAST summary
        if: always()
        run: |
          echo "## 🔍 Static Analysis Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "SAST scanning completed. Review artifacts for findings." >> $GITHUB_STEP_SUMMARY

  dependency-scan:
    name: Dependency Vulnerability Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      
      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.11'
      
      - name: Install scanning tools
        run: |
          python -m pip install --upgrade pip
          pip install uv pip-audit safety
      
      - name: Export locked dependencies
        run: |
          # Audit the exact pinned dependency set that actually ships (uv.lock),
          # including the optional "hosted" extra, rather than a fresh resolve.
          uv export --frozen --format requirements-txt \
            --no-emit-project --no-hashes --all-extras > requirements-audit.txt
      
      - name: Run pip-audit (fails the build on vulnerabilities)
        run: |
          # Gate on findings. CVE-2025-69872 (diskcache) is ignored because no
          # fixed release exists and it is an optional/transitive dependency;
          # revisit when an upstream fix ships.
          pip-audit -r requirements-audit.txt \
            --ignore-vuln CVE-2025-69872 \
            --format json --output pip-audit-report.json
      
      - name: Run Safety check (informational)
        run: |
          safety check --json --output safety-report.json || true
        continue-on-error: true
      
      - name: Upload dependency scan results
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: dependency-scan-reports
          path: |
            pip-audit-report.json
            safety-report.json
      
      - name: Generate dependency summary
        if: always()
        run: |
          echo "## 📦 Dependency Security Scan" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Dependency vulnerability scanning completed." >> $GITHUB_STEP_SUMMARY

  secret-scan:
    name: Secret Detection Scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
    
    steps:
      - name: Checkout code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0  # Full history for secret scanning
      
      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.11'
      
      - name: Install secret scanning tools
        run: |
          pip install detect-secrets
      
      - name: Run detect-secrets
        run: |
          detect-secrets scan --all-files --force-use-all-plugins > secrets-baseline.json
        continue-on-error: true
      
      - name: TruffleHog scan
        uses: trufflesecurity/trufflehog@a7082b69f5bc6167bbe27ebab82bf6707f267bf6 # main@2026-08-10
        with:
          path: ./
          base: main
          head: HEAD
        continue-on-error: true
      
      - name: Upload secret scan results
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: secret-scan-reports
          path: secrets-baseline.json
      
      - name: Generate secret scan summary
        if: always()
        run: |
          echo "## 🔐 Secret Detection Scan" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Secret detection scanning completed. Review artifacts for findings." >> $GITHUB_STEP_SUMMARY

  codeql-analysis:
    name: CodeQL Security Analysis
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Initialize CodeQL
        uses: github/codeql-action/init@5595ccaf912efad79be6eef63a5619ff05969be3 # v4
        with:
          languages: python
          queries: security-and-quality

      - name: Autobuild
        uses: github/codeql-action/autobuild@5595ccaf912efad79be6eef63a5619ff05969be3 # v4

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@5595ccaf912efad79be6eef63a5619ff05969be3 # v4
        with:
          category: "/language:python"
