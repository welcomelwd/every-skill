Codebase uses TypeScript ESM with moduleResolution nodenext, strict typing, and Node 22+. Imports in TS source use .js extensions. Formatting/linting is via Biome with tabs for indentation and double quotes in JavaScript/TypeScript. Architectural convention is generated topology/specs under src/generated with handwritten runtime logic in src/skills, src/workflows, src/resources, src/tools, and src/runtime.