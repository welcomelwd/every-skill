name: Release

# Triggered by semver tags (v*.*.*) pushed to the repository.
# Runs full CI first, then publishes NPM, builds+pushes Docker, and creates a GitHub Release.
# Docker :latest is also built on every push to main (no NPM publish, no GH release).

on:
  push:
    branches: [main]
    tags: ["v*.*.*"]

env:
  NODE_VERSION: "22"
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ── 1. Build & publish NPM package ──────────────────────────────────────────
  publish-npm:
    name: Publish to NPM
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: startsWith(github.ref, 'refs/tags/')
    environment:
      name: npm
      url: https://www.npmjs.com/package/mcp-ai-agent-guidelines
    permissions:
      contents: read
      id-token: write  # OIDC trusted publishing — no token needed

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          registry-url: https://registry.npmjs.org

      # Trusted publishing requires npm ≥ 11.5.1
      - name: Update npm to latest
        run: npm install -g npm@11.5.1

      - name: Install dependencies
        run: npm ci

      - name: Build package
        run: npm run build

      - name: Publish to NPM (OIDC provenance)
        run: npm publish --access public --provenance

  # ── 2. Build & push Docker image ─────────────────────────────────────────────
  build-docker:
    name: Build Docker image
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      packages: write
      id-token: write  # Cosign keyless signing
      issues: write    # File an issue instead of blocking the release on Trivy findings

    steps:
      - uses: actions/checkout@v6

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf # v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install Cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Build and push
        id: build-push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Sign image with Cosign (keyless)
        if: startsWith(github.ref, 'refs/tags/')
        env:
          DIGEST: ${{ steps.build-push.outputs.digest }}
          TAGS: ${{ steps.meta.outputs.tags }}
        run: |
          images=""
          for tag in ${TAGS}; do
            images+="${tag}@${DIGEST} "
          done
          cosign sign --yes ${images}

      - name: Scan image for vulnerabilities (Trivy)
        id: trivy-scan
        continue-on-error: true
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: ${{ fromJSON(steps.meta.outputs.json).tags[0] }}
          format: table
          exit-code: "1"
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          trivyignores: .trivyignore

      - name: File an issue for Trivy findings
        if: steps.trivy-scan.outcome == 'failure'
        continue-on-error: true
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          gh issue create \
            --repo '${{ github.repository }}' \
            --title 'Trivy found HIGH/CRITICAL vulnerabilities in release image ${{ fromJSON(steps.meta.outputs.json).tags[0] }}' \
            --label security \
            --body "Trivy's scan of the release Docker image reported HIGH or CRITICAL findings during [this run](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}).

          The release proceeded (Trivy is non-blocking for releases; see the job log above for details). Review the findings and either patch the affected package or add a documented entry to \`.trivyignore\` if the finding doesn't apply."

  # ── 2b. Build & pack npm tarball ─────────────────────────────────────────
  build-npm:
    name: Build npm package
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: Install dependencies
        run: npm ci
      - name: Build & pack
        run: npm run build && npm pack --pack-destination dist/
      - name: Upload npm tarball
        uses: actions/upload-artifact@v7
        with:
          name: npm-package
          path: dist/*.tgz
          if-no-files-found: error

  # ── 3. Build .mcpb bundle ─────────────────────────────────────────────────
  build-mcpb:
    name: Build .mcpb bundle
    runs-on: ubuntu-latest
    timeout-minutes: 15
    if: startsWith(github.ref, 'refs/tags/')
    needs: [build-npm]
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v6
      - name: Download npm package
        uses: actions/download-artifact@v4
        with:
          name: npm-package
          path: dist/
      - name: Build .mcpb bundle
        id: mcp2mcpb
        uses: Anselmoo/mcp2mcpb@b040babc466902b721e2367d2e5611908c3603d8
        with:
          package: mcp-ai-agent-guidelines
          registry: npm
          mode: reference
          from-dist: dist/
          no-probe: 'true'
          attach-to-release: 'false'
      - name: Upload .mcpb artifact
        uses: actions/upload-artifact@v7
        with:
          name: mcpb-package
          path: dist/*.mcpb
          if-no-files-found: error

  # ── 4. Create GitHub Release ──────────────────────────────────────────────────
  github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: [publish-npm, build-docker, build-mcpb]
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
      id-token: write  # Sigstore artifact signing

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build & pack NPM tarball
        run: npm run build && npm pack

      - name: Generate SBOM
        run: npm sbom --sbom-format cyclonedx --package-lock-only --omit dev > sbom.json

      - name: Download .mcpb artifact
        uses: actions/download-artifact@v4
        with:
          name: mcpb-package
          path: dist/

      - name: Sign tarball with Sigstore
        uses: sigstore/gh-action-sigstore-python@v3.5.0
        with:
          inputs: "*.tgz"

      - name: Extract version from tag
        id: ver
        run: |
          echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Create GitHub Release
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          gh release create '${{ steps.ver.outputs.tag }}' \
            --repo '${{ github.repository }}' \
            --title 'v${{ steps.ver.outputs.version }}' \
            --notes "## mcp-ai-agent-guidelines v${{ steps.ver.outputs.version }}

          See [CHANGELOG.md](https://github.com/${{ github.repository }}/blob/main/CHANGELOG.md) for details.

          ### Installation

          \`\`\`bash
          npx mcp-ai-agent-guidelines@${{ steps.ver.outputs.version }}
          \`\`\`

          ### Docker

          \`\`\`bash
          docker run ghcr.io/${{ github.repository }}:${{ steps.ver.outputs.version }}
          \`\`\`

          Platforms: \`linux/amd64\`, \`linux/arm64\` — images signed with Cosign."

      - name: Upload signed tarball to release
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: gh release upload '${{ steps.ver.outputs.tag }}' *.tgz *.sigstore.json sbom.json dist/*.mcpb --repo '${{ github.repository }}'
