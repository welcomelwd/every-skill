name: Conformance

# Runs the @modelcontextprotocol/conformance suite against mcpc to check
# adherence to the MCP specification.  Triggered on-demand only; it is not
# part of the default CI pipeline because the conformance framework is still
# evolving and test coverage via the mcpc adapter is a work in progress.

on:
  workflow_dispatch:
    inputs:
      scenario:
        description: "Scenario name to run (e.g. 'initialize', 'tools_call', 'sse-retry'). Leave blank to run the full matrix of supported scenarios."
        required: false
        type: string
      verbose:
        description: "Enable verbose output from the conformance framework"
        required: false
        type: boolean
        default: false
  workflow_call:
    inputs:
      scenario:
        description: "Scenario name to run. Leave blank to run the full matrix of supported scenarios."
        required: false
        type: string
      verbose:
        description: "Enable verbose output from the conformance framework"
        required: false
        type: boolean
        default: false

jobs:
  conformance:
    name: ${{ matrix.scenario }}
    runs-on: ubuntu-latest
    strategy:
      # Keep running the other scenarios even if one regresses so we see the
      # full picture in a single run.
      fail-fast: false
      matrix:
        # All client-side scenarios the adapter in
        # `test/conformance/client.mjs` understands; see
        # `test/conformance/README.md` for what is covered and what is not.
        # `sse-retry` is opt-in only — it surfaces a known mcpc SSE-reconnect
        # timing issue, so the `if:` guards below keep it out of default runs
        # and only execute it when the workflow is dispatched with
        # `scenario: sse-retry`.
        #
        # `slug` is the scenario name made filesystem- and artifact-safe:
        # scenario names may contain `/`, which is not allowed in an artifact
        # name and would create nested output directories.
        include:
          - scenario: initialize
            slug: initialize
          - scenario: tools_call
            slug: tools_call
          - scenario: auth/client-credentials-basic
            slug: auth-client-credentials-basic
          - scenario: auth/client-credentials-jwt
            slug: auth-client-credentials-jwt
          - scenario: sse-retry
            slug: sse-retry

    steps:
      - uses: actions/checkout@v6

      - name: Set up Node.js 24
        uses: actions/setup-node@v6
        with:
          node-version: 24

      - name: Install pnpm and dependencies
        uses: apify/actions/pnpm-install@v1.1.2

      - name: Build
        run: pnpm run build

      - name: Skip scenario
        if: ${{ (inputs.scenario == '' && matrix.scenario == 'sse-retry') || (inputs.scenario != '' && inputs.scenario != matrix.scenario) }}
        run: echo "Skipping ${{ matrix.scenario }} (not part of the default matrix or a different scenario was requested)."

      - name: Run conformance scenario
        if: ${{ (inputs.scenario == '' && matrix.scenario != 'sse-retry') || inputs.scenario == matrix.scenario }}
        # The adapter spawns a fresh mcpc process per sub-command, which adds
        # a few seconds of startup overhead each.  The default 30s framework
        # timeout is not enough for scenarios that exercise many commands, so
        # bump it to 3 minutes.
        run: |
          CMD=(npx -y @modelcontextprotocol/conformance client
            --command "node test/conformance/client.mjs"
            --scenario "${SCENARIO}"
            --timeout 180000
            --output-dir "results/${SLUG}")
          if [ "${VERBOSE}" = "true" ]; then
            CMD+=(--verbose)
          fi
          "${CMD[@]}"
        env:
          SCENARIO: ${{ matrix.scenario }}
          SLUG: ${{ matrix.slug }}
          VERBOSE: ${{ inputs.verbose }}

      - name: Upload conformance results
        if: ${{ always() && ((inputs.scenario == '' && matrix.scenario != 'sse-retry') || inputs.scenario == matrix.scenario) }}
        uses: actions/upload-artifact@v5
        with:
          name: conformance-results-${{ matrix.slug }}
          path: results/
          if-no-files-found: ignore
