# Update scripts

This directory holds simple scripts which update the current tree in some way
(e.g. generated code).

Every script in this directory should have a corresponding entry in
`../update-all.sh`.
