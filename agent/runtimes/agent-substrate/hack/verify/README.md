# Verify scripts

This directory holds simple scripts which verify the current tree in some way
(e.g. generated code).

Most of the scripts in this directory should have a corresponding entry in
`../verify-all.sh`.
