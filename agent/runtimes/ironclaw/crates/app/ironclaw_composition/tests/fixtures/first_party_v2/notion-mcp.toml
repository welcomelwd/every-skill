schema_version = "reborn.extension_manifest.v2"
id = "notion"
name = "Notion"
version = "0.1.0"
description = "Notion MCP tools for creating, searching, fetching, querying, and managing Notion workspace content."
trust = "third_party"

[runtime]
kind = "mcp"
transport = "http"
url = "https://mcp.notion.com/mcp"

[[host_api]]
id = "ironclaw.capability_provider/v1"
section = "capability_provider.tools"

[capability_provider.tools]

[[capability_provider.tools.capabilities]]
id = "notion.notion-search"
description = "Search Notion workspace content and connected sources through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-search.input.v1.json"
output_schema_ref = "schemas/notion/notion-search.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-search.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-fetch"
description = "Fetch a Notion page, database, or data source by URL or ID through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-fetch.input.v1.json"
output_schema_ref = "schemas/notion/notion-fetch.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-fetch.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-create-pages"
description = "Create one or more Notion pages with properties, content, templates, icons, or covers through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret", "external_write"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-create-pages.input.v1.json"
output_schema_ref = "schemas/notion/notion-create-pages.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-create-pages.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-update-page"
description = "Update a Notion page's properties, content, icon, cover, or template through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret", "external_write"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-update-page.input.v1.json"
output_schema_ref = "schemas/notion/notion-update-page.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-update-page.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-move-pages"
description = "Move one or more Notion pages or databases to a new parent through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret", "external_write"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-move-pages.input.v1.json"
output_schema_ref = "schemas/notion/notion-move-pages.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-move-pages.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-duplicate-page"
description = "Duplicate a Notion page within the connected workspace through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret", "external_write"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-duplicate-page.input.v1.json"
output_schema_ref = "schemas/notion/notion-duplicate-page.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-duplicate-page.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-create-database"
description = "Create a Notion database, initial data source, and initial view through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret", "external_write"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-create-database.input.v1.json"
output_schema_ref = "schemas/notion/notion-create-database.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-create-database.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-update-data-source"
description = "Update a Notion data source's properties, name, description, or other attributes through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret", "external_write"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-update-data-source.input.v1.json"
output_schema_ref = "schemas/notion/notion-update-data-source.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-update-data-source.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-create-view"
description = "Create a Notion database view with filters, sorts, grouping, or display configuration through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret", "external_write"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-create-view.input.v1.json"
output_schema_ref = "schemas/notion/notion-create-view.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-create-view.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-update-view"
description = "Update a Notion database view's name, filters, sorts, grouping, or display configuration through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret", "external_write"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-update-view.input.v1.json"
output_schema_ref = "schemas/notion/notion-update-view.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-update-view.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-query-data-sources"
description = "Query across multiple Notion data sources with structured summaries, grouping, and filters through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-query-data-sources.input.v1.json"
output_schema_ref = "schemas/notion/notion-query-data-sources.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-query-data-sources.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-query-database-view"
description = "Query data from a Notion database using a predefined view's filters and sorts through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-query-database-view.input.v1.json"
output_schema_ref = "schemas/notion/notion-query-database-view.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-query-database-view.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-create-comment"
description = "Add a page-level, block-level, inline, or reply comment through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret", "external_write"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-create-comment.input.v1.json"
output_schema_ref = "schemas/notion/notion-create-comment.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-create-comment.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-get-comments"
description = "List comments and discussions on a Notion page through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-get-comments.input.v1.json"
output_schema_ref = "schemas/notion/notion-get-comments.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-get-comments.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-get-teams"
description = "Retrieve teams and teamspaces in the connected Notion workspace through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-get-teams.input.v1.json"
output_schema_ref = "schemas/notion/notion-get-teams.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-get-teams.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-get-users"
description = "List users in the connected Notion workspace through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-get-users.input.v1.json"
output_schema_ref = "schemas/notion/notion-get-users.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-get-users.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-get-user"
description = "Retrieve Notion user information by ID through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-get-user.input.v1.json"
output_schema_ref = "schemas/notion/notion-get-user.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-get-user.md"
required_host_ports = ["host.runtime.http_egress"]

[[capability_provider.tools.capabilities]]
id = "notion.notion-get-self"
description = "Retrieve the connected bot user and workspace information through Notion MCP."
effects = ["dispatch_capability", "network", "use_secret"]
runtime_credentials = [
  { handle = "mcp_notion_access_token", source = { type = "product_auth_account", provider = "notion", setup = { kind = "oauth", scopes = [] } }, audience = { scheme = "https", host_pattern = "mcp.notion.com" }, target = { type = "header", name = "authorization", prefix = "Bearer " } },
]
default_permission = "ask"
visibility = "model"
input_schema_ref = "schemas/notion/notion-get-self.input.v1.json"
output_schema_ref = "schemas/notion/notion-get-self.output.v1.json"
prompt_doc_ref = "prompts/notion/notion-get-self.md"
required_host_ports = ["host.runtime.http_egress"]
