[package]
name = "ironclaw_extension_manager"
version = "0.1.0"
edition = "2024"
rust-version.workspace = true
description = "Product face of extensions: lifecycle commands/capabilities, the lifecycle product service, admin/operator capability handlers, and the extension hub for IronClaw Reborn"
authors = ["NEAR AI <support@near.ai>"]
license = "MIT OR Apache-2.0"
homepage = "https://github.com/nearai/ironclaw"
repository = "https://github.com/nearai/ironclaw"
publish = false

[package.metadata.ironclaw]
layer = "products"

[features]
# Lightweight in-crate test fixtures (the full lifecycle service bundle)
# reused by downstream integration tests. Zero-byte in production.
test-support = [
    "dep:ironclaw_authorization",
    "dep:ironclaw_extension_support",
    "dep:ironclaw_network",
    "dep:ironclaw_processes",
    "dep:ironclaw_resources",
    "dep:ironclaw_safety",
    "dep:ironclaw_triggers",
    "dep:ironclaw_trust",
    "ironclaw_auth/test-support",
    "ironclaw_extension_host/test-support",
    "ironclaw_filesystem/test-support",
    "ironclaw_processes?/test-support",
]

[dependencies]
async-trait = "0.1"
base64 = "0.23"
chrono = { version = "0.4", default-features = false, features = ["clock", "serde", "std"] }
futures = "0.3"
ironclaw_approvals = { path = "../../kernel/ironclaw_approvals" }
ironclaw_auth = { path = "../../domains/ironclaw_auth" }
# The optional crates below are used only by the `test_support` lifecycle
# fixture (`cfg(any(test, feature = "test-support"))`): production builds of
# this crate never link them. They are enabled by the `test-support` feature
# for downstream fixture consumers and repeated in `[dev-dependencies]` for
# this crate's own `cfg(test)` builds.
ironclaw_authorization = { path = "../../kernel/ironclaw_authorization", optional = true }
ironclaw_extension_contracts = { path = "../../contracts/ironclaw_extension_contracts", version = "0.1.0" }
ironclaw_extension_host = { path = "../ironclaw_extension_host" }
ironclaw_extension_registry = { path = "../ironclaw_extension_registry" }
ironclaw_extension_support = { path = "../ironclaw_extension_support", optional = true }
ironclaw_filesystem = { path = "../../substrates/ironclaw_filesystem" }
ironclaw_host_api = { path = "../../contracts/ironclaw_host_api" }
ironclaw_host_runtime = { path = "../../kernel/ironclaw_host_runtime" }
ironclaw_network = { path = "../../substrates/ironclaw_network", optional = true }
ironclaw_processes = { path = "../../kernel/ironclaw_processes", optional = true }
ironclaw_assistant = { path = "../../product/ironclaw_assistant" }
ironclaw_product_contracts = { path = "../../contracts/ironclaw_product_contracts", version = "0.1.0" }
ironclaw_resources = { path = "../../kernel/ironclaw_resources", optional = true }
ironclaw_safety = { path = "../../substrates/ironclaw_safety", optional = true }
ironclaw_secrets = { path = "../../substrates/ironclaw_secrets" }
ironclaw_skills = { path = "../../domains/ironclaw_skills" }
ironclaw_triggers = { path = "../../domains/ironclaw_triggers", optional = true }
ironclaw_trust = { path = "../../kernel/ironclaw_trust", optional = true }
ed25519-dalek = "2.2.0"
hex = "0.4.3"
hmac = "0.13"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11"
thiserror = "2"
tokio = { version = "1", features = ["macros", "rt", "sync", "time"] }
toml = "1.1"
tracing = "0.1"
url = "2"

[dev-dependencies]
ironclaw_auth = { path = "../../domains/ironclaw_auth", features = ["test-support"] }
ironclaw_authorization = { path = "../../kernel/ironclaw_authorization" }
ironclaw_extension_host = { path = "../ironclaw_extension_host", features = ["test-support"] }
ironclaw_filesystem = { path = "../../substrates/ironclaw_filesystem", features = ["test-support"] }
ironclaw_extension_support = { path = "../ironclaw_extension_support" }
ironclaw_host_api = { path = "../../contracts/ironclaw_host_api", features = ["test-support"] }
ironclaw_host_runtime = { path = "../../kernel/ironclaw_host_runtime", features = ["test-support"] }
ironclaw_loop_contracts = { path = "../../contracts/ironclaw_loop_contracts", version = "0.1.0" }
ironclaw_network = { path = "../../substrates/ironclaw_network" }
ironclaw_processes = { path = "../../kernel/ironclaw_processes", features = ["test-support"] }
ironclaw_assistant = { path = "../../product/ironclaw_assistant", features = ["test-support"] }
ironclaw_config = { path = "../../app/ironclaw_config" }
ironclaw_resources = { path = "../../kernel/ironclaw_resources", features = ["test-support"] }
ironclaw_safety = { path = "../../substrates/ironclaw_safety" }
ironclaw_triggers = { path = "../../domains/ironclaw_triggers" }
ironclaw_trust = { path = "../../kernel/ironclaw_trust" }
secrecy = "0.10"
tempfile = "3"
tokio = { version = "1", features = ["macros", "rt", "rt-multi-thread", "sync", "time"] }
# `tracing` short-circuits on the null dispatcher, so a plain unit test cannot
# tell "logged the cause" apart from "dropped it" — and the sanitized 503 body
# makes that distinction the whole point of the lifecycle warning. Installing a
# scoped subscriber is the only way to assert the diagnostic actually survives.
tracing-subscriber = "0.3"
