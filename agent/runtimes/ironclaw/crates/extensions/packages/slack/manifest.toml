schema_version = "reborn.extension_manifest.v3"
id = "slack"
name = "Slack"
version = "0.1.0"
description = "Slack: DM the assistant via the Slack Events API channel (workspace bot), and act as you via your own user token (xoxp-): search and read all your messages, post, edit, delete, and react as you, open DMs, and look up people in your workspace."
trust = "first_party_requested"

[runtime]
kind = "wasm"
module = "wasm/slack_user_tool.wasm"

[admin_configuration]
group_id = "extension.slack"
display_name = "Slack deployment configuration"
description = "Deployment credentials and routing configuration for the Slack channel."
fields = [
  { handle = "slack_bot_token", label = "Bot token", secret = true, required = true },
  { handle = "slack_signing_secret", label = "Signing secret", secret = true, required = true },
  { handle = "slack_team_id", label = "Workspace (team) ID", secret = false, required = true },
  { handle = "slack_api_app_id", label = "App ID", secret = false, required = true },
  { handle = "slack_installation_id", label = "Installation ID", secret = false, required = true },
  { handle = "slack_bot_user_id", label = "Bot user ID", secret = false, required = true },
  { handle = "slack_oauth_client_id", label = "OAuth client ID", secret = false, required = true },
  { handle = "slack_oauth_client_secret", label = "OAuth client secret", secret = true, required = true },
]
# Retired handles (run-acts-as-invoker): `slack_shared_subject_user_id`,
# `slack_subject_routes`, and the interim `slack_allowed_channels` allowlist
# are gone — a shared channel runs as whoever invoked the bot, and adding the
# bot to a channel is what admits it (presence-based; the message arriving
# through this channel's verified ingress is the membership evidence). Saved
# values for retired handles are inert; new saves fail closed as unknown
# fields.

# Least-privilege scopes: the read-only tools request only read scopes; each
# write tool adds exactly the scope its vendor call needs — chat:write
# (send/edit/delete), reactions:read + reactions:write (the reaction pair),
# im:write (open_dm). The slack OAuth grant is the union of these (the
# [auth.slack] recipe ceiling — a single account shared by all tools), so a
# read-only user still grants every write scope while the write tools are
# default tools. Reducing the grant for read-only users needs a write-opt-in /
# scope-upgrade re-consent flow — tracked separately.
#
# The union grew when the remaining eight core standard ops landed:
# reactions:read, reactions:write, and im:write are the new members. An
# account connected before that change holds a token without them, and the
# HOST's credential staging — not Slack — is what answers those three tools:
# runtime account selection requires the stored grant to carry each tool's
# scopes (`runtime_selection_still_enforces_provider_scope_gate` in
# ironclaw_auth), so the pre-widening account resolves to the AuthRequired
# re-auth gate. Reconnecting through that gate requests THIS manifest's
# widened union and updates the same account in place (OAuth binding
# deliberately skips the scope gate), after which all sixteen tools work.
# Slack's own missing_scope answer still exists for server-side drift on an
# account that PASSED staging (a scope revoked after connect); the guest maps
# that to messaging.permission_denied — a model-visible denial, not a re-auth
# loop, because re-running OAuth would re-request exactly what the account
# already claims to hold. Every pre-widening READ tool keeps working
# untouched in the meantime.

# ---- tool surfaces ---------------------------------------------------------

[[tools]]
standard_op = "search_messages"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.search_messages"
description = "Slack notes: indexed search — for the newest message in a known conversation use get_conversation_history instead. Requires the search:read user scope. Raw Slack ids are for tool calls only — never include one in a reply."
effects = ["network", "use_secret"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/search_messages.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "list_conversations"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.list_conversations"
description = "Slack notes: lists channels, private channels, DMs, and group DMs visible to you; is_member is the authoritative membership signal. DM entries carry the counterpart user."
effects = ["network", "use_secret"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/list_conversations.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "get_conversation_info"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.get_conversation_info"
description = "Slack notes: for a DM, counterpart.user_ref is the authoritative mention target."
effects = ["network", "use_secret"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/get_conversation_info.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "get_conversation_history"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.get_conversation_history"
description = "Slack notes: newest-first; thread replies are NOT included — a reply_count > 0 means fetch get_thread_replies. Limit above 999 is clamped."
effects = ["network", "use_secret"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/get_conversation_history.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "get_thread_replies"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.get_thread_replies"
description = "Slack notes: thread is the parent message's ts."
effects = ["network", "use_secret"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/get_thread_replies.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "get_user_info"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.get_user_info"
description = "Slack notes: includes status text/emoji, timezone, and title for presence-style questions."
effects = ["network", "use_secret"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/get_user_info.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "whoami"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.whoami"
description = ""
effects = ["network", "use_secret"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/whoami.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "send_message"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.send_message"
description = "Slack notes: text is Slack mrkdwn. To notify someone, mention them as <@U…> with their real user id — a plain @name notifies nobody; never guess a user id or derive one from a channel or DM conversation id (for a DM conversation id, get_conversation_info returns the authoritative user). Raw Slack ids (U…/W…/C…/D…) are for tool calls only — never include one in a reply. Requires the chat:write user scope."
effects = ["network", "use_secret", "external_write"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/send_message.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read", "chat:write"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

# The eight entries below complete Slack's coverage of the 16 core standard
# messaging operations. They are appended rather than interleaved so the
# v2-era eight keep their original positions, which the v2->v3 projection
# parity gate compares positionally
# (crates/app/ironclaw_composition/tests/first_party_manifest_v3_parity.rs).

[[tools]]
standard_op = "edit_message"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.edit_message"
description = "Slack notes: text is Slack mrkdwn and replaces the whole message body — Slack has no partial edit. Only your own messages are editable, and a workspace may cap how long after posting. Requires the chat:write user scope."
effects = ["network", "use_secret", "external_write"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/edit_message.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read", "chat:write"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "delete_message"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.delete_message"
description = "Slack notes: deletion is permanent and only your own messages can be deleted. Deleting a thread parent also removes its replies. Requires the chat:write user scope."
effects = ["network", "use_secret", "external_write"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/delete_message.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read", "chat:write"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "add_reaction"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.add_reaction"
description = "Slack notes: emoji is a Slack short name without colons (`thumbsup`, not `:thumbsup:` — surrounding colons are stripped for you); unicode characters are not accepted. A reaction you have already added is reported as applied rather than as an error. Requires the reactions:write user scope."
effects = ["network", "use_secret", "external_write"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/add_reaction.md"

# reactions:write only — this tool calls reactions.add and nothing else. Its
# sibling remove_reaction additionally reads (reactions.get) to find the
# connected account's own reactions, so reactions:read belongs there alone.
[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read", "reactions:write"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "remove_reaction"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.remove_reaction"
description = "Slack notes: emoji is a Slack short name without colons (`thumbsup`, not `:thumbsup:`). Omit emoji to remove every reaction you personally added to the message — that variant reads the message's reactions first and needs the reactions:read user scope as well. Only your own reactions are ever removed. Requires the reactions:write user scope."
effects = ["network", "use_secret", "external_write"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/remove_reaction.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read", "reactions:read", "reactions:write"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "open_dm"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.open_dm"
description = "Slack notes: returns the existing DM when one is already open, so calling it twice is safe and never creates a duplicate. user_ref must be a real Slack user id (U…/W…) from a people operation — never a conversation id. Requires the im:write user scope."
effects = ["network", "use_secret", "external_write"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/open_dm.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read", "im:write"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "get_message"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.get_message"
description = "Slack notes: Slack has no single-message endpoint, so this reads the conversation at that exact timestamp and falls back to the thread when the message is a threaded reply; either way only an exact timestamp match is returned, never a neighbouring message. Raw Slack ids are for tool calls only — never include one in a reply."
effects = ["network", "use_secret"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/get_message.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "resolve_user"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.resolve_user"
description = "Slack notes: Slack has no server-side people search, so this scans up to limit directory entries per call (default and max 200) and matches your query against display name, real name, and handle. Follow next_cursor to keep scanning when the person you want has not appeared yet. Deactivated accounts and bots are skipped."
effects = ["network", "use_secret"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/resolve_user.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

[[tools]]
standard_op = "list_members"
origin_gate_matrix = { loop_run = "gated_unless_granted", product = "forbidden", automation = "forbidden" }
id = "slack.list_members"
description = "Slack notes: works on channels, group DMs, and DMs. Display names are resolved for a bounded prefix of the page; members past that budget come back as ids alone, so page with a smaller limit when you need every name."
effects = ["network", "use_secret"]
default_permission = "ask"
visibility = "model"
prompt_doc_ref = "prompts/slack/list_members.md"

[[tools.credentials]]
handle = "slack_user_token"
vendor = "slack"
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read"]
audience = { scheme = "https", host = "slack.com" }
injection = { type = "header", name = "authorization", prefix = "Bearer " }

# ---- channel surface (at most one per extension) ---------------------------

[channel]
id = "messages"
display_name = "Slack messages"
conversation_model = "continuous"
commands = ["model", "status", "new", "stop", "interrupt"]

# For a conversational vendor the two axes happen to share one mechanism — a
# message in the conversation — which is exactly why the reply/delivery
# distinction stayed invisible until a streaming channel existed. They are
# still declared separately: `message` twice is a coincidence of transport,
# not one concept.
[channel.reply]
transport = "message"

[channel.delivery]
transport = "message"

[channel.connection]
provider = "slack"
strategy = "oauth"
instructions = "Connect your Slack account with OAuth from the Slack extension configuration. The workspace bot is configured once by an administrator; this connection authorizes only your personal Slack tools and identity."
input_placeholder = ""
submit_label = "Connect Slack"
error_message = "Slack OAuth connection failed. Try connecting Slack again."
connection_success_message = "Slack is installed and connected for this user. Slack tools use the user's OAuth account, while inbound and outbound channel messages use the administrator-configured workspace bot."

[channel.connection.notices]
connect_required = "👋 Connect your Slack account in the IronClaw web app, then message me here again."
paired = "✅ Slack is connected. You can talk to IronClaw here."
already_paired_same_user = "✅ This Slack account is already connected to you."
already_bound_to_other_user = "This Slack account is already connected to another IronClaw user."
expired_or_unknown = "That Slack connection is invalid or expired. Start a fresh OAuth connection from IronClaw."

[channel.ingress]
route_suffix = "events"
method = "post"
body_limit_bytes = 1048576

[channel.ingress.verification]
kind = "hmac_sha256"
secret_handle = "slack_signing_secret"
signature_header = "X-Slack-Signature"
signature_prefix = "v0="
signature_encoding = "hex"
timestamp_header = "X-Slack-Request-Timestamp"
max_age_seconds = 300
signed_payload = [
  { literal = "v0:" },
  { header = "X-Slack-Request-Timestamp" },
  { literal = ":" },
  { body = true },
]

[[channel.egress]]
scheme = "https"
host = "slack.com"
methods = ["post"]
credential_handle = "slack_bot_token"
paths = [
  "/api/chat.postMessage",
  "/api/chat.delete",
  "/api/conversations.open",
  "/api/files.completeUploadExternal",
  # 👀→✅ run-lifecycle reaction on the triggering message (needs the
  # reactions:write scope operator-side; a missing scope fails the reaction
  # best-effort and never fails the run).
  "/api/reactions.add",
  "/api/reactions.remove",
]
request_body_limit_bytes = 262144
response_body_limit_bytes = 262144

# conversations.history / conversations.replies hydrate shared-channel pings
# with recent conversation context at ingress. The bot needs the
# channels:history-family scopes granted operator-side; when absent, Slack
# returns missing_scope and the ping proceeds with no context.
[[channel.egress]]
scheme = "https"
host = "slack.com"
methods = ["get"]
credential_handle = "slack_bot_token"
paths = [
  "/api/files.info",
  "/api/files.getUploadURLExternal",
  "/api/conversations.history",
  "/api/conversations.replies",
]
request_body_limit_bytes = 0
response_body_limit_bytes = 262144

# Private downloads remain under Slack's exact file host and prefix. The bot
# credential is injected by the host and the response cap matches the shared
# per-file attachment budget.
[[channel.egress]]
scheme = "https"
host = "files.slack.com"
methods = ["get"]
credential_handle = "slack_bot_token"
path_prefixes = ["/files-pri/"]
request_body_limit_bytes = 0
response_body_limit_bytes = 5242880

# External upload tickets are pre-authorized by Slack. File bytes carry no bot
# credential and are limited to the same per-file attachment budget.
[[channel.egress]]
scheme = "https"
host = "files.slack.com"
methods = ["post"]
path_prefixes = ["/upload/"]
request_body_limit_bytes = 5242880
response_body_limit_bytes = 262144

[channel.presentation]
supports_markdown = true
supports_threads = true
can_reply_in_threads = true
command_prefix = "/ironclaw "

# ---- auth surface: recipe data, zero extension code ------------------------

[auth.slack]
method = "oauth2_code"
display_name = "Slack account"
authorization_endpoint = "https://slack.com/oauth/v2/authorize"
token_endpoint = "https://slack.com/api/oauth.v2.access"
scope_param = "user_scope"
pkce = "s256"
# The union of every [[tools.credentials]] scope list above — one grant shared
# by all tools. reactions:read/reactions:write/im:write joined it with the
# reaction pair and open_dm; see the least-privilege note at the top of this
# file for what that means for accounts connected before then.
scopes = ["search:read", "channels:history", "groups:history", "im:history", "mpim:history", "channels:read", "groups:read", "im:read", "mpim:read", "users:read", "chat:write", "reactions:read", "reactions:write", "im:write"]
client_credentials = { client_id_handle = "slack_oauth_client_id", client_secret_handle = "slack_oauth_client_secret" }

[auth.slack.token_response]
access_token = "/authed_user/access_token"
scope = { path = "/authed_user/scope", missing = "fallback_to_requested" }

[auth.slack.identity]
account_id = "/authed_user/id"
team_id = "/team/id"
enterprise_id = "/enterprise/id"
app_id = "/app_id"
