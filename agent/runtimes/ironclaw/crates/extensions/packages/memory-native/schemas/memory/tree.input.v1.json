{
  "type": "object",
  "properties": {
    "path": {
      "type": "string",
      "default": "",
      "description": "Relative memory directory path to list; omit (or empty) for the memory root. Must not be absolute (no leading '/'), contain '..' traversal, or use backslash separators. The host filesystem additionally resolves the path within the scoped /memory mount.",
      "not": { "pattern": "(^/)|(\\.\\.)|(\\\\)" }
    },
    "depth": {
      "type": "integer",
      "minimum": 1,
      "maximum": 10,
      "default": 1,
      "description": "Maximum directory depth to include"
    }
  },
  "additionalProperties": false
}
