List persistent memory documents as a compact tree, scoped to the current
tenant/user/agent/project. Optionally pass a `path` to list a subdirectory and a
`depth` to bound traversal. Use it to discover what memory documents exist before
reading or writing.
