Search persistent memory documents with a natural-language `query`, scoped to
the current tenant/user/agent/project. Returns the best-matching snippets with
their source path and a relevance score. Use it to find relevant notes when you
do not know the exact document path.
