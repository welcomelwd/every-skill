Download a file's content as text, including binary documents (PDF, PPTX, DOCX, XLSX), or export a Google Workspace file.

Use this for any Drive file you need to read, summarize, or analyze — text files and Google Docs/Sheets/Slides return their text directly, and binary office documents and PDFs are converted to extracted text automatically. You do not need a shell, a downloader, or any other tool first.

The host selects this operation from the capability id. Provide only the parameters described by the input schema; do not include an action field.
