{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Standard messaging search_messages input",
  "type": "object",
  "required": ["query"],
  "properties": {
    "query": {
      "type": "string",
      "minLength": 1,
      "description": "Search query text."
    },
    "sort": {
      "type": "string",
      "enum": ["relevance", "timestamp"],
      "description": "Result ordering. Omit or use relevance for vendor-ranked matches; timestamp returns newest matches first."
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 1000,
      "description": "Maximum matches to return. The host rejects values above 1000; adapters may clamp further to vendor maxima."
    },
    "cursor": {
      "type": "string",
      "minLength": 1,
      "description": "Opaque pagination cursor from a previous call's next_cursor."
    }
  },
  "additionalProperties": false
}
