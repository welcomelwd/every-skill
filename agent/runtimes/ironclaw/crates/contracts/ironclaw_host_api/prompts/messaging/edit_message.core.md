Edit an existing message's text on this extension, as its connected account. `message_ref` must be the exact object returned by a prior send or read on this extension — never invent one and never reuse a ref from a different extension; most vendors only allow editing your own message within a limited window after sending. `text` replaces the message body. Returns the `message_ref` — use it with delete_message and reaction operations where available.
