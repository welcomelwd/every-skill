Delete an existing message on this extension, as its connected account. `message_ref` must be the exact object returned by a prior send or read on this extension — never invent one and never reuse a ref from a different extension; most vendors only allow deleting your own message. Returns `deleted: true` together with the `message_ref` that was removed; a delete that did not happen surfaces as an error, never `deleted: false`.
