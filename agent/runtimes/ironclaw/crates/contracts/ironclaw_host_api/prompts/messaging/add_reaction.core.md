Add an emoji reaction to a message on this extension, as its connected account. `message_ref` must be the exact object returned by a prior send or read on this extension — never invent one and never reuse a ref from a different extension. `emoji` is this vendor's emoji token, a name or a unicode character; the addendum documents the exact dialect. Returns the `message_ref` and `emoji` as confirmation that the reaction was applied.
