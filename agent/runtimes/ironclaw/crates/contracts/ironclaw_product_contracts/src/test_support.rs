//! Feature-gated test-support fixtures for product-DTO consumers (never
//! compiled into production binaries; see `scripts/check_no_panics.py` for the
//! canonical module convention).

pub mod fakes;
