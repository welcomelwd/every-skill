apiVersion: extensions.agents.x-k8s.io/v1beta1
kind: SandboxWarmPool
metadata:
  name: python-sdk-warmpool
  namespace: hpa-test
  labels:
    app: python-sdk-warmpool
spec:
  replicas: 10
  sandboxTemplateRef:
    name: python-sandbox-template