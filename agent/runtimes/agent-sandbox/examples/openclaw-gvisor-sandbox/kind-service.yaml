apiVersion: v1
kind: Service
metadata:
  name: openclaw-gateway
spec:
  type: NodePort
  selector:
    sandbox.users.io/openclaw-claim: openclaw-sandbox-claim
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      nodePort: 30789
