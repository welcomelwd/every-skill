"""Tests for Bedrock models — feature-central files.

Split out of `tests/models/test_bedrock.py` so feature areas (native structured
output, strict tool calls, …) cluster together for review and discoverability.
"""
