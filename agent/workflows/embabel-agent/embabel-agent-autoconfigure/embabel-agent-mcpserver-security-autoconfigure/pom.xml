<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.embabel.agent</groupId>
        <artifactId>embabel-agent-autoconfigure</artifactId>
        <version>2.0.0-SNAPSHOT</version>
    </parent>
    <artifactId>embabel-agent-mcpserver-security-autoconfigure</artifactId>
    <packaging>jar</packaging>
    <name>Embabel Agent MCP Server Security AutoConfiguration</name>
    <description>Spring Boot AutoConfiguration for Embabel MCP Server Security</description>
    <url>https://github.com/embabel/embabel-agent</url>

    <scm>
        <url>https://github.com/embabel/embabel-agent</url>
        <connection>scm:git:https://github.com/embabel/embabel-agent.git</connection>
        <developerConnection>scm:git:https://github.com/embabel/embabel-agent.git</developerConnection>
        <tag>HEAD</tag>
    </scm>

    <dependencies>
        <!-- Main Dependencies -->
        <dependency>
            <groupId>com.embabel.agent</groupId>
            <artifactId>embabel-agent-mcp-security</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-config</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-oauth2-resource-server</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-oauth2-jose</artifactId>
        </dependency>

        <!--
            Spring Boot 4 split the security autoconfigurations out of
            spring-boot-autoconfigure into dedicated modules:
              - spring-boot-security (SecurityAutoConfiguration; package moved to
                org.springframework.boot.security.autoconfigure.servlet)
              - spring-boot-security-oauth2-resource-server
                (OAuth2ResourceServerAutoConfiguration; package moved to
                org.springframework.boot.security.oauth2.server.resource.autoconfigure.servlet)
            Both are referenced by AgentMcpServerSecurityAutoConfigurationTest's
            AutoConfigurations.of(...) list.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-security</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-security-oauth2-resource-server</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
