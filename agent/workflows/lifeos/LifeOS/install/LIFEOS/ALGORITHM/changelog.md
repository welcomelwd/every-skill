# Algorithm Changelog

The doctrine file (`vX.Y.Z.md`) holds **only doctrine** — what the Algorithm does, this run. All change history, migration steps, rollback recipes, and "what did/didn't change" prose live here. New versions add a section to the **Full History** below; the doctrine file stays focused.

- **v8.17.3 changelog (the dead nudge row is cut — PATCH):** The `principal messages mid-run` row asked whether a message sitting in the model's own context revises the goal — exactly what §Events forbids, since a row may only ask about **state the model cannot observe from its own context**. It survived every review because it was inert: the model always sees the message, so the row never taught anything. The row is deleted and the bound it violated is now stated in the paragraph above the table, with this row named as the case that proves it. No behavior change (the hook never carried this row); the doctrine table now obeys its own rule. Landed as an in-place edit to `v8.17.2.md` on 2026-07-30 and re-homed here on the 7.24.0 cut — version files are immutable, so 8.17.2 was restored to its tagged content. Rollback: `echo 8.17.2 > LATEST`; `rm v8.17.3.md`.
- **v8.17.2 changelog (nudge-table row catches up with its own doctrine — PATCH):** Max in-family release audit, 2026-07-30 (7.23.1 cut): the `design-class` nudge row still read "elect it (his `/model`, or dispatch the leg to `Max`), or log the skip" — the exact anti-pattern INC-20260729-max-rung-not-elected records, contradicting §Spend's "no run ever asks which rung it is on" in the same file and the shipped `DESIGN_CLASS_NUDGE` hook text. The row now asks for the design pass (native, top rung by config) plus the claim-11 second look, never a rung election. Behavior was already correct (the hook is authoritative); this aligns the doctrine table with it. Rollback: `echo 8.17.1 > LATEST`; `rm v8.17.2.md`.
- **v8.17.1 changelog (the state enumeration deletes itself — PATCH):** Max in-family release audit, 2026-07-30 (7.23.0 cut, P1): the Telemetry bullet hand-listed all eight ascent states in the same sentence that commands "Never hand-list phase names anywhere." The 2026-07-30 six-state consolidation (routefinding/roped→ascending, summit→cairn in `LIFEOS/TOOLS/ascent.ts`) made the list stale, proving the sentence right. The enumeration is deleted; the table resolves through `ascent.ts` alone. No doctrine semantics changed. Rollback: `echo 8.17.0 > LATEST`; `rm v8.17.1.md`.
- **v8.17.0 changelog (the main loop IS the top rung, by config — FEATURE):** principal correction 2026-07-29, hours after v8.16.0: *"That should have been automatic, and why did it require using an agent? You were supposed to natively use Fable and then alternate / delegate as needed for Opus for the execution work."* **What v8.16.0 got wrong.** It correctly found that v8.14.0's "MAX is the session default" premise was false, and drew the wrong conclusion: *elect* MAX per-run. The observable result, one run later, was a session that read its own doctrine, concluded it could not know its rung, asked {{PRINCIPAL_NAME}} to choose one, and dispatched the design leg of Vector's tenant provisioning to `Agent(subagent_type:"Max")` — briefing a fresh context on material already in front of it. Both failures share a cause: the rung was treated as a per-run decision instead of configuration. **The fix is config, in a version-controlled file.** `settings.json` now carries `"model": "fable"` (alias, so a lineup bump never edits it) plus a `//model` comment key explaining why. That is why the v8.14.0 flip evaporated — it was an interactive `/model` choice living in no file — and why this one will not. **Doctrine change** (§Spend, the MAX paragraph replaced): the main loop is the top rung, always; design, planning, scoping and judgment happen NATIVELY and inline; the only elected rung move is the DOWNSHIFT, dispatched with a tier alias for the execution leg. What survives from v8.16.0 is the work-class trigger, re-pointed: authority/security-boundary, architecture-setting, or ISA-feature-sized work earns a design pass before building and a second look scaled to blast radius (claim 11) — never a rung negotiation. **Surfaces changed in lockstep:** `settings.json`, OPERATIONAL_RULES § Model selection (header + the elect-it bullets), `hooks/AlgorithmNudge.hook.ts` (`DESIGN_CLASS_NUDGE` now says "you are already on the top rung; design natively; Max is a second look, not a rung rental" — the row that fired during the offending run and told it to do the wrong thing), `LifeosSystemArchitecture.md`, and the five version anchors. **Rollback:** delete the `model` key from `settings.json` and revert the four prose surfaces; the rung then falls back to the harness default (second rung) and v8.16.0's elect-it rule becomes correct again.
- **v8.16.0 changelog (MAX is elected, never inherited — FEATURE):** principal directive 2026-07-29: *"you should've used Fable for the design and planning of this whole system, because this is such a big feature. You need to update the algorithm statement around model routing so it would have done that."* **This corrects a false premise introduced by v8.14.0.** That version stated the rubric's shape as "runs the smartest rung (MAX, the session default)" and rewrote OPERATIONAL_RULES to say "the default IS the smartest rung" — both resting on the `/model`→top-rung flip made the same session. The flip did not survive: there is no `model` key in `settings.json` or `~/.claude.json` today, so the harness default resolves to the SECOND rung (`EFFORT_MODEL.high = "opus"`) while MAX is `fable`. The parenthetical was therefore load-bearing in the wrong direction — a run reads "MAX is the default," concludes it is already at the top, and elects nothing. Doctrine that tells you you're already there is doctrine that never fires. Measured cost: the 2026-07-29 Vector admin-plane build (F39/F40 — a new platform authority, a cross-tenant read boundary under operator trust, two shipped editions) was designed, built, and deployed to production entirely one rung below where the rubric says that class belongs. Doctrine change (§Spend, one paragraph added after the rung paragraph; the stale `(MAX, the session default)` parenthetical deleted): **MAX is elected, never inherited.** The election trigger is stated as the WORK CLASS, not an introspected model name — work that introduces or changes an authority/security boundary, sets a system's architecture, or is large enough to earn its own ISA feature block gets its design and planning leg on the top rung BEFORE building, either via the principal's `/model` or by dispatching that leg to the `Max` agent with the goal verbatim. Class-based on purpose: a run cannot reliably read its own rung (no hook reads the session model from its payload today, and the statusline's `model-cache.txt` is documented last-writer-wins across sessions), so a rule keyed on "am I on Fable?" would be unenforceable. Skipping the pass stays allowed and costs one Log row naming the skip, the same shape as the claim-11 second-look skip — the failure mode being killed is silence, not the choice. Companion: OPERATIONAL_RULES § Model selection v1.25.0 (the false "saved `/model` sits on the smartest rung / this is the session default" text replaced by the elected-not-assumed rule + trigger + skip-row). Version-string sweep: ARCHITECTURE_SUMMARY, LifeosSystemArchitecture (both the version line and the § Pipeline Topology Algorithm row, whose prose also carried the stale "smartest rung is the session default" claim), TestingDoctrine, PulseMetadata, ISAHtmlMirror. No hook, tool, or `EFFORT_MODEL` edit — the registry was already correct and is what proved the doctrine wrong. **Enforcement shipped (same session, after the principal rejected doctrine-only as insufficient — "just get it so that this would have used Fable for the planning"):** the `design-class` always-on row in `hooks/AlgorithmNudge.hook.ts`. An earlier pass in this same session declined to build it, reasoning that the session model is not available to a hook — that reasoning was wrong, and wrong in an instructive way: the rule is keyed to the WORK CLASS, so the row never needs the model at all. Trigger is a conjunction (design verb + authority/architecture noun) plus a short unambiguous-phrase list, chosen for precision because a row that fires on half of all prompts is tuned out within a day and leaves the incident just as unprevented as silence. Proof is a replay, not an assertion: the verbatim prompt that opened the Vector admin-plane build now emits the nudge through the live hook at UserPromptSubmit, and is pinned as a regression fixture in `test/hooks/lib/design-class-nudge.test.ts` (9 tests: incident replay, positive classes, and both over-fire traps — bare authority noun, bare design verb). Migration: `echo 8.16.0 > LATEST` (done); v8.15.0.md kept byte-identical. Rollback: `echo 8.15.0 > LATEST`; `rm LIFEOS/ALGORITHM/v8.16.0.md`; revert the OPERATIONAL_RULES § Model selection bullet and the five version-string files.
- **v8.14.0 changelog (MAX-default rung shape — FEATURE):** principal directive 2026-07-28, same session as the `/model`→top-rung flip (triggered by Zvi Mowshowitz's Opus 5 field review: top model for judgment, supervision, and anything intelligence-loaded; one rung down for scoped execution — Opus 5's observed failure shape is wandering off-spec at high effort, more claudisms, higher hallucination rate). Doctrine change (one sentence in §Spend's rung paragraph): the rubric's SHAPE is now stated inline — everything important (judgment, planning, scoping, supervision, second looks) runs the smartest rung (MAX, the session default), pivoting one rung down (HIGH) for the execution leg of scoped, well-defined work, or two (MEDIUM) when execution is trivially easy. The classes' ONE home stays OPERATIONAL_RULES § Model selection (v1.23.0, rewritten same session: the default IS the smartest rung; classes restated as MAX-think / HIGH-execute / MEDIUM-trivial; the second-look/advisor lane was already MAX-aligned — `agents/Max.md` pins the top-tier alias, in-family reviews dispatch at the smartest rung, `Inference.ts --level max` resolves there via `EFFORT_MODEL` — so no machinery changed). Companions: system prompt v3.5.0 (dynamic-range rubric restatement synced), LifeosSystemArchitecture version strings. No hook, tool, or `EFFORT_MODEL` edit; `settings.json` `model` was written by `/model` itself. Migration: `echo 8.14.0 > LATEST` (done); v8.13.0.md kept byte-identical. Rollback: `echo 8.13.0 > LATEST`; `rm LIFEOS/ALGORITHM/v8.14.0.md`; revert the OPERATIONAL_RULES § Model selection bullet and the system prompt sentence; `/model` back down if desired.
- **v8.12.0 changelog (Max joins the menu; heavy-analysis agents named — FEATURE):** principal directive 2026-07-27: build a new agent, Max — "extremely careful, critical, and analytical," pinned to Anthropic's latest top model, thinking deeply about work using all of the system's thinking skills, added on top for super-sensitive things such as public LifeOS releases; "Max is the Anthropic version of Forge." Same directive: reorient Forge so the two share the *exact* same personality, and make the Algorithm aware that both are available for heavy analysis. Doctrine changes (two lines, no structural change): §Spend's one-menu paragraph now NAMES `Max` (Anthropic top rung, read-only — deep analysis + scrutiny on public/irreversible work) and `Forge` (OpenAI lineage — build + cross-vendor audit) as menu items that are easy to forget, with the both-of-them shape stated for public-or-permanent work; the Telemetry §Second-look bullet now names `Agent(subagent_type:"Max")` as the in-family option alongside any other non-forked smartest-rung dispatch. Election stays judgment — nothing here mandates a dispatch. Outside doctrine, and revised mid-session on the principal's challenge ("if it's not changing often why not just put it in the agents themselves? that's mostly for if it's dynamic, no?"): **agent files are now self-contained and the `*Context.md` companions are GONE** — `ForgeContext.md` and the four `*ResearcherContext.md` files were folded into their agents and deleted. The split bought nothing (an agent's .md loads only when that agent spawns, so nothing was saved in the main context) while costing a Read the agent could skip, duplicating whole sections between the pair (Forge carried "role in the Algorithm", "what I return", and "constraints" twice), and duplicating ~28 lines of identical boilerplate across the four researchers. The shared Max/Forge personality is INLINED byte-identically in both agent files between `<!-- SHARED:SCRUTINY-CHARACTER -->` markers, with a new blocking `/ic` check, `agent-shared-blocks`, that fails when any two copies of a `SHARED:<KEY>` block disagree — proven red on a one-word drift, then green (the earlier `ScrutinyDoctrine.md` design lasted one commit). `agents/Max.md` pins `model: fable` (the ALIAS, which always resolves to Anthropic's latest top model — never a pinned ID that rots) and is read-only by permission (Edit/Write/NotebookEdit denied), the constitutional analysis-means-read-only rule with teeth. The researcher rewrite also dropped retired doctrine the old files still carried: mandatory subagent voice curls (subagents never voice — the DA narrates), the retired 8-point SUMMARY/STORY-EXPLANATION response template presented as "CONSTITUTIONAL", `Write(*)`/`Edit(*)` in `permissions.allow` contradicting their own `disallowedTools`, and 16 dead `skills/Research/Workflows/*` pointers (AcademicResearch.md, QueryDecomposition.md et al. do not exist) replaced with the four files that do. `IntegrityCheck.ts` drops the companion-doc exclusion entirely — every `.md` under `agents/` must now carry frontmatter. Statusline: the cross-vendor label stops stripping the codename suffix — renders `+GPT-5.6 SOL`, not `+GPT-5.6` (principal, same directive). Migration: `echo 8.12.0 > LATEST` (done); v8.11.0.md kept byte-identical. Rollback: `echo 8.11.0 > LATEST`; `rm LIFEOS/ALGORITHM/v8.12.0.md agents/Max.md`; `git checkout a1eb049f0~1 -- agents/ LIFEOS/LIFEOS_StatusLine.sh LIFEOS/TOOLS/IntegrityCheck.ts settings.json` (restores the five Context files and the pre-Max agent set).
- **v8.11.0 changelog (one home per rule + native rung routing — FEATURE):** principal directive 2026-07-26, from the rule-management session: "I don't really like how we're doing custom rule management… is there any way to simplify?" → approved design: every rule gets exactly ONE home. Doctrine changes: claims 8 and 16 slimmed — incident narratives replaced by INC-ID citations (`MEMORY/LEARNING/INCIDENTS/INC-20260715-danielsaid-container-sampling.md`, `INC-20260717-ullive-dns-deletion.md`, plus INC-20260627/INC-20260707 files created for Verification.md; home is LEARNING — the self-improvement loop's evidence base — per principal, not KNOWLEDGE); the operative mechanics they narrated now live solely in `LIFEOS/RULES/Verification.md` (renamed from VerificationExpanded.md, upgraded from "expansion" to the single statement of the six verification rules — the system prompt §Verification slimmed to constitutional core + rule names + pointer, v3.3.0). **Second directive, same session: `Inference.ts` is a STANDALONE leveled utility and NOT part of Algorithm/Advisor intelligence selection** — so the v8.10.0 Advisor-subprocess door is retired the same day it shipped: `--mode advisor`/`--auto-state`/`--print-state` removed from Inference.ts, `UplevelGate.ts` + its test deleted (its premise — dispatch-borne uplevel is a violation — inverted into the sanctioned mechanism), claim 11's in-family second look becomes a NON-FORKED dispatch at the smartest rung via native routing, §Spend's carrier bullet now states rung moves ride native routing with Inference.ts as the verified-execution fallback carrier. Third directive answered: the Advisor is NOT kept as parallel backup machinery — two uplevel mechanisms would recreate the dual-home sprawl this run kills; the fallback is Inference.ts existing as a utility, not doctrine wiring. §Spend's role-rubric paragraph deduped to a pointer (the classes live ONLY in OPERATIONAL_RULES § Model selection, v1.22.0). Enforcement shipped: `/ic` gains `rule-duplication` (blocking) — 12-word shingle match across the rule surfaces (8 files today: system prompt, CLAUDE.md, current doctrine, RULES/*, OPERATIONAL_RULES + Expanded), proven RED on the pre-change tree (7 findings, including two the design pass missed: the SelfHealing↔Philosophy shared header and the v8.10.0↔OPERATIONAL_RULES advisor-rule duplicate) and GREEN post-change; replaces the `/ic` `uplevel-routing` check, which retired with its gate. Pure logic in `LIFEOS/TOOLS/lib/rule-duplication.ts` (fence fail-closed, per-line inline-code stripping) with fixtures in `test/tools/rule-duplication.test.ts` — added after the independent review caught the gate shipping testless with untestable in-orchestrator exports. Dead files archived: `modes/`, `mode-detection.md`, `parameter-schema.md`, `target-types.md` → `LIFEOS/ALGORITHM/archive/` (referencing docs re-pointed). Migration: `echo 8.11.0 > LATEST` (done); v8.10.0.md kept byte-identical. Rollback: NOT a single revert — a concurrent session's checkpoint commits (`eba8544d8`, `56d5b1ebe`) swept most of this consolidation in alongside unrelated Bunker work (review finding #2). Roll back per-surface instead: `echo 8.10.0 > LATEST`; `git checkout 876d17534 -- LIFEOS/LIFEOS_SYSTEM_PROMPT.md LIFEOS/TOOLS/Inference.ts LIFEOS/TOOLS/UplevelGate.ts LIFEOS/TOOLS/IntegrityCheck.ts LIFEOS/RULES/` (restores the v8.10.0-era prompt, advisor mode, UplevelGate, VerificationExpanded); delete `LIFEOS/TOOLS/lib/rule-duplication.ts` + its test. Origin ISA: `MEMORY/WORK/20260726-rule-structure-consolidation/ISA.md`.

- **v8.10.0 changelog (the Advisor restored as the single uplevel-routing path + routing policy as roles — FEATURE):** principal directive 2026-07-26, opening with a question — "are we using the advisor system within our system? If not I want it enabled with Fable as the advisor model" — then sharpening on the follow-up: "It's the official way to do uplevel routing so make sure that's the only place we're doing it." Answer to the question: **no.** The Advisor shipped through v7.0.0 as R9 (`Inference.ts --mode advisor --auto-state`, a top-rung subprocess consulted at the commitment boundary) and was deleted in the v8.0.0 claims restructure along with modes/tiers; v8.9.0 had zero mentions and no advisor code survived. **Why the consolidation is newly load-bearing rather than a cleanup:** `DISPATCH_EXECUTES_FABLE` flipped to `true` on 2026-07-12 when a harness fix made Agent dispatch execute its model faithfully. Before that flip, naming the top rung on a dispatch silently ran a lower one — effectively one carrier. After it, there were **three** (main loop, `Inference.ts --level max`, Agent dispatch), with two live sites already riding the uncontrolled ones and two prose files still asserting the pre-flip fact. Doctrine changes: §Spend's carrier fact now states **uplevel routing has exactly one door — the Advisor** (elected, never mandated; downshifting and the principal's `/model` dial explicitly unaffected); claim 11 names an Advisor consult as a sanctioned in-family second look and adds it to the zero-second-look visibility floor; the Telemetry annex's cross-vendor bullet generalizes to **second-look invocations** carrying both Forge and the advisor command. §Spend also gains the **role-based routing policy** (second principal directive, same session): work loads on the session default, then meta work on the system itself and edge-of-difficulty work earn the smartest rung, most work one below, important-but-not-intelligence-hungry work one below again — which **supersedes the 2026-07-11 "no model-routing rubric" baseline**, so §Spend's opening sentence was reconciled (it had asserted no rubric exists). The policy lives in OPERATIONAL_RULES § Model selection as ROLES, never model names; the doctrine file still contains **zero** model names (verified). Code shipped: `Inference.ts` gains `--mode advisor` / `--auto-state` / `--slug` / `--print-state` (`synthesizeAdvisorState` puts the stated goal verbatim above the ISA; consults log to `MEMORY/OBSERVABILITY/advisor-consults.jsonl` with the executed model read back from `modelUsage`); new `LIFEOS/TOOLS/UplevelGate.ts` with **two legs** — static over the committed tree and runtime over real `subagent-events.jsonl` records — wired into `/ic` as `uplevel-routing` (blocking, verified against a seeded stray). Strays closed: `AlgorithmCleanup.mjs` synthesize step de-pinned (meta work still earns the top rung, but via the session dial or the Advisor, not an ad-hoc dispatch); the three Evals model-based graders' hardcoded `judge_model` maps replaced by derived `JudgeLevel.ts` capping at `high`. Stale facts corrected: `OperationalRulesExpanded.md` and `skills/CMUX/ISA.md` D-5 both still claimed no Agent dispatch reaches the top rung. **Two defects found en route, both fixed:** (1) v8.8.0 collapsed the four-key `LEVEL_TO_HARNESS_EFFORT` record into the scalar `UNIFORM_HARNESS_EFFORT` but left `Inference.ts` reading `.low`/`.medium`/`.high`/`.max` — all four resolved to `undefined`, so `--effort undefined` was spawned on **every inference call** and the documented uniform-high reasoning effort had never actually been set (tsc flagged it; nothing gated on tsc); (2) the graders' maps pinned superseded ids while every live eval spec passes a different one, so **every current judge silently graded at `medium`**. **Advisor-review provenance (the tool reviewing its own restoration, executed `claude-fable-5` verified from the envelope):** its heaviest finding killed a single-leg gate — "a seed crafted by the same pass that wrote the detector matches it by construction; that proves the gate CAN fail, not that it can SEE" — producing the runtime leg and an adversarial test set, which then immediately caught the static leg missing the args-**array** spawn form (`["--model", "<id>"]`, exactly how this codebase spawns claude). Its second finding produced `--print-state`. Its third (that capping graders might weaken a control) was **rebutted with evidence** — no eval spec anywhere requested the top rung. Tests: 29 green across `uplevel-gate` / `judge-level` / `models`. Origin ISA: `MEMORY/WORK/20260726-advisor-uplevel-routing/ISA.md`. Migration: `echo 8.10.0 > LATEST` (done) + doc pointers swept + ARCHITECTURE_SUMMARY regenerated; v8.9.0.md kept byte-identical. Rollback: `echo 8.9.0 > LATEST`, revert the `Inference.ts` advisor block, `git rm LIFEOS/TOOLS/UplevelGate.ts` + its `/ic` wire-up, restore the grader maps.

- **v8.9.0 changelog (model-routing doctrine cut from §Spend — FEATURE):** principal directive 2026-07-25 ("let's be aggressive about cutting BPE violating stuff. We're trying to simplify here"), with the keep-priorities named explicitly: the separation invariants (data/code, public/user, user/work) and the constitutional rules in the system prompt. §Spend had accumulated four paragraphs of model prose while its own opening sentence declared "there is no model-routing rubric" — a direct self-contradiction. Three cuts: (1) the **rung-ladder paragraph** (added v8.7.3, 2026-07-22) — a match-the-model-to-the-cheapest-rung rubric plus a Workflow-tool pitch already covered by the one-menu paragraph's "native harness features"; its one durable clause ("this file names roles, never models") is preserved, folded into the carrier bullet. (2) the **execution-class spend fact** (added v8.4.3, 2026-07-15, after a video pipeline burned the top rung) and its matching always-on §Events row — a smarter model downshifts a Descript-driving loop without a lexicon telling it to; the incident it was written for was a model failure, not a missing rule. "Four spend facts" → three. (3) the **carrier bullet trimmed** to its tool-contract core — the 8.7.0-staleness parenthetical and the "operative line at last probe" clause both restated facts the bullet itself says live only in `models.ts`, and "cheap utility inference is fine anywhere" is permission no capable model needs. KEPT and untouched: every separation invariant, the constitutional verification tier, claims 8/11/15/16, the strong-carrier guarantee for core-surface hard reasoning, the inheritance-is-the-default statement, the 110k inline-reachable gotcha, the smart-zone sizing fact. Companion (same session): the five `model: opus` pins removed from `agents/*.md` so every agent inherits the session default. PENDING companion: `hooks/AlgorithmNudge.hook.ts` still carries `matchExecClass()` + its EXEC-CLASS fire for doctrine that no longer exists — hook and its tests to be cut in the same change. Migration: `echo 8.9.0 > LATEST`; v8.8.0.md kept byte-identical. Rollback: `echo 8.8.0 > LATEST` + `git checkout` the agent files.

- **v8.8.0 changelog (doctrine names roles, never models — FEATURE):** principal directive 2026-07-24 ("This needs to be fully BPE in terms of using the best model available as default, and not hardcoding specific models which can go stale; there has to be a way to do this such that when the underlying harness updates it updates everything automatically including our system"), after he set `/model` to Opus 5 [1m] and asked whether the system auto-detects capability. It does not, and it should not: capability ordering is a vendor fact, but every OTHER model reference can derive. Two harness mechanisms carry the load, both verified this session: (1) `--model` accepts a tier ALIAS that resolves to the LATEST model in the tier (`claude --help`: "Provide an alias for the latest model") — so an alias never rots and a pinned ID always will; (2) a dispatch that OMITS `model` inherits the session model — so "use the best available model" is achieved by not naming one, and `/model` becomes the single dial for the whole system. Doctrine change: all four model names removed from the doctrine file (nudge row "Execution classes run on Opus delegates; Fable keeps the judgment legs" → "a delegate a rung below the session default; the judgment legs stay on the session default"; §Spend menu "Sonnet and Haiku... Opus... Fable" → "the cheap rungs... a mid rung... the session default"; §Spend execution-class fact de-named; §Spend carrier fact gains the explicit inheritance-is-the-default statement). Rungs are named ONLY in `LIFEOS/TOOLS/models.ts` (`EFFORT_MODEL`), so a future lineup change never edits doctrine. Semantic effect: execution-class work now downshifts RELATIVE to the session default rather than to a named model — with the default at Opus, execution lands a rung below it instead of on it. Companions (same session): OPERATIONAL_RULES § Model selection rewritten (the "Every Agent dispatch sets model explicitly: Opus unless frontier reasoning" rubric is RETIRED — it was a classifier in prose that `models.ts:41` already forbade, and inheritance now produces the intended carrier by itself); `models.ts` registry bumped `opus: claude-opus-4-8 → claude-opus-5` via `UpdateModels.ts --apply` and stripped of two dead tables (`ALIAS` was an identity map; `LEVEL_TO_HARNESS_EFFORT` had four keys with identical values, now the single `UNIFORM_HARNESS_EFFORT` const); `UpdateModels.ts` scan-excludes widened (`.test.ts` fixtures, `ALGORITHM/changelog.md`, skill `/State/`) so the drift check reports real drift only — it now exits 0 with one honest pinned-but-current landmine; `test/tools/update-models.test.ts` fixtures now DERIVE current IDs from the registry instead of pinning them (the bump broke three tests that hardcoded the ID they were testing — same rot class, one level down); statusline `model-cache.txt` relabelled NON-AUTHORITATIVE (it is last-writer-wins across concurrent sessions; observed reading "Fable 5" while the saved default was Opus); `fallbackModel` de-pinned from `claude-opus-4-8` to the `sonnet` alias. Origin ISA: `MEMORY/WORK/20260724-model-routing-derived-not-pinned/ISA.md`. Migration: `echo 8.8.0 > LATEST` (done); v8.7.4.md kept byte-identical. Rollback: `echo 8.7.4 > LATEST` + `bun LIFEOS/TOOLS/UpdateModels.ts --apply opus claude-opus-4-8`.

- **v8.7.4 changelog (ISA doc-dir path sync, PATCH):** the 2026-07-23 `Isa/` → `ISA/` documentation-dir rename (commit 4f98c1d66) propagated a one-line pointer fix (`LIFEOS/DOCUMENTATION/Isa/IsaFormat.md` → `LIFEOS/DOCUMENTATION/ISA/ISAFormat.md`) into the already-authored v8.7.3.md — a per-version-immutability violation the Algorithm bumper caught as needs-authoring during the 7.10.0 RC. v8.7.4 carries that fix as its own version; v8.7.3.md restored byte-identical to its authored state (666021e38). No doctrine change. Migration: `echo 8.7.4 > LATEST` (done). Rollback: `echo 8.7.3 > LATEST` (the archived v8.7.3 pointer text then points at the pre-rename path, which no longer resolves — cosmetic only in an archive file).

- **v8.7.3 changelog (model ladder widens + Workflow tool joins the menu, PATCH):** shipped 2026-07-22 in the vanilla-harness/BPE-recommendations session (content + LATEST in commit 666021e38; the file rename from v8.7.2.md landed one commit earlier in d2cbade0e). One §Spend paragraph added: the delegate model is the whole quality/cost dial — the ladder has more than two rungs, with Sonnet and Haiku on the menu for mechanical execution, Opus for execution that needs reasoning, Fable reserved for judgment legs; and the Workflow tool joins the capability menu for large elected fan-outs (per-run token budget, schema-forced agent output, pipelining), elected by judgment, never mandated. No other doctrine change. NOTE: this entry was reconstructed 2026-07-23 during the 7.10.0 RC — the 8.7.3 session shipped the v-file and LATEST but never wrote its changelog entry. Also note v8.7.2.md was RENAMED to v8.7.3.md rather than kept: rollback is `git show d2cbade0e^:LIFEOS/ALGORITHM/v8.7.2.md > LIFEOS/ALGORITHM/v8.7.2.md` + `echo 8.7.2 > LATEST`.

- **v8.7.2 changelog (BPE capability-surfacing cleanup — fork-reviewer clause lands in claim 11, PATCH):** principal-approved 2026-07-21 ("Yes, let's do it and get all the documentation updated"), from a two-turn BPE analysis of how capabilities are surfaced to the harness. Finding: capability EXISTENCE is surfaced natively (tool schemas every turn; skill USE WHEN listings; agent rosters; ToolSearch) and must never be restated in LifeOS prose — every hand-maintained capability manual rots (the Delegation skill was teaching `Task()`/`TeamCreate`/`max_turns`/retired effort tiers). Doctrine change (one clause): claim 11 names the forked-subagent case — a fork inherits the build context, counts as the builder, defends its own code; independent second looks run non-forked. This clause is the sole survivor of the deleted Delegation skill (its other keep-content already lived in §Spend fact 1). Companions (same session): `skills/Delegation/` deleted + USE WHEN index rebuilt (132 skills); `DOCUMENTATION/Delegation/DelegationSystem.md` retired with history banner; CLAUDE.md + release templates route it as retired; system prompt v3.2.1 cuts the "consider platform capabilities" line (duplicated doctrine §Spend, paid every turn); `IntegrityCheck.ts` gains the `retired-tokens` check (dead harness-API references in live surfaces = blocking; RETIRED-bannered files exempt); `GenerateReleaseGuide.ts` stale Engineer/Architect/Intern roster replaced with current dispatch surface. Explicitly NOT added: any capability inventory/registry, any new nudge rows — teeth are added only from observed failures. Origin ISA: `MEMORY/WORK/20260721-bpe-capability-cleanup/ISA.md`. Migration: `echo 8.7.2 > LATEST` (done); v8.7.1.md kept byte-identical. Rollback: `echo 8.7.1 > LATEST` + restore skills/Delegation from git history.

- **v8.7.1 changelog (§Spend carrier bullet defers to models.ts, PATCH):** found by the first `/algo` AlgorithmImprovement pass (2026-07-17, `MEMORY/WORK/20260717-algorithm-improvement/REPORT.md`), executed on the principal's "do it". The 8.7.0 §Spend model-execution bullet restated carrier facts as prose ("NO Agent dispatch executes Fable", probes 2026-07-07/07-11) and went stale against the 2026-07-12 CarrierProbe result recorded in `LIFEOS/TOOLS/models.ts` (dispatches execute their requested/inherited model faithfully, Fable included) — exactly the drift class OPERATIONAL_RULES § Model selection warns about ("Never restate the full facts in prose elsewhere"). The bullet now points at models.ts as the single source (+`CarrierProbe.ts --check` freshness gate) and carries only the operative line with its probe date. No other doctrine change; v8.7.0.md kept byte-identical. Migration: `echo 8.7.1 > LATEST` (done). Rollback: `echo 8.7.0 > LATEST`.

- **v8.6.0 changelog (evals join the capability menu — elected, never mandated, FEATURE):** principal-approved 2026-07-16 ("Okay, let's do it"), reinforced mid-build ("this should be yet another capability... just like every other skill... all of this should be on the menu for the algorithm to choose... given complexity, the quality bar, the time we have"). Context: the Evals skill was rebuilt this session into an assertion-first framework aligned to Anthropic's "Demystifying evals for AI agents" (typed deterministic asserts + a forced-structured LLM judge over `{id, prompt, assert[]}` cases, pass^k headline, subscription-billed via Inference.ts). This version wires evals into doctrine as ONE more item on the elect-by-judgment menu alongside skills, agents, thinking tools, and stronger models — governed by the same dynamic-range principle, never a blanket gate. The principal's binding constraint: "we can't make everything require evals — everything will be massively slow." Doctrine changes, all additive: **claim 8** gains `behavioral/quality/regression→eval pass^k` as a verification modality (elected for the multi-sample class, never required); **claim 11**'s visibility floor names an eval suite as one sanctioned second-look for core-surface work whose correctness IS a behavioral/quality property (a system-prompt edit, a skill's routing, a released agent), elected like the cross-vendor audit; **§Spend** gains the menu paragraph (every capability elected the same way against complexity/quality/time; evals earn their place only for the narrow multi-sample class; the default that closes almost every claim is the instant direct probe); **§Events** gains one nudge row (a behavioral/quality/regression claim one probe can't close → is this the multi-sample class? elect an eval scaled to blast-radius, else a direct probe closes it). Companion: `IsaFormat.md` `eval` probe row updated to the assertion-first `EvalRunner` (pass^k ≥ threshold) from the retired rubric form. NOT changed: no mandatory evals anywhere; constitutional verification tier untouched (evals already fit "tool evidence of the right modality"); the direct probe remains the default. Companion build (same session): `skills/Evals` rewritten (Assertions.ts, Judge.ts, EvalRunner.ts; dead AlgorithmBridge.ts + stale PROJECT.md deleted), config-change regression (`hooks/ConfigEvalFire.hook.ts` → `LIFEOS/TOOLS/ConfigEvalOnChange.ts`, proven by a real autonomous fire at pass^k 87%), the discretionary auto-create generator (`GenerateCases.ts`, drafts only), and the {{DA_NAME}}-dispositions suite in the USER customization layer. Origin ISA: `MEMORY/WORK/20260716-eval-system-integration/ISA.md`. Migration: `echo 8.6.0 > LATEST` (done); v8.5.0.md kept byte-identical. Rollback: `echo 8.5.0 > LATEST` + revert the doctrine edits (evals remain a usable skill, just not doctrine-elected).

- **v8.4.4 changelog (claim 8 container-coverage tooth, PATCH):** principal-caught incident INC-20260715-danielsaid-container-sampling: every essay page shipped with visible escaped code while the run closed its verification claim on a homepage pixel, three tab reads, and SPA 200s — and the independent audit shared the blind spot (verified quote DATA inside the broken HTML, never the rendering). Four-link failure chain in the affected site.s ISA Log Log. Doctrine change: claim 8 gains the coverage sentence — when a claim quantifies over a container, the container passing is not evidence for its members; the probe set touches every member TYPE the user consumes (one rendered/executed instance each) plus a deterministic gate over the rest where one exists. Companions: OPERATIONAL_RULES § Deploy & verify content-site rule; Interceptor VerifyDeploy gotcha; canonical gate the site.s own build-time render gate. Note: **v8.4.3.md was reconstructed** in this same commit — the 8.4.3 session shipped its hook (AlgorithmNudge v2.4.0), changelog entry, and LATEST bump but its doctrine file never landed on disk; 8.4.3's §Spend third fact and §Events row were rebuilt verbatim-faithful from its changelog entry. Migration: `echo 8.4.4 > LATEST` (done). Rollback: `echo 8.4.3 > LATEST` (execution-class rule kept) or `8.4.2` (both out).

- **v8.4.3 changelog (execution-class Opus-delegate spend fact + nudge row, PATCH):** principal directive 2026-07-15, mid-incident: a full video-editing pipeline (Descript uploads, Underlord cut jobs, render polling, gate loops) ran inline on the Fable main loop — "update the algorithm system so it would detect that we're doing video editing work. You should not be using Fable... that type of task is not something that requires maximum intelligence." The rule already existed in OPERATIONAL_RULES § Model selection; nothing detected the class or gave it teeth mid-run, and the miss was rationalized as "each call is a short CLI invocation." Changes: §Spend "Two spend facts" → **three** — new fact: execution-class work (media-pipeline driving, browser clickwork, bulk mechanical CLI, research fan-out) is dispatched to Opus `Agent` delegates the moment work reveals itself as execution-shaped; the workflow's class decides, not the granularity of its steps; Fable keeps judgment legs (take-maps, verification reads, synthesis). §Events gains the matching always-on row. Hook v2.4.0: `matchExecClass()` (editing-specific lexicon incl. the literal phrases from the incident prompt, 2500-char paste guard, fence guard, word boundaries, 60-min cooldown, `EXEC-CLASS` telemetry) fires one advisory line on UserPromptSubmit whether or not a run is registered — the miss happened inside a run. Companion: _VIDEO SKILL.md leads its gotchas with the dispatch rule. 32 hook tests green incl. a regression test on the actual incident prompt. Origin ISA: `MEMORY/WORK/20260715-video-redo-7x-and-intro/ISA.md`. Migration: `echo 8.4.3 > LATEST` (done); v8.4.2.md kept byte-identical. Rollback: `echo 8.4.2 > LATEST` + revert the hook commit.

- **v8.4.2 changelog (phase machinery retired — dashboard-driven, PATCH):** principal-approved 2026-07-14 ("go", after the agents-dashboard redesign review + rebuild). §Telemetry's first bullet rewritten: the 8-station byte-stable enum contract is gone — `phase:` is a minimal lifecycle value (an active value at start, `complete` at close, in between only on genuine change; ISASync mirrors it), and Pulse derives lifecycle (scoping/climbing/learning/done) plus the Climb (claims closed over time, folded from work-events.jsonl) from run data instead of rendering declared stations. Rationale: post-8.2.0 frequency evidence showed THINK/PLAN/EXECUTE were never written — the enum's byte-stability was scaffolding sustained solely by the dashboard that consumed it. Companion code (same session, commits 70c9b9249→): agents dashboard rebuilt (Work+Activity tabs, WorkBoard/ClimbChart/lifecycle.ts, 37 dead components deleted); deep strip — phaseHistory pipeline removed from isa-utils/API/types, placeholder rows phase-only, dead tools deleted (`AlgoPhase.ts`, `LIFEOS/TOOLS/algorithm.ts`, `AlgorithmPhaseReport.ts` — zero code callers verified), TheRouter-era `markSessionNative`/`markAlgorithmStarting` exports removed, AlgorithmNudge/tab-setter unaffected (nudge migrated to the ISA-pointer signal in 6eedd776f). OPERATIONAL_RULES AlgoPhase line replaced with the retirement note. Migration: `echo 8.4.2 > LATEST` (done); v8.4.1.md kept byte-identical. Rollback: `echo 8.4.1 > LATEST` + revert the strip commit (AlgoPhase et al. restorable from git history).

- **v8.4.1 changelog (Grok removed from the ecosystem, PATCH):** principal directive 2026-07-13 ("Remove any Grok agents or Grok functionality or models or anything from our ecosystem and update all documentation accordingly."). The xAI/Grok cross-vendor audit voice (Rule 2b lineage) and the `GrokResearcher` agent are gone; Forge (OpenAI GPT-5.6 Sol) is now the sole cross-vendor auditor. Deleted: `agents/GrokResearcher.md`, `agents/GrokResearcherContext.md`, `LIFEOS/TOOLS/Grok.ts`, `LIFEOS/TOOLS/GrokAudit.ts`. Doctrine: claim 11's cross-vendor-audit clause now reads "(Forge)" not "(Forge/Grok)", and the invocation line drops the `GrokAudit.ts` entry. Research skill retuned — Standard is now 3 researchers (Claude + Gemini + Perplexity, was 4), Extensive keeps 7 explorers by reassigning the 2 Grok contrarian slots to Claude+Gemini; `research.mjs` roster matched. Removed from: `models.ts` CROSS_VENDOR, the statusline ACTIVE roster (GROK silver rung), Pulse activity chart colors, all settings sources (`Agent(GrokResearcher)` permission + spinner tips) with settings.json regenerated, `InstallEngine.ts` API-key scan, OSINT/VULNMANAGEMENT/RL-competitive-intel researcher rosters, and the AgentSystem/PulseMetadata/AlgorithmSystem docs. `GROK_API_KEY` deleted from `.env` (revoke upstream at console.x.ai). LEFT as-is: frozen historical Algorithm snapshots (v6.31–v8.4.0) and their changelog entries per the no-history-rewrite convention; archival newsletter/blog content; `ngrok`/`groq`/"grok vi"/"grok the input" (unrelated); the `_HELIOS` bounty-target AI-keyword regex and `_NEWS` competitor-tracking of xAI (external-world awareness, not our functionality); orphaned `MEMORY/VERIFICATION/grok-findings.jsonl` (historical audit log). Migration: `echo 8.4.1 > LATEST` (done); v8.4.0.md kept byte-identical as the frozen snapshot. Rollback: `echo 8.4.0 > LATEST` + `git revert` the removal commit.

- **v8.4.0 changelog (depth-directive nudge — third always-on row, claims 10/15 sharpened, FEATURE):** principal-approved 2026-07-12 ("Okay, let's go ahead and do it. Just keep BPE in mind all the time."), from a live capability-underspend incident the same session: "Think deeply about how you would redesign X" was answered from 4 file reads with zero capability consideration, and nothing could catch it — the turn had no registered run, so claims 10/15 had no teeth, and the routing row matches no depth language. Root cause: a depth directive is a signal the turn IS a run, and nothing treated it as one. Design was RedTeam'd BEFORE build (3 adversarial agents): a Stop-hook blocking gate and every mandated-capability variant were killed (gating on tool-call presence is procedure, not outcome, and Goodharts into token dispatches); only the advisory question row survived. Changes: hook v2.2.0 adds `matchDepthDirective()` (8-phrase lexicon, 200-char head window, fenced/long-paste guards, word boundaries, `depth` cooldown key, `DEPTH:` fires into the routing telemetry) firing ONLY with no active run — mid-run the principal nudge already covers it; doctrine adds the always-on events row, claim 10 names depth/steering directives explicit asks, claim 15 names the silent shallow pass on a directive a surfaced break, and §Events gains the bounding principle (a row may only ask a question about an outcome already in doctrine; phrase→procedure maps and "a tool was invoked" pass conditions are banned by construction). Known-uncovered and stated: undirected under-spend (hard task, no magic words) stays with judgment + reflections. Fresh-context post-ship audit (3 findings, all fixed same session): changelog entry relocated here from the doctrine file per the v6.28.0 lean-file rule; ARCHITECTURE_SUMMARY regenerated; spinner tip version bumped; plus its INFO note promoted to a fix — bare directives ("go deep" as the whole prompt) were eaten by `routeSuppressed`'s 15-char floor, so the depth row gets its own `depthSuppressed` gate (command/tag prefixes only). Origin ISA: `MEMORY/WORK/20260712-depth-directive-nudge/ISA.md`. Migration: `echo 8.4.0 > LATEST` (done). Rollback: `echo 8.3.0 > LATEST` + revert the hook commit.

- **v8.3.0 changelog (AlgorithmNudge — nudge layer unified, two always-on nudges added, FEATURE):** principal-approved 2026-07-11 ("Why wouldn't SkillRouter and ISA Nudge be the same? Could we not unify them? And just call it AlgorithmNudge […] do it"), from the dynamic-range audit (`MEMORY/WORK/20260711-dynamic-range-audit/REPORT.md`). `hooks/IsaNudge.hook.ts` → **`hooks/AlgorithmNudge.hook.ts`** (v2.0.0) — ONE runner, two scopes. **ALWAYS-ON (any session):** *skill-routing* — prompt matches a skill's USE WHEN → "invoke the skill, don't handroll"; deterministic phrase match against a prebuilt index (`MEMORY/STATE/skill-usewhen-index.json`, 129 skills / 1,482 phrases at cut; stale → detached rebuild via `--rebuild-index`, never a synchronous scan on the hot path; noise guards: single words ≥6 chars word-boundary, multiword ≥7 chars, >6 matches = generic = silence, per-skill 60-min cooldown) — motivated by the audit's routing finding (under-use beats over-invocation ~4:1 in 48h traces); *late-ISA* — 25+ tool calls with no registered run → "still trivial, or does done need writing down?" (once per session) — motivated by the observed 2h-uncovered-run. **RUN-SCOPED:** the five existing nudges byte-identical, plus a tier-free **spend** nudge (~75 in-run tool calls with claims open → "escalate, descope, or surface?", 30-min cooldown so it re-fires on long runs) replacing the retired budget-half nudge — closes audit fix #1, where doctrine advertised a nudge the hook had removed. §Events table rewritten: 8 rows with scope markers, runner renamed. Removed vestigials: `tier` parsing, `budgetNudged` state. Nudge prefix `🧭 ISA-NUDGE:` → `🧭 ALGO-NUDGE:`. Registration swapped in settings.system.json (PostToolUseFailure) + settings.user.json (UserPromptSubmit) + PostToolObserver import; MergeSettings applied; first live fire observed same-session. Post-audit hardening (hook v2.1.0, same session): Forge audit (`concerns`, 5 findings) caught a real gate regression — a shared systemish predicate had narrowed the principal nudge — fixed via decoupled `routeSuppressed`/`principalSuppressed` predicates (original byte-equivalent semantics restored, regression-tested with Forge's own counterexamples); plus session_id path-guard, 31-word routing stoplist, parse-floor alignment (index 1,482→1,414 phrases), and routing-fire telemetry (`MEMORY/OBSERVABILITY/algo-nudge-routing.jsonl`) to tune noise floors from real data. Grok cross-vendor pass ($0.0087): `fail` on the known bundle-visibility class; its real catch (stale ISA progress frontmatter) fixed, probe evidence embedded in the ISA Log. 15 unit tests (`hooks/AlgorithmNudge.test.ts`). Capitalization note: the principal flagged `Isa` vs `ISA` — the rename retires the outlier (ISASync/ISARenderOnStop were already correct). Origin ISA: `MEMORY/WORK/20260711-algorithmnudge-unification/ISA.md`. Migration: `echo 8.3.0 > LATEST` (done). Rollback: `echo 8.2.1 > LATEST` + revert the hook/settings commit (IsaNudge restorable from git history).

- **v8.2.1 changelog (model-selection baseline — steering removed, PATCH):** principal directive 2026-07-11: "clean up all this model selection garbage; start from baseline, where you're making all the decisions yourself with no steering." §Spend loses the **Core-surface max-intelligence rule** (the last model-steering mandate in doctrine) and "three spend facts" becomes two; the §Spend intro now states there is no model-routing rubric — model choice is per-dispatch judgment under claim 15's spend check. KEPT as tool-contract facts: the 110k-token inline-reachable gotcha and the Agent(fable)=Opus execution reality (re-dated "at last probe 2026-07-07, re-probe periodically"). Companion changes same session: `AgentInvocation.hook.ts` v1.3.0 (model injector removed — its E4/E5 tier signal died with the 2026-07-11 tier retirement and had silently flattened every dispatch to Opus; lifecycle tracking + dispatch-model logging kept), `models.ts` steering comments rewritten to baseline (EFFORT_MODEL table kept as the `Inference.ts --level` dial), OPERATIONAL_RULES §Default model rewritten to reality (saved default is Fable 5 [1m], not Opus — the doc had gone stale against settings.json). Same session, on the baseline analysis plus a live probe (bare inheritance ALSO executes Opus — the subagent self-reported Fable 5 while its transcript ran claude-opus-4-8 with a `fallback` block), the principal directed the one re-add ("Just fix it"): **the core-surface-max rule returns to §Spend in probed-carrier form** — hard reasoning on core-surface work runs on real Fable (main loop or Inference-max subprocess), never on a downgraded Agent dispatch. Doctrine line only; no hook, classifier, or injection came back. Origin ISA: `MEMORY/WORK/20260711-model-selection-baseline/ISA.md`. Migration: `echo 8.2.1 > LATEST` (done); v8.2.0.md kept byte-identical. Rollback: `echo 8.2.0 > LATEST` + revert the hook/models/rules commits.

- **v8.1.0 changelog (outcome over machinery — modes & tiers removed from doctrine, FEATURE):** the principal's directive — "I'm trying not to define modes and tiers; the desired statement of what I want should be in the Algorithm; it's the outcome that I want, not how to get there; the whole point is euphoric surprise." Structural change: **the `## Mode, tier, budget` section is deleted**, replaced by `## Spend` — pure outcome (spend follows the difficulty and blast radius the work reveals, discovered not predicted; no tier to declare, no mode to pick; aim at euphoric surprise, spend what hitting it takes). Kept from the old section only the three non-tier facts: Core-Surface max-intelligence (safety), the Right-Sizing/110k-token gotcha, and the Agent(fable)=Opus model-execution reality — all detached from the E-ladder. **New `## The point` preamble** states euphoric surprise as the target of every run (exact output the principal wanted, right time, right spend; every capability is a means to it, nothing more). Tier/mode residue swept from claims 2, 6, 11 and the Reflection + cross-vendor telemetry rows (E-gates → plain-language conditions). NOT changed: the 15 done-claims' substance, the ISA contract, verification doctrine, IsaNudge. **This is only the doctrine surface, and the cut is scoped to EXECUTION dictation, not presentation** — the output-format banners/templates (NATIVE/ALGORITHM/MINIMAL as the response shape the principal reads) STAY as an output-format-contract keep-class; what still needs removing is TheRouter's MODE/TIER *prediction* of execution effort and the E1–E5 effort ladder, wherever they dictate how the work happens rather than what the reply looks like (staged every-turn removal that follows). Also folds the prior same-session v8.0.1 carve-outs: **claim 11** visibility floor (core/auth/publish skip of every independent second look needs a Log row — 5 lost teeth in the replay had caught 5 security holes, a mirror-drift, 6 design failure modes); **claim 2** completeness-skill now for substantial/persistent builds, trivial tasks direct-write; **claim 1** `null` goal-escape restored. Origins: `MEMORY/WORK/20260711-bpe-p0-execution` + `20260711-bpe-continuation-analysis`; replay `.../replay-report.md` (19 held / 5 lost). Migration: `git mv v8.0.1.md v8.1.0.md` + `echo 8.1.0 > LATEST` (done); v8.0.0.md kept byte-identical as rollback floor. Rollback: `echo 8.0.0 > LATEST`.

- **v7.0.0 changelog (the Run Contract — procedure → outcome contract, MAJOR):** the 2026-07-10 BPE audit (`MEMORY/WORK/20260710-algorithm-bpe-analysis/REPORT.md`; three parallel audits + Advisor + Forge + Grok; 1,117-run usage corpus) split kernel from shell and v7 restructured the doctrine around it. **Kept (the kernel, verbatim semantics):** Verification Doctrine Rules 1–4 with all three dated modality incidents and hook teeth; inline-verification mandate + forbidden phrases; Reproduce-First; Prerequisite Manifest with MISSING/UNKNOWN semantics; Class-Sweep; Advisor/Forge/Grok contracts with builder≠auditor, conflict cap, earn-the-slot; ISA fourteen-section format contract + ISC quality system (Splitting/Variation/granularity, anti-criteria, property preference, DEFERRED-VERIFY, ID-stability); principal-goal verbatim preservation + revision protocol + surface-vs-intent; checkpoints; Learning Router; doc-sync; phase telemetry contract (7-state enum byte-stable for Pulse); Core-System Override; per-Feature intelligence routing; Right-Sizing gate (relocated in from capabilities.md); Resume-After-Complete (pointer); Context Recovery; classifier contract + overrides + fail-safe. **Cut (the shell, none keep-class — parser-check verified zero hooks reference them):** mandated OUTPUT choreography (reverse-engineer, riskiest/premortem, planning line, parallelism scan, LEARN's 4 questions, capability-selection ritual), the 7-signal density formula + separate Sufficiency Check + PLAN-refresh flag mechanics (replaced by R3: one ambiguity rule, ≤3 questions / flag, `proceed` override — the behavior that fired at a healthy 17%), euphoric-surprise scoring (the insight-naming survives for experiential goals inside doctrine prose), HVA counted outputs + isa_level_audit + sentiment self-scores (99.4%-constant = performed; reflection schema v7 is operational-fields-only), the four-signal goal-detector rubric (verbatim preservation stays; recognition is the model's job), the Rules recap + duplicate final-format sections, FeedbackMemoryConsult (path had no files; LoadContext.hook.ts already injects Learning Context), the in-doctrine capability menu (the system prompt skill list is THE inventory — duplicates provably rot). **Merged:** Frame-Drift T1/T2/T3 + Re-Read Check + Deliverable Compliance + Fluff Sweep → R8 ask-fidelity gate (asks / scope-subset / criteria-integrity, one emission to `ask-fidelity.jsonl`). **Moved:** ~10KB model-routing rationale → `models.ts` (already its declared single binding point); classifier rubric → TheRouter; Resume SHA mechanics → hook docs. **New structure:** tiers are pure budget declarations (the "go heavy"/eN dial scales delegate rungs, cross-vendor teeth, parallelism, verification depth; zero thinking floors); the Run Contract R1–R12 with Enforce (HOOK/CHECK/FIELD, honestly labeled) + Order (happens-before survives from the phase era) columns; SUMMARY block drops the 🧬/🔬 self-score fields (OutputFormatGate verified to not require them). **Companions shipped same-day:** capabilities.md DELETED (~83% stale/duplicate; keep-class content folded into v7); ISA skill de-staled (count floors deleted, twelve→fourteen, density machinery → R3, bureaucracy severity rows removed, anti-criteria hard-fail at E2+); Philosophy.md self-exemption clause rewritten honest; TheRouter explicit-override regex guarded (standalone token + non-principal-message guard — the "(E1/E2)" live misfire); **Ascent** named + documented (`DOCUMENTATION/Ascent/AscentSystem.md`) as the unified thinking system; `<your-release-skill>` gains the AscentAudit efficiency workflow. Size: 72.7KB + 15.1KB → 27.5KB always-loaded (−69%). Cutover gated on a scoped 3-task replay eval (Forge precondition). Origin ISA: `MEMORY/WORK/20260710-algorithm-v7-run-contract/ISA.md`. Migration: `echo 7.0.0 > LATEST` + `git rm LIFEOS/ALGORITHM/capabilities.md` + doc version refs swept. Rollback: `echo 6.31.1 > LATEST` + `git checkout LIFEOS/ALGORITHM/capabilities.md` (one snapshot commit back: bea05b614).

- **v6.31.1 changelog (style-only BPE pass — zero mechanism change):** the core-system ideal-state audit (`MEMORY/WORK/20260709-core-system-bpe-audit`) executed on the doctrine. Four wording edits: `## Doctrine — Read This First, Internalize It` → `## Doctrine` (pep-talk header dropped; position already signals precedence); "the speed range must feel dramatic" → "the speed range is wide by design" (Tier intent stated as fact); the Core-loop recap sentence collapsed into the Goal line (each of its claims already stated once earlier in the Doctrine section); and `capabilities.md`'s Output Format line "use as many appropriate Capabilities as possible given the amount of time you have" → "select the capabilities the task needs within the time budget" — the old wording was a quantity nudge contradicting the v6.25.0 menu-not-floor rule. Every gate, floor, threshold, phase, OUTPUT line, banner, tier table, and Verification-Doctrine rule is byte-identical to v6.31.0 (token-grep verified). Origin ISA: `MEMORY/WORK/20260709-core-system-bpe-audit/ISA.md`. Migration: `echo 6.31.1 > LATEST` (done) + doc version refs swept. Rollback: `echo 6.31.0 > LATEST`.

- **v6.31.0 changelog (Grok audit voice — Rule 2b, third cross-vendor distribution):** the E4/E5 VERIFY sequence gains a second cross-vendor auditor: **xAI grok-4.5** via `LIFEOS/TOOLS/GrokAudit.ts` — one stateless chat call to the OpenAI-compatible `api.x.ai/v1` endpoint, read-only BY CONSTRUCTION (no tool loop, no actuator), same verdict schema as the Forge audit, findings logged to `MEMORY/VERIFICATION/grok-findings.jsonl` under the same earn-the-slot bar (≥3 unique findings per 10 runs or cut). Key doctrine point: on **Forge-BUILT slugs**, where Rule 2a is skipped by the builder≠auditor invariant, the Grok voice is the SOLE cross-vendor pass — Forge-built work previously shipped with no cross-vendor audit at all. Known tradeoff stated in-rule: the API voice audits only what the bundle carries; it cannot explore the repo like Forge's sandboxed CLI. Paired changes: `Grok.ts` default model bumped `grok-4.3 → grok-4.5` (X-search and web-search legs both live-verified on 4.5 first); xAI data terms verified equivalent to the accepted OpenAI baseline (no training on API traffic by default, 30-day retention — docs.x.ai security FAQ, fetched 2026-07-09). Deliberately NOT done: no Grok build agent (Grok Build CLI runs `grok-build-0.1`, not 4.5, and lacks a documented read-only sandbox flag; a Codex-custom-provider path to xAI exists but is a choice deferred — fewer agents, per the live BPE-simplification direction), no new agent file (the voice is a tool + doctrine rule, not a persona), no `EFFORT_MODEL` change (Claude-family only). First live run caught 5 real findings on its own integration ISA ($0.0103). Origin ISA: `MEMORY/WORK/20260708-grok-45-harness-integration/ISA.md`. Migration: `echo 6.31.0 > LATEST` (done) + doc version refs swept. Rollback: `echo 6.30.0 > LATEST`; GrokAudit.ts is inert unless invoked. No gate, floor, threshold, phase count, or tier table Level column changed — one Verification-Doctrine rule added (2b), VERIFY sequence renumbered 7→8 for the hardening gate.

- **v6.30.0 changelog (reconciliation bump — no doctrine rule changed):** a bookkeeping version so `LATEST` honestly reflects that the 6.29.0 doctrine was edited in place after it was authored. Three prune/rename edits landed on `v6.29.0.md` + `capabilities.md` after authoring (ISA-hierarchy narrative relocated to an on-demand reference, the Cato→Forge doctrine-prose rename as a class-sweep, a P5 retirement note) and shipped folded under 6.29.0 rather than a new version — the same tagged-file-mutation pattern the v6.28.0 entry called out. Fixed forward, per that entry's convention: `v6.29.0.md` is frozen as-is (its content is history); `v6.30.0.md` is authored as a byte-for-byte copy of the current doctrine with only the H1 bumped; `LATEST → 6.30.0`. The doctrine content is unchanged from what was already live — no gate, floor, threshold, phase, sequence, tier table, or Verification-Doctrine Rule changed. This entry exists so the version line stops lying about where doctrine actually sits. Cut as part of the 6.8.0 release-candidate `/rc` run.

- **v6.29.0 changelog (Fable via verified subprocess — makes "E4/E5 uses Fable" true and provable):** doctrine now states the honest routing reality plus a verification mechanism that ships with it. THREE facts made load-bearing in a new subsection "Fable via verified subprocess" (Tier→Delegation, above Per-Task Intelligence Routing): (1) genuine `max`/Fable is reached ONLY by SUBPROCESS — `Inference.ts --level max` / the Advisor spawn `claude --model claude-fable-5` and execute real Fable (live-proven via `modelUsage`: key `claude-fable-5`, `downgraded:false`, cost matches Fable pricing); the main loop reaches Fable only via a deliberate `/model fable` and never self-switches (Anthropic feature request #47275 open). (2) `Agent(model:fable)` dispatch downgrades to Opus and is NOT a Fable carrier, so E4/E5 Fable REASONING routes through the Inference-max subprocess. (3) executed-model verification is mandatory: `Inference.ts` reads the executed model back from the JSON envelope's `modelUsage` (`verifyExecutedModel` — keys on FAMILY-PRESENCE, not token counts, because Claude Code fires a background haiku pass every turn that can outweigh a short answer on tokens) and logs downgrades to `MEMORY/OBSERVABILITY/model-verification.jsonl`; the `🤖 DISPATCH … → model` line shows the VERIFIED executed model for subprocess Fable. Paired code (shipped + tested, `29ee7f5e7`): `verifyExecutedModel()` + `executedModel`/`modelDowngraded` on `InferenceResult` + CLI `[model]` stderr line + `logModelVerification()`; 6/6 unit tests (`Inference.verification.test.ts`); `models.ts` MAIN-LOOP caveat rewritten to the verified carrier reality. C/R/L: conjectured: E4/E5 dispatching `model: fable` meant E4/E5 ran on Fable. refuted_by: live `modelUsage` read-back — Agent dispatch executes Opus, only the subprocess reaches Fable, and nothing verified which model actually ran (the system claimed Fable while running Opus). learned: "used model X" is a claim that must be PROVEN from `modelUsage`, and the only carrier reaching Fable without a manual `/model` is the subprocess. criterion_now: Fable via subprocess, executed-model verified against `modelUsage`, downgrades logged and surfaced. Origin ISA: `MEMORY/WORK/20260707-fable-subprocess-verified-routing/ISA.md`. Migration: `echo 6.29.0 > LATEST` (done) + update `CLAUDE.md`/`ARCHITECTURE_SUMMARY` version refs. Rollback: `echo 6.28.0 > LATEST` + revert the `Inference.ts`/`models.ts` verification commit. No gate, floor, threshold, phase, sequence, or Verification-Doctrine Rule changed — one doctrine subsection added + one code capability.

- **v6.28.0 changelog (doctrine relocated to a lean file — corrects a tagged-file mutation):** the embedded changelog stack was moved out of the doctrine `v*.md` into this file (v6.20–v6.27 entries, above). The first pass (shipped in 6.3.1) wrongly edited the ALREADY-TAGGED `v6.27.0.md` in place, giving one version string two byte-states. Fixed here: `v6.27.0.md` restored to its v6.3.0-tagged content (frozen, immutable, keeps its stack as history); the lean doctrine is authored as `v6.28.0.md` (pointer to this changelog, no embedded stack); `LATEST → 6.28.0`. Going forward every version file is lean and its changelog entry lives here. No doctrine rule changed.

## Recovered from embedded doctrine stack (v6.20.0–v6.27.0)

These entries were embedded at the top of the doctrine `v*.md` file (convention drift, 2026-06→07). Relocated here per the standing rule above; the doctrine file is now lean again.

- **v6.27.0 changelog (concise-by-default — principal directive 2026-07-06):** the SUMMARY-block `📃 CONTENT` line dropped its "Up to 128 lines" framing (a ceiling that read as a target and drove verbose output) for "as short as fully answers; 128 lines is a hard ceiling, NEVER a target." Part of a system-wide response-format fix: the constitutional Output Format section and both NATIVE/MINIMAL templates in `LIFEOS_SYSTEM_PROMPT.md` now lead with "concise by default — give the main answer and stop," and a new Format Rule ("length is the answer, not a ceiling") makes short-by-default the loud default. Wording only; no gate, phase, sequence, or mechanism changed.


- **v6.26.0 changelog (reasoning effort flattened to uniform `high` — principal directive 2026-07-06):** the tier table's **Effort** column, previously `low/medium/high/xhigh/xhigh` across E1–E5, is now **`high` for every tier**. LifeOS runs all work at `high` reasoning effort and emits no other level — no `low`, `medium`, `xhigh`, or `max`. Motivation: `xhigh`/`max` make the Claude-5 and GPT-5 families over-reason per step, producing worse, over-changed output at higher cost ("high is the sweet spot"; corroborated by the 2026-07-06 Fable-5 workflow harvest). The **model-rung Level column is unchanged** — E1–E3 `high`→Opus, E4/E5 `max`→Fable — so the intelligence ladder and its dynamic range stay; only the reasoning-effort dial is pinned. Paired system changes (same directive): `models.ts` `LEVEL_TO_HARNESS_EFFORT` all→`high` (the single SoT), `Inference.ts` per-level effort→`high`, the statusline **effort↔tier nudge removed** (it no longer targets/pushes toward `xhigh`; the full scale stays visible as display-only reference), `capabilities.md` mirrored, and the cross-vendor **CodexResearcher** agent default dropped `xhigh`→`high`. A human `/effort xhigh`/`max` still works in the harness; no LifeOS level dispatches it. No gate, floor, threshold, phase, sequence, or the Verification Doctrine changed — one column pinned to a constant.


- **v6.25.0 changelog (ISA scale + a real deletion — from the 2026-07-05 ISA-evolution conversation):** the biggest structural change since the twelve-section body. Five moves, in two buckets.
**Deletions (BPE — the bitter lesson applied to our own process).** (1) The **hard numeric ISC floors are GONE** — E2 ≥16 / E3 ≥32 / E4 ≥128 / E5 ≥256 were quota anchors that bred decomposition theater (split-to-hit-the-number). Replaced by the **Coverage Gate**: every subsystem named in Vision/Goal must have a container ISC, and every container decomposes until its leaves are single-probe testable (the Splitting Test). Coverage is checkable; a count is gameable. The floors' own history indicts them — killed v5.0.0, re-added v5.2.0, made HARD v6.1.0, deleted now. (2) The **numeric thinking floor is GONE** and the closed capability list is **demoted from an enforced enumeration to a menu**. Inventing a reasoning approach is no longer a "phantom/CRITICAL FAILURE." The ONE surviving rule is the anti-lying half: **name a capability → invoke it via tool** (the Capability-Name Audit now checks only that, not list-membership or count). Cutting the cognition cage is the single most bitter-lesson-aligned move in this version; the honesty check is cheap and stays.
**Additions (cross-ISA scale — what takes us from medium apps to Tolkien/enterprise).** (3) **ISA hierarchy** — a `## Dependencies` section + `parent:`/`children:` frontmatter, constraint inheritance (a child ISA cannot violate a parent Constraint), and roll-up progress. (4) **Bridge criteria** — a `## Bridge Criteria` section holding cross-ISA integration ISCs (e.g. "Psionic willpower ≤ Magic budget"), verified at VERIFY like any other ISC but across the seam, anchored `cross: <slug>`. (5) **Blast-radius + re-tier** — when an ISA with parents/children changes, OBSERVE loads its declared dependency ISAs and a blast-radius pass lists downstream ISCs to re-verify before BUILD; and a post-scaffold **RE-TIER checkpoint** re-derives effort from the built ISA's shape (leaf-ISC count, cross-ISA depth, novelty), keeping E1–E5 as labels for what the derivation produced. Detection is automated; cross-ISA conflict *resolution* stays human (governance, not AI). ISA-format mechanics land in IsaFormat.md (spec bump); the harness executor (`isa run`) is the paired build, tracked separately. No change to the seven phases, the SUMMARY block, time budgets, or the Verification Doctrine (Rules 1/2/2a/3/4 all stand).


- **v6.24.0 changelog (additive — motion/interaction evidence, one clause):** Verification Doctrine **Rule 1** gains a **Motion/interaction fidelity** clause: an ISC whose subject is *motion* (animation, transition, drag, hover-state, scroll, loading/skeleton state, or a multi-step flow) closes ONLY on a **ScrubFlow** frame-scrub gallery (dense frames + SSIM anomaly flag) or a VerifyDeploy ordered flow-gallery — never a single still, because one screenshot cannot capture motion. Keyword-anchored trigger so it doesn't tax every web task; static-render ISCs still close on one screenshot. Ships with two new Interceptor surfaces (`skills/Interceptor/Tools/FrameScrub.ts` survey+scrub modes, `skills/Interceptor/Workflows/ScrubFlow.md`) and a `SuccessClaimGate.hook.ts` `INTERCEPTOR_EVIDENCE` extension that accepts the frame-scrub manifest (keyed on the manifest the model viewed, NOT a bare video path). Distilled from the Krieger "verification-pilled" interview; frame-extraction+SSIM half proven live, in-extension recording deferred to a documented next increment. No gate, floor, threshold, tier table, or sequence changed — one Rule 1 clause added.


- **v6.23.0 changelog (retired the Claude-persona agents + corrected the override claim):** The static persona agents **Engineer, Architect, Designer** are retired (deleted from `agents/`) — generic costumes a capable model + a job-specific brief on `general-purpose` beats (BPE), and the Agents skill already prescribed composing on `general-purpose` over the static types. Every live reference (settings permissions, Delegation/Agents docs, Council members, the system-prompt dispatch example, this doctrine) was rewired to `general-purpose`+brief or `ComposeAgent`. **Kept:** cross-vendor agents (Forge, the 5 researchers), ClaudeResearcher. (**Silas** and the **Gene** persona were retired 2026-07-02: Silas was a Claude-only offensive-security costume `_HELIOS` now drives via `general-purpose`+brief; Gene was a `model: sonnet` wrapper over the GLM/`OpenRouter.ts` route, which stays as a tool + `EgressClassGuard` hook.) This also **corrects a false claim in v6.22.0**: the dispatch `model` param does NOT universally override frontmatter — an agent with an `initialPrompt` field (the retired Engineer) deterministically ignored it (ran Opus 2/2 vs `@fable`), while `general-purpose` and frontmatter-opus `Architect` honored it. And the `🤖 DISPATCH` line records INTENT, not execution (a Forge dispatch logged `gpt-5.5` ran Opus) — the only proof is the transcript's `.message.model`. No gate/floor/threshold/phase changed.


- **v6.22.0 changelog (lineup flip + classifier re-pin — Fable is the top rung):** `EFFORT_MODEL.max` flips **opus → fable** (`LIFEOS/TOOLS/models.ts`), executing the long-anticipated "flip when Fable returns" now that Fable is reachable again (probed live 2026-07-01: `claude --model fable` → clean completion). The routing ladder is real again: **max=Fable** (Algorithm E4/E5 + every Core-System Override task) → **high=Opus** (E1–E3 + NATIVE) → **medium=Sonnet** (utility + down-routed Features) → **low=Haiku**; Per-Task Intelligence Routing (v6.18.0) still carries any single Feature down to Sonnet inside an E5 run. **No Fable agent** — Fable is a model rung reached by the dispatch-time `model` param, not a persona; the four-level abstraction was built so adding it is a one-line change. Ships with two guardrails: **(a)** the TheRouter classifier is **re-pinned from `max` to `high`** (`hooks/TheRouter.hook.ts`) — it fires on every prompt, so the per-prompt keystone stays on cheap/fast Opus; its model is UNCHANGED (Opus before via max=opus, Opus now via high). "Keystone" means highest-leverage, never most-expensive rung. **(b)** the **Advisor stays at `max`** (now Fable) for the once-per-deliverable commitment call. `max` and `high` are now distinct models, so the `Inference.ts` max→high fallback (fable→opus) is a genuinely real degrade. **Supersedes the 2026-06-30 "Opus stays top" decision**, which held only while Fable was unavailable. No gate, floor, threshold, phase, or tier-table Level column changed — E4/E5 still dispatch `max`; `max` simply resolves to Fable now.


- **v6.21.0 changelog (additive — prerequisite manifest):** the THINK `☑️ PREREQUISITES CHECK` label is sharpened into a deterministic, blocking **Prerequisite Manifest** with a mandatory `🔑 PREREQUISITES` output line; OBSERVE Gate B now seeds it. Any MISSING external prerequisite (auth token, CLI login, service config, deploy target, required secret) blocks EXECUTE until reachable or deferred in `## Decisions`. Answers the reflection log's most-cited failure (134 occurrences: dead token / un-authed CLI / missing key discovered mid-EXECUTE). **No new hook, tool, gate, floor, threshold, or sequence** — one existing gate sharpened, per a BitterPillEngineering audit that cut the generic probe examples and kept only the non-obvious Cloudflare scope-match gotcha.


- **v6.20.0 changelog (clarification — ultracode is not an effort level):** the tier table's **Effort** column previously put `ultracode` in E5's cell, conflating the composition axis with the reasoning-effort axis. **ultracode is a composition layer** (fan into a `Workflow`), so it moves out of the Effort column into the E5 dispatch profile. **E5 effort stays `xhigh`** — the dispatch cap is unchanged and no LifeOS dispatch emits `max` reasoning. (An interim edit set E5 effort to `max`; it contradicted `LEVEL_TO_HARNESS_EFFORT`'s xhigh cap and was reverted after a cross-vendor BPE audit, 2026-06-29.) No model-rung change (E1–E3 `high`, E4/E5 `max` stand, per the 2026-06-10 directive); statusline untouched. Same-day BPE audit (E4 + cross-vendor Forge) then **trimmed Per-Task Intelligence Routing from six bullets to two paragraphs** — kept producer-lock + shadow-log (the two pieces that stop real under-load failures), cut the rubric/tie-break/fail-expensive/scales/display prose a capable orchestrator doesn't need — **merged the thrice-stated main-loop-immutability rule to one statement**, and removed the embedded v6.15.0 historical note (its policy is already in the table + this block). ~400 words out, zero mechanisms lost.


## Index (one-line headlines)

### Current

- **v6.31.0** (2026-07-09) — **Grok audit voice (Rule 2b).** xAI grok-4.5 joins E4/E5 VERIFY as a second cross-vendor auditor via stateless API call (`GrokAudit.ts`) — third training distribution, read-only by construction, sole cross-vendor voice on Forge-built slugs. Grok.ts search default bumped to grok-4.5. Full entry above.

- **v6.29.0** (2026-07-07) — **Fable via verified subprocess.** Genuine `max`/Fable is reached only by subprocess (`Inference.ts --level max` / Advisor); `Agent(model:fable)` downgrades to Opus; executed-model verification (`verifyExecutedModel` reading `modelUsage`) is now mandatory and logged. The system reports what RAN, not what it requested. Full entry above.

- **v6.19.0** (2026-06-17) — **Agent consolidation: Forge + Cato → one cross-vendor agent.** Forge and Cato shared an engine (GPT-5.5 via `codex exec`) and differed only by sandbox flag + prompt; merged into one `Forge` agent with two prompt-set modes: `MODE: build` (workspace-write, `ForgeProgress.ts`, produces code) and `MODE: audit` (read-only, `CrossVendorAudit.ts`, the E4/E5 cross-vendor verification pass, formerly Cato). Rule 2a now spawns `Forge` in `MODE: audit`; added the **builder ≠ auditor invariant** (Forge never audits a slug Forge built — same-vendor self-review has no cross-vendor value, so the audit is skipped and the Claude Advisor covers the Claude side). `agents/Cato.md` deleted; `Agent(Cato)` removed from settings(.system).json; `cato` dropped from `CROSS_VENDOR` in models.ts (now `forge` covers both modes). Audit mode adopts new codex 0.137+ capabilities: `--output-schema` (schema-enforced verdict JSON), `--ephemeral` (no session litter on read-only passes), `codex doctor` (preflight), `codex exec review --base/--commit` (diff-targeted review for code ISAs). C/R/L: conjectured: cross-vendor build and cross-vendor audit needed two separate agents. refuted_by: both are thin launchers over the same `codex exec` engine, differing only in `--sandbox` + prompt — the separation cost maintenance and bought nothing. learned: the load-bearing separation is builder≠auditor *per task*, not two agent definitions. criterion_now: one agent, mode set by spawn prompt, invariant enforced in Rule 2a. Audit *function* unchanged (44 runs / 231 findings / 36 advisor-disagreements track record preserved). Origin: {{PRINCIPAL_NAME}} directive 2026-06-17 ("unify into a single badass"). Migration: `echo 6.19.0 > LATEST` (done). Rollback: `echo 6.18.0 > LATEST`, restore `agents/Cato.md`, re-add `Agent(Cato)` + `cato` pin. Deferred (descriptive tail, non-breaking): CatoContext.md→ForgeContext.md fold via CreateSkill; spinner tips via /ut; capabilities.md + DOCUMENTATION/* + Mason/Sage persona cross-refs; CrossVendorAudit.ts internal rename + the codex-feature wiring (documented, not yet coded).
  - **Patch 2026-06-29 (no version bump — zero behavior change): the three level axes named.** Audit found the level vocabularies tangled across files: `models.ts` named only the four-rung `EFFORT_MODEL` and never the harness reasoning-effort or composition axes; the model-rung→effort crossover (`max`→`xhigh`) lived buried as a literal in `Inference.ts`; the tier table's `Effort` and `Level` columns shared words without being labeled as different dials. Fix: added the **THREE LEVEL AXES** doc block + exported `HarnessEffort` type and `LEVEL_TO_HARNESS_EFFORT` map to `models.ts` (single source of truth for the crossover); `Inference.ts LEVEL_CONFIG` now consumes that map instead of hardcoding `effort`; the v6.19.0 tier table gained an axis-labeling note (Effort = reasoning, Level = model rung, ultracode = orthogonal composition). The statusline (`LIFEOS_StatusLine.sh`) already encoded this correctly (LEVEL from `effort.level`, XHIGH→ULT via output-style) — the fix lifts its correct model into the core files. tsc clean, runtime smoke green. One open lever left to {{PRINCIPAL_NAME}}: LifeOS dispatch caps reasoning at `xhigh`; harness effort `max` is reachable only via human `/effort max`. Out of scope (flagged): `LifeosSystemArchitecture.md`/`ARCHITECTURE_SUMMARY.md` still say "currently v6.18.0" in the Algorithm pipeline row.
- **v6.18.0** (2026-06-17) — **Per-Task Intelligence Routing.** Features carry an optional `intelligence` level (`low|medium|high|max`) the BUILD dispatch reads, so a mixed-difficulty ISA no longer flattens every delegated Feature to its tier's level. Down-route-only, fail-expensive on unassigned, producer-locked, scored to the Feature's hardest ISC, shadow-logged to `MEMORY/OBSERVABILITY/intelligence-routing.jsonl`, scales E1–E5 (tier curve is the per-tier default). Also fixes the dead `Inference.ts` max→fallback — it resolved to the same model under `max==high==opus`, so a max failure re-hit the failing model (the bug that timed out the advisor twice); it now degrades to the first *distinct* lower rung. Point fixes: `IntegrityMaintenance.ts` + Telos chat route `low→medium` (under-powered). **The `max→fable` lineup flip is HELD** pending the fable/opus economics decision — `EFFORT_MODEL.max` stays `opus`. C/R/L: conjectured: one level per tier was the right dispatch granularity. refuted_by: an E4 run flattens a doc-edit and a concurrency redesign to the same max model — ~70–80% rung-inflated. learned: the unit of intelligence allocation is the task (Feature), not the run, and the existing Features table is already the dispatch unit. criterion_now: a Feature carries an explicit level; the dispatcher reads it; the tier curve is the fallback. Origin ISA: `MEMORY/WORK/20260616-task-intelligence-mapping-review/ISA.md`. Migration: `echo 6.18.0 > LATEST` (done). Rollback: `echo 6.17.0 > LATEST` + revert the `Inference.ts` fallback block.
- **v6.15.0** (2026-06-10) — **Four-Level Effort-Model Abstraction.** All Claude-family model routing now speaks four effort LEVELS — `max`/`high`/`medium`/`low` — bound to the lineup in exactly one place: `EFFORT_MODEL` in `LIFEOS/TOOLS/models.ts` (max→fable, high→opus, medium→sonnet, low→haiku). Two-layer design: a lineup change (model enters/exits the subscription) is a one-line `EFFORT_MODEL` edit; a version release is a one-line `CURRENT` edit. `Inference.ts` migrated from three levels (fast/standard/smart) to the same four, with legacy aliases (fast→low, standard→medium, smart→max) initially accepted as a migration shim, then REMOVED the same day per principal directive ("get rid of the old names", 2026-06-10) — unknown names now hard-error; the Arbol `LLMOptions.tier` vocabulary folded into the same four names (types.v2.ts, runner.v2.ts tierMap deleted, A_EXAMPLE_SUMMARIZE migrated), resolving the Cato flag. **Routing policy per {{PRINCIPAL_NAME}}'s directive 2026-06-10:** NATIVE delegated work → `high`; Algorithm E1–E3 dispatch → `high`; E4/E5 → `max`. **POLICY CHANGE: E3 delegates demote fable→opus** (E1/E2 and E4/E5 unchanged). Keystone-utility exception: TheRouter classifier + Advisor pinned `max` at every tier (pending explicit ratification — Cato flag). Core-System Override restated: LifeOS-core tasks dispatch delegates at `max` regardless of tier. Sage/Mason reframed as "MAX personas"; `UpdateModels.ts --check` gained `checkAgentFrontmatter()` validating MAX-persona pins against `EFFORT_MODEL` (+ fable accepted in `--apply`). 34 consumer files migrated; Evals graders' `opus→smart` map was silently running Fable — fixed to `opus→high` (NOTE: eval-score baselines invalidated across 2026-06-10). C/R/L: conjectured: three levels expressed all caller intent. refuted_by: graders asking for Opus got Fable — under-specification fails upward, invisibly. learned: a level vocabulary one rung too coarse makes intent inexpressible. criterion_now: four levels, each caller states exact intent, mapping owns the binding. Origin ISAs: `MEMORY/WORK/20260610-effort-routing-four-level-abstraction/ISA.md` + `20260610-four-level-doc-propagation/ISA.md`. Migration: `echo 6.15.0 > LATEST` (done). Rollback: `echo 6.14.0 > LATEST` + revert `EFFORT_MODEL` consumers (Inference.ts level names self-degrade via aliases). Known gap: v6.13.0/v6.14.0 never received changelog entries (pre-existing debt, flagged 2026-06-10).

- **v6.12.0** (2026-05-30) — **Standalone doctrine + recovered core.** Two problems fixed at once. (1) **Format:** from v6.6.0 onward each version file became a backward-chaining delta — v6.11.0 pointed to v6.9.0, which pointed to v6.8.0/v6.7.0/v6.6.0 for content marked "preserved unchanged" — so reading the doctrine meant merging 5+ files, and the delta/migration prose leaked into the run-time read. v6.12.0 is the first fully **standalone** version: read top-to-bottom with zero cross-references, zero "preserved unchanged"/"NEW vX"/"delta on" language. Change history lives ONLY here in changelog.md. Convention going forward: every version is authored standalone; historical delta files stay as archived snapshots. (2) **Recovery:** the delta chain had silently DROPPED core doctrine at the v5.6.0→v6.0.0 cut and thinned the top-of-file exposition across the 6.x deltas. v6.12.0 restores from v5.6.0: the full Deutsch hard-to-variability doctrine (master loop, testability=hard-to-variability, opacity→transparency, ISA-as-unit Coverage/Tightness/Uniqueness, euphoric-surprise-as-hard-to-variability-from-outside), the **🧬 ISA-Level Hard-to-Vary Audit (ILA)** and **🔬 Per-ISC Hard-to-Vary Audit** (both gone since v6.0.0), **📚 Feedback Memory Auto-Consult**, and the **🧹 Fluff Sweep** at VERIFY. All 6.x features retained and inlined with full text: twelve-section ISA + ISA Skill (v6.2.0), closed-enumeration thinking capabilities + Capability-Name Audit (v6.3.0), goal semantics/`principal_stated_goal`/anchoring (v6.4.0), density gate (v6.5.0), literal surfacing + calibrated reflection (v6.6.0), Context Sufficiency density+sufficiency+PLAN-refresh+NATIVE flag (v6.7.0), Frame-Drift Check (v6.8.0), Resume After Complete + realtime phase tracking (v6.9.0), Tier→Delegation Effort (v6.11.0). SUMMARY block restores the `🧬 ISA` + `🔬 HVA` lines. 762 lines vs v6.11.0's 115. Origin: {{PRINCIPAL_NAME}} directive 2026-05-30 ("get our core doctrine and everything added back, along with all the new stuff that's still supposed to be in the later versions, and make this 6.12.0"). Migration: `echo 6.12.0 > LATEST` (done). Rollback: `echo 6.11.0 > LATEST`. Deferred: update `LifeosSystemArchitecture.md` + `ARCHITECTURE_SUMMARY.md` version strings (currently say v6.11.0) and the stale `capabilities.md`/`Pipeline Topology` "v6.5.0" reference, on the next doc-sync pass.

- **v6.11.0** (2026-05-28) — **Tier-Scaled Delegation Effort.** The Algorithm tier was a pure text signal that never reached a real effort dial; Claude Code's native `/effort` (low/medium/high/xhigh/max/ultracode) sets model reasoning but a hook cannot set it per turn (confirmed against Claude Code hooks docs via claude-code-guide). v6.11.0 scales effort on the layer where it IS programmable — delegation. New Tier → Delegation Effort policy (E1 low → E4 xhigh → E5 ultracode-by-composition: Workflow orchestration + xhigh agents, reproducing `/effort ultracode` = xhigh + workflows), consumed at the PLAN-phase Delegation Gate with a `🎚️ DELEGATION EFFORT` output line. Mirror table in `capabilities.md`. Additive over capability floors and Forge/Anvil/Cato tier bindings — sets each agent's effort *level*, never overrides count or which agent. Model-policed at PLAN; code-enforcement deferred (no central agent-spawn path to hook). Next-turn main-loop `settings.json effortLevel` ramp documented but NOT built — gated on an unverified mid-session re-read probe (would ship as v6.12.0). Delta on the active base v6.9.0; the v6.10.0 property-testing doctrine remains a SEPARATE unpromoted candidate, neither included nor blocked. Reflection JSONL extends with `delegation_effort` + `ultracode_by_composition`. Origin ISA: `MEMORY/WORK/20260528-algorithm-v6110-tier-scaled-delegation-effort/ISA.md`. Migration: `echo 6.11.0 > LATEST`. Rollback: `echo 6.9.0 > LATEST` plus revert the capabilities.md table, LifeosSystemArchitecture.md + settings.json version strings, and `rm LIFEOS/ALGORITHM/v6.11.0.md`.

- **v6.6.0** (2026-05-14) — **Literal Surfacing + Calibrated Reflection.** Field finding from the 2026-05-14 Upgrade Recommend pass: `principal_stated_goal` was added to ISA frontmatter in v6.4.0 but unsurfaced in responses (247 missed-request + 216 repetition-loop entries in last 30 days of reflections); the LEARN-phase reflection JSONL carried `implied_sentiment` self-score only and never joined `MEMORY/LEARNING/SIGNALS/ratings.jsonl` where {{PRINCIPAL_NAME}}'s actual rating lived (3.2-point calibration gap, {{DA_NAME}} self-rates 7.6/10 over the same period {{PRINCIPAL_NAME}} rates 4.4/10); the weekly synthesis pipeline had been silent since 2026-04-22 because `com.lifeos.deriver.plist` runs ONLY `--hypothesize` and no scheduled runner invokes `LearningPatternSynthesis.ts --week`; format-drift and verification-miss failures (28+ logged + 125 occurrences avg 2.2/10 severity) had no structural enforcement. v6.6.0 fix: (1) NATIVE-mode `🗒️ TASK:` line MUST quote `GOAL_LITERAL` verbatim when present (≤80 chars + `…`); (2) `Inference.ts synthesizeAdvisorState()` prepends `PRINCIPAL STATED GOAL` block above ISA blob; (3) `CrossVendorAudit.ts assembleBundle()` inserts `===== PRINCIPAL STATED GOAL =====` as first section in all three bundle paths; (4) `Pulse/modules/work.ts` parses `principal_stated_goal` from local ISA frontmatter and renders inline on each Kanban card; (5) LEARN-phase reflection JSONL joins `ratings.jsonl` by `session_id` and inlines `principal_rating`, `principal_rating_source`, `principal_rating_confidence` (null when rating not yet captured). Two new Stop hooks ship at the same release: `OutputFormatGate.hook.ts` enforces banner presence on every assistant response (decision:block on miss); `SuccessClaimGate.hook.ts` blocks "shipped/deployed/done/verified" claims that lack same-response tool-output evidence. Both honor `input.stop_hook_active` to prevent deadlock; the optional field is added to canonical `hooks/lib/hook-io.ts HookInput` interface. New `com.lifeos.synthesis.plist` launchd job runs `LearningPatternSynthesis.ts --week` daily at 03:05 PT (5-min offset from `com.lifeos.deriver`), restoring the silent weekly pipeline. C/R/L: conjectured: capturing `principal_stated_goal` in ISA frontmatter (v6.4.0) was sufficient to anchor downstream work to the literal. refuted_by: reflection-mining surfaced 463 entries in 30 days where the executor lost the literal between OBSERVE and EXECUTE because the field was never surfaced in the response banner, the advisor blob, the Cato bundle, the Pulse dashboard, or the reflection record — the field was captured but not consumed. learned: capturing a load-bearing field is necessary but not sufficient — every consumption surface must surface it explicitly, or the field becomes write-only memory. criterion_now: v6.6.0 doctrine lists five named surfaces (NATIVE banner, Inference state, Cato bundle, Pulse work module, reflection JSONL) that MUST surface the literal when present; each is enforced by code (Inference/Cato/Pulse), doctrine (system prompt), or a separate gate hook (OutputFormatGate for banner presence; literal-echo audit deferred). Migration: `echo 6.6.0 > LATEST && launchctl load ~/Library/LaunchAgents/com.lifeos.synthesis.plist && bun LIFEOS/TOOLS/LearningPatternSynthesis.ts --week`. Rollback: `echo 6.5.0 > LATEST && launchctl unload ~/Library/LaunchAgents/com.lifeos.synthesis.plist && rm ~/Library/LaunchAgents/com.lifeos.synthesis.plist` plus revert the two new hooks + the Pulse/Inference/Cato edits. v6.6.x deferred: extend `OutputFormatGate` to verify the literal echo in NATIVE banners (not just banner presence); reconcile density-gate threshold drift between v6.5.0 doctrine (`< 0.3` at E3 / `< 0.5` at E4+) and `Scaffold.md` Step 3.5 (single `< 0.5`).

- **v6.5.0** (2026-05-12, backfilled to Index on 2026-05-14) — **Density × Tier Gate at OBSERVE.** Stage 1 eligibility in `TheRouter.hook.ts` emits `INTERVIEW_ELIGIBLE: true | false`; Stage 2 necessity in `skills/ISA/Workflows/Scaffold.md` Step 3.5 computes a deterministic 7-signal density score and fires `Skill("ISA", "interview")` with ≤3 questions + `proceed` override when below the tier threshold (`< 0.3` at E3 doctrine / `< 0.5` Scaffold implementation — drift noted in v6.6.0). Four new optional frontmatter keys: `density_score`, `interview_invoked`, `divergence_risk`, `density_gate_acknowledged`. Backwards-compat: presence-of-key is the v6.5.0+ marker; pre-v6.5.0 ISAs unaffected. Implementation ISA at `MEMORY/WORK/20260512-algorithm-v6.5.0-density-gate/ISA.md`.

- **v6.4.0** (2026-05-12, backfilled to Index on 2026-05-14) — **ISA Absorbs `/goal` Semantics.** Four-signal goal-detection step in OBSERVE captures the user's verbatim ask into `principal_stated_goal:` frontmatter when min-content rule passes. New `anchors_to` column on Test Strategy links each ISC to the literal. Phase-by-phase mapping for OBSERVE→LEARN consumes the literal as the evidence anchor (Rule 1 probes use it as reference), not the optimization target (BUILD/EXECUTE optimize for the intent the literal expresses). Cato (Rule 2a) is the cross-vendor probe that surfaces literal-vs-intent drift. Implementation ISA at `MEMORY/WORK/20260512-isa-absorbs-goal-v6.4.0-ship/ISA.md`.

- **v6.3.0** (2026-04-29) — **Thinking-capability vocabulary closed-enumeration release.** Field regression: when {{PRINCIPAL_NAME}} mandated thinking-skill counts at E2+, {{DA_NAME}} routinely produced invented generic labels ("decomposition", "edge-case enumeration", "tradeoff analysis") instead of selecting verbatim entries from the canonical list — meeting the hard floor cosmetically while the actual investments (FirstPrinciples, IterativeDepth, ApertureOscillation, SystemsThinking, RootCauseAnalysis, etc.) sat unused. Root cause: v6.2.0 doctrine pointed at "the table in capabilities.md" but didn't print the closed list inside the doctrine read at run time, and didn't have a name-vs-table check at the OBSERVE→THINK boundary. v6.3.0 fix: (1) the closed enumeration of 19 thinking capabilities is printed verbatim INSIDE `v6.3.0.md` doctrine — IterativeDepth, ApertureOscillation, FeedbackMemoryConsult, Advisor, ReReadCheck, FirstPrinciples, SystemsThinking, RootCauseAnalysis, Council, RedTeam, Science, BeCreative, Ideate, BitterPillEngineering, Evals, WorldThreatModel, Fabric patterns, ContextSearch, ISA; (2) a new Capability-Name Audit Gate fires at OBSERVE→THINK requiring each thinking name in `🏹 CAPABILITIES SELECTED` to appear verbatim in the closed list — phantom names do NOT count toward the floor and are a CRITICAL FAILURE; (3) the Rules section restates the closed list and the phantom rule so it survives compaction; (4) output template requires the literal closed-list name as the leading bolded token. Adding new thinking capabilities now requires editing `capabilities.md` AND bumping the Algorithm minor version + updating doctrine — closed-enum drift is no longer silent. C/R/L: conjectured: pointing at "the table in capabilities.md" was a sufficient binding for thinking-floor selection. refuted_by: across v6.1.0 and v6.2.0 runs the executor met the count by inventing generic labels under tier time pressure; the table reference produced the same drift the closed list was supposed to prevent. learned: the doctrine read at run time must contain the closed enumeration inline — pointers to other files leak vocabulary under pressure. Closed enumerations beat open vocabularies for any rule the model has shown willingness to wallpaper. criterion_now: v6.3.0.md prints the 19-name closed list inline, declares non-list names PHANTOM (CRITICAL FAILURE, doesn't count toward floor), and the OBSERVE→THINK Capability-Name Audit Gate is HARD. Migration: `echo 6.3.0 > LATEST` (LATEST is sole source — no settings.json field per v6.2.0 cleanup). Rollback: `echo 6.2.0 > LATEST`. v6.3.x deferred: hook that audits `🏹 CAPABILITIES SELECTED` blocks against the closed list and emits a phantom-warning to observability, plus CI check that doctrine list = capabilities.md table.

- **v6.2.0** (2026-04-28) — **ISA twelve-section frame + ISA Skill release.** The ISA body grew from five sections (Goal/Context/Criteria/Decisions/Verification) to twelve in fixed order: Problem, Vision, Out of Scope, Principles, Constraints, Goal, Criteria, Test Strategy, Features, Decisions, Changelog, Verification. Three-guardrail taxonomy locks the conceptual surface: **Principles** bind the thinking, **Constraints** bind the solution space, **Out of Scope** binds the vision (anti-vision, declared upfront), **Anti-criteria** bind the test surface (granular `Anti:` ISCs derived from the first three). Tier completeness gate is HARD at every tier (E1 = Goal+Criteria; E2 + Problem + TestStrategy; E3 + Vision + OutOfScope + Constraints + Features; E4 = all twelve; E5 + active Interview workflow before BUILD). New **ISA Skill** at `~/.claude/skills/ISA/` owns the canonical template and six workflows (Scaffold, Interview, CheckCompleteness, Reconcile, Seed, Append) — Algorithm OBSERVE invokes `Skill("ISA")` to scaffold instead of inlining ISA logic; PLAN invokes for ephemeral feature extraction; LEARN routes Decisions/Changelog/Verification entries through the canonical Append writer so the Deutsch conjecture/refutation/learning Changelog format doesn't degrade. **ID-stability rule** — ISC IDs never re-number on edit (splits become ISC-N.M, drops become tombstones), making Reconcile's deterministic merge safe. **Ephemeral feature files** (Ralph Loop / Maestro pattern) formalized — derived views from master, ISC-keyed merge, archived under `_ephemeral/.archive/`. v6.2.x deferred: hook parser updates (`isa-utils.ts`, `ISASync`, `CheckpointPerISC`, `EscalationGate` regex extension), Pulse rendering for project ISAs, ISA skill `Tools/*.ts` CLI implementations, project-ISA seeding migration. Migration: `echo 6.2.0 > LATEST && jq '.pai.algorithmVersion="6.2.0"' settings.json | sponge settings.json && sed -i '' 's|v6.1.0.md|v6.2.0.md|' CLAUDE.md`. Rollback: same command with `6.1.0`/`v6.1.0.md`.

- **v6.1.0** (2026-04-28) — **Thinking-floor hardening.** Field regression: a Deep (E4) framework-comparison task ran with zero thinking-skill invocations because "show your math" was used as a universal escape valve to skip the capability floor entirely. Root cause: v6.0.0 made the entire capability floor soft, including thinking. v6.1.0 splits the floor into two axes: thinking-capability count is now a HARD FLOOR that cannot be relaxed via show-your-math (E2 ≥2, E3 ≥4, E4 ≥6, E5 ≥8 — drawn from the "Thinking & Analysis" table in capabilities.md: IterativeDepth, ApertureOscillation, FirstPrinciples, SystemsThinking, RootCauseAnalysis, Council, RedTeam, Science, BeCreative, Ideate, BitterPillEngineering, Evals, WorldThreatModel, Advisor, FeedbackMemoryConsult, ContextSearch, Fabric patterns). Delegation-capability count remains soft and show-your-math relaxable (E2 ≥1, E3 ≥2, E4 ≥2, E5 ≥4) — sometimes the work is genuinely single-author and delegation adds noise, but thinking depth is structural. Selecting fewer thinking capabilities than the tier requires is now a doctrine violation, not a justification. Migration: `echo 6.1.0 > LATEST && jq '.pai.algorithmVersion="6.1.0"' settings.json | sponge settings.json && sed -i '' 's|v6.0.0.md|v6.1.0.md|' CLAUDE.md`. Rollback: same command with `6.0.0`/`v6.0.0.md`.

- **v6.0.0** (2026-04-27) — **Frame shift release.** ISA elevated to universal primitive with five identities (ideal state articulation, test harness, build verification, done condition, hard-to-vary explanation) — one document, no parallel acceptance.yaml/acceptance.ts artifacts. Unit reframed from "task" to "thing whose ideal state we're articulating": project ISAs at `<project>/ISA.md` for any persistent thing (applications, CLI tools, libraries, content pipelines, infrastructure, the Algorithm itself); task ISAs at `MEMORY/WORK/{slug}/ISA.md` reserved for ad-hoc one-shot work. **Mode-selection regression fixed:** `EscalationGate.hook.ts` (UserPromptSubmit) deterministically floors NATIVE→ALGORITHM via 5 triggers (doctrine-affecting, architectural-locator, multi-project, soft-user-signal, hard-to-vary) plus 3-axis test (retrievability/blast-radius/hard-to-vary depth) for residual cases. Telemetry hook (SessionStart) surfaces last-7d off-ramp failures, closing the feedback loop missing through v5.x. **Capability floors restored** at v4.1.0 numbers, field-validated: E1=0-1 (preserves fast-path), E2≥3, E3≥6, E4≥8 (≥6 thinking + ≥2 delegation), E5≥12 (≥8 thinking + ≥4 delegation). Soft floors with "show your math" override in `## Decisions`. Anti-criteria gate stays at ≥1 with new prose reminder at OBSERVE — no count bump per {{PRINCIPAL_NAME}}'s BPE preference. v6.0.x mechanics deferred: `<project>/ISA.md` parser/hook updates, OBSERVE/PLAN inheritance resolver, Pulse two-home rendering, project ISA seeding migration.

### Prior

- **v5.5.0** (2026-04-27) — BPE compaction continues: dropped the residual `ISC-A-N` numbering convention for anti-criteria.
- **v5.4.0** (2026-04-27) — Unified Learning Router in LEARN phase. Replaces the narrow "Knowledge capture" step (which only wrote to `MEMORY/KNOWLEDGE/`) with a router that handles every kind of learning: knowledge, operational rules, skill gotchas, project state, business facts, identity edits, doctrine changes, hook proposals, permission changes, reflection metrics. Each TYPE has a target LifeOS surface and a gate (inline write vs. surface-to-user). Routing table mirrors `LIFEOS_SYSTEM_PROMPT.md` "Self-Healing Infrastructure" so Algorithm and constitution agree. Forbidden destination: harness auto-memory dir (already blocked by `autoMemoryEnabled: false` + permissions deny). Documentation sync step (existing) becomes the natural downstream — any inline write to a system file triggers `Skill("<your-release-skill>", "documentation update ...")`. **Drift fix:** `settings.json pai.algorithmVersion` was stuck at `5.2.0` while LATEST/CLAUDE.md said `5.3.0` — bumped to `5.4.0` correctly across all 4 files this release.
- **v5.3.0** (2026-04-27) — BPE-driven removal of ISC category tags `[F]`/`[S]`/`[B]`/`[N]`/`[E]`/`[A]` from the on-disk format. Format collapses to `- [ ] ISC-N: criterion text`. Two doctrinal kinds preserved as prefix conventions, not bracket letters: anti-criteria via `ISC-A-N: Anti: ...` (≥1 required), antecedent via `Antecedent: ...` prefix in description (≥1 required when goal is experiential). Parser (`hooks/lib/isa-utils.ts`) is backward-compatible — legacy bracketed ISAs still parse, the captured `category` field is retained for them but never emitted on new ISAs. Eliminates triple-redundancy of anti-criteria (was `ISC-A-N` + `[N]` tag + `Anti:` prefix); collapses to two surface signals.
- **v5.2.0** (2026-04-26) — Reintroduces per-tier ISC count floors at E2+ as soft minimums on the count axis only: E2 ≥16, E3 ≥32, E4 ≥128, E5 ≥256. E1 stays floor-free. No category percentages, no capability-mix splits — just count. Soft = document under-decomposition in `## Decisions` and proceed; ILA Coverage axis remains the structural enforcer.
- **v5.1.0** (2026-04-26) — Per-step durability via `CheckpointPerISC` hook (PostToolUse on ISA edits, auto-commits per ISC transition with `--no-verify --no-gpg-sign`, sidecar idempotency, allowlist for opt-in repos). Doctrine elsewhere unchanged from v5.0.0.
- **v5.0.0** (2026-04-26) — BPE compaction. Removed prescriptive count floors (ISC counts per tier, min capabilities, category percentages). Replaced with two operational rules: ISC granularity (one binary tool probe each) and capability binding-commitment (named = invoked, no minimum count). Tier time budgets remain the only quantitative anchor. All doctrine preserved verbatim.
- **v4.1.0** (2026-04-23) — Vocabulary release: PRD → ISA (Ideal State Artifact). No doctrine changes.
- **v4.0.1** (2026-04-22) — Patch: `[A]` category parser fix in `prd-utils.ts`, reflection JSONL gains `prd_level_audit` and `hva` fields.
- **v4.0.0** (2026-04-21) — Frame-shift: PRD-as-unit (Coverage / Tightness / Uniqueness — the ISA-Level HVA). Doctrine preamble added.
- **v3.30.0** (2026-04-19) — Hard-to-Vary doctrine grounding (R1 ISC quality gate, R3 `[A]` Antecedent category, R4 per-ISC HVA, R5 Euphoric Surprise prediction, R8 Fluff Sweep).
- **v3.29.0** (2026-04-19) — RR1 Re-Read Check, RR2 Deliverable Manifest tier-extended, RR3 SYNTHESIS auto-load.
- **v3.28.0** (2026-04-17) — Proportional-weight tuning of Effort Levels.
- **v3.27.0** (2026-04-16) — Rule 2a (Cato cross-vendor audit, E4/E5).
- **v3.26.0** (2026-04-15) — T1 Deliverable Manifest, T2 Inline Verification, T3 Reproduce-First blocking.
- **v3.25.0** (2026-04-14) — Capabilities expansion: SystemsThinking, RootCauseAnalysis.
- **v3.24.0** (2026-04-13) — RedTeam-driven hardening patches P1-P5.
- **v3.23.0** (2026-04-12) — Verification Doctrine Rules 1/2/3, Feedback Memory Consult, Parallelism Opportunity Scan.
- **v3.21.0 and earlier** — see individual version files in this directory.

## How to read

- To understand the doctrine **today**: read whatever version `LATEST` points to.
- To understand **why** a rule exists: each rule in the active version carries a one-line `(provenance: ...)` pointer. Chase that into the corresponding section below, or `git log` the rule.
- To **roll back** to a prior version: see the migration recipe in that version's section below. Hooks parse only the ISA frontmatter `effort` enum and ISC checkboxes — both version-stable — so rollback is non-destructive. (v5.3.0 also dropped category tags from the parse contract; legacy bracketed ISAs are still accepted via backward-compat parsing.)

---

## Full History

### v7.0.0 → v8.0.0 (2026-07-11)

v8.0.0 is the **claims restructure**: the Algorithm becomes a one-page ISA whose subject is "a run" — a Loop preamble (hill-climb, ISA-as-instrument, continuous enhancement, ISA-outlives-runs/Bunker, dynamic range) plus **15 teeth-annotated done-claims**, the ISA slot-questions, and a live **event-nudge table** run by the new `hooks/IsaNudge.hook.ts` (UserPromptSubmit + PostToolUse + PostToolUseFailure; deterministic, session-gated, cooldowns; registered via settings.user.json overlay + settings.system.json). 28.2KB → 11.7KB. Nothing enforcement-bearing was deleted: every HOOK/CHECK R-row maps to a claim (R1→c1, R2→c2, R3→c6, R4→c7, R5→c5, R6→c8, R7→c9, R8→c10, R9→c11, R10→c13, R12→c12 — Forge-verified 2026-07-11); R11's reflection record (FIELD-class) moved to the Telemetry & records annex, still required at E2+; the Order column survives inside claim wording and the annex. What left was mechanism prose now living in the gates, the tools, and this file. Design provenance: `MEMORY/WORK/20260710-ascent-redesign-analysis/` (E5, Advisor+Forge reviewed) and the 2026-07-10 evening design conversation (claims 13–15 added on {{PRINCIPAL_NAME}}'s direction: living-ISA, spend-matched, dynamics). Known accepted risk (Decisions row, {{PRINCIPAL_NAME}}-gated): replay-eval precondition deferred; v7 kept intact.

**Migration:** `LATEST` → `8.0.0`. Hooks parse only ISA frontmatter + checkboxes — unchanged. The SUMMARY close block moved into v8 verbatim (spaced field layout, 2026-07-10 readability directive). **Rollback:** `echo 7.0.0 > ~/.claude/LIFEOS/ALGORITHM/LATEST`; optionally remove the three IsaNudge registrations (settings.user.json ×2, settings.system.json ×1 — .bak-isanudge copies of both sit beside them).

### v7.0.0 in-place patches (2026-07-10, {{PRINCIPAL_NAME}}-directed)

- **SUMMARY 📃 CONTENT starts on its own line below the label** (readability directive from a screenshot review; same change applied to NATIVE/MINIMAL templates in `LIFEOS_SYSTEM_PROMPT.md` v2.1.2 — TASK and CONTENT both).
- **Provenance blockquote removed from the doctrine file's head** — change history, the v7 BPE-audit pointer, and the Ascent membership pointer live here and in the system doc (then `DOCUMENTATION/Ascent/AscentSystem.md`; since the 2026-07-11 rename, `DOCUMENTATION/Algorithm/AlgorithmSystem.md`), not in the every-turn token budget. v7 provenance for the record: the 2026-07-10 BPE audit (`MEMORY/WORK/20260710-algorithm-bpe-analysis/REPORT.md`) replaced phase-scripted procedure with the Run Contract; v7 is part of Ascent.

### v6.9.0 → v6.11.0

v6.11.0 ships the tier→effort wiring conversation from 2026-05-28. {{PRINCIPAL_NAME}} noticed the native `/effort` modes change the real model reasoning level and asked whether the Algorithm could select them dynamically by tier. Investigation (claude-code-guide against Claude Code docs) confirmed a hard boundary: hooks cannot set model/effort/reasoning per turn, and the main loop's effort is locked at turn start. The `/effort` ladder has six levels — low, medium, high, xhigh, max, and **ultracode** ("xhigh + workflows", past the divider in its own zone).

The resolution: don't chase the main loop. Scale effort in the delegation layer, where it IS programmable (Inference.ts fast/standard/smart, Agent model override, Forge/Anvil/CodexResearcher intrinsic effort, Workflow agent() model + orchestration). The tier already governs *which* agents spawn (capabilities.md Forge/Anvil E3+, Council/RedTeam E4+); v6.11.0 adds the *effort level* dimension and the ultracode-by-composition idea: at E5 the Algorithm spawns xhigh agents AND drives Workflow orchestration, reproducing what the principal would get by manually selecting `/effort ultracode`.

**What's new:** Tier → Delegation Effort table (doctrine + capabilities.md mirror); PLAN-phase Delegation Gate selects the profile and emits `🎚️ DELEGATION EFFORT`; reflection JSONL gains `delegation_effort` + `ultracode_by_composition`; two tunables (`DELEGATION_EFFORT_MAP`, `E5_ULTRACODE_MIN_VERIFIERS`).

**What didn't change:** all v6.9.0 semantics (Resume After Complete, Frame-Drift, Context Sufficiency, the seven phases, twelve-section ISA) preserved verbatim. Capability floors and Forge/Anvil/Cato tier bindings preserved verbatim — the profile sets effort level, never count or agent choice.

**Version landscape note:** v6.10.0 was already taken by an unpromoted property-testing candidate when this shipped. v6.11.0 is therefore a delta on the active base v6.9.0, leaving v6.10.0 to promote on its own checklist. {{PRINCIPAL_NAME}} said "6.1.0" in the request; the Algorithm was at 6.9.0, so the next-minor is 6.11.0.

C/R/L: conjectured — the Algorithm tier could drive the main model's effort directly. refuted_by — Claude Code hooks cannot set model/effort/reasoning per turn (docs, "Important Notes"); main-loop effort is locked at turn start. learned — effort scales on the delegation layer where it's programmable; E5 reproduces `/effort ultracode` by composition. criterion_now — v6.11.0 Tier → Delegation Effort policy, model-policed at the PLAN Delegation Gate, promoted active.

Migration: `echo 6.11.0 > LIFEOS/ALGORITHM/LATEST`. Rollback: `echo 6.9.0 > LIFEOS/ALGORITHM/LATEST`, revert the capabilities.md Tier-Scaled Delegation Effort section, revert LifeosSystemArchitecture.md + settings.json version strings, `rm LIFEOS/ALGORITHM/v6.11.0.md`.

### v6.1.0 → v6.2.0

v6.2.0 ships the long-running ISA expansion conversation {{PRINCIPAL_NAME}} and {{DA_NAME}} had across the 2026-04-27/28 turn boundary. The doctrine bump preserves v6.1.0's thinking-floor hardening verbatim; everything new is additive.

**The work captured in C/R/L form (eat-our-own-dog-food on the new Changelog format):**

- 2026-04-28 | conjectured: the ISA's five sections (Goal/Context/Criteria/Decisions/Verification) carry enough structure for any project's articulation of ideal state
  refuted by: a long synthesis conversation surfaced that Problem (the why), Vision (the experiential intent), Out of Scope (the anti-vision), Principles (substrate-independent truths), Constraints (immovable architectural mandates), Test Strategy (per-ISC verification), Features (work breakdown with parallelization graph), and Changelog (Deutsch error-correction trail) all serve real, distinct purposes that the five-section frame was silently overloading or missing entirely
  learned: the ISA body needs twelve sections in fixed order, with hard tier-completeness gating that prevents ceremony bloat at E1 (just Goal+Criteria) while requiring full structure at E4/E5 (all twelve, plus active Interview at E5)
  criterion now: v6.2.0.md ISA-as-System-of-Record subsection lists the twelve sections, the IsaFormat.md spec bumps to v2.7 with the full schema, and the Tier Completeness Gate is HARD at every tier

- 2026-04-28 | conjectured: the existing skill-and-hook surface was sufficient infrastructure for the ISA primitive
  refuted by: filesystem check showed no ISA skill on disk, IsaFormat.md was the only documentation, no project ISAs existed anywhere, and the changelog format that v6.2.0 introduces (conjecture/refutation/learning) had no canonical writer
  learned: novel formats degrade without a skill that owns them — the C/R/L Changelog would have diluted to free-form prose within three projects
  criterion now: new `~/.claude/skills/ISA/` skill with Scaffold / Interview / CheckCompleteness / Reconcile / Seed / Append workflows, and Append specifically gates the C/R/L format (refuses partial entries)

- 2026-04-28 | conjectured: Out of Scope and Anti-criteria are the same concept, just at different granularities — both can fold into anti-criteria ISCs
  refuted by: {{PRINCIPAL_NAME}} articulated the distinction directly — Out of Scope is declarative ("we are not building mobile native apps"), anti-criteria are derived testable probes ("`/admin` returns 404")
  learned: the three-guardrail taxonomy needs four surfaces, not three: Principles bind thinking, Constraints bind solution space, Out of Scope binds the vision (anti-vision), Anti-criteria bind the test surface (derived). Out of Scope is its own ISA section, not an ISC type
  criterion now: ISA body has `## Out of Scope` as section 3, distinct from `## Criteria` anti-criteria

- 2026-04-28 | conjectured: ephemeral feature files (Ralph Loop / Maestro pattern) need a separate workflow file in the ISA skill
  refuted by: Scaffold workflow already had to know how to build the master ISA from a prompt; the ephemeral excerpt is the same operation against a feature-scoped subset
  learned: Scaffold owns both halves of the spawn lifecycle; Reconcile owns the merge half. No separate Spawn workflow needed.
  criterion now: Scaffold.md has an "Ephemeral feature mode" section; Reconcile.md is the deterministic ISC-keyed merge — together they form the lifecycle

**The Forge / Advisor / Cato collaboration on this run:**
- Advisor commitment-boundary call before BUILD flagged that the C/R/L format would degrade without a canonical writer → Append.md added during BUILD (closing what would have been ISC-21.2 in retrospect).
- Forge drafted the AlgorithmSystem.md full rewrite (334 lines, swapped from v3.26.0-stale to v6.2.0-current) in parallel with the doctrine spec writes, in 209 seconds.
- Advisor flagged two real risks during the v6.2.0.md write: (1) E1 fast-path bypass risks becoming a doctrine-evasion route — addressed by specifying the bypass as a whitelist condition, not a heuristic; (2) section-name drift between the skill's `canonical-isa.md` and `IsaFormat.md` v2.7 — addressed by writing both with the same twelve-section list in the same order this run.

**v6.2.0 in-flight refactor (LATEST as single source of truth):** initially shipped an `AlgorithmVersionSync.hook.ts` to keep `settings.json .pai.algorithmVersion` synced with `LIFEOS/ALGORITHM/LATEST` on every Write/Edit/MultiEdit. {{PRINCIPAL_NAME}} called Bitter Pill — a hook that synchronizes duplicated state is overhead a smarter design renders unnecessary. Refactored to make LATEST the sole record: statusline reads `cat $LIFEOS_DIR/ALGORITHM/LATEST` directly; `Banner.ts` and `ArchitectureSummaryGenerator.ts::detectAlgorithmVersion()` read LATEST first; `UpdateCounts.ts` no longer mirrors CLAUDE.md regex into settings; `CLAUDE.md MANDATORY FIRST ACTION` became a two-step "Read LATEST → Read v${contents}.md" instruction; `.pai.algorithmVersion` deleted from settings.json; hook + its three Write/Edit/MultiEdit registrations removed. Net change: one source, drift physically impossible. Live-probed end-to-end: bumping LATEST to `6.99.99` flowed through the statusline within one second with no other file touched. Lesson written to v6.2.x doctrine note: when the symptom is "synced field drifted," the right answer is usually to remove the duplicated field, not to add a synchronizer.

**Files updated:**
- NEW: `LIFEOS/ALGORITHM/v6.2.0.md` (doctrine file)
- NEW: `~/.claude/skills/ISA/` (eleven files: SKILL.md, four Examples, six Workflows)
- NEW: `~/.claude/ISA.md` (dog-food: LifeOS-as-project ISA, seeded via the Seed workflow)
- EDIT: `LIFEOS/ALGORITHM/LATEST` → `6.2.0`
- EDIT: `LIFEOS/ALGORITHM/changelog.md` (this entry, plus Index demotion of v6.1.0 to Prior)
- EDIT: `LIFEOS/DOCUMENTATION/ISA/IsaFormat.md` → v2.7 (twelve-section schema + tier completeness gate + ID-stability rule + ephemeral pattern + C/R/L format)
- EDIT: `LIFEOS/DOCUMENTATION/Algorithm/AlgorithmSystem.md` (full rewrite from v3.26.0-stale to v6.2.0-current, via Forge)
- EDIT: `~/.claude/CLAUDE.md` (MANDATORY FIRST ACTION → v6.2.0.md)
- EDIT: `LIFEOS/ALGORITHM/capabilities.md` (Skill("ISA") added to OBSERVE-phase capabilities)
- EDIT: `LIFEOS/ALGORITHM/mode-detection.md` (E1 fast-path bypass specified as whitelist, ISA skill invocation mandatory at E2+)

**v6.2.x deferred (automation surface):**
- `hooks/lib/isa-utils.ts` parser updates for `<project>/ISA.md` discovery and twelve-section parsing
- `hooks/ISASync.hook.ts` extension to project ISAs
- `hooks/CheckpointPerISC.hook.ts` extension to project ISAs
- hooks/PromptProcessing.hook.ts (formerly EscalationGate.hook.ts, now consolidated) regex extension to add Append/Reconcile/Seed triggers
- `LIFEOS/PULSE/Observability/observability.ts` rendering for project ISAs
- `~/.claude/skills/ISA/Tools/*.ts` CLI implementations (Scaffold.ts, Interview.ts, Completeness.ts, Reconcile.ts, Append.ts)
- Project-ISA seeding migration for the ~30 projects in PROJECTS.md (lazy-seed on first task per {{PRINCIPAL_NAME}}'s Q5 answer)

**Migration command (rollback to v6.1.0):**
```bash
echo 6.1.0 > ~/.claude/LIFEOS/ALGORITHM/LATEST && \
  jq '.pai.algorithmVersion="6.1.0"' ~/.claude/settings.json | sponge ~/.claude/settings.json && \
  sed -i '' 's|v6.2.0.md|v6.1.0.md|' ~/.claude/CLAUDE.md
# The ISA skill stays on disk and is harmless under v6.1.0 (skill is invocation-agnostic)
# IsaFormat.md v2.7 is additive — v6.1.0 still parses correctly
# AlgorithmSystem.md is documentation-only — v6.1.0 doesn't depend on it
```

### v5.5.0 → v6.0.0

v6.0.0 is a **frame-shift release** in two coupled moves: (1) closing the v5.0.0 BPE under-cut at the mode-selection layer, and (2) elevating the ISA from "task artifact" to "universal primitive with five identities." Both moves were triggered by the same {{PRINCIPAL_NAME}}-led session — the mode-selection regression was the symptom that forced the deeper architectural conversation.

**The two coupled moves.**

**Move 1 — Mode-selection floor (closes the v5.0.0 → v5.7.0 regression).** v5.0.0 BPE-compacted the Algorithm internals correctly but left the new NATIVE/ALGORITHM/MINIMAL mode-selection gate un-floored. Two un-floored trust layers in series: model picks mode without floor → IF Algorithm, picks capabilities without floor. Either layer failing produces silent under-escalation. v5.2.0 partially restored ISC count floors INSIDE the Algorithm but missed the mode-selection layer entirely. v6.0.0 adds:

- `EscalationGate.hook.ts` (UserPromptSubmit) — five deterministic triggers (doctrine-affecting / architectural-locator / multi-project / soft-user-signal / hard-to-vary explanation work) plus a three-axis NATIVE→ALGORITHM test (Retrievability / Blast-Radius / Hard-to-Vary Depth) for residual cases. Writes `MODE_FLOOR` to additionalContext.
- `EscalationTelemetry.hook.ts` (SessionStart) — scans last 7 days of escalation-gate.jsonl, surfaces "in last 7 days, N prompts triggered floor" so off-ramp failures become visible. The feedback loop missing through v5.x.
- **Capability floors restored** at v4.1.0 numbers, field-validated this session: E1=0-1 (preserves <90s fast-path), E2≥3, E3≥6, E4≥8 (≥6 thinking + ≥2 delegation), E5≥12 (≥8 thinking + ≥4 delegation). Soft floors with explicit "show your math" override required in `## Decisions` if model picks fewer.
- Anti-criteria gate stays at ≥1 (no v3.30 ≥2 restoration per {{PRINCIPAL_NAME}}'s BPE preference). New prose reminder at OBSERVE: "Have you included anti-criteria? What must NOT happen?"

**Move 2 — ISA as universal primitive (frame shift).** The ISA isn't a task artifact — it's THE artifact for any thing whose ideal state we're articulating. It has five identities simultaneously: (1) ideal state articulation (Deutsch hard-to-vary explanation), (2) test harness (ISCs ARE the tests, with named probes), (3) build verification (passing ISCs verifies what was built), (4) done condition (task complete when all ISCs pass), (5) system of record. Forge's pressure-test of an earlier "acceptance.yaml per project" proposal killed that direction — the ISA already covers the surface; inventing a parallel artifact creates a second source of truth that immediately rots. The new structural rule: project ISAs live AT `<project>/ISA.md` (in the project's repo, system of record); task ISAs at `MEMORY/WORK/{slug}/ISA.md` reserved for ad-hoc one-shot work that doesn't belong to a persistent thing. For complex apps, the ISA naturally has many more ISCs because the application logic IS the ideal state — RBAC matrix, perf budgets, security model, auth flow, data integrity, build verification, deploy contract, all encoded as ISCs.

**v6.0.0 ships the FRAME; v6.0.x ships the mechanics.** {{PRINCIPAL_NAME}} signaled "deeper conversation we need to continue" on the mechanics side — `<project>/ISA.md` parser updates, OBSERVE/PLAN inheritance resolver, Pulse two-home rendering, project-ISA seeding migration. v6.0.0 doctrine commits the conceptual move; mechanics earn implementation through continued design conversation.

**What did NOT change:** every other v5.5.0 doctrine. Seven phases, Hard-to-Vary Doctrine preamble, ISA as system of record (extended to two homes, not replaced), the variation test, ISC granularity rule, ISA-Level HVA (Coverage/Tightness/Uniqueness), Verification Doctrine (Rules 1/2/2a/3 + P1-P5 hardening), `[DEFERRED-VERIFY]` ISC status, Inline Verification mandate, Reproduce-First blocking gate, Intent Echo, Reverse Engineering block, Preflight Gates A-D, Deliverable Manifest, Parallelism Opportunity Scan, Re-Read Check, Euphoric Surprise Prediction, Learning Router, Forge/Anvil/Cato bindings, voice-curl format, frontmatter contract, fast-path trigger, the 7/7 SUMMARY block as terminator, the reflection JSONL schema, per-tier ISC count floors at E2+ (16/32/128/256), CheckpointPerISC hook. The two doctrinal minimums survive: anti-criteria ≥1, antecedent ≥1 when experiential.

**Files updated:**
- NEW: `LIFEOS/ALGORITHM/v6.0.0.md` (doctrine file)
- NEW: hooks/EscalationGate.hook.ts (UserPromptSubmit gate — later consolidated into PromptProcessing.hook.ts)
- NEW: hooks/EscalationTelemetry.hook.ts (SessionStart telemetry — later consolidated into PromptProcessing.hook.ts)
- EDIT: `LIFEOS/ALGORITHM/LATEST` → `6.0.0`
- EDIT: `LIFEOS/ALGORITHM/changelog.md` (this entry, plus Index demotion of v5.5.0 to Prior)
- EDIT: `~/.claude/CLAUDE.md` (MANDATORY FIRST ACTION → v6.0.0.md)
- EDIT: `~/.claude/settings.json` (`pai.algorithmVersion` → `6.0.0`, hook registration for new UserPromptSubmit + SessionStart entries)

**Migration command (rollback to v5.5.0):**
```bash
echo 5.5.0 > ~/.claude/LIFEOS/ALGORITHM/LATEST && \
  jq '.pai.algorithmVersion="5.5.0"' ~/.claude/settings.json | sponge ~/.claude/settings.json && \
  sed -i '' 's|v6.0.0.md|v5.5.0.md|' ~/.claude/CLAUDE.md
# Remove the two new hooks from settings.json hook arrays manually
# Hooks themselves can be left on disk (they no-op unless registered)
```

**Provenance:** Direct {{PRINCIPAL_NAME}}-led session 2026-04-27 (this run). Triggered by an under-escalation event in the same session: {{PRINCIPAL_NAME}} asked "where in our ISCs do standing invariants live?" and {{DA_NAME}} answered in NATIVE mode with a 1-paragraph response, when the question genuinely needed E4/E5 doctrinal-design treatment. Root-cause analysis traced the regression to v5.0.0 BPE leaving the mode-selection layer un-floored. The conversation that followed surfaced the deeper insight: the answer to {{PRINCIPAL_NAME}}'s original question wasn't to add a parallel acceptance framework — it was to recognize that the ISA already IS the test harness, and the v5.0.0 cut wasn't the only thing missing. Two coupled fixes from one session.

### v5.4.0 → v5.5.0

v5.5.0 is the **third BPE compaction round on the ISC tagging system**, applied directly to the residual `ISC-A-N` numbering convention that v5.3.0 left in place. Audit logic is identical to v5.3.0's: would a smarter model make this convention unnecessary? Answer: yes — the `Anti:` prose prefix already encodes the gate, and the dual `-A-` namespace was a triple-redundant decoration in disguise.

**What changed (and what didn't).** The `ISC-A-N` numbering is gone. New ISAs number every criterion sequentially as `ISC-N`, anti-criteria included. The `Anti:` prose prefix at the start of the description carries the doctrinal flag alone — the same role `Antecedent:` already played for experiential preconditions. **The ≥1 anti-criterion gate is unchanged.** Splitting Test, Hard-to-Vary Audit, ILA, tier floors, and every other v5.4.0 doctrine survive verbatim.

**Surface format before / after:**

```
v5.4.0:  - [ ] ISC-1: Haiku exists and JSON schema validates
         - [ ] ISC-A-1: Anti: bun build does not error
         - [ ] ISC-7: Antecedent: novel juxtaposition of metric + sketch

v5.5.0:  - [ ] ISC-1: Haiku exists and JSON schema validates
         - [ ] ISC-2: Anti: bun build does not error              (sequential numbering)
         - [ ] ISC-7: Antecedent: novel juxtaposition of metric + sketch
```

The visible win: ISA bodies stop alternating two numbering pools (`ISC-1, ISC-2, ISC-A-1, ISC-3..ISC-12, ISC-A-2, ISC-13...`) and read as one continuous count.

**Migration / parser compatibility.** Both parsers (`hooks/lib/isa-utils.ts` and `LIFEOS/TOOLS/algorithm.ts`) now detect anti-criterion via `Anti:` prefix on the trimmed description. The legacy `id.includes('-A-')` test is retained as a backward-compat fallback so historical ISAs in `MEMORY/WORK/` still classify correctly. Domain-prefixed IDs (`ISC-CLI-3`, `ISC-TIER-1`, etc.) are unaffected — the dropped namespace was only the single-letter `-A-` anti-criterion convention. Historical ISAs are not migrated (frozen records, same precedent as v5.3.0).

**Files updated:**
- `LIFEOS/ALGORITHM/v5.5.0.md` (new doctrine; v5.4.0 copied with the Anti-criterion table row + provenance note rewritten)
- `LIFEOS/ALGORITHM/LATEST` → `v5.5.0`
- `LIFEOS/DOCUMENTATION/ISA/IsaFormat.md` (anti-criterion examples + Tags table now show `ISC-N: Anti:`)
- `LIFEOS/DOCUMENTATION/Algorithm/AlgorithmSystem.md` (ISC Quality System Anti-criterion line)
- `LIFEOS/ALGORITHM/optimize-loop.md` (Guard Rails + Eval mode guard rails examples renumbered)
- `LIFEOS/TOOLS/algorithm.ts` (line 558 anti-criterion detector switched to description-prefix; line 717 comment example updated)
- `hooks/lib/isa-utils.ts` (line 243 detector switched to `Anti:` prefix with legacy fallback; comments updated)
- `hooks/lib/isa-template.ts` (template comment block uses sequential `ISC-N: Anti:` example, version note bumped to v5.5.0+)
- `~/.claude/CLAUDE.md` (MANDATORY FIRST ACTION → v5.5.0.md)
- `~/.claude/settings.json` (`pai.algorithmVersion` → `5.5.0`)

**Migration command (rollback):** `echo v5.4.0 > ~/.claude/LIFEOS/ALGORITHM/LATEST && jq '.pai.algorithmVersion="5.4.0"' ~/.claude/settings.json | sponge ~/.claude/settings.json && sed -i '' 's|v5.5.0.md|v5.4.0.md|' ~/.claude/CLAUDE.md`. Hooks need no rollback — both detection paths (legacy `-A-` and new `Anti:` prefix) coexist permanently.

**Provenance:** Direct BPE audit by {{PRINCIPAL_NAME}} against v5.4.0 — surfaced visible inconsistency in `MEMORY/WORK/20260427-040000_skill-convention-audit/ISA.md`'s ISC list (mixing `ISC-1, ISC-2, ISC-A-1, ISC-3..ISC-12, ISC-A-2, ISC-13...`). Five Questions: (1) does the `-A-` ID add information the `Anti:` prefix does not? No. (2) is the parser logic load-bearing on `-A-`? It was, but moving the test to `Anti:` is a one-line change. (3) does the gate (≥1 anti-criterion) require the dual namespace? No — the gate is a count of `Anti:`-prefixed ISCs, however numbered. Conclusion: cut the decoration, keep the gate via prefix. The pattern is now applied three times (v5.0.0 counts, v5.3.0 brackets, v5.5.0 numbering); doctrine is converging on prose-prefix-only as the surface contract.

### v5.2.0 → v5.3.0

v5.3.0 is a **BPE compaction release on the ISC category tag system**. Audit driven by direct user challenge against v5.2.0's `[F]`/`[S]`/`[B]`/`[N]`/`[E]`/`[A]` table: would a smarter model make these tags unnecessary? Answer: yes for four of the six. The other two carried doctrinal gates — the gates survive, the tags don't.

**What changed (and what didn't).** Four tags (`[F]` Functional, `[S]` Structural, `[B]` Behavioral, `[E]` Edge) were pure description: a competent reader infers category from the criterion text in well under a second. Removing them costs nothing and saves visual noise on every ISA {{PRINCIPAL_NAME}} ever reads again. Two tags (`[N]` Negative, `[A]` Antecedent) carried doctrinal weight as gates (≥1 anti-criterion always; ≥1 antecedent when goal is experiential) — but the *gates*, not the *labels*, are load-bearing. The gates survive. Anti-criteria already use `ISC-A-N` numbering and an `Anti:` prose prefix; the `[N]` tag was triple-redundant. Antecedent ISCs now use an `Antecedent:` prose prefix in the criterion description.

**Surface format before / after:**

```
v5.2.0:  - [ ] ISC-1 [F]: Haiku exists and JSON schema validates
         - [ ] ISC-A-1 [N]: Anti: bun build does not error

v5.3.0:  - [ ] ISC-1: Haiku exists and JSON schema validates
         - [ ] ISC-A-1: Anti: bun build does not error
         - [ ] ISC-7: Antecedent: novel juxtaposition of metric + sketch    (when experiential)
```

**What did NOT change:** every other v5.2.0 doctrine. Seven phases, Hard-to-Vary Doctrine preamble, ISA as system of record, the variation test, ISA-Level HVA (Coverage/Tightness/Uniqueness), per-ISC HVA with cluster-bearing rule, Verification Doctrine (Rules 1/2/2a/3 + P1-P5 hardening), `[DEFERRED-VERIFY]` ISC status, Inline Verification mandate, Reproduce-First blocking gate, Intent Echo, Reverse Engineering block, Preflight Gates A-D, Feedback Memory Auto-Consult, Deliverable Manifest, Parallelism Opportunity Scan, Re-Read Check, Fluff Sweep, Euphoric Surprise Prediction, Knowledge Capture, Documentation Sync, Forge/Anvil/Cato bindings, voice-curl format, frontmatter contract, fast-path trigger, the 7/7 SUMMARY block as terminator, the reflection JSONL schema, the per-tier ISC count floors at E2+ (16/32/128/256), and the CheckpointPerISC hook. The two doctrinal minimums also survive verbatim — only their *expression* moves from a bracket letter to a prose prefix.

**Migration / parser compatibility.** The parser (`hooks/lib/isa-utils.ts`) is **backward-compatible**: legacy ISAs with `[F]`/`[S]`/`[B]`/`[N]`/`[E]`/`[A]` still parse correctly and the `category` field is retained on `CriterionEntry` for them. New ISAs simply omit the bracket. Hooks (ISASync, CheckpointPerISC) require no changes — they consume `parseCriteriaList()` which now accepts both shapes. Historical ISAs in `MEMORY/WORK/` are not migrated (they are frozen records).

**Files updated:**
- `LIFEOS/ALGORITHM/v5.3.0.md` (new doctrine file, copy of v5.2.0 with category section rewritten)
- `LIFEOS/ALGORITHM/LATEST` → `v5.3.0`
- `LIFEOS/DOCUMENTATION/ISA/IsaFormat.md` (Tags section + format examples + doctrinal-minimums language)
- `LIFEOS/DOCUMENTATION/Algorithm/AlgorithmSystem.md` (outdated v3.26.0-era category table replaced with pointer to canonical doctrine)
- `hooks/lib/isa-template.ts` (ISA stub example uses bare `ISC-N:` format)
- `hooks/lib/isa-utils.ts` (regex made format-agnostic; comments updated; `VALID_CATEGORIES` retained for legacy parse, marked `@deprecated`)

**Migration command (rollback):** `echo v5.2.0 > LATEST && jq '.pai.algorithmVersion="5.2.0"' ~/.claude/settings.json | sponge ~/.claude/settings.json && sed -i '' 's|v5.3.0.md|v5.2.0.md|' ~/.claude/CLAUDE.md`. Hooks need no rollback — the parser still understands the old bracket format.

**Provenance:** Direct BPE audit by {{PRINCIPAL_NAME}} against v5.2.0 lines 85-97 + the `Anti-criteria` line 97. The Five Questions test surfaced (1) descriptive labels add zero information a competent reader can't infer, (2) the only doctrinal force was in `[N]`/`[A]` as gates, (3) `Anti:` prefix already encoded `[N]` — making `ISC-A-N` + `[N]` + `Anti:` triple-redundant. Conclusion: cut decoration, keep gate via prefix. Pattern reusable for at least three other doctrine spots that share the same shape.

### v5.1.0 → v5.2.0

v5.2.0 reintroduces **per-tier ISC count floors at E2+** as a soft minimum on the *count* axis only. v5.0.0's BPE compaction cut the v4.1.0 count floors (6/14/28/48/72) along with all category percentages and capability mixes. Field experience showed the granularity rule alone under-decomposes at the top tiers — a smart-enough model can pass the granularity gate with N atomic ISCs that nonetheless fail the ISA-Level HVA Coverage axis post-hoc. Floors close that gap before THINK, not after.

**The new floors:**

| Tier | ISC floor | vs v4.1.0 | vs v5.0/5.1 |
|------|-----------|-----------|--------------|
| Standard (E1) | none — preserves fast-path | -6 | unchanged |
| Extended (E2) | 16 | +2 | NEW soft floor |
| Advanced (E3) | 32 | +4 | NEW soft floor |
| Deep (E4) | 128 | +80 (2.7×) | NEW soft floor |
| Comprehensive (E5) | 256 | +184 (3.6×) | NEW soft floor |

**Soft, not strict.** The floor is a coverage anchor, not a blocking ceremony. If the task surface genuinely produces fewer atomic ISCs after the granularity rule has been applied honestly, document the under-decomposition and the reason in `## Decisions` and proceed. The ILA Coverage axis at E2+ remains the structural enforcer — the floor surfaces an under-decomposed ISA earlier, before THINK.

**Why these specific numbers.** E2/E3 are gentle bumps from v4.1.0 (16/32 vs 14/28). E4/E5 are aggressive (128/256 vs 48/72) because that's where decomposition pays the most — a 30-min E4 task that ships 30 ISCs has almost certainly compressed the spec to per-step bullets that hide real complexity; 128 forces real surface coverage. E5 at 256 averages ~28 sec per atomic check across the 120-min budget, which is the right order of magnitude for actually verifiable steps.

**What did NOT change from v5.1.0:** the CheckpointPerISC hook and EXECUTE Checkpoints subsection (preserved verbatim). What did NOT change from v5.0.0: every doctrine listed in the v5.0.0 "What did NOT change" paragraph below. **Category percentages stay cut** — no [S]≤40%, no [B]≥20%, no thinking/delegation splits. The floor is a count anchor only; the model still picks the category mix the task needs.

**Migration:** v5.2.0 lives alongside v5.1.0 and v5.0.0. To roll back: `echo v5.1.0 > LATEST && jq '.pai.algorithmVersion="5.1.0"' ~/.claude/settings.json | sponge ~/.claude/settings.json && sed -i '' 's|v5.2.0.md|v5.1.0.md|' ~/.claude/CLAUDE.md`. Hooks unchanged (parse only `effort` enum + ISC checkboxes + category tags). Floors are doctrine-level only — `isa-utils.ts` does not enforce them; the Algorithm prompt does.

### v5.0.0 → v5.1.0

v5.1.0 adds **per-step durability** — the one design move LifeOS hadn't yet committed to. The `CheckpointPerISC` hook (PostToolUse Edit/Write/MultiEdit on `MEMORY/WORK/{slug}/ISA.md`) auto-commits a git checkpoint in every opted-in repo on each `[ ]`→`[x]` ISC transition. Commits use `--no-verify --no-gpg-sign` to avoid husky/GPG hangs that would block the session. Sidecar state at `MEMORY/WORK/{slug}/.checkpoint-state.json` makes commits idempotent (no double-commit). Allowlist at `~/.claude/checkpoint-repos.txt` — defaults to `~/.claude` only; other repos require explicit {{PRINCIPAL_NAME}} opt-in (no surprise commits in WIP branches). Inspection + rollback via `bun ~/.claude/LIFEOS/TOOLS/Checkpoint.ts {list|show|rollback} <slug> [<isc-id>]`. **Rollback is preview-only** — prints the suggested `git reset --hard <sha>` per repo and exits; never executes destructive ops without {{PRINCIPAL_NAME}} running them himself. Doctrine elsewhere is unchanged from v5.0.0; the only addition is the **Checkpoints subsection** in EXECUTE below. Provenance: R1 from session `20260426-230039_hankweave-pai-improvements-analysis` — Hankweave's per-codon git checkpoint absorbed as a LifeOS-native primitive without adopting Hankweave's runtime.

**Migration:** v5.1.0 lives alongside v5.0.0. To roll back: `echo v5.0.0 > LATEST && jq '.pai.algorithmVersion="5.0.0"' ~/.claude/settings.json | sponge ~/.claude/settings.json && sed -i '' 's|v5.1.0.md|v5.0.0.md|' ~/.claude/CLAUDE.md`. The hook can be left registered with no allowlist file (it no-ops); or remove its entries from `settings.json` PostToolUse arrays.

### v4.1.0 → v5.0.0

v5.0.0 is a **BPE compaction release** — Bitter Pill Engineering applied to the Algorithm itself. Doctrine is preserved verbatim where the wording is load-bearing; **prescriptive scaffolding (ISC count floors per tier, min-capability counts, category percentage caps and minimums, "≥1 thinking + ≥1 delegation" splits) is removed.** The model picks the right shape inside the only quantitative anchor that survives — the **tier time budget**.

Three structural moves:

1. **Counts and ratios out.** ISC floors (6/14/28/48/72), min-capability counts (0-1/2/4-5/6-10/10-14), category caps (`[S]` ≤40%, `[B]` ≥20%, `[E]` ≥10%, `[A]` ≤60%), `[N]` ≥2, "category diversity ≥3 tags", "max 2 ISCs may require human judgment", "≥1 thinking + ≥1 delegation" — all deleted. They were the model's judgment hard-coded as constants; a smarter model picks the right shape per task.

2. **Replaced by two operational rules.** **ISC granularity:** *"Split until each criterion is one binary tool probe."* If you cannot name the probe, the criterion is not yet atomic. **Capability selection:** *"Select what the task genuinely needs within the tier time budget."* Naming a capability is a binding commitment to invoke it — phantom capabilities = CRITICAL FAILURE. The granularity rule does the work the count floors used to do; the binding-commitment rule does the work the min-capability counts used to do.

3. **History moves out.** v3.21.0 → v4.1.0 CHANGES sections (~250 lines of historical churn) move to this file. Per-rule "Why this exists" boilerplate compacts to one-line provenance pointers. The full rationale lives here. Read this file once if you want the archaeology; the doctrine file stays focused on what to do this run.

**What did NOT change:** the seven phases, the Hard-to-Vary Doctrine preamble, the ISA as system of record, the six ISC categories ([F]/[S]/[B]/[N]/[E]/[A]), the variation test, the ISA-Level HVA (Coverage/Tightness/Uniqueness), the per-ISC HVA with cluster-bearing rule, the Verification Doctrine (Rules 1/2/2a/3 + P1-P5 hardening), `[DEFERRED-VERIFY]` ISC status, Inline Verification mandate, Reproduce-First blocking gate, Intent Echo, Reverse Engineering block, Preflight Gates A-D, Feedback Memory Auto-Consult, Deliverable Manifest (any tier with 2+ explicit sub-tasks), Parallelism Opportunity Scan, Re-Read Check (RR1), Fluff Sweep at VERIFY, Euphoric Surprise Prediction, Knowledge Capture rules, Documentation Sync, Forge/Anvil/Cato bindings, voice-curl format and identifier fields, frontmatter contract, fast-path trigger, the 7/7 SUMMARY block as terminator, the reflection JSONL schema. Two minimums survive on doctrinal grounds, not numerical ones: **`[N]` anti-criteria ≥1** (a goal with zero failure modes worth naming is under-specified) and **`[A]` antecedent ≥1 when the goal is experiential** (the doctrinal hook for aesthetic/resonant work).

**Migration:** v5.0.0 lives alongside v4.1.0. The `LATEST` pointer determines which version `CLAUDE.md` loads (currently a plain text file, not a symlink). Hooks (`isa-utils.ts`, ISASync) parse only the `effort` enum, ISC checkboxes, and category tags — none of which changed. State sync, work.json, Pulse rendering all carry forward unchanged. To roll back: `echo v4.1.0 > LATEST && jq '.pai.algorithmVersion="4.1.0"' settings.json | sponge settings.json`.

For v3.21.0 through v4.1.0 history: see individual version files in this directory; their `### CHANGES FROM` sections at the top are the source of truth for that era.

---

## Policy

When releasing a new Algorithm version:

1. Doctrine changes go into the new `vX.Y.Z.md` (no CHANGES section at top — start with `## The Algorithm X.Y.Z` then doctrine).
2. The CHANGES prose, migration recipe, and rollback recipe go **here**, as a new section under **Full History**, in reverse-chronological order (newest at top of Full History).
3. Add a one-line entry under **Index → Current**, demote the previous Current entry to **Prior**.
4. Update `LATEST` and `~/.claude/CLAUDE.md`'s algorithm reference to the new version.

The doctrine file is read every session. This file is read on demand. Keep them separate.
