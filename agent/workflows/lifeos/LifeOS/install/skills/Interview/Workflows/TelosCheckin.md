# TelosCheckin — superseded by ContextCheckin

This workflow has been generalized. See `ContextCheckin.md` for the active workflow.

The freshness convention extended from TELOS-only to all seven constitutional context files (TELOS plus DA_IDENTITY, PRINCIPAL_IDENTITY, PROJECTS, LIFEOS_SYSTEM_PROMPT, PRINCIPAL_TELOS, ARCHITECTURE_SUMMARY). The workflow's identity changed accordingly: it's no longer specifically about TELOS sections, it's about the whole constitutional context surface. ContextCheckin still drives the per-section TELOS conversation as its core, plus the file-level review of the other constitutional files.

If a routing decision lands here (e.g., user typed `/interview telos`), redirect to `Workflows/ContextCheckin.md`. The Interview skill's SKILL.md routing table maps both the default `/interview` and any `telos checkin` triggers to ContextCheckin.
