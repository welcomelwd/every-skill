# weather-fs workspace

Files in this directory are **seed files**: at build/dev time they are mirrored
into this agent's workspace, so the running agent starts with them on disk.

`cities.json` lists cities this assistant commonly answers about.
