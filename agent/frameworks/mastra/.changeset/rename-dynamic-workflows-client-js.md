---
'@mastra/client-js': minor
---

Renamed the stored workflow client methods to dynamic workflows: `upsertStoredWorkflow()`, `listStoredWorkflows()`, and `getStoredWorkflow()` are now `upsertDynamicWorkflow()`, `listDynamicWorkflows()`, and `getDynamicWorkflow()`.
