---
'@internal/playground': patch
---

Added per-item scorer controls in Studio datasets. Each item can inherit the dataset scorers, override them with selected scorers, or run without scorers.

Improved Studio scorer selectors to show only scorers registered with the current Mastra instance, including the shared run-level experiment picker.
