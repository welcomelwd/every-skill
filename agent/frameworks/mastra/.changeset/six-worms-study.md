---
'@mastra/server': minor
---

Added experiment provenance and grouping fields to dataset HTTP APIs, including grouping filters for experiment lists.

```text
GET /api/datasets/my-dataset/experiments?experimentSetId=benchmark-1&trialIndex=0
```
